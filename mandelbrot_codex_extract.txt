Mandelbrot Codex Local Conversation Extract
================================================
Readable raw-text extraction from local ~/.codex JSONL history.
Scope: user/assistant conversation plus relevant Mandelbrot/GPGPU/display tool snippets; runtime boilerplate removed.


================================================================================================
RECENT: gate/start conditions for Mandelbrot GPU sidequest
source: /home/t4ce/.codex/archived_sessions/rollout-2026-05-12T06-49-47-019e1a85-0c3d-73e0-90f7-745fd2a0aca3.jsonl
================================================================================================

[2026-05-12T04:50:12.515Z] meta line=1
------------------------------------------------------------------------------------------------
id=019e1a85-0c3d-73e0-90f7-745fd2a0aca3 | cwd=/home/t4ce/REPOS/TRUEOS | source=vscode | git=9aeda8d1241d

[2026-05-12T04:50:12.517Z] user line=6
------------------------------------------------------------------------------------------------
mandelbrot-gpu-sidequest: gpgpu-primary-framebuffer-mandelbrot8-loop frame=196 submitted=0 finished=0 readback_ok=0 reason=no-device program_source=gfx12-primary-scanout-mandelbrot32-scalar-strip-hdc1-stateless-store-then-ts-eot target_gpu=0x0 first_expected=0xC0DE7747 before=0x00000000 after=0x00000000 lane_hit_mask=0x0000000000000000 finish_marker=0x00000000 preview_cursor=0 pixels_per_tick=8192 lumen_released=0 action=hold-lumen-load next=fix-gpgpu-mandelbrot8-strip does_not_prove=fragment_render_path

please find an applicable gate to the start of it because what is currently the conditions?

[2026-05-12T04:50:17.382Z] assistant line=9
------------------------------------------------------------------------------------------------
I’ll trace where that sidequest string and gate conditions live, then I’ll read the surrounding scheduler/test logic before touching anything. The key clue looks like `reason=no-device`, so I’m going to find where the Mandelbrot8 strip is skipped or held and what “applicable gate” means in this repo.

[2026-05-12T04:50:17.382Z] assistant line=10
------------------------------------------------------------------------------------------------
I’ll trace where that sidequest string and gate conditions live, then I’ll read the surrounding scheduler/test logic before touching anything. The key clue looks like `reason=no-device`, so I’m going to find where the Mandelbrot8 strip is skipped or held and what “applicable gate” means in this repo.

[2026-05-12T04:50:17.384Z] tool-call:exec_command line=11
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"mandelbrot-gpu-sidequest|gpgpu-primary-framebuffer-mandelbrot8-loop|fix-gpgpu-mandelbrot8-strip|hold-lumen-load|no-device\" .","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":12000}

[2026-05-12T04:50:17.405Z] tool-call:exec_command line=12
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"gfx12-primary-scanout-mandelbrot32|mandelbrot8|mandelbrot32|lumen_released|lane_hit_mask|finish_marker\" .","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":12000}

[2026-05-12T04:50:17.406Z] tool-call:exec_command line=13
------------------------------------------------------------------------------------------------
{"cmd":"rg --files . | rg \"(mandelbrot|gpgpu|lumen|sidequest|gpu|framebuffer|gfx12)\"","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":12000}

[2026-05-12T04:50:17.458Z] tool-output line=15
------------------------------------------------------------------------------------------------
Chunk ID: 8af156
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 605
Output:
./src/tst/mandelbrot_gpu_sidequest.rs:7:pub(crate) const MANDELBROT_GPU_SIDEQUEST_NAME: &str = "mandelbrot-gpu-sidequest";
./src/tst/mandelbrot_gpu_sidequest.rs:127:        "mandelbrot-gpu-sidequest: attempted name={} called=1 hot=0 artifact_stage={} source={} spirv={} spirv_bytes={} spirv_sig=0x{:016X} target={}x{} rgba_bytes=0x{:X} push_constants={} render_path={} fallback_present={} scanout={}x{} primary_gpu=0x{:X} shader_helper_ready={} upload=custom-intel-gpgpu-program action=hold-lumen-load next=visible-gpgpu-primary-framebuffer-mandelbrot8-pilot\n",
./src/tst/mandelbrot_gpu_sidequest.rs:148:        "mandelbrot-gpu-sidequest: primary-scanout-marker-preflight submitted={} finished={} readback_ok={} reason={} program_source={} target_gpu=0x{:X} sentinel=0x{:08X} before=0x{:08X} after=0x{:08X} hit_mask=0x{:016X} finish_marker=0x{:08X} action={} next={}\n",
./src/tst/mandelbrot_gpu_sidequest.rs:186:            "mandelbrot-gpu-sidequest: gpgpu-primary-framebuffer-mandelbrot8-loop frame={} submitted={} finished={} readback_ok={} reason={} program_source={} target_gpu=0x{:X} first_expected=0x{:08X} before=0x{:08X} after=0x{:08X} lane_hit_mask=0x{:016X} finish_marker=0x{:08X} preview_cursor={} pixels_per_tick={} lumen_released={} action={} next={} does_not_prove=fragment_render_path\n",
./src/tst/mandelbrot_gpu_sidequest.rs:205:                "hold-lumen-load"
./src/tst/mandelbrot_gpu_sidequest.rs:210:                "fix-gpgpu-mandelbrot8-strip"
./src/r/spawn_service.rs:1365:        "mandelbrot-gpu-sidequest",
./src/intel/gpgpu.rs:1860:            "no-device",
./src/intel/gpgpu.rs:2203:        return gpgpu_one_tile_sentinel_failure("no-device", program, output_gpu);
./src/intel/gpgpu.rs:2434:            "hold-lumen-load"
./src/intel/gpgpu.rs:2449:        return gpgpu_one_tile_sentinel_failure("no-device", program, 0);
./src/intel/gpgpu.rs:2933:        return (gpgpu_one_tile_sentinel_failure("no-device", program, 0), cursor);
./src/intel/gpgpu.rs:3077:        return gpgpu_one_tile_compare_failure("no-device", program, output_gpu, cpu_expected_bits);
./src/intel/gpgpu.rs:4036:            "no-device",
./src/intel/gpgpu.rs:4354:            "no-device",
./src/intel/intel/render/gpgpu.rs:153:        crate::log!("intel/gpgpu: preflight skipped reason=no-device\n");
./src/intel/intel/render/primary.rs:34:        crate::log!("intel/render: primary-triangle skipped reason=no-device\n");

[2026-05-12T04:50:17.470Z] tool-output line=16
------------------------------------------------------------------------------------------------
Chunk ID: c864fa
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 6662
Output:
./crates/trueos-eu/src/gfx12.rs:433:    "gfx12-primary-scanout-mandelbrot32-scalar-strip-hdc1-stateless-store-then-ts-eot";
./src/lumen/gpu_shadow.rs:316:                "lumen-gpu-proof: director-step step=7 backend=local-gpu mode=one-worker-output-sentinel submitted={} finished={} readback_ok={} reason={} program={} output_gpu=0x{:X} sentinel=0x{:08X} output_first_before=0x{:08X} output_first_after=0x{:08X} output_nonzero_before={} output_nonzero_after={} output_hits_lo64=0x{:016X} lane_dispatch={} finish_marker=0x{:08X} finish_expected=0x{:08X} batch_bytes=0x{:X} cpu_expected_bits=0x{:08X} output_owner=cpu-ap action=hold-scale next=one-tile-output-compare does_not_prove=model_matvec\n",
./src/lumen/gpu_shadow.rs:330:                sentinel.finish_marker,
./src/lumen/gpu_shadow.rs:331:                sentinel.expected_finish_marker,
./src/lumen/gpu_shadow.rs:345:                "lumen-gpu-proof: director-step step=8 backend=local-gpu mode=one-worker-output-compare submitted={} finished={} readback_ok={} compare_ok={} reason={} program={} output_gpu=0x{:X} gpu_value=0x{:08X} cpu_expected_bits=0x{:08X} output_first_before=0x{:08X} output_first_after=0x{:08X} output_hits_lo64=0x{:016X} lane_dispatch={} finish_marker=0x{:08X} finish_expected=0x{:08X} batch_bytes=0x{:X} output_owner=cpu-ap action=hold-scale next=t5-live4-packed-bf16-dot does_not_prove=model_matvec_or_gpu_live_load\n",
./src/lumen/gpu_shadow.rs:359:                compare.finish_marker,
./src/lumen/gpu_shadow.rs:360:                compare.expected_finish_marker,
./src/lumen/gpu_shadow.rs:383:            "lumen-gpu-proof: director-step step=9 backend=local-gpu mode=t5-small-live4-bf16-dot submitted={} finished={} readback_ok={} compare_ok={} reason={} program={} tile_index={} armed_tiles={} row={} output_gpu=0x{:X} gpu_value=0x{:08X} cpu_expected_bits=0x{:08X} full_row_cpu_bits=0x{:08X} output_first_before=0x{:08X} output_first_after=0x{:08X} output_hits_lo64=0x{:016X} lane_dispatch={} live_k_dim={} requires_live_gpu_load={} finish_marker=0x{:08X} finish_expected=0x{:08X} batch_bytes=0x{:X} output_owner=cpu-ap action=hold-scale next=scale-live-k-or-row-count does_not_prove=full_model_matvec\n",
./src/lumen/gpu_shadow.rs:403:            t5.finish_marker,
./src/lumen/gpu_shadow.rs:404:            t5.expected_finish_marker,
./src/lumen/gpu_shadow.rs:426:            "lumen-gpu-proof: director-step step=10 backend=local-gpu mode=t6-small-live8-bf16-dot submitted={} finished={} readback_ok={} compare_ok={} reason={} program={} tile_index={} armed_tiles={} row={} output_gpu=0x{:X} gpu_value=0x{:08X} cpu_expected_bits=0x{:08X} full_row_cpu_bits=0x{:08X} t5_gpu_value=0x{:08X} t5_cpu_expected_bits=0x{:08X} output_first_before=0x{:08X} output_first_after=0x{:08X} output_hits_lo64=0x{:016X} lane_dispatch={} live_k_dim={} requires_live_gpu_load={} finish_marker=0x{:08X} finish_expected=0x{:08X} batch_bytes=0x{:X} output_owner=cpu-ap action=hold-scale next=t6-1-live16-bf16-dot does_not_prove=full_model_matvec\n",
./src/lumen/gpu_shadow.rs:448:            t6.finish_marker,
./src/lumen/gpu_shadow.rs:449:            t6.expected_finish_marker,
./src/lumen/gpu_shadow.rs:471:            "lumen-gpu-proof: director-step step=11 backend=local-gpu mode=t6-1-live16-bf16-dot submitted={} finished={} readback_ok={} compare_ok={} reason={} program={} tile_index={} armed_tiles={} row={} output_gpu=0x{:X} gpu_value=0x{:08X} cpu_expected_bits=0x{:08X} full_row_cpu_bits=0x{:08X} t6_gpu_value=0x{:08X} t6_cpu_expected_bits=0x{:08X} output_first_before=0x{:08X} output_first_after=0x{:08X} output_hits_lo64=0x{:016X} lane_dispatch={} live_k_dim={} requires_live_gpu_load={} finish_marker=0x{:08X} finish_expected=0x{:08X} batch_bytes=0x{:X} output_owner=cpu-ap action=hold-scale next=t6-2-lane-indexed-live16-partial does_not_prove=full_model_matvec\n",
./src/lumen/gpu_shadow.rs:493:            t61.finish_marker,
./src/lumen/gpu_shadow.rs:494:            t61.expected_finish_marker,
./src/lumen/gpu_shadow.rs:594:                "lumen-gpu-proof: director-step step=14 backend=local-gpu mode=t6-2-row-block-live16-partial submitted={} finished={} readback_ok={} compare_ok={} reason={} program={} tile_index={} armed_tiles={} row_block={} global_row_start={} tile_row_start={} row_count={} output_gpu=0x{:X} compare_mask=0x{:08X} expected_mask=0x{:08X} first_gpu=0x{:08X} first_cpu_expected=0x{:08X}
[... truncated in extract ...]

[2026-05-12T04:50:17.471Z] tool-output line=17
------------------------------------------------------------------------------------------------
Chunk ID: c4ec43
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1139
Output:
./crates/trueos-shader/gfx12_eot_g127_tgl.hex
./crates/trueos-shader/gfx12_eot_g127_send1_tgl.hex
./crates/trueos-shader/gfx12_eot_g120_tgl.hex
./crates/trueos-shader/mandelbrot_gpu_sidequest_scaffold.md
./crates/trueos-shader/gfx12_eot_g126.asm
./crates/trueos-shader/gfx12_nop_then_eot_g126.asm
./crates/trueos-shader/gfx12_eot_g127_acc_clear.asm
./crates/trueos-shader/gfx12_eot_g126_tgl.hex
./crates/trueos-shader/gfx12_eot_g127_dg2.hex
./crates/trueos-shader/host_shader_validation/pipeline_exec/trueos_vs_mesa_gfx125_compare.md
./crates/trueos-shader/mesa_gfx125_compare.md
./crates/trueos-shader/gfx125_gateway_eot_g127.asm
./crates/trueos-shader/mandelbrot_fragment_1440p_parametric.spv
./crates/trueos-shader/gfx12_gateway_eot_g127.asm
./crates/trueos-shader/gfx12_eot_g127_send1.asm
./crates/trueos-shader/gfx12_eot_g112.asm
./crates/trueos-shader/gfx125_gateway_eot_g127_dg2.hex
./crates/trueos-shader/gfx12_eot_g120.asm
./crates/trueos-shader/mandelbrot_fragment_1440p_parametric.frag
./crates/trueos-shader/gfx12_gateway_eot_g127_tgl.hex
./crates/trueos-shader/gfx12_eot_g127_acc_clear_tgl.hex
./crates/trueos-gboi/gpu.rs
./crates/trueos-eu/src/gfx12.rs
./vendor/lumen/README.md
./vendor/lumen/Cargo.lock
./vendor/lumen/Cargo.toml
./vendor/lumen/src/tokenizer.rs
./vendor/lumen/src/mnist_loader.rs
./vendor/lumen/src/layers/mod.rs
./vendor/lumen/src/layers/activation.rs
./vendor/lumen/src/layers/norm/rms_norm.rs
./vendor/lumen/src/layers/norm/mod.rs
./vendor/lumen/src/layers/rnn/mod.rs
./vendor/lumen/src/layers/rnn/rnn.rs
./vendor/lumen/src/layers/rnn/lstm.rs
./vendor/lumen/src/layers/rnn/gru.rs
./vendor/lumen/src/layers/conv/mod.rs
./vendor/lumen/src/layers/conv/conv2d.rs
./vendor/lumen/src/layers/conv/pool.rs
./vendor/lumen/src/layers/basic/mod.rs
./vendor/lumen/src/layers/basic/flatten.rs
./vendor/lumen/src/layers/basic/dropout.rs
./vendor/lumen/src/layers/basic/linear.rs
./vendor/lumen/src/layers/basic/embedding.rs
./vendor/lumen/README_zh-CN.md
./vendor/lumen/lumen.backends.json
./vendor/lumen/src/autograd.rs
./vendor/lumen/src/loader.rs
./vendor/lumen/src/optim.rs
./vendor/lumen/src/layers/attention/mod.rs
./vendor/lumen/src/layers/attention/self_attention.rs
./vendor/lumen/src/layers/attention/encoding.rs
./vendor/lumen/src/models/mod.rs
./vendor/lumen/src/models/llama.rs
./vendor/lumen/src/parallel.rs
./vendor/lumen/src/loss.rs
./vendor/lumen/src/arch.rs
./vendor/lumen/src/main.rs
./vendor/lumen/src/bin/cuda_cpu_bench.rs
./vendor/lumen/src/bin/prefill_decode_bench.rs
./vendor/lumen/src/bin/quantize_safetensors.rs
./vendor/lumen/src/bin/kernel_bench.rs
./vendor/lumen/src/lib.rs
./vendor/lumen/src/init.rs
./vendor/lumen/src/precision.rs
./vendor/lumen/src/backend.rs
./vendor/lumen/src/module.rs
./vendor/lumen/build.rs
./vendor/lumen/LICENSE
./vendor/lumen/src/ops/cuda.rs
./vendor/lumen/src/ops/mod.rs
./vendor/lumen/src/ops/fp_kernels.rs
./vendor/lumen/src/ops/fused.rs
./vendor/lumen/src/ops/convolution.rs
./vendor/lumen/src/ops/int8_kernels.rs
./vendor/lumen/src/ops/cuda_stub.rs
./vendor/lumen/src/ops/cuda/lumen_cuda.cu
./vendor/lumen/src/ops/matmul.rs.bak
./vendor/lumen/src/ops/matmul.rs
./vendor/lumen/src/ops/arithmetic.rs
./vendor/lumen/src/ops/shape.rs
./crates/trueos-shader/mandelbrot_fragment_1440p_parametric_memo.md
./crates/trueos-shader/gfx12_eot_g112_tgl.hex
./crates/trueos-shader/gfx12_hdc1_bti34_store_eot_tgl.hex
./crates/trueos-shader/gfx12_hdc1_bti34_store_eot.asm
./crates/trueos-shader/gfx12_eot_g127.asm
./crates/trueos-shader/t1_t4_repro_20260510_222548/t1_gfx12_eot_g127_send1.asm
./crates/trueos-shader/t1_t4_repro_20260510_222548/t2_gfx12_hdc1_stateless_store_eot.asm
./crates/trueos-shader/gfx12_mesa_tgl_ts_eot_g126.asm
./crates/trueos-shader/gfx12_hdc1_stateless_store_eot.asm
./crates/trueos-shader/gfx12_hdc1_stateless_store_eot_tgl.hex
./crates/trueos-shader/gfx12_eot_g126_g127_mlen2.asm
./src/tst/ui2_mandelbrot_demo.rs
./src/tst/mandelbrot_gpu_sidequest.rs
./src/pci/reference/virtio_gpu.rs
./src/lumen/lumen_net.rs
./src/lumen/mod.rs
./src/lumen/cgp.rs
./src/lumen/gpu_shadow.rs
./src/lumen/gpgpu_ladder.md
./src/lumen/burn_baby.rs
./src/lumen/lumen_service.rs
./src/lumen/burn_baba.rs
./src/gfx/mandelbrot.rs
./src/intel/intel/may_help/intel_igpu770.rs
./src/intel/intel/may_help/intel_igpu770_rcs.rs
./src/gfx/virtio_gpu_3d.rs
./src/intel/intel/render/gpgpu.rs
./src/intel/gpgpu.rs
./src/intel/intel/ref
[... truncated in extract ...]

[2026-05-12T04:50:22.804Z] assistant line=19
------------------------------------------------------------------------------------------------
I found the exact emission site in [src/tst/mandelbrot_gpu_sidequest.rs] and the lower-level `no-device` returns in [src/intel/gpgpu.rs]. I’m going to read those ranges now and map the current conditions before adding the gate at the start.

[2026-05-12T04:50:22.805Z] assistant line=20
------------------------------------------------------------------------------------------------
I found the exact emission site in [src/tst/mandelbrot_gpu_sidequest.rs] and the lower-level `no-device` returns in [src/intel/gpgpu.rs]. I’m going to read those ranges now and map the current conditions before adding the gate at the start.

[2026-05-12T04:50:22.805Z] tool-call:exec_command line=21
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1,240p' src/tst/mandelbrot_gpu_sidequest.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":20000}

[2026-05-12T04:50:22.823Z] tool-call:exec_command line=23
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1380,1460p' src/intel/gpgpu.rs && sed -n '2920,3070p' src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":24000}

[2026-05-12T04:50:22.876Z] tool-output line=25
------------------------------------------------------------------------------------------------
Chunk ID: 7033ab
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 2079
Output:
#![allow(dead_code)]

use embassy_executor::{SpawnError, Spawner};
use embassy_time::{Duration as EmbassyDuration, Timer};
use trueos_gfx_core::{ShaderDesc, ShaderFormat, ShaderStage};

pub(crate) const MANDELBROT_GPU_SIDEQUEST_NAME: &str = "mandelbrot-gpu-sidequest";
pub(crate) const MANDELBROT_FRAGMENT_SOURCE_PATH: &str =
    "crates/trueos-shader/mandelbrot_fragment_1440p_parametric.frag";
pub(crate) const MANDELBROT_FRAGMENT_SPIRV_PATH: &str =
    "crates/trueos-shader/mandelbrot_fragment_1440p_parametric.spv";
pub(crate) const MANDELBROT_FRAGMENT_SPIRV_BYTES: &[u8] =
    include_bytes!("../../crates/trueos-shader/mandelbrot_fragment_1440p_parametric.spv");
pub(crate) const MANDELBROT_TARGET_WIDTH: u32 = 2560;
pub(crate) const MANDELBROT_TARGET_HEIGHT: u32 = 1440;
pub(crate) const MANDELBROT_PUSH_CONSTANT_BYTES: u16 = 24;
pub(crate) const MANDELBROT_GPGPU_LOOP_MS: u64 = 100;
pub(crate) const MANDELBROT_GPGPU_PREVIEW_PIXELS_PER_TICK: usize = 8192;

#[derive(Copy, Clone, Debug, Eq, PartialEq)]
pub(crate) enum MandelbrotGpuArtifactStage {
    Fragment,
    Compute,
}

impl MandelbrotGpuArtifactStage {
    pub(crate) const fn as_str(self) -> &'static str {
        match self {
            Self::Fragment => "fragment",
            Self::Compute => "compute",
        }
    }
}

#[derive(Copy, Clone, Debug, Eq, PartialEq)]
pub(crate) enum MandelbrotPresentPath {
    BufferFirst,
    CopyToIntelOverlay,
    DirectFramebuffer,
}

impl MandelbrotPresentPath {
    pub(crate) const fn as_str(self) -> &'static str {
        match self {
            Self::BufferFirst => "buffer-first",
            Self::CopyToIntelOverlay => "copy-to-intel-overlay",
            Self::DirectFramebuffer => "direct-framebuffer",
        }
    }
}

#[derive(Copy, Clone, Debug, Eq, PartialEq)]
pub(crate) struct MandelbrotGpuSidequestPlan {
    pub(crate) name: &'static str,
    pub(crate) stage: MandelbrotGpuArtifactStage,
    pub(crate) source_path: &'static str,
    pub(crate) spirv_path: &'static str,
    pub(crate) target_width: u32,
    pub(crate) target_height: u32,
    pub(crate) push_constant_bytes: u16,
    pub(crate) render_path: MandelbrotPresentPath,
    pub(crate) fallback_present_path: MandelbrotPresentPath,
}

impl MandelbrotGpuSidequestPlan {
    pub(crate) const fn default_fragment_buffer_first() -> Self {
        Self {
            name: MANDELBROT_GPU_SIDEQUEST_NAME,
            stage: MandelbrotGpuArtifactStage::Compute,
            source_path: MANDELBROT_FRAGMENT_SOURCE_PATH,
            spirv_path: MANDELBROT_FRAGMENT_SPIRV_PATH,
            target_width: MANDELBROT_TARGET_WIDTH,
            target_height: MANDELBROT_TARGET_HEIGHT,
            push_constant_bytes: MANDELBROT_PUSH_CONSTANT_BYTES,
            render_path: MandelbrotPresentPath::DirectFramebuffer,
            fallback_present_path: MandelbrotPresentPath::CopyToIntelOverlay,
        }
    }

    pub(crate) const fn target_rgba_bytes(self) -> usize {
        (self.target_width as usize)
            .saturating_mul(self.target_height as usize)
            .saturating_mul(4)
    }
}

pub(crate) const fn mandelbrot_gpu_sidequest_plan() -> MandelbrotGpuSidequestPlan {
    MandelbrotGpuSidequestPlan::default_fragment_buffer_first()
}

pub(crate) fn mandelbrot_fragment_shader_desc(spirv_bytes: &'static [u8]) -> ShaderDesc<'static> {
    ShaderDesc {
        stage: ShaderStage::Fragment,
        format: ShaderFormat::SpirV,
        bytes: spirv_bytes,
    }
}

fn byte_signature(bytes: &[u8]) -> u64 {
    let mut sig = 0xCBF2_9CE4_8422_2325u64;
    for &byte in bytes {
        sig ^= byte as u64;
        sig = sig.wrapping_mul(0x0000_0100_0000_01B3);
    }
    sig
}

pub(crate) fn spawn_mandelbrot_gpu_sidequest(spawner: Spawner) -> Result<(), SpawnError> {
    let token = mandelbrot_gpu_sidequest_task()?;
    spawner.spawn(token);
    Ok(())
}

#[embassy_executor::task(pool_size = 1)]
pub(crate) async fn mandelbrot_gpu_sidequest_task() {
    let plan = mandelbrot_gpu_sidequest_plan();
    let scanout = crate::intel::active_scanout_dimensions();
    let scanout_w = scanout.map(|dims| dims.0).unwrap_or(0);
    let scanout_h = scanout.map(|dims| dims.1).unwrap_or(0);
    let primary_gpu = crate::intel::primary_surface_gpu_addr().unwrap_or(0);
    let shader_helper_ready = true;
    let shader_desc = mandelbrot_fragment_shader_desc(MANDELBROT_FRAGMENT_SPIRV_BYTES);
    let
[... truncated in extract ...]

[2026-05-12T04:50:22.876Z] tool-output line=26
------------------------------------------------------------------------------------------------
Chunk ID: ba1b5f
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 539
Output:
        NET_ANY_CONFIGURED_AND_ROOT_READY,
        &FTP_SERVER_STARTED,
        spawn_ftp_server,
    ),
    TaskSpec::enabled(
        "tga",
        crate::r::readiness::NET_ANY_CONFIGURED,
        &TGA_TASK_STARTED,
        spawn_tga_task,
    ),
    TaskSpec::enabled_gated(
        "gfx-virgl-backend-ready",
        0,
        gfx_backend_boot_gate,
        &GFX_VIRGL_READY_TASK_STARTED,
        spawn_gfx_virgl_ready_task,
    ),
    TaskSpec::enabled(
        "gfx-virgl-cursor-overlay",
        crate::r::readiness::GFX_BACKEND_READY,
        &GFX_VIRGL_CURSOR_OVERLAY_STARTED,
        spawn_gfx_virgl_cursor_overlay_task,
    ),
    TaskSpec::enabled_gated(
        "intel-cursor-service",
        0,
        intel_cursor_service_gate,
        &INTEL_CURSOR_SERVICE_STARTED,
        spawn_intel_cursor_service_task,
    ),
    TaskSpec::disabled("intel-hda-probe", 0, &INTEL_HDA_PROBE_STARTED, spawn_intel_hda_probe_task),
    TaskSpec::enabled("raple-service", 0, &RAPLE_SERVICE_STARTED, spawn_raple_service),
    TaskSpec::disabled(
        "html_fetch_service",
        crate::r::readiness::NET_V4_CONFIGURED | crate::r::readiness::TRUEOSFS_ROOT_MOUNTED,
        &HTML_SHACK_SERVICE_STARTED,
        html_fetch_service,
    ),
    TaskSpec::enabled(
        "gfx-texture-upload-service",
        crate::r::readiness::GFX_BACKEND_READY,
        &GFX_TEXTURE_UPLOAD_SERVICE_STARTED,
        spawn_gfx_texture_upload_service,
    ),
    TaskSpec::enabled(
        "mandelbrot-gpu-sidequest",
        0,
        &MANDELBROT_GPU_SIDEQUEST_STARTED,
        spawn_mandelbrot_gpu_sidequest,
    ),
    TaskSpec::enabled_gated(
        "ui2",
        crate::r::readiness::GFX_BACKEND_READY,
        ui2_core_task_gate,
        &UI2_STARTED,
        spawn_ui2,
    ),
    TaskSpec::enabled_gated(
        "ui2-hosted",
        crate::r::readiness::GFX_BACKEND_READY,
        ui2_core_task_gate,
        &UI2_HOSTED_SYNC_TASK_STARTED,
        spawn_ui2_hosted,
    ),
    TaskSpec::enabled_gated(
        "ui2-hit",
        crate::r::readiness::GFX_BACKEND_READY,
        ui2_core_task_gate,
        &UI2_HIT_TASK_STARTED,
        spawn_ui2_hit,
    ),

[2026-05-12T04:50:22.876Z] tool-output line=27
------------------------------------------------------------------------------------------------
Chunk ID: 8da4cc
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 2430
Output:
        words: artifact.words,
        expects_store: artifact.expects_store,
        expected_store_value: GPU_PROGRAM_SHARED_RAM_WRITE_EXPECTED,
        store_send_dword: Some(trueos_eu::gfx12::HDC1_STATELESS_STATIC_DP4A_STORE_SEND_DWORD),
        visible_seed_dword: Some(trueos_eu::gfx12::HDC1_STATELESS_STATIC_DP4A_BASE_DWORD),
    }
}

fn gpgpu_one_tile_output_sentinel_program() -> GpgpuEuProgram {
    let artifact = trueos_eu::gfx12::HDC1_STATELESS_STORE_THEN_TS_EOT;
    GpgpuEuProgram {
        name: "gfx12-t47-one-tile-output-sentinel-hdc1-stateless-store-then-ts-eot",
        kind: artifact.kind,
        words: artifact.words,
        expects_store: artifact.expects_store,
        expected_store_value: GPGPU_ONE_TILE_OUTPUT_SENTINEL,
        store_send_dword: Some(trueos_eu::gfx12::HDC1_BTI34_STORE_SEND_DWORD),
        visible_seed_dword: Some(trueos_eu::gfx12::HDC1_BTI34_STORE_IMM_DWORD),
    }
}

fn gpgpu_primary_scanout_pixel_quiet_program() -> GpgpuEuProgram {
    let artifact = trueos_eu::gfx12::HDC1_STATELESS_STORE_THEN_TS_EOT;
    GpgpuEuProgram {
        name: "gfx12-t47-primary-scanout-pixel-quiet-hdc1-stateless-store-then-ts-eot",
        kind: artifact.kind,
        words: artifact.words,
        expects_store: artifact.expects_store,
        expected_store_value: GPGPU_ONE_TILE_OUTPUT_SENTINEL,
        store_send_dword: Some(trueos_eu::gfx12::HDC1_BTI34_STORE_SEND_DWORD),
        visible_seed_dword: Some(trueos_eu::gfx12::HDC1_BTI34_STORE_IMM_DWORD),
    }
}

fn gpgpu_primary_scanout_mandelbrot8_program() -> GpgpuEuProgram {
    let artifact = trueos_eu::gfx12::PRIMARY_SCANOUT_MANDELBROT8_HDC1_STATELESS_STORE_THEN_TS_EOT;
    GpgpuEuProgram {
        name: artifact.name,
        kind: artifact.kind,
        words: artifact.words,
        expects_store: artifact.expects_store,
        expected_store_value: 0,
        store_send_dword: Some(trueos_eu::gfx12::PRIMARY_SCANOUT_MANDELBROT8_STORE_SEND_DWORD),
        visible_seed_dword: None,
    }
}

fn gpgpu_one_tile_output_compare_program() -> GpgpuEuProgram {
    let artifact = trueos_eu::gfx12::STATIC_DP4A_HDC1_STATELESS_STORE_THEN_TS_EOT;
    GpgpuEuProgram {
        name: "gfx12-t48-one-tile-output-compare-dp4a-echo-hdc1-stateless-store-then-ts-eot",
        kind: artifact.kind,
        words: artifact.words,
        expects_store: artifact.expects_store,
        expected_store_value: 0,
        store_send_dword: Some(trueos_eu::gfx12::HDC1_STATELESS_STATIC_DP4A_STORE_SEND_DWORD),
        visible_seed_dword: Some(trueos_eu::gfx12::HDC1_STATELESS_STATIC_DP4A_BASE_DWORD),
    }
}

fn gpgpu_t5_one_row_matvec_program() -> GpgpuEuProgram {
    let artifact =
        trueos_eu::gfx12::T5_SMALL_LIVE4_TRUEOS_ARENA_BF16_DOT_HDC1_STATELESS_STORE_THEN_TS_EOT;
    GpgpuEuProgram {
        name: artifact.name,
        kind: artifact.kind,
        words: artifact.words,
        expects_store: artifact.expects_store,
        expected_store_value: 0,
        store_send_dword: Some(trueos_eu::gfx12::T5_SMALL_LIVE4_TRUEOS_ARENA_STORE_SEND_DWORD),
        visible_seed_dword: Some(trueos_eu::gfx12::T5_SMALL_LIVE4_TRUEOS_ARENA_SENTINEL_DWORD),
    }
}

fn gpgpu_t6_one_row_matvec_program() -> GpgpuEuProgram {
    let artifact =
        trueos_eu::gfx12::T6_SMALL_LIVE8_TRUEOS_ARENA_BF16_DOT_HDC1_STATELESS_STORE_THEN_TS_EOT;
    GpgpuEuProgram {
        name: artifact.name,
        kind: artifact.kind,
        words: artifact.words,
}

pub(crate) fn submit_gpgpu_primary_scanout_mandelbrot_preview(
    cursor: usize,
    pixel_budget: usize,
) -> (crate::intel::GpgpuOneTileSentinelProof, usize) {
    const PREVIEW_X: u32 = 0;
    const PREVIEW_Y: u32 = 0;
    const ROW_INTERLACE: usize = 16;
    const LANES: usize = trueos_eu::gfx12::PRIMARY_SCANOUT_MANDELBROT8_LANES;

    let program = gpgpu_primary_scanout_mandelbrot8_program();
    let Some(dev) = crate::intel::claimed_device() else {
        return (gpgpu_one_tile_sentinel_failure("no-device", program, 0), cursor);
    };
    let Some(warm) = warm_state() else {
        return (gpgpu_one_tile_sentinel_failure("no-warm-state", program, 0), cursor);
    };
    let Some(target) = super::display::primary_surface_gpgpu_marker_target() else {
        return (gpgpu_one_tile_sentinel_failure("no-primary-scanout", program, 0), cursor);
    };
    if !forcewake_render_acquire(warm) {
        return (gpgpu_one_tile_sentinel_f
[... truncated in extract ...]

[2026-05-12T04:50:27.156Z] tool-call:exec_command line=30
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"fn .*_gate|enabled_gated|active_scanout_dimensions|primary_surface_gpu_addr|claimed_device\\(|warm_state\\(|primary_surface_gpgpu_marker_target|MANDELBROT_GPU_SIDEQUEST_READY|GFX_BACKEND_READY\" src","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":24000}

[2026-05-12T04:50:27.200Z] tool-output line=33
------------------------------------------------------------------------------------------------
Chunk ID: f844ea
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1394
Output:
use alloc::{string::String, vec::Vec};
use core::sync::atomic::{AtomicBool, AtomicU32, AtomicU64, Ordering};

use embassy_executor::{SendSpawner, SpawnError, SpawnToken, Spawner};
use embassy_time::{Duration as EmbassyDuration, Timer};
use spin::Mutex;

use crate::r::spawn_spec::{SpawnAttempt, TaskSpec};
// NOTE: This file is intended to become the single source of truth for Embassy task startup.

const SPAWN_SERVICE_AFTER_START_MS: u64 = 25;
const SPAWN_SERVICE_PENDING_MS: u64 = 100;
const SPAWN_SERVICE_IDLE_MS: u64 = 500;
static PCIIDS_GIT_PENDING_MISSING: AtomicU32 = AtomicU32::new(0);

/// Central task orchestrator ("FSM spawn service").
///
/// Ideal-world model:
/// - One file owns the boot task registry (what runs + under which readiness conditions).
/// - Individual tasks can still contain internal gating today; later we can delete those
///   once this registry is trusted.
/// - Readiness is monotonic, so this service only ever adds tasks; it never stops them.
///
/// This is intentionally simple: a small polling loop over a static registry.

macro_rules! define_started_flags {
    ($($name:ident),+ $(,)?) => {
        $(static $name: AtomicBool = AtomicBool::new(false);)+
    };
}

define_started_flags!(
    JOB_RUNNER_STARTED,
    GLOBALOG_PERSIST_ONCE_STARTED,
    QJS_ASYNC_FS_SERVICE_STARTED,
    TRUEOSFS_MOUNT_SERVICE_STARTED,
    TRUEOSFS_INDEX_SERVICE_STARTED,
    HV_VM_STORE_STARTED,
    HV_VM_STORE_NET_STARTED,
    NET_POLL_STARTED,
    NET_SERVICE_STARTED,
    TLS_SOCKET_SERVICE_STARTED,
    NTP_SYNC_STARTED,
    SNTP_SERVICE_STARTED,
    NET_SHELL_STARTED,
    AI_QJS_ONESHOT_STARTED,
    HTTP_TRUEOSFS_STARTED,
    HYPER_HTTP1_PROBE_STARTED,
    CHAT_HTTP_STARTED,
    MAIL_HTTP_STARTED,
    AXUM_BOOT_STARTED,
    FILEEXPLORER_HTTP_STARTED,
    WEBDEVICES_HTTP_STARTED,
    WS_TIME_STARTED,
    ESP_GATE_STARTED,
    ESP_GATE_REGISTRY_STARTED,
    ESP_PIANO_UDP_STARTED,
    FTP_SERVER_STARTED,
    TGA_TASK_STARTED,
    GFX_VIRGL_READY_TASK_STARTED,
    GFX_VIRGL_CURSOR_OVERLAY_STARTED,
    MANDELBROT_GPU_SIDEQUEST_STARTED,
    INTEL_CURSOR_SERVICE_STARTED,
    INTEL_HDA_PROBE_STARTED,
    RAPLE_SERVICE_STARTED,
    GFX_TEXTURE_UPLOAD_SERVICE_STARTED,
    HTML_SHACK_SERVICE_STARTED,
    UI2_HOSTED_SYNC_TASK_STARTED,
    UI2_HIT_TASK_STARTED,
    UI2_STARTED,
    UI2_ATHLAS_THIRD_DEMO_STARTED,
    UI2_ATHLAS_HALF_DEMO_STARTED,
    UI2_ATHLAS_1X_DEMO_STARTED,
    UI2_ATHLAS_2X_DEMO_STARTED,
    UI2_PALATINO_1X_DEMO_STARTED,
    UI2_TWEMOJI_1X_STARTED,
    UI2_ANALOG_CLOCK_DEMO_STARTED,
    UI2_TEXT_INPUT_DEMO_STARTED,
    UI2_BGRT_DEMO_STARTED,
    UI2_CORETICKS_DEMO_STARTED,
    UI2_CURSORPICKER_DEMO_STARTED,
    UI2_MANDELBROT_DEMO_STARTED,
    UI2_PLAYER_DEMO_STARTED,
    UI2_RAPLE_DEMO_STARTED,
    UI2_SMILEY_FOUNTAIN_DEMO_STARTED,
    UI2_SHELL_DEMO_STARTED,
    UI2_SWARM_DEMO_STARTED,
    USB_CONTROLLER_TASKS_STARTED,
    TRUEOSFS_READY_HOOK_STARTED,
    BP_AUTOSTART_STARTED,
    BOOT_WS_SMOKE_STARTED,
    BOOT_NETBENCH_STARTED,
    UAS_SKHYNIX_ROUTE_PROBE_STARTED,
    APP_VM_RUN_QUEUE_STARTED,
    FACTORY_RAM_PROBE_STARTED,
    UART_SHELL_STARTED,
    NET_TCP_SHELL_STARTED,
    LOGTOTCP_STARTED,
    LUMEN_SERVICE_STARTED,
    PCIIDS_GIT_STARTED,
    ATOMIC_BOMB_STARTED,
    HTML_DEMO_STARTED,
    SURFER_FACTORY_STARTED
);

macro_rules! define_stop_flags {
    ($($name:ident),* $(,)?) => {
        $(static $name: AtomicBool = AtomicBool::new(false);)*
    };
}

define_stop_flags!(
    STOP_UI2_TEXT_INPUT_DEMO,
    STOP_UI2_ANALOG_CLOCK_DEMO,
    STOP_UI2_BGRT_DEMO,
    STOP_UI2_CORETICKS_DEMO,
    STOP_UI2_CURSORPICKER_DEMO,
    STOP_UI2_MANDELBROT_DEMO,
    STOP_UI2_PLAYER_DEMO,
    STOP_UI2_RAPLE_DEMO,
    STOP_UI2_SMILEY_FOUNTAIN_DEMO,
    STOP_UI2_SHELL_DEMO,
    STOP_UI2_SWARM_DEMO,
);

fn stop_flag_by_task_name(name: &str) -> Option<&'static AtomicBool> {
    match name {
        "ui2-text-input-demo" => Some(&STOP_UI2_TEXT_INPUT_DEMO),
        "ui2-analog-clock-demo" => Some(&STOP_UI2_ANALOG_CLOCK_DEMO),
        "ui2-bgrt-demo" => Some(&STOP_UI2_BGRT_DEMO),
        "ui2-coreticks-demo" => Some(&STOP_UI2_CORETICKS_DEMO),
        "ui2-cursorpicker-demo" => Some(&STOP_UI2_CURSORPICKER_DEMO),
        "ui2-mandelbrot-demo" => Some(&STOP_UI2_MANDELBROT_DEMO),
        "ui2-player-demo" => Some(&STOP_UI2_PLAYER_DEMO),
        "ui2-raple-demo" => Some(&STOP_UI2_RAPLE_DEMO),
        "ui2-smiley-fou
[... truncated in extract ...]

[2026-05-12T04:50:27.234Z] tool-output line=34
------------------------------------------------------------------------------------------------
Chunk ID: b98788
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 2818
Output:
src/tst/mandelbrot_gpu_sidequest.rs:117:    let scanout = crate::intel::active_scanout_dimensions();
src/tst/mandelbrot_gpu_sidequest.rs:120:    let primary_gpu = crate::intel::primary_surface_gpu_addr().unwrap_or(0);
src/tst/mandelbrot_gpu_sidequest.rs:182:            crate::r::readiness::set(crate::r::readiness::MANDELBROT_GPU_SIDEQUEST_READY);
src/ui2/mod.rs:714:        let (view_w, view_h) = crate::intel::active_scanout_dimensions()
src/r/spawn_spec.rs:5:fn task_gate_always() -> bool {
src/r/spawn_spec.rs:41:    pub(super) const fn enabled_gated(
src/r/readiness.rs:45:pub const GFX_BACKEND_READY: u32 = 1 << 22;
src/r/readiness.rs:51:pub const MANDELBROT_GPU_SIDEQUEST_READY: u32 = 1 << 28;
src/r/readiness.rs:71:    (GFX_BACKEND_READY, "GFX_BACKEND_READY"),
src/r/readiness.rs:77:    (MANDELBROT_GPU_SIDEQUEST_READY, "MANDELBROT_GPU_SIDEQUEST_READY"),
src/gfx/backends/intel.rs:99:        if !crate::intel::has_claimed_device() {
src/gfx/backends/intel.rs:113:        let (width, height) = crate::intel::active_scanout_dimensions()
src/gfx/backends/intel.rs:361:            let guc_status = crate::intel::warm_state()
src/gfx/mod.rs:112:    crate::r::readiness::set(crate::r::readiness::GFX_BACKEND_READY);
src/gfx/mod.rs:225:            crate::r::readiness::set(crate::r::readiness::GFX_BACKEND_READY);
src/r/io_cursor.rs:315:    crate::intel::active_scanout_dimensions()
src/intel/hw_cursor.rs:171:    let dev = crate::intel::claimed_device()?;
src/intel/intel/mod.rs:298:pub fn has_claimed_device() -> bool {
src/intel/intel/mod.rs:302:pub(crate) fn claimed_device() -> Option<Dev> {
src/intel/intel/mod.rs:306:pub fn warm_state() -> Option<self::render::RenderWarmState> {
src/intel/intel/mod.rs:307:    self::render::warm_state()
src/intel/intel/mod.rs:330:pub fn active_scanout_dimensions() -> Option<(u32, u32)> {
src/intel/intel/mod.rs:331:    self::display::active_scanout_dimensions()
src/r/net/esp.rs:1277:pub async fn esp_gate_task() {
src/r/net/esp.rs:1663:pub async fn esp_gate_registry_task() {
src/r/spawn_service.rs:527:fn spawn_esp_gate(spawner: Spawner) -> SpawnAttempt {
src/r/spawn_service.rs:531:fn spawn_esp_gate_registry(spawner: Spawner) -> SpawnAttempt {
src/r/spawn_service.rs:551:    if crate::r::readiness::is_set(crate::r::readiness::GFX_BACKEND_READY) {
src/r/spawn_service.rs:561:        if crate::r::readiness::is_set(crate::r::readiness::GFX_BACKEND_READY) {
src/r/spawn_service.rs:565:            crate::r::readiness::set(crate::r::readiness::GFX_BACKEND_READY);
src/r/spawn_service.rs:574:            crate::r::readiness::set(crate::r::readiness::GFX_BACKEND_READY);
src/r/spawn_service.rs:786:fn gfx_backend_boot_gate() -> bool {
src/r/spawn_service.rs:791:fn intel_cursor_service_gate() -> bool {
src/r/spawn_service.rs:792:    crate::intel::has_claimed_device()
src/r/spawn_service.rs:796:fn ui2_core_task_gate() -> bool {
src/r/spawn_service.rs:801:fn ui2_demo_task_gate() -> bool {
src/r/spawn_service.rs:1254:    TaskSpec::enabled_gated(
src/r/spawn_service.rs:1330:    TaskSpec::enabled_gated(
src/r/spawn_service.rs:1339:        crate::r::readiness::GFX_BACKEND_READY,
src/r/spawn_service.rs:1343:    TaskSpec::enabled_gated(
src/r/spawn_service.rs:1360:        crate::r::readiness::GFX_BACKEND_READY,
src/r/spawn_service.rs:1370:    TaskSpec::enabled_gated(
src/r/spawn_service.rs:1372:        crate::r::readiness::GFX_BACKEND_READY,
src/r/spawn_service.rs:1377:    TaskSpec::enabled_gated(
src/r/spawn_service.rs:1379:        crate::r::readiness::GFX_BACKEND_READY,
src/r/spawn_service.rs:1384:    TaskSpec::enabled_gated(
src/r/spawn_service.rs:1386:        crate::r::readiness::GFX_BACKEND_READY,
src/r/spawn_service.rs:1428:    TaskSpec::enabled_gated(
src/r/spawn_service.rs:1498:    TaskSpec::enabled_gated(
src/r/spawn_service.rs:1501:            | crate::r::readiness::MANDELBROT_GPU_SIDEQUEST_READY,
src/r/spawn_service.rs:1508:    TaskSpec::enabled_gated(
src/intel/xelp_media2_ngin.rs:784:    let dev = super::claimed_device()?;
src/intel/xelp_media2_ngin.rs:2976:    let dev = super::claimed_device()?;
src/intel/display.rs:529:pub(crate) fn active_scanout_dimensions() -> Option<(u32, u32)> {
src/intel/display.rs:530:    let dev = crate::intel::claimed_device()?;
src/intel/display.rs:536:pub(crate) fn primary_surface_gpu_addr() -> Option<u64> {
src/intel/display.rs:543:pub(super) fn primary_surface_gpgpu_marker_target()
[... truncated in extract ...]

[2026-05-12T04:50:27.234Z] tool-output line=35
------------------------------------------------------------------------------------------------
Chunk ID: 4e7060
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1225
Output:
use core::sync::atomic::{AtomicU32, Ordering};

use embassy_time::{Duration as EmbassyDuration, Instant, Timer};

// V-layer readiness flags.
//
// These are monotonic: once set, they are never cleared.
// Consumers can `await` prerequisites instead of guessing boot ordering.
pub const PIANO_CLAIMED: u32 = 1 << 3;

// Network readiness.
//
// `NET_GATEWAY_REACHABLE` is kept for backward compatibility ("any network").
// Prefer the per-protocol bits for new code.
pub const NET_GATEWAY_REACHABLE: u32 = 1 << 8;
pub const TLS_SOCKET_SERVICE_READY: u32 = 1 << 9;

pub const NET_V4_GATEWAY_REACHABLE: u32 = 1 << 10;
pub const NET_V6_GATEWAY_REACHABLE: u32 = 1 << 11;

// Network configuration readiness.
//
// These bits are about address/config state, not about being able to ping the
// router. Some networks drop ICMP echo; TCP/DNS can still work.
//
// `NET_ANY_CONFIGURED`: at least one IP stack has usable config.
pub const NET_ANY_CONFIGURED: u32 = 1 << 12;
pub const NET_V4_CONFIGURED: u32 = 1 << 13;
pub const NET_V6_CONFIGURED: u32 = 1 << 14;

// Socket readiness.
//
// `NET_SOCKET_READY` means the VNet/socket surface has proven forward progress:
// an open command was processed, an Opened event returned, and selector
// readiness reached a Mio/Tokio-style socket user. This is stronger than IP
// configuration and is the right gate for Hyper/Hickory/Tokio socket clients.
pub const NET_SOCKET_READY: u32 = 1 << 15;

pub const TRUEOSFS_ROOT_MOUNTED: u32 = 1 << 16;
pub const QJS_ASYNC_FS_READY: u32 = 1 << 17;
pub const INTEL_HDA_READY: u32 = 1 << 18;
pub const GFX_VIRGL_READY: u32 = 1 << 19;
pub const HTTP_TRUEOSFS_LISTENING: u32 = 1 << 20;
pub const TOKIO_RUNTIME_READY: u32 = 1 << 21;
pub const GFX_BACKEND_READY: u32 = 1 << 22;
pub const UI2_READY: u32 = 1 << 23;
pub const APP_VM_READY: u32 = 1 << 24;
pub const GFX_TEXTURE_UPLOAD_SERVICE_READY: u32 = 1 << 25;
pub const BACKGROUND_AP_WORKER_READY: u32 = 1 << 26;
pub const VTHREAD_HW_TAG_READY: u32 = 1 << 27;
pub const MANDELBROT_GPU_SIDEQUEST_READY: u32 = 1 << 28;

const APP_VM_READY_REQUIRED: u32 = NET_ANY_CONFIGURED | TRUEOSFS_ROOT_MOUNTED;

const READINESS_FLAGS: &[(u32, &str)] = &[
    (PIANO_CLAIMED, "PIANO_CLAIMED"),
    (NET_GATEWAY_REACHABLE, "NET_GATEWAY_REACHABLE"),
    (TLS_SOCKET_SERVICE_READY, "TLS_SOCKET_SERVICE_READY"),
    (NET_V4_GATEWAY_REACHABLE, "NET_V4_GATEWAY_REACHABLE"),
    (NET_V6_GATEWAY_REACHABLE, "NET_V6_GATEWAY_REACHABLE"),
    (NET_ANY_CONFIGURED, "NET_ANY_CONFIGURED"),
    (NET_V4_CONFIGURED, "NET_V4_CONFIGURED"),
    (NET_V6_CONFIGURED, "NET_V6_CONFIGURED"),
    (NET_SOCKET_READY, "NET_SOCKET_READY"),
    (TRUEOSFS_ROOT_MOUNTED, "TRUEOSFS_ROOT_MOUNTED"),
    (QJS_ASYNC_FS_READY, "QJS_ASYNC_FS_READY"),
    (INTEL_HDA_READY, "INTEL_HDA_READY"),
    (GFX_VIRGL_READY, "GFX_VIRGL_READY"),
    (HTTP_TRUEOSFS_LISTENING, "HTTP_TRUEOSFS_LISTENING"),
    (TOKIO_RUNTIME_READY, "TOKIO_RUNTIME_READY"),
    (GFX_BACKEND_READY, "GFX_BACKEND_READY"),
    (UI2_READY, "UI2_READY"),
    (APP_VM_READY, "APP_VM_READY"),
    (GFX_TEXTURE_UPLOAD_SERVICE_READY, "GFX_TEXTURE_UPLOAD_SERVICE_READY"),
    (BACKGROUND_AP_WORKER_READY, "BACKGROUND_AP_WORKER_READY"),
    (VTHREAD_HW_TAG_READY, "VTHREAD_HW_TAG_READY"),
    (MANDELBROT_GPU_SIDEQUEST_READY, "MANDELBROT_GPU_SIDEQUEST_READY"),
];

static READY: AtomicU32 = AtomicU32::new(0);

#[inline]
pub fn mask() -> u32 {
    READY.load(Ordering::Acquire)
}

#[inline]
pub fn is_set(required: u32) -> bool {
    mask() & required == required
}

pub fn for_each_flag(mask: u32, mut f: impl FnMut(u32, &'static str)) {
    for &(flag, name) in READINESS_FLAGS {
        if mask & flag != 0 {
            f(flag, name);
        }
    }
}

/// Mark one or more readiness flags as set.
#[inline]
pub fn set(flags: u32) {
    let mut next = flags;
    if flags & APP_VM_READY_REQUIRED == APP_VM_READY_REQUIRED {
        next |= APP_VM_READY;
    }

    let prev = READY.fetch_or(next, Ordering::AcqRel);
    let combined = prev | next;
    if combined & APP_VM_READY_REQUIRED == APP_VM_READY_REQUIRED {
        READY.fetch_or(APP_VM_READY, Ordering::AcqRel);
    }
}

/// Wait until all required flags are set.
///
/// This is a simple polling waiter to avoid additional dependencies.
pub async fn wait_for(required: u32) {
    loop {
        if is_set(required) {
            return;
        }
        Timer::after(EmbassyDuration::from_millis(25)).
[... truncated in extract ...]

[2026-05-12T04:50:31.592Z] assistant line=36
------------------------------------------------------------------------------------------------
The current start condition is very permissive: the registry starts `mandelbrot-gpu-sidequest` with required readiness mask `0` and no predicate gate. The task itself immediately probes Intel state, so on boots without a claimed Intel device it enters the loop and logs `reason=no-device` every 100 ms, which matches the line you pasted.

[2026-05-12T04:50:31.592Z] assistant line=37
------------------------------------------------------------------------------------------------
The current start condition is very permissive: the registry starts `mandelbrot-gpu-sidequest` with required readiness mask `0` and no predicate gate. The task itself immediately probes Intel state, so on boots without a claimed Intel device it enters the loop and logs `reason=no-device` every 100 ms, which matches the line you pasted.

[2026-05-12T04:50:31.619Z] tool-call:exec_command line=40
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '520,560p' src/intel/display.rs && sed -n '430,455p' src/intel/mod.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":12000}

[2026-05-12T04:50:31.663Z] tool-output line=42
------------------------------------------------------------------------------------------------
Chunk ID: e6d0a1
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 320
Output:
                browser_instance_id
            );
            None
        }
    }
}

fn spawn_truesurfer_factory(spawner: Spawner) -> SpawnAttempt {
    spawn_truesurfer_batch(spawner, truesurfer_factory_boot_count())
}

fn spawn_ui2(spawner: Spawner) -> SpawnAttempt {
    spawn_on_ap1(spawner, |_ap1_spawner| crate::r::ui2::ui2_task())
}

fn spawn_ui2_hit(spawner: Spawner) -> SpawnAttempt {
    spawn_on_ap1(spawner, |_ap1_spawner| crate::r::ui2::ui2_hit_task())
}

fn spawn_ui2_hosted(spawner: Spawner) -> SpawnAttempt {
    spawn_on_ap1(spawner, |_ap1_spawner| crate::r::ui2::ui2_hosted_task())
}

const UI2_DEMOS_ENABLED: bool = true;

#[inline]
fn gfx_backend_boot_gate() -> bool {
    true
}

#[inline]
fn intel_cursor_service_gate() -> bool {
    crate::intel::has_claimed_device()
}

#[inline]
fn ui2_core_task_gate() -> bool {
    true
}

#[inline]
fn ui2_demo_task_gate() -> bool {
    UI2_DEMOS_ENABLED
}

fn spawn_ui2_demo_on_worker<S: Send, F>(spawner: Spawner, spawn: F) -> SpawnAttempt
where
    F: FnOnce(SendSpawner) -> Result<SpawnToken<S>, SpawnError>,
{
    spawn_on_worker(spawner, spawn)
}

fn spawn_ui2_athlas_half_demo(spawner: Spawner) -> SpawnAttempt {
    spawn_on_ap1(spawner, |_ap1_spawner| crate::r::ui2::ui2_font_bucketproducer_demo_task(0))
}

[2026-05-12T04:50:31.663Z] tool-output line=44
------------------------------------------------------------------------------------------------
Chunk ID: e00d2e
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 509
Output:
        "intel/display: bottom-color pipe={} reg=0x{:05X} rgb=0x{:06X} readback=0x{:08X}\n",
        pipe.name,
        reg,
        rgb & 0x00FF_FFFF,
        readback
    );
}

#[allow(dead_code)]
pub(crate) fn active_scanout_dimensions() -> Option<(u32, u32)> {
    let dev = crate::intel::claimed_device()?;
    let pipe = active_pipe(dev)?;
    decode_pipe_src(crate::intel::mmio_read(dev, pipe.pipe_src_off)).or_else(framebuffer_hint)
}

#[allow(dead_code)]
pub(crate) fn primary_surface_gpu_addr() -> Option<u64> {
    PRIMARY_SURFACE
        .lock()
        .as_ref()
        .map(|_| crate::intel::GPU_VA_DISPLAY_PRIMARY_BASE)
}

pub(super) fn primary_surface_gpgpu_marker_target() -> Option<PrimarySurfaceGpgpuTarget> {
    let Some(surface) = *PRIMARY_SURFACE.lock() else {
        return None;
    };
    if surface.virt.is_null()
        || surface.width == 0
        || surface.height == 0
        || surface.pitch_bytes < PRIMARY_BYTES_PER_PIXEL
    {
        return None;
    }

    let marker_x = core::cmp::min(32, surface.width.saturating_sub(1));
    let marker_y = core::cmp::min(32, surface.height.saturating_sub(1));
    let marker_offset = (marker_y as usize)
        .saturating_mul(surface.pitch_bytes as usize)
        .saturating_add((marker_x as usize).saturating_mul(PRIMARY_BYTES_PER_PIXEL as usize));
    let byte_len = (surface.pitch_bytes as usize).saturating_mul(surface.height as usize);
pub fn guc_ready() -> bool {
    self::guc::ready()
}

pub(crate) fn guc_h2g_mmio_accepted() -> bool {
    self::guc::h2g_mmio_accepted()
}

pub fn has_claimed_device() -> bool {
    CLAIMED_DEVICE.lock().is_some()
}

pub(crate) fn claimed_device() -> Option<Dev> {
    *CLAIMED_DEVICE.lock()
}

pub fn warm_state() -> Option<RenderWarmState> {
    claimed_device().map(|dev| RenderWarmState {
        device_id: dev.device_id,
        revision_id: dev.revision_id,
        mmio_base: dev.mmio as usize,
        mmio_len: dev.mmio_len,
    })
}

pub(crate) fn gpgpu_preflight_status() -> GpgpuPreflightStatus {

[2026-05-12T04:50:38.638Z] assistant line=46
------------------------------------------------------------------------------------------------
I’m going to make this a registry-level start gate, not an inner sleep-loop workaround. The gate I’m adding is: `GFX_BACKEND_READY` plus an Intel claimed device, Intel warm state, active scanout dimensions, and a primary surface GPU address, which lines up with the first things the sidequest needs before its marker and Mandelbrot strip probes can mean anything.

[2026-05-12T04:50:38.639Z] assistant line=47
------------------------------------------------------------------------------------------------
I’m going to make this a registry-level start gate, not an inner sleep-loop workaround. The gate I’m adding is: `GFX_BACKEND_READY` plus an Intel claimed device, Intel warm state, active scanout dimensions, and a primary surface GPU address, which lines up with the first things the sidequest needs before its marker and Mandelbrot strip probes can mean anything.

[2026-05-12T04:50:43.899Z] assistant line=53
------------------------------------------------------------------------------------------------
That edit is in place. I’ll do a quick formatting/build-oriented check now so we catch any visibility or style mismatch, especially because this gate calls into the Intel facade from the spawn registry.

[2026-05-12T04:50:43.900Z] assistant line=54
------------------------------------------------------------------------------------------------
That edit is in place. I’ll do a quick formatting/build-oriented check now so we catch any visibility or style mismatch, especially because this gate calls into the Intel facade from the spawn registry.

[2026-05-12T04:50:45.083Z] tool-output line=61
------------------------------------------------------------------------------------------------
Chunk ID: 8355c4
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 258
Output:
diff --git a/src/r/spawn_service.rs b/src/r/spawn_service.rs
index 2c7426d7..9409b10f 100644
--- a/src/r/spawn_service.rs
+++ b/src/r/spawn_service.rs
@@ -792,6 +792,14 @@ fn intel_cursor_service_gate() -> bool {
     crate::intel::has_claimed_device()
 }
 
+#[inline]
+fn mandelbrot_gpu_sidequest_gate() -> bool {
+    crate::intel::has_claimed_device()
+        && crate::intel::warm_state().is_some()
+        && crate::intel::active_scanout_dimensions().is_some()
+        && crate::intel::primary_surface_gpu_addr().is_some()
+}
+
 #[inline]
 fn ui2_core_task_gate() -> bool {
     true
@@ -1361,9 +1369,10 @@ static TASKS: [TaskSpec; 71] = [
         &GFX_TEXTURE_UPLOAD_SERVICE_STARTED,
         spawn_gfx_texture_upload_service,
     ),
-    TaskSpec::enabled(
+    TaskSpec::enabled_gated(
         "mandelbrot-gpu-sidequest",
-        0,
+        crate::r::readiness::GFX_BACKEND_READY,
+        mandelbrot_gpu_sidequest_gate,
         &MANDELBROT_GPU_SIDEQUEST_STARTED,
         spawn_mandelbrot_gpu_sidequest,
     ),

[2026-05-12T04:50:48.637Z] assistant line=63
------------------------------------------------------------------------------------------------
`cargo fmt --check` is failing, but on pre-existing formatting drift in unrelated files (`hyper_probe.rs`, `tokio_probe.rs`, etc.), not on this patch. `cargo check` is still running; I’m going to wait for that and also check just the touched file’s formatting separately.

[2026-05-12T04:50:48.637Z] assistant line=64
------------------------------------------------------------------------------------------------
`cargo fmt --check` is failing, but on pre-existing formatting drift in unrelated files (`hyper_probe.rs`, `tokio_probe.rs`, etc.), not on this patch. `cargo check` is still running; I’m going to wait for that and also check just the touched file’s formatting separately.

[2026-05-12T04:50:53.804Z] tool-output line=75
------------------------------------------------------------------------------------------------
Chunk ID: a46b72
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 154
Output:
   786	fn gfx_backend_boot_gate() -> bool {
   787	    true
   788	}
   789	
   790	#[inline]
   791	fn intel_cursor_service_gate() -> bool {
   792	    crate::intel::has_claimed_device()
   793	}
   794	
   795	#[inline]
   796	fn mandelbrot_gpu_sidequest_gate() -> bool {
   797	    crate::intel::has_claimed_device()
   798	        && crate::intel::warm_state().is_some()
   799	        && crate::intel::active_scanout_dimensions().is_some()
   800	        && crate::intel::primary_surface_gpu_addr().is_some()
   801	}
   802	
   803	#[inline]
   804	fn ui2_core_task_gate() -> bool {
   805	    true
   806	}

[2026-05-12T04:50:53.804Z] tool-output line=76
------------------------------------------------------------------------------------------------
Chunk ID: 7403e9
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 184
Output:
  1360	    TaskSpec::disabled(
  1361	        "html_fetch_service",
  1362	        crate::r::readiness::NET_V4_CONFIGURED | crate::r::readiness::TRUEOSFS_ROOT_MOUNTED,
  1363	        &HTML_SHACK_SERVICE_STARTED,
  1364	        html_fetch_service,
  1365	    ),
  1366	    TaskSpec::enabled(
  1367	        "gfx-texture-upload-service",
  1368	        crate::r::readiness::GFX_BACKEND_READY,
  1369	        &GFX_TEXTURE_UPLOAD_SERVICE_STARTED,
  1370	        spawn_gfx_texture_upload_service,
  1371	    ),
  1372	    TaskSpec::enabled_gated(
  1373	        "mandelbrot-gpu-sidequest",
  1374	        crate::r::readiness::GFX_BACKEND_READY,
  1375	        mandelbrot_gpu_sidequest_gate,
  1376	        &MANDELBROT_GPU_SIDEQUEST_STARTED,

[2026-05-12T04:50:55.476Z] meta line=78
------------------------------------------------------------------------------------------------
id=019e1a85-0c3d-73e0-90f7-745fd2a0aca3 | cwd=/home/t4ce/REPOS/TRUEOS | source=vscode | git=9aeda8d1241d

[2026-05-12T04:50:59.767Z] assistant line=80
------------------------------------------------------------------------------------------------
Added the start gate in [src/r/spawn_service.rs](/home/t4ce/REPOS/TRUEOS/src/r/spawn_service.rs:796).

Current old condition was effectively: start immediately. The task registry had:

- required readiness: `0`
- gate: always true, via `TaskSpec::enabled`

So the sidequest could start before Intel was claimed, which is why it looped with `reason=no-device`.

New condition is:

```rust
GFX_BACKEND_READY
&& crate::intel::has_claimed_device()
&& crate::intel::warm_state().is_some()
&& crate::intel::active_scanout_dimensions().is_some()
&& crate::intel::primary_surface_gpu_addr().is_some()
```

And the task is now registered with `TaskSpec::enabled_gated(...)` at [src/r/spawn_service.rs](/home/t4ce/REPOS/TRUEOS/src/r/spawn_service.rs:1372).

Verification:
- `cargo check` passes.
- `rustfmt --check src/r/spawn_service.rs` passes.
- Full `cargo fmt --check` still fails on unrelated existing formatting drift in other files.

[2026-05-12T04:50:59.768Z] assistant line=81
------------------------------------------------------------------------------------------------
Added the start gate in [src/r/spawn_service.rs](/home/t4ce/REPOS/TRUEOS/src/r/spawn_service.rs:796).

Current old condition was effectively: start immediately. The task registry had:

- required readiness: `0`
- gate: always true, via `TaskSpec::enabled`

So the sidequest could start before Intel was claimed, which is why it looped with `reason=no-device`.

New condition is:

```rust
GFX_BACKEND_READY
&& crate::intel::has_claimed_device()
&& crate::intel::warm_state().is_some()
&& crate::intel::active_scanout_dimensions().is_some()
&& crate::intel::primary_surface_gpu_addr().is_some()
```

And the task is now registered with `TaskSpec::enabled_gated(...)` at [src/r/spawn_service.rs](/home/t4ce/REPOS/TRUEOS/src/r/spawn_service.rs:1372).

Verification:
- `cargo check` passes.
- `rustfmt --check src/r/spawn_service.rs` passes.
- Full `cargo fmt --check` still fails on unrelated existing formatting drift in other files.

[2026-05-12T04:50:59.785Z] user line=83
------------------------------------------------------------------------------------------------
we cant run it if there is not the GPGPU intel hardware path on, makes only sence iwth the UHD770

[2026-05-12T04:51:06.519Z] assistant line=87
------------------------------------------------------------------------------------------------
Right, agreed: “has some Intel device and a primary surface” is still too broad. I’ll tighten the gate to the actual Intel GPGPU path and UHD770-class device identity, then verify it compiles.

[2026-05-12T04:51:06.520Z] assistant line=88
------------------------------------------------------------------------------------------------
Right, agreed: “has some Intel device and a primary surface” is still too broad. I’ll tighten the gate to the actual Intel GPGPU path and UHD770-class device identity, then verify it compiles.

[2026-05-12T04:51:06.523Z] tool-call:exec_command line=89
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"UHD|770|0x4680|4680|device_id|GpgpuPreflightStatus|gpgpu_preflight_status|has_claimed_device|claimed_device|IntelDevice|Dev\" src/intel src/gfx src/r crates -g '*.rs'","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":24000}

[2026-05-12T04:51:06.524Z] tool-call:exec_command line=90
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '300,430p' src/intel/gpgpu.rs && sed -n '430,470p' src/intel/mod.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":16000}

[2026-05-12T04:51:06.542Z] tool-call:exec_command line=91
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '90,130p' src/gfx/backends/intel.rs && sed -n '350,370p' src/gfx/backends/intel.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":10000}

[2026-05-12T04:51:06.574Z] tool-output line=93
------------------------------------------------------------------------------------------------
Chunk ID: 2751e2
Wall time: 0.0001 seconds
Process exited with code 0
Original token count: 22908
Output:
Total output lines: 943

src/r/stream.rs:218:    disk: crate::disc::block::DeviceHandle,
src/r/stream.rs:227:        disk: crate::disc::block::DeviceHandle,
src/r/stream.rs:360:    disk: crate::disc::block::DeviceHandle,
src/r/stream.rs:364:    pub const fn new(disk: crate::disc::block::DeviceHandle) -> Self {
src/r/stream.rs:414:    disk: crate::disc::block::DeviceHandle,
src/r/stream.rs:419:    pub const fn new(disk: crate::disc::block::DeviceHandle) -> Self {
src/r/stream.rs:474:    disk: crate::disc::block::DeviceHandle,
src/r/stream.rs:494:    disk: crate::disc::block::DeviceHandle,
src/r/stream.rs:608:    disk: crate::disc::block::DeviceHandle,
src/r/stream.rs:625:    disk: crate::disc::block::DeviceHandle,
src/intel/intel/mod.rs:7:// - PCI claim has reached `00:02.0 device=0x4680 rev=0x0C`.
src/intel/intel/mod.rs:76:static CLAIMED_DEVICE: Mutex<Option<Dev>> = Mutex::new(None);
src/intel/intel/mod.rs:143:pub(crate) struct Dev {
src/intel/intel/mod.rs:147:    pub(crate) device_id: u16,
src/intel/intel/mod.rs:152:unsafe impl Send for Dev {}
src/intel/intel/mod.rs:153:unsafe impl Sync for Dev {}
src/intel/intel/mod.rs:180:        dev.device_id,
src/intel/intel/mod.rs:298:pub fn has_claimed_device() -> bool {
src/intel/intel/mod.rs:302:pub(crate) fn claimed_device() -> Option<Dev> {
src/intel/intel/mod.rs:310:pub(crate) fn gpgpu_preflight_status() -> self::render::GpgpuPreflightStatus {
src/intel/intel/mod.rs:311:    self::render::gpgpu_preflight_status()
src/intel/intel/mod.rs:315:    self::guc::status(Dev {
src/intel/intel/mod.rs:319:        device_id: warm.device_id,
src/intel/intel/mod.rs:326:pub(crate) fn log_guc_submission_contract(dev: Dev, label: &'static str) {
src/intel/intel/mod.rs:378:fn find_dev() -> Option<Dev> {
src/intel/intel/mod.rs:402:                out = Some(Dev {
src/intel/intel/mod.rs:406:                    device_id: d.device,
src/intel/intel/mod.rs:417:fn forcewake(dev: Dev) {
src/intel/intel/mod.rs:434:fn map_ggtt_pages(dev: Dev, phys: u64, len: usize, gpu: u64) -> bool {
src/intel/intel/mod.rs:462:pub(crate) fn map_ggtt(dev: Dev, phys: u64, len: usize, gpu: u64) -> bool {
src/intel/intel/mod.rs:466:pub(crate) fn map_display_scanout_ggtt(dev: Dev, phys: u64, len: usize, gpu: u64) -> bool {
src/intel/intel/mod.rs:470:pub(crate) fn read_ggtt_pte(dev: Dev, gpu: u64) -> Option<u64> {
src/intel/intel/mod.rs:475:fn ggtt_invalidate(dev: Dev) {
src/intel/intel/mod.rs:478:pub(crate) fn mmio_read(dev: Dev, off: usize) -> u32 {
src/intel/intel/mod.rs:485:pub(crate) fn mmio_write(dev: Dev, off: usize, v: u32) {
src/intel/intel/mod.rs:490:fn wait_eq(dev: Dev, reg: usize, mask: u32, want: u32, n: usize) {
src/intel/display.rs:233:pub(crate) fn init_primary_boot_surface(dev: crate::intel::Dev) {
src/intel/display.rs:378:fn log_cpu_scanout_proof(dev: crate::intel::Dev, surface: PrimarySurface) {
src/intel/display.rs:444:fn log_transcoder_a_state(dev: crate::intel::Dev, label: &str) {
src/intel/display.rs:515:fn program_pipe_bottom_color(dev: crate::intel::Dev, pipe: PipeInfo, rgb: u32) {
src/intel/display.rs:530:    let dev = crate::intel::claimed_device()?;
src/intel/display.rs:671:    let Some(dev) = crate::intel::claimed_device() else {
src/intel/display.rs:882:    let Some(dev) = crate::intel::claimed_device() else {
src/intel/display.rs:1018:fn wait_for_pipe_next_frame(dev: crate::intel::Dev, pipe: PipeInfo) -> (u32, u32, usize) {
src/intel/display.rs:1032:    dev: crate::intel::Dev,
src/intel/display.rs:1048:    dev: crate::intel::Dev,
src/intel/display.rs:1093:    dev: crate::intel::Dev,
src/intel/display.rs:1160:fn log_primary_scanout_pte_window(dev: crate::intel::Dev, label: &str, byte_len: usize) {
src/intel/display.rs:1205:    dev: crate::intel::Dev,
src/intel/display.rs:1309:fn overlay_plane_needs_rearm(dev: crate::intel::Dev, surface: OverlaySurface) -> bool {
src/intel/display.rs:1328:fn arm_overlay_plane(dev: crate::intel::Dev, surface: OverlaySurface, reason: &str) -> bool {
src/intel/display.rs:1385:pub(super) fn active_pipe(dev: crate::intel::Dev) -> Option<PipeInfo> {
src/intel/display.rs:1409:fn log_pipe_scanout_probe(dev: crate::intel::Dev, label: &str) {
src/intel/display.rs:1431:fn log_primary_plane_probe(dev: crate::intel::Dev, pipe: PipeInfo, label: &str) {
src/intel/display.rs:1524:    dev: crate::intel::Dev,
src/intel/intel/xelp_media2_ngin.rs:701:fn snapshot_forcewake(dev
[... truncated in extract ...]

[2026-05-12T04:51:06.574Z] tool-output line=94
------------------------------------------------------------------------------------------------
Chunk ID: d6cca3
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1368
Output:
            );
        }
    }

    unsafe {
        core::ptr::write_bytes(warm.ring_virt, 0, warm.ring_len);
        core::ptr::write_bytes(warm.context_virt, 0, warm.context_len);
        core::ptr::write_bytes(warm.batch_virt, 0, warm.batch_len);
        core::ptr::write_bytes(warm.draw_state_virt, 0, warm.draw_state_len);
        core::ptr::write_bytes(warm.vertex_virt, 0, warm.vertex_len);
        core::ptr::write_bytes(warm.result_virt, 0, warm.result_len);
        core::ptr::write_bytes(warm.streamout_virt, 0, warm.streamout_len);
        core::ptr::write_bytes(warm.ppgtt_virt, 0, warm.ppgtt_len);
        if !warm.gpgpu_arena_virt.is_null() {
            core::ptr::write_bytes(warm.gpgpu_arena_virt, 0, warm.gpgpu_arena_len);
        }
    }

    *WARM_STATE.lock() = Some(warm);
    warm
}

fn warm_state() -> Option<RenderWarmState> {
    *WARM_STATE.lock()
}

fn init_minimal_ppgtt(warm: RenderWarmState) -> bool {
    if warm.ppgtt_virt.is_null() || warm.ppgtt_len < WARM_PPGTT_BYTES {
        return false;
    }

    let pml4_off = 0usize;
    let pdp_off = 4096usize;
    let pd_off = 8192usize;
    let pt_off = 12288usize;
    let pte_present_rw = crate::intel::GEN8_PAGE_PRESENT | GEN8_PAGE_RW;
    let pde_present_rw_uc = pte_present_rw | GEN8_PAGE_PWT | GEN8_PAGE_PCD;

    unsafe {
        core::ptr::write_bytes(warm.ppgtt_virt, 0, warm.ppgtt_len);
        let pml4 = warm.ppgtt_virt.add(pml4_off) as *mut u64;
        let pdp = warm.ppgtt_virt.add(pdp_off) as *mut u64;
        let pd = warm.ppgtt_virt.add(pd_off) as *mut u64;
        core::ptr::write_volatile(pml4, (warm.ppgtt_phys + pdp_off as u64) | pde_present_rw_uc);
        core::ptr::write_volatile(pdp, (warm.ppgtt_phys + pd_off as u64) | pde_present_rw_uc);
        for index in 0..PPGTT_PT_COUNT {
            let pt_phys = warm.ppgtt_phys + pt_off as u64 + (index as u64) * 4096;
            core::ptr::write_volatile(pd.add(index), pt_phys | pde_present_rw_uc);
        }
    }

    let ok = map_minimal_ppgtt_region(
        warm,
        GPU_VA_RING_BASE,
        warm.ring_phys,
        warm.ring_len,
        pte_present_rw,
    ) && map_minimal_ppgtt_region(
        warm,
        GPU_VA_CONTEXT_BASE,
        warm.context_phys,
        warm.context_len,
        pte_present_rw,
    ) && map_minimal_ppgtt_region(
        warm,
        GPU_VA_BATCH_BASE,
        warm.batch_phys,
        warm.batch_len,
        pte_present_rw,
    ) && map_minimal_ppgtt_region(
        warm,
        GPU_VA_DRAW_STATE_BASE,
        warm.draw_state_phys,
        warm.draw_state_len,
        pte_present_rw,
    ) && map_minimal_ppgtt_region(
        warm,
        GPU_VA_VERTEX_BASE,
        warm.vertex_phys,
        warm.vertex_len,
        pte_present_rw,
    ) && map_minimal_ppgtt_region(
        warm,
        GPU_VA_RESULT_BASE,
        warm.result_phys,
        warm.result_len,
        pte_present_rw,
    ) && map_minimal_ppgtt_region(
        warm,
        GPU_VA_STREAMOUT_BASE,
        warm.streamout_phys,
        warm.streamout_len,
        pte_present_rw,
    ) && (warm.gpgpu_arena_virt.is_null()
        || map_minimal_ppgtt_region(
            warm,
            GPU_VA_GPGPU_TILE_ARENA_BASE,
            warm.gpgpu_arena_phys,
            warm.gpgpu_arena_len,
            pte_present_rw,
        ))
        && super::display::primary_surface_gpgpu_marker_target().is_none_or(|target| {
            map_minimal_ppgtt_region(warm, target.gpu, target.phys, target.byte_len, pte_present_rw)
        });

    crate::intel::dma_flush(warm.ppgtt_virt, warm.ppgtt_len);
    ok
}

fn map_minimal_ppgtt_region(
    warm: RenderWarmState,
    gpu: u64,
    phys: u64,
    len: usize,
    entry_flags: u64,
) -> bool {
    let pt_off = 12288usize;
    for page in 0..len.div_ceil(4096) {
        let va_page = (gpu >> 12) + page as u64;
        let pd_index = ((va_page >> 9) & 0x1FF) as usize;
        let pt_index = (va_page & 0x1FF) as usize;
        if pd_index >= PPGTT_PT_COUNT {
            return false;
        }
        let pte_off = pt_off + pd_index * 4096 + pt_index * core::mem::size_of::<u64>();
        let pte = (phys + (page as u64) * 4096) & !0xFFF;
        unsafe {
            core::ptr::write_volatile(warm.ppgtt_virt.add(pte_off) as *mut u64, pte | entry_flags);
        }
    }
    true
pub fn guc_ready() -> bool {
    self::guc::ready()
}

pub(crate) fn guc_h2g_mmio_accepte
[... truncated in extract ...]

[2026-05-12T04:51:06.574Z] tool-output line=95
------------------------------------------------------------------------------------------------
Chunk ID: fa7236
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 544
Output:
    rcs_retry_after_present_seq: u32,
    rcs_present_failures: u32,
    next_image_gpu_addr: u64,
}

unsafe impl Send for IntelGfxBackend {}

impl IntelGfxBackend {
    pub fn init(framebuffers: Option<&'static crate::limine::FramebufferResponse>) -> Option<Self> {
        if !crate::intel::has_claimed_device() {
            return None;
        }

        let fb = *framebuffers?.framebuffers().first()?;
        if fb.memory_model != ::limine::framebuffer::FRAMEBUFFER_RGB
            || fb.bpp != 32
            || fb.address().is_null()
        {
            return None;
        }

        let framebuffer_width = fb.width() as u32;
        let framebuffer_height = fb.height() as u32;
        let (width, height) = crate::intel::active_scanout_dimensions()
            .unwrap_or((framebuffer_width, framebuffer_height));
        let pitch = fb.pitch() as usize;
        if framebuffer_width == 0
            || framebuffer_height == 0
            || pitch < framebuffer_width as usize * 4
        {
            return None;
        }

        let swapchain_desc = SwapchainDesc {
            format: ImageFormat::Rgbx8888,
            extent: trueos_gfx_core::Extent2D { width, height },
        };
        let screen_len = rgba_len(width, height)?;
        Some(Self {
            swapchain_desc,
            framebuffer_ptr: fb.addr() as *mut u8,
            crate::log!(
                "intel/gfx-backend: present seq={} rcs-present-failed failures={} cooldown_until_seq={} size={}x{}\n",
                self.present_seq,
                self.rcs_present_failures,
                self.rcs_retry_after_present_seq,
                copy_w,
                copy_h
            );
        }

        if !guc_ready && (self.present_seq <= 8 || self.present_seq.is_multiple_of(120)) {
            let guc_status = crate::intel::warm_state()
                .map(crate::intel::guc_status)
                .unwrap_or(0);
            crate::log!(
                "intel/gfx-backend: present seq={} waiting-for-guc status=0x{:08X} size={}x{}\n",
                self.present_seq,
                guc_status,
                copy_w,
                copy_h
            );

[2026-05-12T04:51:12.935Z] assistant line=97
------------------------------------------------------------------------------------------------
I found the repo already documents the active GPGPU milestone as ADL-S `8086:4680`, which is the UHD 770 target. I’m checking whether there’s already a public helper for that identity; if not, I’ll add a tiny one in the Intel facade and use it in the spawn gate.

[2026-05-12T04:51:12.935Z] assistant line=98
------------------------------------------------------------------------------------------------
I found the repo already documents the active GPGPU milestone as ADL-S `8086:4680`, which is the UHD 770 target. I’m checking whether there’s already a public helper for that identity; if not, I’ll add a tiny one in the Intel facade and use it in the spawn gate.

[2026-05-12T04:51:12.936Z] tool-call:exec_command line=99
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '120,180p' src/intel/mod.rs && sed -n '120,180p' src/intel/intel/mod.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":10000}

[2026-05-12T04:51:12.936Z] tool-call:exec_command line=100
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1680,1750p' src/intel/gpgpu.rs && sed -n '1240,1265p' src/intel/gpgpu.rs && sed -n '6500,6520p' src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":14000}

[2026-05-12T04:51:12.936Z] tool-call:exec_command line=101
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"device_is_gfx125|device_is_|gfx125|ADL-S|0x4680\" src/intel crates/trueos-eu/src -g '*.rs'","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":14000}

[2026-05-12T04:51:12.983Z] tool-output line=103
------------------------------------------------------------------------------------------------
Chunk ID: 8fd04e
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 933
Output:
    pub(crate) output_zeroed: bool,
    pub(crate) arena_mapped: bool,
    pub(crate) arena_gpu_base: u64,
    pub(crate) x_gpu: u64,
    pub(crate) row_gpu: u64,
    pub(crate) output_gpu: u64,
    pub(crate) x_bytes: usize,
    pub(crate) row_bytes: usize,
    pub(crate) output_bytes: usize,
    pub(crate) tile_rows: usize,
    pub(crate) k_dim: usize,
    pub(crate) output_first_bits: u32,
    pub(crate) output_nonzero_dwords: usize,
    pub(crate) output_expected_hits_lo64: u64,
    pub(crate) output_checksum: u64,
}

#[derive(Copy, Clone, Debug)]
pub(crate) struct GpgpuTileRowsStageProof {
    pub(crate) staged: bool,
    pub(crate) reason: &'static str,
    pub(crate) readback_ok: bool,
    pub(crate) output_zeroed: bool,
    pub(crate) output_gpu: u64,
    pub(crate) row_count: usize,
    pub(crate) row_bytes: usize,
    pub(crate) rows_checksum: u64,
    pub(crate) staged_rows_checksum: u64,
    pub(crate) output_nonzero_dwords: usize,
}

#[derive(Copy, Clone, Debug)]
pub(crate) struct GpgpuOneTileSentinelProof {
    pub(crate) submitted: bool,
    pub(crate) finished: bool,
    pub(crate) readback_ok: bool,
    pub(crate) reason: &'static str,
    pub(crate) program_name: &'static str,
    pub(crate) output_gpu: u64,
    pub(crate) sentinel: u32,
    pub(crate) output_first_before: u32,
    pub(crate) output_first_after: u32,
    pub(crate) output_nonzero_before: usize,
    pub(crate) output_nonzero_after: usize,
    pub(crate) output_hits_lo64: u64,
    pub(crate) dispatch_delta: u64,
    pub(crate) finish_marker: u32,
    pub(crate) expected_finish_marker: u32,
    pub(crate) batch_bytes: usize,
}

#[derive(Copy, Clone, Debug)]
pub(crate) struct GpgpuOneTileCompareProof {
    pub(crate) submitted: bool,
    pub(crate) finished: bool,
    pub(crate) readback_ok: bool,
    pub(crate) compare_ok: bool,
    pub(crate) reason: &'static str,
    pub(crate) program_name: &'static str,
    pub(crate) output_gpu: u64,
    pub(crate) gpu_value: u32,
    } else {
        crate::log!(
            "intel/media: task profile origin={} slot=? lapic=? core=? requested_slot={} queued_ms={} executor=0x{:016X}\n",
            origin,
            requested_slot,
            queued_ms,
            executor_ptr,
        );
    }
}

#[embassy_executor::task]
async fn media_boot_demo_task(requested_slot: u32, queued_at_ms: u64) {
    log_media_demo_task_profile("worker", requested_slot, queued_at_ms);
    crate::log!("intel/media: boot demo begin\n");
    let first_frame = run_media2_first_frame_async().await;
    crate::log!(
        "intel/media: boot demo first-frame origin=worker returned={}\n",
        first_frame.is_some() as u8,
    );
}

#[derive(Copy, Clone)]
pub(crate) struct Dev {
    pub(crate) bus: u8,
    pub(crate) slot: u8,
    pub(crate) function: u8,
    pub(crate) device_id: u16,
    pub(crate) revision_id: u8,
    pub(crate) mmio: *mut u8,
    pub(crate) mmio_len: usize,
}
unsafe impl Send for Dev {}
unsafe impl Sync for Dev {}
#[derive(Copy, Clone)]
pub(crate) struct Buf {
    pub(crate) phys: u64,
    pub(crate) virt: *mut u8,
    pub(crate) len: usize,
    pub(crate) gpu: u64,
    pub(crate) css_offset: usize,
    pub(crate) xfer_len: usize,
    pub(crate) private_data_size: usize,
    pub(crate) rsa_offset: usize,
    pub(crate) rsa_size: usize,
}

pub fn init_once() {
    if INIT.swap(true, Ordering::AcqRel) {
        return;
    }
    let Some(dev) = find_dev() else {
        crate::log!("intel: no Intel display-class PCI device claimed\n");
        return;
    };
    crate::log!(
        "intel: claimed {:02X}:{:02X}.{} device=0x{:04X} rev=0x{:02X} mmio_len=0x{:X}\n",
        dev.bus,
        dev.slot,
        dev.function,
        dev.device_id,

[2026-05-12T04:51:12.985Z] tool-output line=104
------------------------------------------------------------------------------------------------
Chunk ID: a31d47
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1484
Output:
static GPGPU_TILE_ARENA_STATUS_LOGGED: AtomicBool = AtomicBool::new(false);
static GPGPU_EU_KERNEL_UPLOADED: AtomicBool = AtomicBool::new(false);
static GPGPU_EU_WALKER_ENCODED: AtomicBool = AtomicBool::new(false);
static GPGPU_EU_WALKER_SUBMITTED: AtomicBool = AtomicBool::new(false);
static GPGPU_EU_WALKER_RETIRED: AtomicBool = AtomicBool::new(false);
static GPGPU_EU_DISPATCH_DELTA: AtomicU32 = AtomicU32::new(0);
static GPGPU_EU_C_STORE_VALUE: AtomicU32 = AtomicU32::new(0);
static GPGPU_EU_PROVEN_WALKER_RETIRED: AtomicBool = AtomicBool::new(false);
static GPGPU_EU_PROVEN_DISPATCH_DELTA: AtomicU32 = AtomicU32::new(0);
static GPGPU_EU_PROVEN_C_STORE_VALUE: AtomicU32 = AtomicU32::new(0);

#[derive(Copy, Clone, Debug)]
pub(crate) struct GpgpuPreflightStatus {
    pub(crate) submitted: bool,
    pub(crate) accepted: bool,
    pub(crate) completed: bool,
    pub(crate) guc_ready: bool,
    pub(crate) marker: u32,
    pub(crate) dot: u32,
    pub(crate) sum_a: u32,
    pub(crate) sum_b: u32,
    pub(crate) lanes: u32,
    pub(crate) min_burn_rows: usize,
    pub(crate) min_burn_k_dim: usize,
    pub(crate) arena_gpu_base: u64,
    pub(crate) arena_bytes: usize,
    pub(crate) tile_rows: usize,
    pub(crate) max_tiles: usize,
    pub(crate) enough_for_shape: bool,
    pub(crate) eu_kernel_uploaded: bool,
    pub(crate) eu_walker_encoded: bool,
    pub(crate) eu_walker_submitted: bool,
    pub(crate) eu_walker_retired: bool,
    pub(crate) eu_dispatch_delta: u32,
    pub(crate) eu_c_store_value: u32,
    pub(crate) eu_expected_store_value: u32,
    pub(crate) eu_program_name: &'static str,
    pub(crate) result_c_changed_by_eu: bool,
}

pub(crate) fn gpgpu_preflight_status() -> GpgpuPreflightStatus {
    let warm = warm_state();
    let arena_bytes = warm.map_or(0, |warm| warm.gpgpu_arena_len);
    let proven_retired = GPGPU_EU_PROVEN_WALKER_RETIRED.load(Ordering::Acquire);
    let proven_dispatch_delta = GPGPU_EU_PROVEN_DISPATCH_DELTA.load(Ordering::Acquire);
    let use_proven_static = proven_retired && proven_dispatch_delta != 0;
    let eu_dispatch_delta = if use_proven_static {
        proven_dispatch_delta
    } else {
        GPGPU_EU_DISPATCH_DELTA.load(Ordering::Acquire)
    };
    let eu_c_store_value = if use_proven_static {
        GPGPU_EU_PROVEN_C_STORE_VALUE.load(Ordering::Acquire)
    } else {
        GPGPU_EU_C_STORE_VALUE.load(Ordering::Acquire)
    };
    let active_program = selected_gpgpu_eu_program();
    GpgpuPreflightStatus {
        submitted: GPGPU_PREFLIGHT_SUBMITTED.load(Ordering::Acquire),
        accepted: GPGPU_PREFLIGHT_ACCEPTED.load(Ordering::Acquire),
        completed: GPGPU_PREFLIGHT_COMPLETED.load(Ordering::Acquire),
        guc_ready: crate::intel::guc_ready(),
        marker: GPGPU_PREFLIGHT_MARKER.load(Ordering::Acquire),
        dot: GPGPU_PREFLIGHT_DOT.load(Ordering::Acquire),
        sum_a: GPGPU_PREFLIGHT_SUM_A.load(Ordering::Acquire),
        sum_b: GPGPU_PREFLIGHT_SUM_B.load(Ordering::Acquire),
        lanes: GPGPU_PREFLIGHT_LANES_OBSERVED.load(Ordering::Acquire),
        min_burn_rows: GPGPU_BURN_MIN_ROWS,
        min_burn_k_dim: GPGPU_BURN_MIN_K_DIM,
        arena_gpu_base: gpgpu_arena_gpu_base(arena_bytes),
        arena_bytes,
const GPU_PROGRAM_SHARED_RAM_WRITE_EXPECTED: u32 = trueos_eu::gfx12::STORE_SENTINEL_U32;
const GPGPU_LOAD_DUMMY_CURBE: bool = true;
const GPGPU_DUMMY_CURBE_BYTES: usize = 64;
const GPGPU_CONTIGUOUS_VFE_IDD_WALKER: bool = false;
const GPGPU_MESA_POST_VFE_PIPE_CONTROL: bool = false;
const GPGPU_USE_MINIMAL_PPGTT: bool = true;
const GPGPU_BATCH_START_IN_PPGTT: bool = false;
// ADL-S 8086:4680 is Gfx12.0/Xe-LP.  Keep the active milestone probe as the
// smallest Mesa-shaped root thread: no CURBE payload, one SIMD8 group, TS EOT.
const ACTIVE_GFX12_EOT_VARIANT: trueos_eu::gfx12::Gfx12EotVariant =
    trueos_eu::gfx12::Gfx12EotVariant::TsR0ToG127Send1;
const GPGPU_ENABLE_SIP_EXCEPTIONS: bool = false;
const GPGPU_SIP_HANDLER_OFFSET_BYTES: usize = GPGPU_EU_KERNEL_OFFSET_BYTES + 0x200;
const GPGPU_SIP_HANDLER_VARIANT: trueos_eu::gfx12::Gfx12EotVariant =
    trueos_eu::gfx12::Gfx12EotVariant::TsR0ToG127Send1;
const GPGPU_USE_STATIC_DP4A_STORE_THEN_EOT: bool = true;
const GPGPU_USE_HDC_STORE_THEN_EOT: bool = true;
const GPGPU_USE_GFX125_COMPUTE_WALKER: bool = false;
const GPGPU_WALKER_GROUP_X_DIM_LADDER: &[u32] = &[186];
// T5 live4 proof cap: 4096 groups reti
[... truncated in extract ...]

[2026-05-12T04:51:12.987Z] tool-output line=105
------------------------------------------------------------------------------------------------
Chunk ID: 1fda88
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 2461
Output:
crates/trueos-eu/src/gfx12.rs:106:// MEDIA_INTERFACE_DESCRIPTOR_LOAD + GPGPU_WALKER on ADL-S 8086:4680.
crates/trueos-eu/src/gfx12.rs:154:// Thread Spawner in the same way, so this is not the ADL-S 8086:4680 EOT path.
crates/trueos-eu/src/gfx12.rs:160:    name: "gfx125-gateway-eot-r0-to-g127-dg2-assembled",
src/intel/intel/mod.rs:7:// - PCI claim has reached `00:02.0 device=0x4680 rev=0x0C`.
src/intel/hw_cursor.rs:755:        0x4680 | 0x4682 | 0x4688 | 0x468A | 0x468B | 0x4690 | 0x4692 | 0x4693 => {
src/intel/hw_cursor.rs:1318:        0x4680
src/intel/intel/render/warmup.rs:412:    apply_gfx125_raster_workarounds(dev);
src/intel/intel/render/warmup.rs:429:fn apply_gfx125_raster_workarounds(dev: crate::intel::Dev) {
src/intel/intel/render/warmup.rs:430:    if !device_is_gfx125(dev.device_id) {
src/intel/intel/render/warmup.rs:434:    // Mesa's gfx125 init path enables these TBIMR-related raster controls up
src/intel/intel/render/warmup.rs:438:    crate::intel::mmio_write(dev, CHICKEN_RASTER_2, gfx125_chicken_raster_2_value());
src/intel/intel/render/warmup.rs:443:            "intel/render: gfx125-raster-wa chicken_raster_2 before=0x{:08X} after=0x{:08X} tbimr_batch_override={} tbimr_open_batch={} tbimr_fast_clip={}\n",
src/intel/intel/render/warmup.rs:453:fn gfx125_chicken_raster_2_value() -> u32 {
src/intel/intel/render/warmup.rs:467:fn gfx125_slice_hash_config(warm: RenderWarmState) -> Gfx125SliceHashConfig {
src/intel/intel/render/warmup.rs:517:fn gfx125_logbase2_ceil(value: usize) -> usize {
src/intel/intel/render/warmup.rs:525:fn gfx125_compute_pixel_hash_table_nway(
src/intel/intel/render/warmup.rs:554:    let bits = gfx125_logbase2_ceil(num_ids);
src/intel/intel/render/warmup.rs:618:fn gfx125_pack_slice_hash_tables(
src/intel/intel/render/warmup.rs:623:    gfx125_compute_pixel_hash_table_nway(config.ppipe_mask1, config.ppipe_mask2, &mut entries);
src/intel/intel/render/warmup.rs:636:fn gfx125_3d_mode_dw1(config: Gfx125SliceHashConfig) -> u32 {
src/intel/intel/render/warmup.rs:640:fn gfx125_3d_mode_dw3() -> u32 {
src/intel/intel/render/gpgpu.rs:9:// ADL-S 8086:4680 is Gfx12.0/Xe-LP.  The PRM-length GPGPU_WALKER now launches
src/intel/intel/render/gpgpu.rs:1070:// example DG2); the current baremetal target is 8086:4680 ADL-S GT1/UHD 770,
src/intel/intel/render/gpgpu.rs:1074:fn encode_gfx125_compute_walker_probe_batch(
src/intel/intel/hw_cursor.rs:755:        0x4680 | 0x4682 | 0x4688 | 0x468A | 0x468B | 0x4690 | 0x4692 | 0x4693 => {
src/intel/intel/hw_cursor.rs:1318:        0x4680
src/intel/intel/render/resources.rs:475:    if !device_is_gfx125(warm.device_id) {
src/intel/intel/render/resources.rs:493:    gfx125_pack_slice_hash_tables(gfx125_slice_hash_config(warm), &mut packed);
src/intel/intel/may_help/intel_igpu770.rs:17:const INTEL_IGPU770_DEVICE_ID: u16 = 0x4680;
src/intel/intel/render/primary.rs:1124:fn oa_a_counter_gfx125(report: &[u32], index: usize) -> Option<u64> {
src/intel/intel/render/primary.rs:1147:fn oa_a_delta_gfx125(begin: &[u32], end: &[u32], index: usize) -> u64 {
src/intel/intel/render/primary.rs:1148:    let Some(before) = oa_a_counter_gfx125(begin, index) else {
src/intel/intel/render/primary.rs:1151:    let Some(after) = oa_a_counter_gfx125(end, index) else {
src/intel/intel/render/primary.rs:1182:                oa_a_delta_gfx125(begin, end, 6),
src/intel/intel/render/primary.rs:1183:                oa_a_delta_gfx125(begin, end, 21).saturating_mul(4),
src/intel/intel/render/primary.rs:1184:                oa_a_delta_gfx125(begin, end, 24).saturating_mul(4),
src/intel/intel/render/primary.rs:1185:                oa_a_delta_gfx125(begin, end, 25).saturating_mul(4),
src/intel/intel/render/primary.rs:1194:            oa_a_delta_gfx125(begin, end, 26).saturating_mul(4),
src/intel/intel/render/primary.rs:1195:            oa_a_delta_gfx125(begin, end, 27).saturating_mul(4),
src/intel/mod.rs:7:// - PCI claim has reached `00:02.0 device=0x4680 rev=0x0C`.
src/intel/intel/may_help/intel_old.rs:10:const INTEL_IGPU770_DEVICE_ID: u16 = 0x4680;
src/intel/intel/render/lrc.rs:489:        0x4680 | 0x4682 | 0x4688 | 0x468A | 0x468B | 0x4690 | 0x4692 | 0x4693 => CursorPlaneCaps {
src/intel/intel/render/lrc.rs:490:            platform: "ADL-S",
src/intel/intel/render/lrc.rs:516:        0x4680 | 0x4682 | 0x4688 | 0x468A | 0x468B | 0x4690 | 0x4692 | 0x4693 => SpritePlaneCaps {
src/intel/intel/render/lrc.rs:51
[... truncated in extract ...]

[2026-05-12T04:51:18.305Z] tool-call:exec_command line=107
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1,40p' src/intel/mod.rs && sed -n '140,180p' src/intel/mod.rs && sed -n '438,455p' src/intel/mod.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":12000}

[2026-05-12T04:51:18.310Z] tool-call:exec_command line=108
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1,40p' src/intel/intel/mod.rs && sed -n '298,315p' src/intel/intel/mod.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":12000}

[2026-05-12T04:51:18.329Z] tool-call:exec_command line=109
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1745,1770p' src/intel/gpgpu.rs && sed -n '755,765p' src/intel/hw_cursor.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":8000}

[2026-05-12T04:51:18.348Z] tool-output line=111
------------------------------------------------------------------------------------------------
Chunk ID: 3de6c8
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 823
Output:
// Intel bring-up source contracts.
//
// These comments intentionally mirror `may_help/stateoftheart`, but live next
// to the code so the documentary facts do not drift away from the driver.
//
// Current proven hardware claim:
// - PCI claim has reached `00:02.0 device=0x4680 rev=0x0C`.
// - The observed MMIO aperture is `0x1000000` bytes.
//
// This module proves only ownership, MMIO bounds, shared address constants, and
// top-level sequencing.  It does not prove render progress; render/display/GuC
// modules must each emit their own named proof lines.

mod display;
pub(crate) mod format;
mod gpgpu;
mod guc;
pub mod hda;
mod hw_cursor;
pub(crate) mod state;
pub(crate) mod stats;
pub(crate) mod xelp_media2_ngin;
pub(crate) mod xelp_media_h264src;
pub(crate) mod xelp_media_matroska;
pub(crate) mod xelp_media_mp4;
pub(crate) mod xelp_media_source;

use core::sync::atomic::{AtomicBool, Ordering};
use embassy_executor::SendSpawner;
use embassy_time::{Duration as EmbassyDuration, Timer};
use spin::Mutex;

pub(crate) const INTEL_VENDOR_ID: u16 = 0x8086;
pub(crate) const PCI_CLASS_DISPLAY: u8 = 0x03;
pub(crate) const GPU_VA_GUC_FW_BASE: u64 = 0x0085_0000;
pub(crate) const GPU_VA_GUC_ADS_BASE: u64 = 0x0100_0000;
pub(crate) const GPU_VA_DISPLAY_PRIMARY_BASE: u64 = 0x0200_0000;
pub(crate) const GPU_VA_DISPLAY_OVERLAY_BASE: u64 = 0x0300_0000;
pub(crate) const GPU_VA_DISPLAY_CURSOR_BASE: u64 = 0x0600_0000;
pub(crate) const WARM_ALIGN: usize = 4096;
    pub(crate) reason: &'static str,
    pub(crate) readback_ok: bool,
    pub(crate) output_zeroed: bool,
    pub(crate) output_gpu: u64,
    pub(crate) row_count: usize,
    pub(crate) row_bytes: usize,
    pub(crate) rows_checksum: u64,
    pub(crate) staged_rows_checksum: u64,
    pub(crate) output_nonzero_dwords: usize,
}

#[derive(Copy, Clone, Debug)]
pub(crate) struct GpgpuOneTileSentinelProof {
    pub(crate) submitted: bool,
    pub(crate) finished: bool,
    pub(crate) readback_ok: bool,
    pub(crate) reason: &'static str,
    pub(crate) program_name: &'static str,
    pub(crate) output_gpu: u64,
    pub(crate) sentinel: u32,
    pub(crate) output_first_before: u32,
    pub(crate) output_first_after: u32,
    pub(crate) output_nonzero_before: usize,
    pub(crate) output_nonzero_after: usize,
    pub(crate) output_hits_lo64: u64,
    pub(crate) dispatch_delta: u64,
    pub(crate) finish_marker: u32,
    pub(crate) expected_finish_marker: u32,
    pub(crate) batch_bytes: usize,
}

#[derive(Copy, Clone, Debug)]
pub(crate) struct GpgpuOneTileCompareProof {
    pub(crate) submitted: bool,
    pub(crate) finished: bool,
    pub(crate) readback_ok: bool,
    pub(crate) compare_ok: bool,
    pub(crate) reason: &'static str,
    pub(crate) program_name: &'static str,
    pub(crate) output_gpu: u64,
    pub(crate) gpu_value: u32,
pub fn has_claimed_device() -> bool {
    CLAIMED_DEVICE.lock().is_some()
}

pub(crate) fn claimed_device() -> Option<Dev> {
    *CLAIMED_DEVICE.lock()
}

pub fn warm_state() -> Option<RenderWarmState> {
    claimed_device().map(|dev| RenderWarmState {
        device_id: dev.device_id,
        revision_id: dev.revision_id,
        mmio_base: dev.mmio as usize,
        mmio_len: dev.mmio_len,
    })
}

pub(crate) fn gpgpu_preflight_status() -> GpgpuPreflightStatus {

[2026-05-12T04:51:18.348Z] tool-output line=112
------------------------------------------------------------------------------------------------
Chunk ID: 8ff5c3
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 479
Output:
// Intel bring-up source contracts.
//
// These comments intentionally mirror `may_help/stateoftheart`, but live next
// to the code so the documentary facts do not drift away from the driver.
//
// Current proven hardware claim:
// - PCI claim has reached `00:02.0 device=0x4680 rev=0x0C`.
// - The observed MMIO aperture is `0x1000000` bytes.
//
// This module proves only ownership, MMIO bounds, shared address constants, and
// top-level sequencing.  It does not prove render progress; render/display/GuC
// modules must each emit their own named proof lines.

mod display;
pub(crate) mod format;
mod guc;
pub mod hda;
mod hw_cursor;
mod render;
pub(crate) mod shader;
pub(crate) mod state;
pub(crate) mod stats;
pub(crate) mod xelp_media2_ngin;
pub(crate) mod xelp_media_h264src;
pub(crate) mod xelp_media_matroska;
pub(crate) mod xelp_media_mp4;
pub(crate) mod xelp_media_source;

use core::sync::atomic::{AtomicBool, Ordering};
use embassy_executor::SendSpawner;
use embassy_time::{Duration as EmbassyDuration, Timer};
use spin::Mutex;

pub(crate) const INTEL_VENDOR_ID: u16 = 0x8086;
pub(crate) const PCI_CLASS_DISPLAY: u8 = 0x03;
pub(crate) const GPU_VA_GUC_FW_BASE: u64 = 0x0085_0000;
pub(crate) const GPU_VA_GUC_ADS_BASE: u64 = 0x0100_0000;
pub(crate) const GPU_VA_DISPLAY_PRIMARY_BASE: u64 = 0x0200_0000;
pub(crate) const GPU_VA_DISPLAY_OVERLAY_BASE: u64 = 0x0300_0000;
pub(crate) const GPU_VA_DISPLAY_CURSOR_BASE: u64 = 0x0600_0000;
pub fn has_claimed_device() -> bool {
    CLAIMED_DEVICE.lock().is_some()
}

pub(crate) fn claimed_device() -> Option<Dev> {
    *CLAIMED_DEVICE.lock()
}

pub fn warm_state() -> Option<self::render::RenderWarmState> {
    self::render::warm_state()
}

pub(crate) fn gpgpu_preflight_status() -> self::render::GpgpuPreflightStatus {
    self::render::gpgpu_preflight_status()
}

pub fn guc_status(warm: self::render::RenderWarmState) -> u32 {
    self::guc::status(Dev {

[2026-05-12T04:51:18.362Z] tool-output line=113
------------------------------------------------------------------------------------------------
Chunk ID: 8929d6
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 371
Output:
        sum_b: GPGPU_PREFLIGHT_SUM_B.load(Ordering::Acquire),
        lanes: GPGPU_PREFLIGHT_LANES_OBSERVED.load(Ordering::Acquire),
        min_burn_rows: GPGPU_BURN_MIN_ROWS,
        min_burn_k_dim: GPGPU_BURN_MIN_K_DIM,
        arena_gpu_base: gpgpu_arena_gpu_base(arena_bytes),
        arena_bytes,
        tile_rows: GPGPU_TILE_ROWS,
        max_tiles: gpgpu_arena_max_tiles(arena_bytes),
        enough_for_shape: gpgpu_arena_enough_for_shape(arena_bytes),
        eu_kernel_uploaded: GPGPU_EU_KERNEL_UPLOADED.load(Ordering::Acquire),
        eu_walker_encoded: GPGPU_EU_WALKER_ENCODED.load(Ordering::Acquire),
        eu_walker_submitted: GPGPU_EU_WALKER_SUBMITTED.load(Ordering::Acquire),
        eu_walker_retired: if use_proven_static {
            true
        } else {
            GPGPU_EU_WALKER_RETIRED.load(Ordering::Acquire)
        },
        eu_dispatch_delta,
        eu_c_store_value,
        eu_expected_store_value: active_program.expected_store_value,
        eu_program_name: active_program.name,
        result_c_changed_by_eu: eu_dispatch_delta != 0
            && eu_c_store_value == active_program.expected_store_value,
    }
}

        0x4680 | 0x4682 | 0x4688 | 0x468A | 0x468B | 0x4690 | 0x4692 | 0x4693 => {
            KERNEL_CURSOR_DBUF_BLOCKS_ADL_S
        }
        _ => return None,
    };

    let start = total_blocks.checked_sub(KERNEL_CURSOR_DBUF_BLOCKS_ONE_PIPE)?;
    Some(encode_dbuf_cfg(start, total_blocks))
}

fn probe_cursor_pos_write(

[2026-05-12T04:51:27.435Z] tool-call:exec_command line=115
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"submit_gpgpu_preflight|gpgpu_preflight_status|GPGPU_PREFLIGHT|eu_walker_retired|result_c_changed_by_eu\" src -g '*.rs'","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":20000}

[2026-05-12T04:51:27.438Z] tool-call:exec_command line=116
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '5600,5665p' src/intel/gpgpu.rs && sed -n '5665,5735p' src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":20000}

[2026-05-12T04:51:27.456Z] tool-call:exec_command line=117
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"run_gpgpu|gpgpu.*task|gpgpu.*probe|gpu-program-proof|submit_gpgpu\" src/intel src/r src/lumen -g '*.rs'","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":20000}

[2026-05-12T04:51:27.483Z] tool-output line=119
------------------------------------------------------------------------------------------------
Chunk ID: 2c2bde
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 3875
Output:
src/lumen/gpu_shadow.rs:36:    let gpu = crate::intel::gpgpu_preflight_status();
src/lumen/gpu_shadow.rs:40:        gpu.eu_walker_retired && gpu.result_c_changed_by_eu && gpu.eu_dispatch_delta != 0;
src/lumen/gpu_shadow.rs:189:    let gpu = crate::intel::gpgpu_preflight_status();
src/lumen/burn_baba.rs:200:    let gpu = crate::intel::gpgpu_preflight_status();
src/lumen/burn_baba.rs:285:    } else if gpu.result_c_changed_by_eu {
src/lumen/burn_baba.rs:287:    } else if gpu.eu_walker_retired {
src/lumen/burn_baba.rs:332:    } else if gpu.result_c_changed_by_eu {
src/lumen/burn_baba.rs:334:    } else if gpu.eu_walker_retired {
src/lumen/burn_baba.rs:345:    } else if gpu.result_c_changed_by_eu {
src/lumen/burn_baba.rs:347:    } else if gpu.eu_walker_retired {
src/lumen/burn_baba.rs:357:        "burn-baba: gpgpu-dispatch-gate director={} role={} cgp_backend={} output_owner={} enabled={} h2g_mmio={} input_buffers_ab_in_ggtt={} ctb_enabled=0 guc_context_registered=0 guc_sched_enabled=0 eu_kernel_uploaded={} eu_walker_encoded={} eu_walker_submitted={} eu_walker_retired={} eu_execution_runs={} eu_dispatch_delta={} result_c_slot={} result_c_value=0x{:08X} result_c_changed_by_eu={} cpu_reads_c_back={} arena_ready={} cpu_reference_compare=1 dispatch=disabled blocker={} next={} net_gpu_role={} net_gpu_action=deferred does_not_prove=gpu_matmul\n",
src/lumen/burn_baba.rs:368:        gpu.eu_walker_retired as u8,
src/lumen/burn_baba.rs:373:        gpu.result_c_changed_by_eu as u8,
src/intel/intel/mod.rs:234:    self::render::submit_gpgpu_preflight_once();
src/intel/intel/mod.rs:310:pub(crate) fn gpgpu_preflight_status() -> self::render::GpgpuPreflightStatus {
src/intel/intel/mod.rs:311:    self::render::gpgpu_preflight_status()
src/intel/mod.rs:107:    pub(crate) eu_walker_retired: bool,
src/intel/mod.rs:112:    pub(crate) result_c_changed_by_eu: bool,
src/intel/mod.rs:386:    self::gpgpu::submit_gpgpu_preflight_once();
src/intel/mod.rs:455:pub(crate) fn gpgpu_preflight_status() -> GpgpuPreflightStatus {
src/intel/mod.rs:456:    let status = self::gpgpu::gpgpu_preflight_status();
src/intel/mod.rs:477:        eu_walker_retired: status.eu_walker_retired,
src/intel/mod.rs:482:        result_c_changed_by_eu: status.result_c_changed_by_eu,
src/intel/intel/render/constants.rs:143:const RCS_EXEC_RESULT_GPGPU_PREFLIGHT_DONE: u32 = 0xC0DE_7731;
src/intel/intel/render/constants.rs:326:const RESULT_SLOT_GPGPU_PREFLIGHT_MARKER_DWORD: usize = 16;
src/intel/intel/render/constants.rs:327:const RESULT_SLOT_GPGPU_PREFLIGHT_DOT_DWORD: usize = 17;
src/intel/intel/render/constants.rs:328:const RESULT_SLOT_GPGPU_PREFLIGHT_SUM_A_DWORD: usize = 18;
src/intel/intel/render/constants.rs:329:const RESULT_SLOT_GPGPU_PREFLIGHT_SUM_B_DWORD: usize = 19;
src/intel/intel/render/constants.rs:330:const RESULT_SLOT_GPGPU_PREFLIGHT_LANES_DWORD: usize = 20;
src/intel/intel/render/submit.rs:116:            RESULT_SLOT_GPGPU_PREFLIGHT_MARKER_DWORD => {
src/intel/intel/render/submit.rs:117:                read_result_dword(warm, RESULT_SLOT_GPGPU_PREFLIGHT_MARKER_DWORD)
src/intel/intel/render/gpgpu.rs:1:const GPGPU_PREFLIGHT_LANES: usize = 4;
src/intel/intel/render/gpgpu.rs:69:static GPGPU_PREFLIGHT_SUBMITTED: AtomicBool = AtomicBool::new(false);
src/intel/intel/render/gpgpu.rs:70:static GPGPU_PREFLIGHT_ACCEPTED: AtomicBool = AtomicBool::new(false);
src/intel/intel/render/gpgpu.rs:71:static GPGPU_PREFLIGHT_COMPLETED: AtomicBool = AtomicBool::new(false);
src/intel/intel/render/gpgpu.rs:72:static GPGPU_PREFLIGHT_MARKER: AtomicU32 = AtomicU32::new(0);
src/intel/intel/render/gpgpu.rs:73:static GPGPU_PREFLIGHT_DOT: AtomicU32 = AtomicU32::new(0);
src/intel/intel/render/gpgpu.rs:74:static GPGPU_PREFLIGHT_SUM_A: AtomicU32 = AtomicU32::new(0);
src/intel/intel/render/gpgpu.rs:75:static GPGPU_PREFLIGHT_SUM_B: AtomicU32 = AtomicU32::new(0);
src/intel/intel/render/gpgpu.rs:76:static GPGPU_PREFLIGHT_LANES_OBSERVED: AtomicU32 = AtomicU32::new(0);
src/intel/intel/render/gpgpu.rs:108:    pub(crate) eu_walker_retired: bool,
src/intel/intel/render/gpgpu.rs:111:    pub(crate) result_c_changed_by_eu: bool,
src/intel/intel/render/gpgpu.rs:114:pub(crate) fn gpgpu_preflight_status() -> GpgpuPreflightStatus {
src/intel/intel/render/gpgpu.rs:120:        submitted: GPGPU_PREFLIGHT_SUBMITTED.load(Ordering::Acquire),
src/intel/intel/render/gpgpu.rs:121:        accepted: GPGPU_PREFLIGHT_ACCEPTED.loa
[... truncated in extract ...]

[2026-05-12T04:51:27.483Z] tool-output line=120
------------------------------------------------------------------------------------------------
Chunk ID: 0413e5
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1197
Output:
    mapped
}

fn ensure_gpgpu_tile_arena_mapped(dev: crate::intel::Dev, warm: RenderWarmState) -> bool {
    if GPGPU_TILE_ARENA_MAPPED.load(Ordering::Acquire) {
        return true;
    }
    if warm.gpgpu_arena_len == 0 {
        return false;
    }

    let mapped = super::map_ggtt(
        dev,
        warm.gpgpu_arena_phys,
        warm.gpgpu_arena_len,
        GPU_VA_GPGPU_TILE_ARENA_BASE,
    );
    if mapped {
        super::ggtt_invalidate(dev);
        GPGPU_TILE_ARENA_MAPPED.store(true, Ordering::Release);
    }
    mapped
}

fn log_gpgpu_tile_arena_status(_warm: RenderWarmState, _mapped: bool) {
    let _ = GPGPU_TILE_ARENA_STATUS_LOGGED.swap(true, Ordering::AcqRel);
}

fn submit_gpgpu_preflight(dev: crate::intel::Dev, warm: RenderWarmState) -> bool {
    let a = [1u32, 2, 3, 4];
    let b = [10u32, 20, 30, 40];
    let sum_a = a.iter().copied().fold(0u32, u32::wrapping_add);
    let sum_b = b.iter().copied().fold(0u32, u32::wrapping_add);
    let dot = a
        .iter()
        .copied()
        .zip(b.iter().copied())
        .fold(0u32, |acc, (lhs, rhs)| acc.wrapping_add(lhs.wrapping_mul(rhs)));

    unsafe {
        core::ptr::write_bytes(warm.batch_virt, 0, warm.batch_len);
        core::ptr::write_bytes(warm.ring_virt, 0, warm.ring_len);
        core::ptr::write_bytes(warm.result_virt, 0, warm.result_len);

        let input_a = warm.vertex_virt as *mut u32;
        let input_b = warm.streamout_virt as *mut u32;
        for i in 0..GPGPU_PREFLIGHT_LANES {
            core::ptr::write_volatile(input_a.add(i), a[i]);
            core::ptr::write_volatile(input_b.add(i), b[i]);
        }
    }
    seed_result_debug_slots(warm);
    crate::intel::dma_flush(warm.vertex_virt, GPGPU_PREFLIGHT_LANES * core::mem::size_of::<u32>());
    crate::intel::dma_flush(
        warm.streamout_virt,
        GPGPU_PREFLIGHT_LANES * core::mem::size_of::<u32>(),
    );
    crate::intel::dma_flush(warm.result_virt, warm.result_len);

    let total_dwords = warm.batch_len / core::mem::size_of::<u32>();
    let batch =
        unsafe { core::slice::from_raw_parts_mut(warm.batch_virt as *mut u32, total_dwords) };
    let batch_tail_bytes = match encode_gpgpu_preflight_batch(batch, dot, sum_a, sum_b) {
        Ok(bytes) => bytes,
        Err(_reason) => return false,
    };
    };
    crate::intel::dma_flush(warm.batch_virt, batch_tail_bytes);

    let completed = submit_warm_render_batch(
        dev,
        warm,
        RCS_EXEC_RESULT_GPGPU_PREFLIGHT_DONE,
        RESULT_SLOT_GPGPU_PREFLIGHT_MARKER_DWORD,
        "gpgpu-preflight",
    );
    crate::intel::dma_flush(warm.result_virt, warm.result_len);

    let marker = read_result_dword(warm, RESULT_SLOT_GPGPU_PREFLIGHT_MARKER_DWORD);
    let gpu_dot = read_result_dword(warm, RESULT_SLOT_GPGPU_PREFLIGHT_DOT_DWORD);
    let gpu_sum_a = read_result_dword(warm, RESULT_SLOT_GPGPU_PREFLIGHT_SUM_A_DWORD);
    let gpu_sum_b = read_result_dword(warm, RESULT_SLOT_GPGPU_PREFLIGHT_SUM_B_DWORD);
    let gpu_lanes = read_result_dword(warm, RESULT_SLOT_GPGPU_PREFLIGHT_LANES_DWORD);
    let accepted = completed
        && marker == RCS_EXEC_RESULT_GPGPU_PREFLIGHT_DONE
        && gpu_dot == dot
        && gpu_sum_a == sum_a
        && gpu_sum_b == sum_b
        && gpu_lanes == GPGPU_PREFLIGHT_LANES as u32;
    GPGPU_PREFLIGHT_COMPLETED.store(completed, Ordering::Release);
    GPGPU_PREFLIGHT_ACCEPTED.store(accepted, Ordering::Release);
    GPGPU_PREFLIGHT_MARKER.store(marker, Ordering::Release);
    GPGPU_PREFLIGHT_DOT.store(gpu_dot, Ordering::Release);
    GPGPU_PREFLIGHT_SUM_A.store(gpu_sum_a, Ordering::Release);
    GPGPU_PREFLIGHT_SUM_B.store(gpu_sum_b, Ordering::Release);
    GPGPU_PREFLIGHT_LANES_OBSERVED.store(gpu_lanes, Ordering::Release);

    let _ = batch_tail_bytes;

    accepted
}

#[derive(Copy, Clone)]
struct GpgpuProgramArtifactProof {
    program_name: &'static str,
    expects_store: bool,
    program_uploaded: bool,
    walker_encoded: bool,
    result_changed_by_current_backend: bool,
    program_gpu: u64,
    program_bytes: usize,
    program_sig: u64,
    walker_gpu: u64,
    walker_bytes: usize,
}

#[derive(Copy, Clone)]
struct GpgpuStoreSurfaceState {
    ready: bool,
    binding_table_offset: usize,
    surface_state_offset: usize,
    binding_table_index: usize,
    surface_gpu: u64,
    target_gpu: u64,
    surface_dword0: u32,
    surface_dword2: u32,
    s
[... truncated in extract ...]

[2026-05-12T04:51:27.490Z] tool-output line=121
------------------------------------------------------------------------------------------------
Chunk ID: 95914b
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 4089
Output:
src/intel/intel/mod.rs:234:    self::render::submit_gpgpu_preflight_once();
src/lumen/gpu_shadow.rs:263:        let stage = crate::intel::stage_gpgpu_one_tile_record_probe(
src/lumen/gpu_shadow.rs:310:            let sentinel = crate::intel::submit_gpgpu_one_tile_output_sentinel_probe(
src/lumen/gpu_shadow.rs:339:            let compare = crate::intel::submit_gpgpu_one_tile_output_compare_probe(
src/lumen/gpu_shadow.rs:370:        let t5 = crate::intel::submit_gpgpu_t5_one_row_matvec_probe(
src/lumen/gpu_shadow.rs:413:        let t6 = crate::intel::submit_gpgpu_t6_one_row_matvec_probe(
src/lumen/gpu_shadow.rs:458:        let t61 = crate::intel::submit_gpgpu_t61_one_row_matvec_probe(
src/lumen/gpu_shadow.rs:540:            let t62_stage = crate::intel::stage_gpgpu_tile_record_rows_probe(
src/lumen/gpu_shadow.rs:577:            let t62 = crate::intel::submit_gpgpu_t62_partial_matvec_probe(
src/lumen/gpu_shadow.rs:632:            let t63 = crate::intel::submit_gpgpu_t63_accum16_hi_live32_partial_matvec_probe(
src/intel/gpgpu.rs:1771:pub(crate) fn submit_gpgpu_preflight_once() {
src/intel/gpgpu.rs:1808:    let accepted = submit_gpgpu_preflight(dev, warm);
src/intel/gpgpu.rs:1816:            let walker = submit_gpgpu_compute_walker_probe(dev, warm, scale_index, group_x_dim);
src/intel/gpgpu.rs:1849:pub(crate) fn stage_gpgpu_one_tile_record_probe(
src/intel/gpgpu.rs:2051:pub(crate) fn stage_gpgpu_tile_record_rows_probe(
src/intel/gpgpu.rs:2196:pub(crate) fn submit_gpgpu_one_tile_output_sentinel_probe(
src/intel/gpgpu.rs:2281:        encode_gfx12_gpgpu_walker_probe_batch(warm, batch, store_surface, program, 1);
src/intel/gpgpu.rs:2375:pub(crate) fn submit_gpgpu_sidequest_target_buffer_probe() -> crate::intel::GpgpuOneTileSentinelProof
src/intel/gpgpu.rs:2409:    let proof = submit_gpgpu_one_tile_output_sentinel_probe(
src/intel/gpgpu.rs:2445:pub(crate) fn submit_gpgpu_primary_scanout_marker_probe() -> crate::intel::GpgpuOneTileSentinelProof
src/intel/gpgpu.rs:2512:        match encode_gfx12_gpgpu_walker_probe_batch(warm, batch, store_surface, program, 1) {
src/intel/gpgpu.rs:2644:fn submit_gpgpu_primary_scanout_pixel_quiet(
src/intel/gpgpu.rs:2685:        match encode_gfx12_gpgpu_walker_probe_batch(warm, batch, store_surface, program, 1) {
src/intel/gpgpu.rs:2745:fn submit_gpgpu_primary_scanout_mandelbrot_strip(
src/intel/gpgpu.rs:2849:        match encode_gfx12_gpgpu_walker_probe_batch(warm, batch, store_surface, program, 1) {
src/intel/gpgpu.rs:2922:pub(crate) fn submit_gpgpu_primary_scanout_mandelbrot_preview(
src/intel/gpgpu.rs:2994:        let proof = submit_gpgpu_primary_scanout_mandelbrot_strip(
src/intel/gpgpu.rs:3070:pub(crate) fn submit_gpgpu_one_tile_output_compare_probe(
src/intel/gpgpu.rs:3178:        encode_gfx12_gpgpu_walker_probe_batch(warm, batch, store_surface, program, 1);
src/intel/gpgpu.rs:3307:fn submit_gpgpu_t5_store_only_control_probe(
src/intel/gpgpu.rs:3371:        encode_gfx12_gpgpu_walker_probe_batch(warm, batch, store_surface, program, 1);
src/intel/gpgpu.rs:3487:fn submit_gpgpu_t5_load_echo_probe(
src/intel/gpgpu.rs:3571:        encode_gfx12_gpgpu_walker_probe_batch(warm, batch, store_surface, program, 1);
src/intel/gpgpu.rs:3718:pub(crate) fn submit_gpgpu_t5_one_row_matvec_probe(
src/intel/gpgpu.rs:3724:    submit_gpgpu_one_row_matvec_probe_for(
src/intel/gpgpu.rs:3733:pub(crate) fn submit_gpgpu_t6_one_row_matvec_probe(
src/intel/gpgpu.rs:3739:    submit_gpgpu_one_row_matvec_probe_for(
src/intel/gpgpu.rs:3748:pub(crate) fn submit_gpgpu_t61_one_row_matvec_probe(
src/intel/gpgpu.rs:3754:    submit_gpgpu_one_row_matvec_probe_for(
src/intel/gpgpu.rs:3763:pub(crate) fn submit_gpgpu_t62_partial_matvec_probe(
src/intel/gpgpu.rs:3770:    submit_gpgpu_partial_matvec_probe_for(
src/intel/gpgpu.rs:3780:pub(crate) fn submit_gpgpu_t63_partial_matvec_probe(
src/intel/gpgpu.rs:3787:    submit_gpgpu_partial_matvec_probe_for(
src/intel/gpgpu.rs:3797:pub(crate) fn submit_gpgpu_t63_accum16_hi_live32_partial_matvec_probe(
src/intel/gpgpu.rs:3804:    submit_gpgpu_partial_matvec_probe_for(
src/intel/gpgpu.rs:4024:fn submit_gpgpu_partial_matvec_probe_for(
src/intel/gpgpu.rs:4220:    let batch_bytes = match encode_gfx12_gpgpu_walker_probe_batch(
src/intel/gpgpu.rs:4344:fn submit_gpgpu_one_row_matvec_probe_for(
src/intel/gpgpu.rs:4571:    log_gpgpu_t5_arena_store_probe(
src/intel/gpgpu.rs:4580:    submit_gpgpu_t5_s
[... truncated in extract ...]

[2026-05-12T04:51:32.438Z] tool-call:exec_command line=123
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '360,392p' src/intel/mod.rs && sed -n '220,238p' src/intel/intel/mod.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":8000}

[2026-05-12T04:51:32.446Z] tool-call:exec_command line=124
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1,60p' src/lumen/gpu_shadow.rs && sed -n '180,210p' src/lumen/gpu_shadow.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":10000}

[2026-05-12T04:51:32.495Z] tool-output line=126
------------------------------------------------------------------------------------------------
Chunk ID: d76a1d
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 526
Output:
        return;
    }
    ggtt_invalidate(dev);
    forcewake(dev);
    let ready = self::guc::bootstrap(dev, fw, ads);
    let status = self::guc::status(dev);
    let (bootrom, ukernel, auth) = self::guc::describe_status(status);
    crate::log!(
        "intel/guc: bootstrap ready={} status=0x{:08X} bootrom={} ukernel={} auth=0x{:X}\n",
        ready as u8,
        status,
        bootrom,
        ukernel,
        auth
    );
    if ready {
        self::guc::prove_h2g_mmio_once(dev, "boot-control-ctb-disable");
    }
    if DISPLAY_PLANE1_BOOT_DEMO_ENABLED {
        self::display::init_primary_boot_surface(dev);
    } else {
        crate::log!("intel/display: plane1 boot demo disabled\n");
    }
    crate::log!(
        "intel/render: disabled reason=primary-render-module-removed gpgpu_probe=enabled\n"
    );
    self::gpgpu::submit_gpgpu_preflight_once();
    crate::log!("intel/media: source warmup disabled trigger=trueosfs-root-mounted\n",);
    if MEDIA_BOOT_DEMO_ENABLED {
        crate::log!("intel/media: scheduled boot demo delay_ms={}\n", MEDIA_BOOT_DEMO_DELAY_MS);
        crate::wait::spawn_local_detached(async move {
            Timer::after(EmbassyDuration::from_millis(MEDIA_BOOT_DEMO_DELAY_MS)).await;
            let queued_at_ms = embassy_time::Instant::now().as_millis() as u64;
        self::guc::prove_h2g_mmio_once(dev, "boot-control-ctb-disable");
    }
    let warm = self::render::warm_once(dev);
    self::render::log_cursor_plane_info(warm);
    self::render::log_sprite_plane_info(warm);
    if DISPLAY_PLANE1_BOOT_DEMO_ENABLED {
        self::display::init_primary_boot_surface(dev);
    } else {
        crate::log!("intel/display: plane1 boot demo disabled\n");
    }
    if self::render::forcewake_render_acquire(warm) {
        self::render::forcewake_render_sanity(warm);
    }
    self::render::submit_primary_triangle_once();
    self::render::submit_gpgpu_preflight_once();
    if RENDER_BOOT_PROBE_LOOP_ENABLED {
        crate::log!(
            "intel/render: scheduled periodic probe interval_ms={}\n",
            RENDER_BOOT_PROBE_INTERVAL_MS

[2026-05-12T04:51:32.495Z] tool-output line=127
------------------------------------------------------------------------------------------------
Chunk ID: 0dde8f
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1008
Output:
use core::sync::atomic::{AtomicBool, AtomicU64, Ordering};

static SEEN_BF16_MATVECS: AtomicU64 = AtomicU64::new(0);
static LOGGED_SHADOW_PLAN: AtomicBool = AtomicBool::new(false);
static LOGGED_PROMPT_SHADOW_PLAN: AtomicBool = AtomicBool::new(false);
static LOGGED_STATIC_TILE_PROOF: AtomicBool = AtomicBool::new(false);
static LOGGED_T4_WAITING: AtomicBool = AtomicBool::new(false);
static LOGGED_T4_LIVE_ROW_PROBE: AtomicBool = AtomicBool::new(false);
static LOGGED_PROMPT_LIVE_ROW_PROBE: AtomicBool = AtomicBool::new(false);

// T6.2/T6.3 are 8-lane row-block artifacts. The cap is coordination-only:
// each block is restaged into the same tile-record prefix, so raising this
// increases proved row coverage without changing artifact math.
const T62_ROW_BLOCK_DISPATCH_BLOCK_CAP: usize = 8;

#[derive(Copy, Clone, Debug)]
pub(crate) struct LocalGpuProofPlan {
    pub(crate) source_label: &'static str,
    pub(crate) call_index: u64,
    pub(crate) candidate: bool,
    pub(crate) static_tile_proven: bool,
    pub(crate) lane_dispatch_count: u32,
    pub(crate) expected_store_value: u32,
    pub(crate) observed_store_value: u32,
    pub(crate) program_name: &'static str,
}

pub(crate) fn observe_bf16_matvec_call(
    source_label: &'static str,
    n_rows: usize,
    k_dim: usize,
    chunk_rows: usize,
    chunks: usize,
) -> LocalGpuProofPlan {
    let call_index = SEEN_BF16_MATVECS.fetch_add(1, Ordering::AcqRel) + 1;
    let gpu = crate::intel::gpgpu_preflight_status();
    let shape_candidate =
        n_rows >= gpu.min_burn_rows && k_dim >= gpu.min_burn_k_dim && chunk_rows != 0;
    let static_tile_proven =
        gpu.eu_walker_retired && gpu.result_c_changed_by_eu && gpu.eu_dispatch_delta != 0;
    let candidate = shape_candidate && static_tile_proven && gpu.enough_for_shape;
    let plan = LocalGpuProofPlan {
        source_label,
        call_index,
        candidate,
        static_tile_proven,
        lane_dispatch_count: gpu.eu_dispatch_delta,
        expected_store_value: gpu.eu_expected_store_value,
        observed_store_value: gpu.eu_c_store_value,
        program_name: gpu.eu_program_name,
    };

    let log_global = !LOGGED_SHADOW_PLAN.swap(true, Ordering::AcqRel);
    let log_prompt =
        source_label == "lumen-prompt" && !LOGGED_PROMPT_SHADOW_PLAN.swap(true, Ordering::AcqRel);
    if log_global || log_prompt {
        crate::log!(
            "lumen-gpu-proof: director-step step=2 backend=local-gpu source={} mode=proof-only call={} rows={} k_dim={} chunk_rows={} chunks={} min_rows={} min_k_dim={} arena_ready={} shape_candidate={} candidate={} static_tile_proven={} program={} lane_dispatch={} expected=0x{:08X} observed=0x{:08X} output_owner=cpu-ap action=no-output-ownership next=one-live-row-proof-compare\n",
            source_label,
            call_index,
        w_rowmajor_bf16.as_ptr() as usize,
        x.as_ptr() as usize,
        k_dim.saturating_mul(core::mem::size_of::<f32>()),
        x_checksum,
        row_checksum,
        static4.to_bits(),
        row0.to_bits()
    );

    let gpu = crate::intel::gpgpu_preflight_status();
    let proof_tile_rows = gpu.tile_rows.max(1);
    let armed_tiles = n_rows.div_ceil(proof_tile_rows).min(gpu.max_tiles);
    if armed_tiles == 0 {
        crate::log!(
            "lumen-gpu-proof: director-step step=5 backend=local-gpu mode=gpgpu-actual-work-tiles ready=0 reason=no-armed-tiles rows={} tile_rows={} arena_max_tiles={} action=hold-scale does_not_prove=full_model_matvec\n",
            n_rows,
            proof_tile_rows,
            gpu.max_tiles,
        );
        return crate::lumen::cgp::CgpBf16PrefixContribution::none();
    }

    let mut staged_tiles = 0usize;
    let mut t5_submitted_tiles = 0usize;
    let mut t5_finished_tiles = 0usize;
    let mut t5_compare_ok_tiles = 0usize;
    let mut t6_submitted_tiles = 0usize;
    let mut t6_finished_tiles = 0usize;
    let mut t6_compare_ok_tiles = 0usize;
    let mut t61_submitted_tiles = 0usize;
    let mut t61_finished_tiles = 0usize;

[2026-05-12T04:51:38.502Z] assistant line=129
------------------------------------------------------------------------------------------------
There is an existing GPGPU proof vocabulary already used by Lumen: `eu_walker_retired && result_c_changed_by_eu && eu_dispatch_delta != 0`. I’m going to reuse that as the “Intel GPGPU hardware path is on” test, and pair it with `device_id == 0x4680` so this sidequest only starts on the UHD 770 target.

[2026-05-12T04:51:38.502Z] assistant line=130
------------------------------------------------------------------------------------------------
There is an existing GPGPU proof vocabulary already used by Lumen: `eu_walker_retired && result_c_changed_by_eu && eu_dispatch_delta != 0`. I’m going to reuse that as the “Intel GPGPU hardware path is on” test, and pair it with `device_id == 0x4680` so this sidequest only starts on the UHD 770 target.

[2026-05-12T04:51:45.111Z] assistant line=135
------------------------------------------------------------------------------------------------
The gate now requires both the UHD 770 PCI device ID and the proven EU walker/store result. I’ll run the touched-file format check and `cargo check`; the latter caught the previous gate cleanly, so it’s the right smoke test here too.

[2026-05-12T04:51:45.111Z] assistant line=136
------------------------------------------------------------------------------------------------
The gate now requires both the UHD 770 PCI device ID and the proven EU walker/store result. I’ll run the touched-file format check and `cargo check`; the latter caught the previous gate cleanly, so it’s the right smoke test here too.

[2026-05-12T04:51:46.273Z] tool-output line=143
------------------------------------------------------------------------------------------------
Chunk ID: f15f08
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 354
Output:
diff --git a/src/r/spawn_service.rs b/src/r/spawn_service.rs
index 2c7426d7..910f0ae7 100644
--- a/src/r/spawn_service.rs
+++ b/src/r/spawn_service.rs
@@ -792,6 +792,24 @@ fn intel_cursor_service_gate() -> bool {
     crate::intel::has_claimed_device()
 }
 
+#[inline]
+fn mandelbrot_gpu_sidequest_gate() -> bool {
+    const UHD770_ADL_S_DEVICE_ID: u16 = 0x4680;
+
+    let uhd770_claimed = crate::intel::claimed_device()
+        .map(|dev| dev.device_id == UHD770_ADL_S_DEVICE_ID)
+        .unwrap_or(false);
+    let gpu = crate::intel::gpgpu_preflight_status();
+    let gpgpu_path_proven =
+        gpu.eu_walker_retired && gpu.result_c_changed_by_eu && gpu.eu_dispatch_delta != 0;
+
+    uhd770_claimed
+        && gpgpu_path_proven
+        && crate::intel::warm_state().is_some()
+        && crate::intel::active_scanout_dimensions().is_some()
+        && crate::intel::primary_surface_gpu_addr().is_some()
+}
+
 #[inline]
 fn ui2_core_task_gate() -> bool {
     true
@@ -1361,9 +1379,10 @@ static TASKS: [TaskSpec; 71] = [
         &GFX_TEXTURE_UPLOAD_SERVICE_STARTED,
         spawn_gfx_texture_upload_service,
     ),
-    TaskSpec::enabled(
+    TaskSpec::enabled_gated(
         "mandelbrot-gpu-sidequest",
-        0,
+        crate::r::readiness::GFX_BACKEND_READY,
+        mandelbrot_gpu_sidequest_gate,
         &MANDELBROT_GPU_SIDEQUEST_STARTED,
         spawn_mandelbrot_gpu_sidequest,
     ),

[2026-05-12T04:51:52.629Z] assistant line=149
------------------------------------------------------------------------------------------------
Yep, tightened it to that exact idea.

`mandelbrot-gpu-sidequest` now starts only when:

```rust
GFX_BACKEND_READY
&& claimed_device.device_id == 0x4680 // ADL-S / UHD 770
&& gpu.eu_walker_retired
&& gpu.result_c_changed_by_eu
&& gpu.eu_dispatch_delta != 0
&& warm_state exists
&& active scanout exists
&& primary surface GPU address exists
```

So it will no longer run just because some Intel/display path exists. It waits for the UHD 770 GPGPU proof path to be live first.

Changed: [src/r/spawn_service.rs](/home/t4ce/REPOS/TRUEOS/src/r/spawn_service.rs:796)

Verification passed:
- `rustfmt --check src/r/spawn_service.rs`
- `cargo check`

[2026-05-12T04:51:52.630Z] assistant line=150
------------------------------------------------------------------------------------------------
Yep, tightened it to that exact idea.

`mandelbrot-gpu-sidequest` now starts only when:

```rust
GFX_BACKEND_READY
&& claimed_device.device_id == 0x4680 // ADL-S / UHD 770
&& gpu.eu_walker_retired
&& gpu.result_c_changed_by_eu
&& gpu.eu_dispatch_delta != 0
&& warm_state exists
&& active scanout exists
&& primary surface GPU address exists
```

So it will no longer run just because some Intel/display path exists. It waits for the UHD 770 GPGPU proof path to be live first.

Changed: [src/r/spawn_service.rs](/home/t4ce/REPOS/TRUEOS/src/r/spawn_service.rs:796)

Verification passed:
- `rustfmt --check src/r/spawn_service.rs`
- `cargo check`

[2026-05-12T04:52:21.180Z] user line=155
------------------------------------------------------------------------------------------------
is there not a single higher level better marker

[2026-05-12T04:52:21.212Z] meta line=157
------------------------------------------------------------------------------------------------
id=019e1a85-0c3d-73e0-90f7-745fd2a0aca3 | cwd=/home/t4ce/REPOS/TRUEOS | source=vscode | git=9aeda8d1241d

[2026-05-12T04:52:28.592Z] assistant line=160
------------------------------------------------------------------------------------------------
Good question. I’ll look for an existing consolidated “local GPGPU usable” or “UHD770 path ready” helper/flag before keeping the gate as a handful of raw proof fields.

[2026-05-12T04:52:28.592Z] assistant line=161
------------------------------------------------------------------------------------------------
Good question. I’ll look for an existing consolidated “local GPGPU usable” or “UHD770 path ready” helper/flag before keeping the gate as a handful of raw proof fields.

[2026-05-12T04:52:28.596Z] tool-call:exec_command line=162
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"GPGPU.*READY|LOCAL_GPU|GPU.*READY|gpgpu.*ready|local.*gpu|candidate|static_tile_proven|eu_walker_retired|result_c_changed_by_eu|gpgpu_preflight_status\" src crates -g '*.rs'","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":24000}

[2026-05-12T04:52:28.614Z] tool-call:exec_command line=163
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '80,125p' src/intel/mod.rs && sed -n '455,485p' src/intel/mod.rs && sed -n '1690,1772p' src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":18000}

[2026-05-12T04:52:28.615Z] tool-call:exec_command line=164
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '30,50p' src/lumen/gpu_shadow.rs && sed -n '190,210p' src/lumen/burn_baba.rs && sed -n '275,292p' src/lumen/burn_baba.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":14000}

[2026-05-12T04:52:28.647Z] tool-output line=166
------------------------------------------------------------------------------------------------
Chunk ID: 62aa4b
Wall time: 0.0001 seconds
Process exited with code 0
Original token count: 11780
Output:
Total output lines: 383

src/shell2/shell2_surf.rs:42:    let candidate = strip_wrapping_quotes(line.trim());
src/shell2/shell2_surf.rs:43:    if !looks_like_inline_html(candidate) {
src/shell2/shell2_surf.rs:46:    Some(String::from(candidate))
src/shell2/shell2_surf.rs:57:    let candidate = strip_wrapping_quotes(line.trim());
src/shell2/shell2_surf.rs:58:    if candidate.is_empty() {
src/shell2/shell2_surf.rs:63:        SurfPromptPrefix::Html => Some(SurfSubmit::Html(String::from(candidate))),
src/shell2/shell2_surf.rs:64:        SurfPromptPrefix::File => Some(SurfSubmit::File(String::from(candidate))),
src/shell2/shell2_surf.rs:66:            if candidate.split_whitespace().nth(1).is_some() || !is_url_token(candidate) {
src/shell2/shell2_surf.rs:69:            Some(SurfSubmit::Url(prepare_url_with_prefix(candidate, prefix)))
src/shell2/shell2_surf.rs:75:    let candidate = strip_wrapping_quotes(line.trim());
src/shell2/shell2_surf.rs:76:    let path = candidate.strip_prefix("file://")?;
src/tst/mandelbrot_gpu_sidequest.rs:182:            crate::r::readiness::set(crate::r::readiness::MANDELBROT_GPU_SIDEQUEST_READY);
src/tst/fileexplorer/backend/src/main.rs:1270:fn is_descendant(root: &FileNode, ancestor_id: &str, candidate_id: &str) -> bool {
src/tst/fileexplorer/backend/src/main.rs:1275:    contains_node_below(&ancestor, candidate_id)
src/tst/fileexplorer/backend/src/main.rs:1278:fn contains_node_below(node: &FileNode, candidate_id: &str) -> bool {
src/tst/fileexplorer/backend/src/main.rs:1281:        .any(|child| child.id == candidate_id || contains_node_below(child, candidate_id))
src/tst/ui2_swarm.rs:188:        let mut candidate = out.clone();
src/tst/ui2_swarm.rs:189:        candidate.push(ch);
src/tst/ui2_swarm.rs:190:        candidate.push_str(ellipsis);
src/tst/ui2_swarm.rs:191:        if ui2::ui2_font_measure_text(tier, candidate.as_str()).width_px as usize > max_width_px {
src/phys.rs:151:/// Attempts to reserve and install a heap arena from a list of candidate sizes.
src/phys.rs:158:pub fn try_install_heap_arena_candidates<F>(mut install: F) -> bool
src/shell2/cmds/tlb.rs:1684:                    "  AML offset candidate: {} @ 0x{:016X} + 0x{:X}",
src/shell2/cmds/tlb.rs:1693:            line(io, alloc::format!("  ... {} more candidate hits", def_hits.len() - 8).as_str());
src/shell2/cmds/tlb.rs:2707:                        if let Some(full) = detailed_devices.iter().find(|candidate| {
src/shell2/cmds/tlb.rs:2708:                            candidate.stable_id == dev.stable_id && candidate.port_id == dev.port
src/shell2/cmds/tlb.rs:3140:                        if let Some(full) = detailed_devices.iter().find(|candidate| {
src/shell2/cmds/tlb.rs:3141:                            candidate.stable_id == dev.stable_id && candidate.port_id == dev.port
src/lumen/mod.rs:736:        "lumen: generation start prompt_tokens={} context_tokens={} max_new_tokens={} max_seq_len={} prefill_mode=incremental-decode-ap cgp_backend=local-gpgpu-burn-baby cgp_role=proof-only note=first-token-is-prompt-ingest\n",
src/lumen/mod.rs:2147:            log("bench lumen: real-hi skipped; no Hi candidate token in tokenizer vocab");
src/lumen/cgp.rs:42:        label: "local-gpgpu-burn-baby",
src/lumen/cgp.rs:139:    pub(crate) candidate_tiles: usize,
src/lumen/cgp.rs:158:    let candidate_tiles = if eligible {
src/lumen/cgp.rs:163:    let pilot_tiles = candidate_tiles.min(pilot_tile_cap).min(arena_max_tiles);
src/lumen/cgp.rs:169:        candidate_tiles,
src/t/net/dns.rs:1079:            let candidate = DnsIpv4Candidate { transport, ip };
src/t/net/dns.rs:1084:                    warn_secure_dns_disagreement(host_trimmed, dev_idx, first, candidate);
src/t/net/dns.rs:1087:                *first_healthy = Some(candidate);
src/t/net/dns.rs:1180:        .map(|candidate| candidate.ip)
src/t/net/dns.rs:1213:            let candidate = DnsIpv6Candidate { transport, ip };
src/t/net/dns.rs:1218:                    warn_secure_dns6_disagreement(host_trimmed, dev_idx, first, candidate);
src/t/net/dns.rs:1221:                *first_healthy = Some(candidate);
src/t/net/dns.rs:1319:        .map(|candidate| candidate.ip)
src/r/keyboard.rs:235:    key != 0 && keys.iter().copied().any(|candidate| candidate == key)
src/r/keyboard.rs:240:    ascii != 0 && emitted.iter().copied().any(|candidate| candidate == ascii)
src/r/keyboard.rs:249:            .an
[... truncated in extract ...]

[2026-05-12T04:52:28.649Z] tool-output line=167
------------------------------------------------------------------------------------------------
Chunk ID: a39faa
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1545
Output:
    pub device_id: u16,
    pub revision_id: u8,
    pub mmio_base: usize,
    pub mmio_len: usize,
}

#[derive(Copy, Clone, Debug)]
pub(crate) struct GpgpuPreflightStatus {
    pub(crate) submitted: bool,
    pub(crate) accepted: bool,
    pub(crate) completed: bool,
    pub(crate) guc_ready: bool,
    pub(crate) marker: u32,
    pub(crate) dot: u32,
    pub(crate) sum_a: u32,
    pub(crate) sum_b: u32,
    pub(crate) lanes: u32,
    pub(crate) min_burn_rows: usize,
    pub(crate) min_burn_k_dim: usize,
    pub(crate) arena_gpu_base: u64,
    pub(crate) arena_bytes: usize,
    pub(crate) tile_rows: usize,
    pub(crate) max_tiles: usize,
    pub(crate) enough_for_shape: bool,
    pub(crate) eu_kernel_uploaded: bool,
    pub(crate) eu_walker_encoded: bool,
    pub(crate) eu_walker_submitted: bool,
    pub(crate) eu_walker_retired: bool,
    pub(crate) eu_dispatch_delta: u32,
    pub(crate) eu_c_store_value: u32,
    pub(crate) eu_expected_store_value: u32,
    pub(crate) eu_program_name: &'static str,
    pub(crate) result_c_changed_by_eu: bool,
}

#[derive(Copy, Clone, Debug)]
pub(crate) struct GpgpuOneTileStageProof {
    pub(crate) staged: bool,
    pub(crate) reason: &'static str,
    pub(crate) readback_ok: bool,
    pub(crate) output_zeroed: bool,
    pub(crate) arena_mapped: bool,
    pub(crate) arena_gpu_base: u64,
    pub(crate) x_gpu: u64,
    pub(crate) row_gpu: u64,
    pub(crate) output_gpu: u64,
pub(crate) fn gpgpu_preflight_status() -> GpgpuPreflightStatus {
    let status = self::gpgpu::gpgpu_preflight_status();
    GpgpuPreflightStatus {
        submitted: status.submitted,
        accepted: status.accepted,
        completed: status.completed,
        guc_ready: status.guc_ready,
        marker: status.marker,
        dot: status.dot,
        sum_a: status.sum_a,
        sum_b: status.sum_b,
        lanes: status.lanes,
        min_burn_rows: status.min_burn_rows,
        min_burn_k_dim: status.min_burn_k_dim,
        arena_gpu_base: status.arena_gpu_base,
        arena_bytes: status.arena_bytes,
        tile_rows: status.tile_rows,
        max_tiles: status.max_tiles,
        enough_for_shape: status.enough_for_shape,
        eu_kernel_uploaded: status.eu_kernel_uploaded,
        eu_walker_encoded: status.eu_walker_encoded,
        eu_walker_submitted: status.eu_walker_submitted,
        eu_walker_retired: status.eu_walker_retired,
        eu_dispatch_delta: status.eu_dispatch_delta,
        eu_c_store_value: status.eu_c_store_value,
        eu_expected_store_value: status.eu_expected_store_value,
        eu_program_name: status.eu_program_name,
        result_c_changed_by_eu: status.result_c_changed_by_eu,
    }
}


#[derive(Copy, Clone, Debug)]
pub(crate) struct GpgpuPreflightStatus {
    pub(crate) submitted: bool,
    pub(crate) accepted: bool,
    pub(crate) completed: bool,
    pub(crate) guc_ready: bool,
    pub(crate) marker: u32,
    pub(crate) dot: u32,
    pub(crate) sum_a: u32,
    pub(crate) sum_b: u32,
    pub(crate) lanes: u32,
    pub(crate) min_burn_rows: usize,
    pub(crate) min_burn_k_dim: usize,
    pub(crate) arena_gpu_base: u64,
    pub(crate) arena_bytes: usize,
    pub(crate) tile_rows: usize,
    pub(crate) max_tiles: usize,
    pub(crate) enough_for_shape: bool,
    pub(crate) eu_kernel_uploaded: bool,
    pub(crate) eu_walker_encoded: bool,
    pub(crate) eu_walker_submitted: bool,
    pub(crate) eu_walker_retired: bool,
    pub(crate) eu_dispatch_delta: u32,
    pub(crate) eu_c_store_value: u32,
    pub(crate) eu_expected_store_value: u32,
    pub(crate) eu_program_name: &'static str,
    pub(crate) result_c_changed_by_eu: bool,
}

pub(crate) fn gpgpu_preflight_status() -> GpgpuPreflightStatus {
    let warm = warm_state();
    let arena_bytes = warm.map_or(0, |warm| warm.gpgpu_arena_len);
    let proven_retired = GPGPU_EU_PROVEN_WALKER_RETIRED.load(Ordering::Acquire);
    let proven_dispatch_delta = GPGPU_EU_PROVEN_DISPATCH_DELTA.load(Ordering::Acquire);
    let use_proven_static = proven_retired && proven_dispatch_delta != 0;
    let eu_dispatch_delta = if use_proven_static {
        proven_dispatch_delta
    } else {
        GPGPU_EU_DISPATCH_DELTA.load(Ordering::Acquire)
    };
    let eu_c_store_value = if use_proven_static {
        GPGPU_EU_PROVEN_C_STORE_VALUE.load(Ordering::Acquire)
    } else {
        GPGPU_EU_C_STORE_VALUE.load(Ordering::Acquire)
    };
[... truncated in extract ...]

[2026-05-12T04:52:28.650Z] tool-output line=168
------------------------------------------------------------------------------------------------
Chunk ID: e76e7b
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 621
Output:
    n_rows: usize,
    k_dim: usize,
    chunk_rows: usize,
    chunks: usize,
) -> LocalGpuProofPlan {
    let call_index = SEEN_BF16_MATVECS.fetch_add(1, Ordering::AcqRel) + 1;
    let gpu = crate::intel::gpgpu_preflight_status();
    let shape_candidate =
        n_rows >= gpu.min_burn_rows && k_dim >= gpu.min_burn_k_dim && chunk_rows != 0;
    let static_tile_proven =
        gpu.eu_walker_retired && gpu.result_c_changed_by_eu && gpu.eu_dispatch_delta != 0;
    let candidate = shape_candidate && static_tile_proven && gpu.enough_for_shape;
    let plan = LocalGpuProofPlan {
        source_label,
        call_index,
        candidate,
        static_tile_proven,
        lane_dispatch_count: gpu.eu_dispatch_delta,
        expected_store_value: gpu.eu_expected_store_value,
        observed_store_value: gpu.eu_c_store_value,
        program_name: gpu.eu_program_name,
    }
}

// BF16 matvec is a solid practical baseline for LLM decode, and it gives
// Lumen a simple shared CPU/GPU contract today. The real pinnacle is
// hardware-aware fused kernels using the lowest precision that preserves
// model quality, with batching and memory layout optimized for the workload.
pub(crate) fn share_matvec_rowmajor_bf16(n_rows: usize, k_dim: usize, chunk_rows: usize) {
    SEEN_BF16_MATVECS.fetch_add(1, Ordering::AcqRel);

    let gpu = crate::intel::gpgpu_preflight_status();
    let shape_candidate =
        n_rows >= gpu.min_burn_rows && k_dim >= gpu.min_burn_k_dim && chunk_rows != 0;
    if shape_candidate {
        GPU_SHAPE_CANDIDATES.fetch_add(1, Ordering::AcqRel);
    }

    let gpu_ready =
        LOCAL_GPGPU_PROOF_BACKEND_ENABLED && shape_candidate && gpu.accepted && gpu.guc_ready;
    if gpu_ready {
        GPU_READY_CANDIDATES.fetch_add(1, Ordering::AcqRel);
        gpu_budget.target_rows,
        n_rows,
        director.future_gpu_role.as_str(),
        director.future_net_gpu_deferred as u8,
        plan.next_kernel.as_str(),
        plan.next_precision.as_str(),
        plan.next_memory_layout.as_str(),
    );
    let pilot_reason = if !LOCAL_GPGPU_PROOF_BACKEND_ENABLED {
        "local-gpu-proof-disabled-by-selector"
    } else if gpu.result_c_changed_by_eu {
        "eu-c-store-proven-pilot-still-guarded"
    } else if gpu.eu_walker_retired {
        "eu-walker-retired-awaiting-c-store"
    } else if gpu.eu_walker_submitted {
        "eu-walker-not-retired"
    } else if gpu.eu_walker_encoded {
        "eu-walker-encoded-awaiting-submit"

[2026-05-12T04:52:38.665Z] assistant line=170
------------------------------------------------------------------------------------------------
There isn’t a single clean marker right now. The closest high-level concept exists only as repeated field logic: Lumen calls it `static_tile_proven`, and the spawn gate now repeats the same expression. I’m going to promote that into the Intel facade so the registry reads as one intent-level condition.

[2026-05-12T04:52:38.665Z] assistant line=171
------------------------------------------------------------------------------------------------
There isn’t a single clean marker right now. The closest high-level concept exists only as repeated field logic: Lumen calls it `static_tile_proven`, and the spawn gate now repeats the same expression. I’m going to promote that into the Intel facade so the registry reads as one intent-level condition.

[2026-05-12T04:52:44.768Z] user line=176
------------------------------------------------------------------------------------------------
<turn_aborted>
The user interrupted the previous turn on purpose. Any running unified exec processes may still be running in the background. If any tools/commands were aborted, they may have partially executed.
</turn_aborted>

[2026-05-12T04:52:52.578Z] user line=180
------------------------------------------------------------------------------------------------
no something abstract like, intel claimed or such

[2026-05-12T04:52:52.626Z] meta line=182
------------------------------------------------------------------------------------------------
id=019e1a85-0c3d-73e0-90f7-745fd2a0aca3 | cwd=/home/t4ce/REPOS/TRUEOS | source=vscode | git=9aeda8d1241d

[2026-05-12T04:53:05.536Z] assistant line=185
------------------------------------------------------------------------------------------------
Got it: you mean don’t bake a Mandelbrot-specific “UHD770 GPGPU proof” abstraction into Intel. Use an existing broad boot marker, like “Intel was claimed,” as the start gate. I’ll back out the helper I had started and simplify the task gate accordingly.

[2026-05-12T04:53:05.536Z] assistant line=186
------------------------------------------------------------------------------------------------
Got it: you mean don’t bake a Mandelbrot-specific “UHD770 GPGPU proof” abstraction into Intel. Use an existing broad boot marker, like “Intel was claimed,” as the start gate. I’ll back out the helper I had started and simplify the task gate accordingly.

[2026-05-12T04:53:05.537Z] tool-call:exec_command line=187
------------------------------------------------------------------------------------------------
{"cmd":"git diff -- src/intel/mod.rs src/r/spawn_service.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":16000}

[2026-05-12T04:53:05.560Z] tool-call:exec_command line=188
------------------------------------------------------------------------------------------------
{"cmd":"nl -ba src/intel/mod.rs | sed -n '96,122p;455,500p'","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":12000}

[2026-05-12T04:53:05.589Z] tool-output line=191
------------------------------------------------------------------------------------------------
Chunk ID: 23fd93
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 652
Output:
diff --git a/src/intel/mod.rs b/src/intel/mod.rs
index 57b8ec91..1daa3694 100644
--- a/src/intel/mod.rs
+++ b/src/intel/mod.rs
@@ -112,6 +112,12 @@ pub(crate) struct GpgpuPreflightStatus {
     pub(crate) result_c_changed_by_eu: bool,
 }
 
+impl GpgpuPreflightStatus {
+    pub(crate) const fn static_tile_proven(self) -> bool {
+        self.eu_walker_retired && self.result_c_changed_by_eu && self.eu_dispatch_delta != 0
+    }
+}
+
 #[derive(Copy, Clone, Debug)]
 pub(crate) struct GpgpuOneTileStageProof {
     pub(crate) staged: bool,
@@ -483,6 +489,20 @@ pub(crate) fn gpgpu_preflight_status() -> GpgpuPreflightStatus {
     }
 }
 
+pub(crate) fn uhd770_primary_scanout_gpgpu_ready() -> bool {
+    const UHD770_ADL_S_DEVICE_ID: u16 = 0x4680;
+
+    let uhd770_claimed = claimed_device()
+        .map(|dev| dev.device_id == UHD770_ADL_S_DEVICE_ID)
+        .unwrap_or(false);
+
+    uhd770_claimed
+        && gpgpu_preflight_status().static_tile_proven()
+        && warm_state().is_some()
+        && active_scanout_dimensions().is_some()
+        && primary_surface_gpu_addr().is_some()
+}
+
 pub(crate) fn stage_gpgpu_one_tile_record_probe(
     x: &[f32],
     row_bf16: &[u8],
diff --git a/src/r/spawn_service.rs b/src/r/spawn_service.rs
index 2c7426d7..910f0ae7 100644
--- a/src/r/spawn_service.rs
+++ b/src/r/spawn_service.rs
@@ -792,6 +792,24 @@ fn intel_cursor_service_gate() -> bool {
     crate::intel::has_claimed_device()
 }
 
+#[inline]
+fn mandelbrot_gpu_sidequest_gate() -> bool {
+    const UHD770_ADL_S_DEVICE_ID: u16 = 0x4680;
+
+    let uhd770_claimed = crate::intel::claimed_device()
+        .map(|dev| dev.device_id == UHD770_ADL_S_DEVICE_ID)
+        .unwrap_or(false);
+    let gpu = crate::intel::gpgpu_preflight_status();
+    let gpgpu_path_proven =
+        gpu.eu_walker_retired && gpu.result_c_changed_by_eu && gpu.eu_dispatch_delta != 0;
+
+    uhd770_claimed
+        && gpgpu_path_proven
+        && crate::intel::warm_state().is_some()
+        && crate::intel::active_scanout_dimensions().is_some()
+        && crate::intel::primary_surface_gpu_addr().is_some()
+}
+
 #[inline]
 fn ui2_core_task_gate() -> bool {
     true
@@ -1361,9 +1379,10 @@ static TASKS: [TaskSpec; 71] = [
         &GFX_TEXTURE_UPLOAD_SERVICE_STARTED,
         spawn_gfx_texture_upload_service,
     ),
-    TaskSpec::enabled(
+    TaskSpec::enabled_gated(
         "mandelbrot-gpu-sidequest",
-        0,
+        crate::r::readiness::GFX_BACKEND_READY,
+        mandelbrot_gpu_sidequest_gate,
         &MANDELBROT_GPU_SIDEQUEST_STARTED,
         spawn_mandelbrot_gpu_sidequest,
     ),

[2026-05-12T04:53:05.595Z] tool-output line=192
------------------------------------------------------------------------------------------------
Chunk ID: 215f30
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 779
Output:
    96	    pub(crate) lanes: u32,
    97	    pub(crate) min_burn_rows: usize,
    98	    pub(crate) min_burn_k_dim: usize,
    99	    pub(crate) arena_gpu_base: u64,
   100	    pub(crate) arena_bytes: usize,
   101	    pub(crate) tile_rows: usize,
   102	    pub(crate) max_tiles: usize,
   103	    pub(crate) enough_for_shape: bool,
   104	    pub(crate) eu_kernel_uploaded: bool,
   105	    pub(crate) eu_walker_encoded: bool,
   106	    pub(crate) eu_walker_submitted: bool,
   107	    pub(crate) eu_walker_retired: bool,
   108	    pub(crate) eu_dispatch_delta: u32,
   109	    pub(crate) eu_c_store_value: u32,
   110	    pub(crate) eu_expected_store_value: u32,
   111	    pub(crate) eu_program_name: &'static str,
   112	    pub(crate) result_c_changed_by_eu: bool,
   113	}
   114	
   115	impl GpgpuPreflightStatus {
   116	    pub(crate) const fn static_tile_proven(self) -> bool {
   117	        self.eu_walker_retired && self.result_c_changed_by_eu && self.eu_dispatch_delta != 0
   118	    }
   119	}
   120	
   121	#[derive(Copy, Clone, Debug)]
   122	pub(crate) struct GpgpuOneTileStageProof {
   455	        revision_id: dev.revision_id,
   456	        mmio_base: dev.mmio as usize,
   457	        mmio_len: dev.mmio_len,
   458	    })
   459	}
   460	
   461	pub(crate) fn gpgpu_preflight_status() -> GpgpuPreflightStatus {
   462	    let status = self::gpgpu::gpgpu_preflight_status();
   463	    GpgpuPreflightStatus {
   464	        submitted: status.submitted,
   465	        accepted: status.accepted,
   466	        completed: status.completed,
   467	        guc_ready: status.guc_ready,
   468	        marker: status.marker,
   469	        dot: status.dot,
   470	        sum_a: status.sum_a,
   471	        sum_b: status.sum_b,
   472	        lanes: status.lanes,
   473	        min_burn_rows: status.min_burn_rows,
   474	        min_burn_k_dim: status.min_burn_k_dim,
   475	        arena_gpu_base: status.arena_gpu_base,
   476	        arena_bytes: status.arena_bytes,
   477	        tile_rows: status.tile_rows,
   478	        max_tiles: status.max_tiles,
   479	        enough_for_shape: status.enough_for_shape,
   480	        eu_kernel_uploaded: status.eu_kernel_uploaded,
   481	        eu_walker_encoded: status.eu_walker_encoded,
   482	        eu_walker_submitted: status.eu_walker_submitted,
   483	        eu_walker_retired: status.eu_walker_retired,
   484	        eu_dispatch_delta: status.eu_dispatch_delta,
   485	        eu_c_store_value: status.eu_c_store_value,
   486	        eu_expected_store_value: status.eu_expected_store_value,
   487	        eu_program_name: status.eu_program_name,
   488	        result_c_changed_by_eu: status.result_c_changed_by_eu,
   489	    }
   490	}
   491	
   492	pub(crate) fn uhd770_primary_scanout_gpgpu_ready() -> bool {
   493	    const UHD770_ADL_S_DEVICE_ID: u16 = 0x4680;
   494	
   495	    let uhd770_claimed = claimed_device()
   496	        .map(|dev| dev.device_id == UHD770_ADL_S_DEVICE_ID)
   497	        .unwrap_or(false);
   498	
   499	    uhd770_claimed
   500	        && gpgpu_preflight_status().static_tile_proven()

[2026-05-12T04:53:05.595Z] tool-output line=193
------------------------------------------------------------------------------------------------
Chunk ID: 8465cd
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 445
Output:
   790	#[inline]
   791	fn intel_cursor_service_gate() -> bool {
   792	    crate::intel::has_claimed_device()
   793	}
   794	
   795	#[inline]
   796	fn mandelbrot_gpu_sidequest_gate() -> bool {
   797	    const UHD770_ADL_S_DEVICE_ID: u16 = 0x4680;
   798	
   799	    let uhd770_claimed = crate::intel::claimed_device()
   800	        .map(|dev| dev.device_id == UHD770_ADL_S_DEVICE_ID)
   801	        .unwrap_or(false);
   802	    let gpu = crate::intel::gpgpu_preflight_status();
   803	    let gpgpu_path_proven =
   804	        gpu.eu_walker_retired && gpu.result_c_changed_by_eu && gpu.eu_dispatch_delta != 0;
   805	
   806	    uhd770_claimed
   807	        && gpgpu_path_proven
   808	        && crate::intel::warm_state().is_some()
   809	        && crate::intel::active_scanout_dimensions().is_some()
   810	        && crate::intel::primary_surface_gpu_addr().is_some()
   811	}
   812	
   813	#[inline]
   814	fn ui2_core_task_gate() -> bool {
   815	    true
   816	}
  1370	    TaskSpec::disabled(
  1371	        "html_fetch_service",
  1372	        crate::r::readiness::NET_V4_CONFIGURED | crate::r::readiness::TRUEOSFS_ROOT_MOUNTED,
  1373	        &HTML_SHACK_SERVICE_STARTED,
  1374	        html_fetch_service,
  1375	    ),
  1376	    TaskSpec::enabled(
  1377	        "gfx-texture-upload-service",
  1378	        crate::r::readiness::GFX_BACKEND_READY,
  1379	        &GFX_TEXTURE_UPLOAD_SERVICE_STARTED,
  1380	        spawn_gfx_texture_upload_service,
  1381	    ),
  1382	    TaskSpec::enabled_gated(
  1383	        "mandelbrot-gpu-sidequest",
  1384	        crate::r::readiness::GFX_BACKEND_READY,
  1385	        mandelbrot_gpu_sidequest_gate,
  1386	        &MANDELBROT_GPU_SIDEQUEST_STARTED,
  1387	        spawn_mandelbrot_gpu_sidequest,
  1388	    ),

[2026-05-12T04:53:08.431Z] assistant line=194
------------------------------------------------------------------------------------------------
I see the half-step I’d added in `src/intel/mod.rs`; I’m removing that. Then the sidequest registry will just use the existing abstract Intel claim gate, like `intel-cursor-service` already does, plus `GFX_BACKEND_READY` so the display side is not still cold.

[2026-05-12T04:53:08.431Z] assistant line=195
------------------------------------------------------------------------------------------------
I see the half-step I’d added in `src/intel/mod.rs`; I’m removing that. Then the sidequest registry will just use the existing abstract Intel claim gate, like `intel-cursor-service` already does, plus `GFX_BACKEND_READY` so the display side is not still cold.

[2026-05-12T04:53:22.753Z] assistant line=204
------------------------------------------------------------------------------------------------
That’s now using the existing abstract predicate instead of inventing a new one. I’ll verify the patch is exactly that narrow and run the usual compile check.

[2026-05-12T04:53:22.754Z] assistant line=205
------------------------------------------------------------------------------------------------
That’s now using the existing abstract predicate instead of inventing a new one. I’ll verify the patch is exactly that narrow and run the usual compile check.

[2026-05-12T04:53:22.756Z] tool-call:exec_command line=206
------------------------------------------------------------------------------------------------
{"cmd":"git diff -- src/intel/mod.rs src/r/spawn_service.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":16000}

[2026-05-12T04:53:22.763Z] tool-call:exec_command line=207
------------------------------------------------------------------------------------------------
{"cmd":"rustfmt --check src/r/spawn_service.rs src/intel/mod.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":12000}

[2026-05-12T04:53:22.811Z] tool-output line=210
------------------------------------------------------------------------------------------------
Chunk ID: 04807c
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 147
Output:
diff --git a/src/r/spawn_service.rs b/src/r/spawn_service.rs
index 2c7426d7..22ce3d0a 100644
--- a/src/r/spawn_service.rs
+++ b/src/r/spawn_service.rs
@@ -1361,9 +1361,10 @@ static TASKS: [TaskSpec; 71] = [
         &GFX_TEXTURE_UPLOAD_SERVICE_STARTED,
         spawn_gfx_texture_upload_service,
     ),
-    TaskSpec::enabled(
+    TaskSpec::enabled_gated(
         "mandelbrot-gpu-sidequest",
-        0,
+        crate::r::readiness::GFX_BACKEND_READY,
+        intel_cursor_service_gate,
         &MANDELBROT_GPU_SIDEQUEST_STARTED,
         spawn_mandelbrot_gpu_sidequest,
     ),

[2026-05-12T04:53:22.835Z] tool-output line=211
------------------------------------------------------------------------------------------------
Chunk ID: f5b620
Wall time: 0.0000 seconds
Process exited with code 1
Original token count: 82
Output:
error: let chains are only allowed in Rust 2024 or later
    --> /home/t4ce/REPOS/TRUEOS/src/intel/display.rs:1213:12
     |
1213 |         if let Some(surface) = *state
     |            ^^^^^^^^^^^^^^^^^^^^^^^^^^

Error writing files: failed to resolve mod `display`: cannot parse /home/t4ce/REPOS/TRUEOS/src/intel/display.rs

[2026-05-12T04:53:29.123Z] assistant line=214
------------------------------------------------------------------------------------------------
The diff is now one line of behavior: `mandelbrot-gpu-sidequest` uses `GFX_BACKEND_READY` plus the existing `intel_cursor_service_gate` (`has_claimed_device`). `rustfmt` on `src/intel/mod.rs` tripped over an unrelated Rust edition parse issue in `src/intel/display.rs`, so I’m checking the touched spawn file by itself and waiting for `cargo check`.

[2026-05-12T04:53:29.123Z] assistant line=215
------------------------------------------------------------------------------------------------
The diff is now one line of behavior: `mandelbrot-gpu-sidequest` uses `GFX_BACKEND_READY` plus the existing `intel_cursor_service_gate` (`has_claimed_device`). `rustfmt` on `src/intel/mod.rs` tripped over an unrelated Rust edition parse issue in `src/intel/display.rs`, so I’m checking the touched spawn file by itself and waiting for `cargo check`.

[2026-05-12T04:53:33.733Z] assistant line=221
------------------------------------------------------------------------------------------------
Yes, that’s cleaner. I changed it to use the existing abstract gate:

```rust
TaskSpec::enabled_gated(
    "mandelbrot-gpu-sidequest",
    crate::r::readiness::GFX_BACKEND_READY,
    intel_cursor_service_gate,
    ...
)
```

`intel_cursor_service_gate` is already the repo’s simple “Intel claimed” predicate:

```rust
crate::intel::has_claimed_device()
```

So the start condition is now: graphics backend ready, and Intel device claimed. No raw GPGPU proof fields, no new helper.

Changed: [src/r/spawn_service.rs](/home/t4ce/REPOS/TRUEOS/src/r/spawn_service.rs:1361)

Verification:
- `cargo check` passes.
- `rustfmt --check src/r/spawn_service.rs` passes.

[2026-05-12T04:53:33.733Z] assistant line=222
------------------------------------------------------------------------------------------------
Yes, that’s cleaner. I changed it to use the existing abstract gate:

```rust
TaskSpec::enabled_gated(
    "mandelbrot-gpu-sidequest",
    crate::r::readiness::GFX_BACKEND_READY,
    intel_cursor_service_gate,
    ...
)
```

`intel_cursor_service_gate` is already the repo’s simple “Intel claimed” predicate:

```rust
crate::intel::has_claimed_device()
```

So the start condition is now: graphics backend ready, and Intel device claimed. No raw GPGPU proof fields, no new helper.

Changed: [src/r/spawn_service.rs](/home/t4ce/REPOS/TRUEOS/src/r/spawn_service.rs:1361)

Verification:
- `cargo check` passes.
- `rustfmt --check src/r/spawn_service.rs` passes.

[2026-05-12T04:53:42.339Z] user line=227
------------------------------------------------------------------------------------------------
on baremetal gfx backend will never be ready

[2026-05-12T04:53:42.375Z] meta line=229
------------------------------------------------------------------------------------------------
id=019e1a85-0c3d-73e0-90f7-745fd2a0aca3 | cwd=/home/t4ce/REPOS/TRUEOS | source=vscode | git=9aeda8d1241d

[2026-05-12T04:53:45.806Z] assistant line=232
------------------------------------------------------------------------------------------------
Ah, right. If baremetal never raises `GFX_BACKEND_READY`, then that readiness bit makes the sidequest unreachable exactly where we need it. I’ll switch the registry to the abstract Intel-claimed gate only, with required readiness back to `0`.

[2026-05-12T04:53:45.806Z] assistant line=233
------------------------------------------------------------------------------------------------
Ah, right. If baremetal never raises `GFX_BACKEND_READY`, then that readiness bit makes the sidequest unreachable exactly where we need it. I’ll switch the registry to the abstract Intel-claimed gate only, with required readiness back to `0`.

[2026-05-12T04:53:50.832Z] assistant line=238
------------------------------------------------------------------------------------------------
Now the sidequest is gated only by the existing Intel claim predicate, so it can start on baremetal without waiting for the generic graphics backend flag. I’ll verify the final diff and compile.

[2026-05-12T04:53:50.832Z] assistant line=239
------------------------------------------------------------------------------------------------
Now the sidequest is gated only by the existing Intel claim predicate, so it can start on baremetal without waiting for the generic graphics backend flag. I’ll verify the final diff and compile.

[2026-05-12T04:53:50.832Z] tool-call:exec_command line=240
------------------------------------------------------------------------------------------------
{"cmd":"git diff -- src/r/spawn_service.rs src/intel/mod.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":12000}

[2026-05-12T04:53:50.897Z] tool-output line=244
------------------------------------------------------------------------------------------------
Chunk ID: 53c143
Wall time: 0.0001 seconds
Process exited with code 0
Original token count: 134
Output:
diff --git a/src/r/spawn_service.rs b/src/r/spawn_service.rs
index 2c7426d7..17a28ea2 100644
--- a/src/r/spawn_service.rs
+++ b/src/r/spawn_service.rs
@@ -1361,9 +1361,10 @@ static TASKS: [TaskSpec; 71] = [
         &GFX_TEXTURE_UPLOAD_SERVICE_STARTED,
         spawn_gfx_texture_upload_service,
     ),
-    TaskSpec::enabled(
+    TaskSpec::enabled_gated(
         "mandelbrot-gpu-sidequest",
         0,
+        intel_cursor_service_gate,
         &MANDELBROT_GPU_SIDEQUEST_STARTED,
         spawn_mandelbrot_gpu_sidequest,
     ),

[2026-05-12T04:53:56.547Z] assistant line=250
------------------------------------------------------------------------------------------------
Fixed. The sidequest now has:

```rust
required = 0
gate = intel_cursor_service_gate
```

So it starts once Intel is claimed, without waiting for `GFX_BACKEND_READY`.

Changed: [src/r/spawn_service.rs](/home/t4ce/REPOS/TRUEOS/src/r/spawn_service.rs:1361)

Verification passed:
- `rustfmt --check src/r/spawn_service.rs`
- `cargo check`

[2026-05-12T04:53:56.547Z] assistant line=251
------------------------------------------------------------------------------------------------
Fixed. The sidequest now has:

```rust
required = 0
gate = intel_cursor_service_gate
```

So it starts once Intel is claimed, without waiting for `GFX_BACKEND_READY`.

Changed: [src/r/spawn_service.rs](/home/t4ce/REPOS/TRUEOS/src/r/spawn_service.rs:1361)

Verification passed:
- `rustfmt --check src/r/spawn_service.rs`
- `cargo check`

[extracted_items=122]

================================================================================================
OLDER: locate existing Mandelbrot shader/artifact path
source: /home/t4ce/.codex/sessions/2026/05/11/rollout-2026-05-11T03-12-50-019e1498-1030-7303-b886-4a97cc286cd1.jsonl
================================================================================================

[2026-05-11T01:12:51.088Z] meta line=1
------------------------------------------------------------------------------------------------
id=019e1498-1030-7303-b886-4a97cc286cd1 | cwd=/home/t4ce/REPOS/TRUEOS | source={'subagent': {'thread_spawn': {'parent_thread_id': '019e1270-c5b3-7552-b990-18526e80915b', 'depth': 1, 'agent_path': None, 'agent_nickname': 'Turing', 'agent_role': 'explorer'}}} | git=e33615d9c75e

[2026-05-11T01:12:51.091Z] user line=6
------------------------------------------------------------------------------------------------
In /home/t4ce/REPOS/TRUEOS, inspect the existing Intel GPGPU / EU artifact code for any Mandelbrot shader/artifact or test-render path. I need a concise answer: existing artifact names/bytes/constants, upload/submit functions to reuse, whether current GPGPU store target can be pointed at an arbitrary buffer, and the smallest integration point for a one-shot Mandelbrot render. Do not edit files.

[2026-05-11T01:12:54.717Z] assistant line=9
------------------------------------------------------------------------------------------------
I’ll inspect the repo without touching files, focusing on Intel GPU/EU artifact generation, tests, and submit/upload helpers. I’ll keep the answer tight and anchored to concrete symbols and paths.

[2026-05-11T01:12:54.717Z] assistant line=10
------------------------------------------------------------------------------------------------
I’ll inspect the repo without touching files, focusing on Intel GPU/EU artifact generation, tests, and submit/upload helpers. I’ll keep the answer tight and anchored to concrete symbols and paths.

[2026-05-11T01:12:54.719Z] tool-call:exec_command line=11
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"Mandel|mandel|gpgpu|GPGPU|EU|execution unit|shader|artifact|store|submit|batch|MI_|MEDIA|walker|compute\" -S .","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":20000}

[2026-05-11T01:12:54.807Z] tool-output line=15
------------------------------------------------------------------------------------------------
Chunk ID: b498d7
Wall time: 0.0008 seconds
Process exited with code 0
Original token count: 262144
Output:
Total output lines: 6567

./arm_Makefile:9:ARM_ARTIFACT_DIR := bld/artifacts/arm-$(BUILD_MODE)
./pxe2.js:135:        "Cache-Control": "no-store",
./Makefile:6:ARTIFACT_DIR = bld/artifacts/$(BUILD_MODE)-$(ARTIFACT_BUILD_ID)
./Makefile:22:# leaving room for FAT metadata and small growth in embedded artifacts.
./Makefile:65:.PHONY: kernel empty-libs blueprints artifacts kernel-stages baremetal-reboot-log iso iso-build iso-release iso-debug snipe dbg dbg-vscode run run-with-nvme run-installed lc
./Makefile:119:artifacts: blueprints kernel
./Makefile:135:kernel-stages: artifacts
./Makefile:145:iso-build: artifacts images
./docs/intel-uhd770-cpu-reference.html:370:          TRUEOS shader and GPGPU target note
./docs/intel-uhd770-cpu-reference.html:374:          Intel UHD Graphics 770 is the 32-EU Xe-architecture integrated GPU found in selected Alder Lake,
./docs/intel-uhd770-cpu-reference.html:375:          Raptor Lake, Raptor Lake Refresh, and Bartlett Lake processors. For T1-T6 shader artifacts, treat it
./docs/intel-uhd770-cpu-reference.html:379:          <span class="tag"><svg class="icon" viewBox="0 0 24 24" aria-hidden="true"><rect x="4" y="4" width="16" height="16" rx="2"/><rect x="9" y="9" width="6" height="6"/><path d="M9 1v3M15 1v3M9 20v3M15 20v3M20 9h3M20 14h3M1 9h3M1 14h3"/></svg>32 execution units</span>
./docs/intel-uhd770-cpu-reference.html:394:          <div class="spec"><span class="icon-box"><svg class="icon" viewBox="0 0 24 24"><path d="M3 12h18"/><path d="M12 3v18"/><path d="M7 7h10v10H7z"/></svg></span><div><strong>Compute profile</strong><span>32 EUs, OpenCL 3.0, OpenGL 4.5, DirectX 12</span></div></div>
./docs/intel-uhd770-cpu-reference.html:487:          <p>Compact target tuple for TRUEOS hardware checks and shader artifact labeling.</p>
./docs/intel-uhd770-cpu-reference.html:524:    <p class="foot">Prepared for TRUEOS shader/GPGPU hardware targeting. Last research pass: 2026-05-11.</p>
./src/turbo.rs:35:    pub submitted_aps: usize,
./src/turbo.rs:44:    pub submitted_aps: usize,
./src/turbo.rs:60:    TURBO_ARMED.store(if v { 1 } else { 0 }, Ordering::Release);
./src/turbo.rs:164:    let submit = crate::smp::submit_to_all_online_aps(smp_apply_turbo, if enable { 1 } else { 0 });
./src/turbo.rs:169:        targeted_aps: submit.targeted_aps,
./src/turbo.rs:170:        submitted_aps: submit.submitted_aps,
./src/turbo.rs:171:        busy_aps: submit.busy_aps,
./src/turbo.rs:172:        seq: submit.seq,
./src/turbo.rs:193:    let submit = crate::smp::submit_to_all_online_aps(smp_read_turbo, 0);
./src/turbo.rs:198:    let waited = if submit.seq == 0 {
./src/turbo.rs:212:                if r.seq != submit.seq {
./src/turbo.rs:238:        if r.seq != submit.seq || r.state != crate::smp::STATE_DONE {
./src/turbo.rs:252:        online_aps: submit.targeted_aps,
./src/turbo.rs:253:        submitted_aps: submit.submitted_aps,
./src/turbo.rs:254:        busy_aps: submit.busy_aps,
./src/turbo.rs:259:        seq: submit.seq,
./tools/intel_userland_oracle/README.md:3:Tiny Linux-host oracle for the TRUEOS Intel EU bring-up endgame.
./tools/intel_userland_oracle/README.md:6:compute shader on the host UHD 770 and records the ordered userspace-to-i915
./tools/intel_userland_oracle/README.md:13:registers, ACTHD/IPEHR, fault registers, INSTDONE, GPGPU thread counters, TDL,
./tools/intel_userland_oracle/README.md:22:The default workload is the original sentinel shader. To run the T5-small
./tools/intel_userland_oracle/README.md:33:The TRUEOS-layout T5 variant keeps the compact shader preserved and uses the
./tools/intel_userland_oracle/README.md:44:The main artifact is:
./src/aud/audio_sync.rs:14://! The resulting offset is stored in `MusicPlayerState::av_offset_ms`.
./src/aud/audio_sync.rs:128:            DMA_LATENCY_US.store(latency, Ordering::SeqCst);
./src/aud/audio_sync.rs:163:    SYNC_ACTIVE.store(true, Ordering::SeqCst);
./src/aud/audio_sync.rs:164:    TAP_COUNT.store(0, Ordering::SeqCst);
./src/aud/audio_sync.rs:165:    BEAT_INDEX.store(0, Ordering::SeqCst);
./src/aud/audio_sync.rs:166:    RESULT_READY.store(false, Ordering::SeqCst);
./src/aud/audio_sync.rs:167:    COMPUTED_OFFSET_MS.store(0, Ordering::SeqCst);
./src/aud/audio_sync.rs:174:    SESSION_START_US.store(now, Ordering::SeqCst);
./src/aud/audio_sync.rs:175:    LAST_BEAT_US.store(0, Ordering::SeqCst);
./src/aud/audio_sync.rs:182:    LAST_BEAT_US.stor
[... truncated in extract ...]

[2026-05-11T01:12:54.807Z] tool-output line=16
------------------------------------------------------------------------------------------------
Chunk ID: d6b3d4
Wall time: 0.0001 seconds
Process exited with code 0
Original token count: 31364
Output:
Total output lines: 3009

build_support/limloader.rs
cnt
crates/trueos-c4/Cargo.toml
crates/trueos-c4/src/backend_rust.rs
crates/trueos-c4/src/parser.rs
crates/trueos-c4/src/backend_vm.rs
crates/trueos-c4/src/ast.rs
crates/trueos-c4/src/lexer.rs
crates/trueos-c4/src/lib.rs
crates/trueos-v/LICENSE-MIT.template
crates/trueos-v/Cargo.toml
crates/trueos-v/src/env.rs
crates/trueos-v/src/vfetch.rs
crates/trueos-v/src/iter.rs
crates/trueos-v/src/vgfx.rs
crates/trueos-v/src/vfs.rs
crates/trueos-v/src/vui2.rs
crates/trueos-v/src/vsys.rs
crates/trueos-v/src/vhttp_srv.rs
crates/trueos-v/src/vnetfs.rs
crates/trueos-v/src/borrow.rs
crates/trueos-v/src/vio.rs
crates/trueos-v/src/vinput.rs
crates/trueos-v/src/vclock.rs
crates/trueos-v/src/fmt.rs
crates/trueos-v/src/ffi.rs
crates/trueos-v/src/vcabi.rs
crates/trueos-v/src/vnet.rs
crates/trueos-v/src/collections.rs
crates/trueos-v/src/lib.rs
crates/trueos-v/src/vshell.rs
crates/trueos-v/src/sync.rs
crates/trueos-v/LICENSE-APACHE.template
crates/trueos-vm/Cargo.toml
crates/trueos-vm/src/stream.rs
crates/trueos-vm/src/guest.rs
crates/trueos-vm/src/vmcall.rs
crates/trueos-vm/src/vfetch_job.rs
crates/trueos-vm/src/v.rs
crates/trueos-vm/src/lib.rs
crates/trueos-vm/src/demo.rs
crates/trueos-vm/src/vpanic.rs
crates/trueos-vm/build.rs
crates/trueos-qjs/Cargo.toml
crates/trueos-qjs/src/cmd_stream/draw_cmd_stream.rs
crates/trueos-qjs/src/cmd_stream/lyon_cmd_stream.rs
crates/trueos-qjs/src/trueos_shims.rs
crates/trueos-qjs/src/qjs_diag.rs
crates/trueos-qjs/src/cmd_stream.rs
crates/trueos-qjs/src/trueos_module_loader/mod.rs
crates/trueos-qjs/src/trueos_module_loader/trueos_module_loader.rs
crates/trueos-qjs/src/trueos_module_loader/embedded.rs
crates/trueos-qjs/src/trueos_module_loader/compiled.rs
crates/trueos-qjs/src/vm.rs
crates/trueos-qjs/src/lightningcss_native.rs
crates/trueos-qjs/src/threejs/mod.rs
crates/trueos-qjs/src/parse5.mjs
crates/trueos-qjs/src/jsbind.rs
crates/trueos-qjs/src/node/mod.rs
crates/trueos-qjs/src/timers.rs
crates/trueos-qjs/src/lib.rs
crates/trueos-qjs/src/workers.rs
crates/trueos-qjs/src/async_fs.rs
crates/trueos-qjs/src/yoga/yoga.mjs
crates/trueos-qjs/src/yoga/mod.rs
crates/trueos-qjs/src/yoga/yoga_native.rs
crates/trueos-qjs/src/yoga/ffi.rs
crates/trueos-qjs/src/yoga/yoga_cabi.c
crates/trueos-qjs/src/async_ops.rs
crates/trueos-qjs/build.rs
crates/trueos-qjs/app/truesurfer/truesurfer_extract.mjs
crates/trueos-qjs/app/truesurfer/content/html_demo.rs
crates/trueos-qjs/app/truesurfer/parry_reworkpls_needui2concept.mjs
crates/trueos-qjs/app/truesurfer/cssDefaults.mjs
crates/trueos-qjs/app/truesurfer/htmlDefaults.mjs
crates/trueos-qjs/app/truesurfer/truesurfer_assets.mjs
crates/trueos-qjs/app/truesurfer/truesurfer_qjs_vm_task.rs
crates/trueos-qjs/app/truesurfer/theme.mjs
crates/trueos-qjs/app/truesurfer/truesurfer.mjs
crates/trueos-qjs/app/truesurfer/css.mjs
crates/trueos-qjs/app/dd/driverdev.mjs
crates/trueos-qjs/app/node/events.mjs
crates/trueos-qjs/app/node/async_hooks.mjs
crates/trueos-qjs/app/node/process.mjs
crates/trueos-qjs/app/node/tty.mjs
crates/trueos-qjs/app/node/buffer.mjs
crates/trueos-qjs/app/cdn/39d8f7d0658e6d2e.mjs
crates/trueos-qjs/app/vendor/yoga.mjs
crates/trueos-qjs/app/vendor/parse5.mjs
crates/trueos-gfx-core/Cargo.toml
crates/trueos-gfx-core/src/lib.rs
crates/trueos-esp/iot/led.py
crates/trueos-esp/iot/led2.py
crates/trueos-esp/iot/led_pinkfade.py
crates/trueos-esp/iot/led4.py
crates/trueos-esp/iot/C3_tmc2226_stepper.py
crates/trueos-esp/iot/1OktavePianoLed.py
crates/trueos-esp/Cargo.toml
crates/trueos-esp/src/gate.rs
crates/trueos-esp/src/swarm.rs
crates/trueos-esp/src/lib.rs
crates/trueos-esp/src/piano.rs
crates/trueos-esp/uploadexample
crates/trueos-math/Cargo.toml
crates/trueos-math/src/avl.rs
crates/trueos-math/src/tree.rs
crates/trueos-math/src/matrix.rs
crates/trueos-math/src/bst_arena.rs
crates/trueos-math/src/bst.rs
crates/trueos-math/src/complex.rs
crates/trueos-math/src/avl_old.rs
crates/trueos-math/src/pbltree.rs
crates/trueos-math/src/html_tree.rs
crates/trueos-math/src/lib.rs
crates/trueos-math/src/ascii_tree.rs
crates/trueos-fs/Cargo.toml
crates/trueos-fs/src/lib.rs
crates/trueos-chat/Cargo.toml
crates/trueos-chat/src/lib.rs
crates/trueos-sys/Cargo.toml
crates/trueos-sys/src/vcabi.rs
crates/trueos-sys/src/lib.rs
crates/trueos-gboi/mod.rs
crates/trueos-gboi/cartridge.rs
crates/trueos-gboi/timer.rs
crates/trueos-gboi/gpu.rs
crates/t
[... truncated in extract ...]

[2026-05-11T01:12:59.616Z] assistant line=19
------------------------------------------------------------------------------------------------
I found the relevant island: `src/intel/intel/render/gpgpu.rs`, `submit.rs`, `resources.rs`, `state.rs`, plus a separate CPU/UI Mandelbrot demo. I’m narrowing now to the actual artifact bytes/constants and the target-buffer wiring.

[2026-05-11T01:12:59.616Z] assistant line=20
------------------------------------------------------------------------------------------------
I found the relevant island: `src/intel/intel/render/gpgpu.rs`, `submit.rs`, `resources.rs`, `state.rs`, plus a separate CPU/UI Mandelbrot demo. I’m narrowing now to the actual artifact bytes/constants and the target-buffer wiring.

[2026-05-11T01:12:59.618Z] tool-call:exec_command line=21
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1,260p' src/intel/intel/render/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":20000}

[2026-05-11T01:12:59.641Z] tool-call:exec_command line=22
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1,260p' src/intel/intel/render/submit.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":20000}

[2026-05-11T01:12:59.641Z] tool-call:exec_command line=23
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1,260p' src/intel/intel/render/resources.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":20000}

[2026-05-11T01:12:59.641Z] tool-call:exec_command line=24
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1,260p' src/intel/intel/render/state.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":20000}

[2026-05-11T01:12:59.658Z] tool-output line=26
------------------------------------------------------------------------------------------------
Chunk ID: 2c1ce4
Wall time: 0.0001 seconds
Process exited with code 0
Original token count: 2693
Output:
const GPGPU_PREFLIGHT_LANES: usize = 4;
const GPGPU_BURN_MIN_ROWS: usize = 512;
const GPGPU_BURN_MIN_K_DIM: usize = 512;
const GPU_PROGRAM_SHARED_RAM_WRITE_EXPECTED: u32 = trueos_eu::gfx12::STORE_SENTINEL_U32;
const GPGPU_LOAD_DUMMY_CURBE: bool = true;
const GPGPU_DUMMY_CURBE_BYTES: usize = 64;
const GPGPU_CONTIGUOUS_VFE_IDD_WALKER: bool = false;
const GPGPU_MESA_POST_VFE_PIPE_CONTROL: bool = false;
// ADL-S 8086:4680 is Gfx12.0/Xe-LP.  The PRM-length GPGPU_WALKER now launches
// EU threads, while one-GRF and two-GRF TS EOT variants both leave the walker
// waiting.  Probe the Media chapter's Gateway EOT path next.
const ACTIVE_GFX12_EOT_VARIANT: trueos_eu::gfx12::Gfx12EotVariant =
    trueos_eu::gfx12::Gfx12EotVariant::GatewayR0ToG127;

#[derive(Copy, Clone)]
struct GpgpuEuProgram {
    name: &'static str,
    words: &'static [u32],
    expects_store: bool,
}

// Legacy diagnostic dataport probe.  This hand-written EU blob is not the final
// Burn/matmul kernel path; it is only a bounded oscilloscope for the current
// phase: if the dispatched EU thread can write shared RAM, then we know it
// decoded enough instructions to issue a dataport side effect before/around EOT.
static GPU_PROGRAM_SHARED_RAM_WRITE_CODE: [u32; 12] = [
    0xA07E0061,
    0x00010000,
    0xA0780061,
    GPU_PROGRAM_SHARED_RAM_WRITE_EXPECTED,
    0xA07A0061,
    0x3F810000,
    0xA07C0061,
    0x3F810000,
    0x00040132,
    0x00000004,
    0x50007E14,
    0x00C47834,
];

fn selected_gpgpu_eu_program() -> GpgpuEuProgram {
    let artifact = trueos_eu::gfx12::eot_artifact(ACTIVE_GFX12_EOT_VARIANT);
    GpgpuEuProgram {
        name: artifact.name,
        words: artifact.words,
        expects_store: artifact.expects_store,
    }
}

fn gpgpu_store_eot_program() -> GpgpuEuProgram {
    let artifact = trueos_eu::gfx12::HDC1_BTI34_STORE_THEN_TS_EOT;
    GpgpuEuProgram {
        name: artifact.name,
        words: artifact.words,
        expects_store: artifact.expects_store,
    }
}

const GPGPU_C_STORE_KERNEL_SEND_DWORD: usize = trueos_eu::gfx12::HDC1_BTI34_STORE_SEND_DWORD;
const GPGPU_C_STORE_KERNEL_IMM_DWORD: usize = trueos_eu::gfx12::HDC1_BTI34_STORE_IMM_DWORD;
const GPGPU_STORE_BINDING_TABLE_OFFSET_BYTES: usize = 0x3400;
const GPGPU_STORE_SURFACE_STATE_OFFSET_BYTES: usize = 0x3500;
const GPGPU_STORE_BINDING_TABLE_INDEX: usize = 0x34;
const GPGPU_STORE_BINDING_TABLE_ENTRIES: usize = GPGPU_STORE_BINDING_TABLE_INDEX + 1;
const GPGPU_STORE_SURFACE_DWORDS: usize = 16;
const SURFTYPE_BUFFER: u32 = 4;
const SURFACE_FORMAT_RAW: u32 = 0x1FF;

static GPGPU_PREFLIGHT_SUBMITTED: AtomicBool = AtomicBool::new(false);
static GPGPU_PREFLIGHT_ACCEPTED: AtomicBool = AtomicBool::new(false);
static GPGPU_PREFLIGHT_COMPLETED: AtomicBool = AtomicBool::new(false);
static GPGPU_PREFLIGHT_MARKER: AtomicU32 = AtomicU32::new(0);
static GPGPU_PREFLIGHT_DOT: AtomicU32 = AtomicU32::new(0);
static GPGPU_PREFLIGHT_SUM_A: AtomicU32 = AtomicU32::new(0);
static GPGPU_PREFLIGHT_SUM_B: AtomicU32 = AtomicU32::new(0);
static GPGPU_PREFLIGHT_LANES_OBSERVED: AtomicU32 = AtomicU32::new(0);
static GPGPU_WARM_BUFFERS_MAPPED: AtomicBool = AtomicBool::new(false);
static GPGPU_TILE_ARENA_MAPPED: AtomicBool = AtomicBool::new(false);
static GPGPU_TILE_ARENA_STATUS_LOGGED: AtomicBool = AtomicBool::new(false);
static GPGPU_EU_KERNEL_UPLOADED: AtomicBool = AtomicBool::new(false);
static GPGPU_EU_WALKER_ENCODED: AtomicBool = AtomicBool::new(false);
static GPGPU_EU_WALKER_SUBMITTED: AtomicBool = AtomicBool::new(false);
static GPGPU_EU_WALKER_RETIRED: AtomicBool = AtomicBool::new(false);
static GPGPU_EU_DISPATCH_DELTA: AtomicU32 = AtomicU32::new(0);
static GPGPU_EU_C_STORE_VALUE: AtomicU32 = AtomicU32::new(0);

#[derive(Copy, Clone, Debug)]
pub(crate) struct GpgpuPreflightStatus {
    pub(crate) submitted: bool,
    pub(crate) accepted: bool,
    pub(crate) completed: bool,
    pub(crate) guc_ready: bool,
    pub(crate) marker: u32,
    pub(crate) dot: u32,
    pub(crate) sum_a: u32,
    pub(crate) sum_b: u32,
    pub(crate) lanes: u32,
    pub(crate) min_burn_rows: usize,
    pub(crate) min_burn_k_dim: usize,
    pub(crate) arena_gpu_base: u64,
    pub(crate) arena_bytes: usize,
    pub(crate) tile_rows: usize,
    pub(crate) max_tiles: usize,
    pub(crate) enough_for_shape: bool,
    pub(crate) eu_kernel_uploaded: bool,
    pub(crate) eu_walker_encoded: bool,
    pub(crate) eu_walker_submi
[... truncated in extract ...]

[2026-05-11T01:12:59.672Z] tool-output line=27
------------------------------------------------------------------------------------------------
Chunk ID: 3fd878
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 3358
Output:
fn submit_warm_render_batch(
    dev: crate::intel::Dev,
    warm: RenderWarmState,
    expected_result: u32,
    expected_result_slot_dword: usize,
    submit_name: &'static str,
) -> bool {
    let stats_before = capture_triangle_stage_stats(dev);
    let surface_samples_before = if is_surface_draw_submit_name(submit_name) {
        crate::intel::display::capture_primary_surface_samples()
    } else {
        None
    };
    if is_triangle_debug_submit_name(submit_name) {
        log_triangle_stage_stats(submit_name, "before-submit", true, stats_before, None);
    }
    if is_gpgpu_submit_name(submit_name) {
        recover_render_engine_after_nonretired_submit(dev, warm, "gpgpu-pre-submit");
        crate::intel::mmio_write(
            dev,
            GEN12_RCU_MODE,
            masked_bit_enable(GEN12_RCU_MODE_CCS_ENABLE),
        );
    }
    let ring_tail_bytes = build_ring_batch_start(warm, GPU_VA_BATCH_BASE);
    let Some(ring_ctl) = ring_ctl_value(warm.ring_len) else {
        return false;
    };
    if !init_gen12_lrc_context_image(
        warm,
        GPU_VA_RING_BASE as u32,
        ring_tail_bytes as u32,
        ring_ctl,
    ) {
        return false;
    }
    let (context_desc_lo, context_desc_hi) = build_execlist_context_descriptor(GPU_VA_CONTEXT_BASE);
    write_lrc_ring_tail(warm, ring_tail_bytes as u32);
    log_lrc_ring_image(warm, submit_name);
    let pphwsp_gpu = (GPU_VA_CONTEXT_BASE & !0xFFF) as u32;

    crate::intel::mmio_write(
        dev,
        RCS_RING_MODE_GEN7,
        masked_bit_enable(GFX_RUN_LIST_ENABLE | GEN11_GFX_DISABLE_LEGACY_MODE),
    );
    let ctx_ctl_after = rcs_ctx_control_value(false);
    crate::intel::mmio_write(dev, RCS_RING_CONTEXT_CONTROL, ctx_ctl_after);
    crate::intel::mmio_write(dev, RCS_RING_CONTEXT_CONTROL_REF, ctx_ctl_after);
    crate::intel::mmio_write(
        dev,
        RCS_RING_MI_MODE,
        masked_bit_disable(RING_MI_MODE_STOP_RING),
    );
    crate::intel::mmio_write(dev, RCS_RING_HWS_PGA, pphwsp_gpu);
    let hws_after = crate::intel::mmio_read(dev, RCS_RING_HWS_PGA);

    crate::intel::ggtt_invalidate(dev);
    core::sync::atomic::fence(core::sync::atomic::Ordering::SeqCst);
    execlist_submit_port_push(dev, context_desc_lo, context_desc_hi, 0, 0);
    crate::intel::mmio_write(dev, RCS_RING_EXECLIST_CONTROL, EL_CTRL_LOAD);

    if should_log_primary_probe_detail() {
        crate::log!(
            "intel/render: {} execlist-start desc=0x{:08X}:0x{:08X} hws=0x{:08X} sq0=0x{:08X}:0x{:08X} sq1=0x{:08X}:0x{:08X} ctx_ctl=0x{:08X} mi_mode=0x{:08X} tail_req=0x{:08X} tail_rb=0x{:08X} gen12_sq_load=1\n",
            submit_name,
            context_desc_hi,
            context_desc_lo,
            hws_after,
            crate::intel::mmio_read(dev, RCS_RING_EXECLIST_SQ_HI),
            crate::intel::mmio_read(dev, RCS_RING_EXECLIST_SQ_LO),
            crate::intel::mmio_read(dev, RCS_RING_EXECLIST_SQ_HI + 8),
            crate::intel::mmio_read(dev, RCS_RING_EXECLIST_SQ_LO + 8),
            crate::intel::mmio_read(dev, RCS_RING_CONTEXT_CONTROL),
            crate::intel::mmio_read(dev, RCS_RING_MI_MODE),
            ring_tail_bytes as u32,
            crate::intel::mmio_read(dev, RCS_RING_TAIL),
        );
    }

    let mut completed = false;
    let mut iter = 0usize;
    while iter < 4096 {
        let result0 = read_result_dword(warm, RESULT_SLOT_PRE3D_DWORD);
        let result1 = read_result_dword(warm, RESULT_SLOT_POST3D_DWORD);
        let result2 = read_result_dword(warm, RESULT_SLOT_FINAL_DWORD);
        let result3 = read_result_dword(warm, RESULT_SLOT_POST_VF_DWORD);
        let result4 = read_result_dword(warm, RESULT_SLOT_POST_VS_DWORD);
        let result5 = read_result_dword(warm, RESULT_SLOT_POST_PS_STATE_DWORD);
        let result6 = read_result_dword(warm, RESULT_SLOT_POST_CLIP_DWORD);
        let result7 = read_result_dword(warm, RESULT_SLOT_POST_RASTER_DWORD);
        let result_post3d_eop = read_result_dword(warm, RESULT_SLOT_POST3D_PIPE_CONTROL_LO_DWORD);
        let result_post3d_eop_hi =
            read_result_dword(warm, RESULT_SLOT_POST3D_PIPE_CONTROL_HI_DWORD);
        let result_post3d_light =
            read_result_dword(warm, RESULT_SLOT_POST3D_LIGHT_PIPE_CONTROL_LO_DWORD);
        let result_post3d_light_hi =
            read_result_dword(warm, RESULT_SLOT_POST3D_LIGHT_PIPE_CONTROL_HI_DWORD);
        let re
[... truncated in extract ...]

[2026-05-11T01:12:59.673Z] tool-output line=28
------------------------------------------------------------------------------------------------
Chunk ID: c7076d
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 2479
Output:
fn upload_triangle_shader_pipeline(
    warm: RenderWarmState,
    pipeline: &'static crate::intel::shader::TrianglePipeline,
) -> Result<TriangleShaderLayout, &'static str> {
    let vs = stage_range("vs", pipeline.vs.meta.kernel, pipeline.vs.code)?;
    let ps = stage_range("ps", pipeline.ps.meta.kernel, pipeline.ps.code)?;

    if pipeline.vs.meta.kernel.grf_used == 0 {
        return Err("vs-shader-grf-used-zero");
    }
    if pipeline.ps.meta.kernel.grf_used == 0 {
        return Err("ps-shader-grf-used-zero");
    }
    if pipeline.vs.meta.max_threads == 0 {
        return Err("vs-max-threads-zero");
    }

    if ranges_overlap(
        vs.code_offset_bytes,
        vs.code_size_bytes,
        ps.code_offset_bytes,
        ps.code_size_bytes,
    ) {
        return Err("shader-code-overlap");
    }

    let used_end = core::cmp::max(
        stage_end(vs.code_offset_bytes, vs.code_size_bytes).ok_or("shader-code-overflow")?,
        stage_end(ps.code_offset_bytes, ps.code_size_bytes).ok_or("shader-code-overflow")?,
    );
    if used_end > warm.draw_state_len {
        return Err("shader-code-exceeds-state-bo");
    }

    upload_stage_code(warm.draw_state_virt, vs.code_offset_bytes, pipeline.vs.code)?;
    upload_stage_code(warm.draw_state_virt, ps.code_offset_bytes, pipeline.ps.code)?;

    crate::intel::dma_flush(warm.draw_state_virt, used_end);

    let state_region_offset_bytes =
        crate::intel::align_up(used_end, crate::intel::WARM_ALIGN).ok_or("state-region-align")?;
    if state_region_offset_bytes > warm.draw_state_len {
        return Err("state-region-exceeds-state-bo");
    }

    let bo_gpu_base = GPU_VA_DRAW_STATE_BASE;
    let vs_gpu = bo_gpu_base + vs.code_offset_bytes as u64;
    let ps_gpu = bo_gpu_base + ps.code_offset_bytes as u64;

    Ok(TriangleShaderLayout {
        vs: TriangleShaderStageLayout {
            code_offset_bytes: vs.code_offset_bytes as u32,
            code_gpu_addr: vs_gpu,
            ksp_offset_bytes: pipeline.vs.meta.kernel.ksp_offset_bytes,
            ksp_gpu_addr: vs_gpu + pipeline.vs.meta.kernel.ksp_offset_bytes as u64,
            code_size_bytes: vs.code_size_bytes as u32,
        },
        ps: TriangleShaderStageLayout {
            code_offset_bytes: ps.code_offset_bytes as u32,
            code_gpu_addr: ps_gpu,
            ksp_offset_bytes: pipeline.ps.meta.kernel.ksp_offset_bytes,
            ksp_gpu_addr: ps_gpu + pipeline.ps.meta.kernel.ksp_offset_bytes as u64,
            code_size_bytes: ps.code_size_bytes as u32,
        },
        state_region_gpu_addr: bo_gpu_base + state_region_offset_bytes as u64,
        state_region_offset_bytes: state_region_offset_bytes as u32,
        used_bytes: used_end as u32,
    })
}

#[derive(Copy, Clone)]
struct StageUploadRange {
    code_offset_bytes: usize,
    code_size_bytes: usize,
}

fn stage_range(
    stage_name: &'static str,
    meta: crate::intel::shader::ShaderKernelMetadata,
    code: &'static [u32],
) -> Result<StageUploadRange, &'static str> {
    if meta.code_size_bytes == 0 || code.is_empty() {
        return Err(stage_error(stage_name, "shader-empty"));
    }

    let code_len_bytes = code
        .len()
        .checked_mul(core::mem::size_of::<u32>())
        .ok_or(stage_error(stage_name, "shader-code-len-overflow"))?;
    let declared_size = usize::try_from(meta.code_size_bytes)
        .map_err(|_| stage_error(stage_name, "shader-size-convert"))?;
    if declared_size != code_len_bytes {
        return Err(stage_error(stage_name, "shader-size-mismatch"));
    }

    let code_offset = usize::try_from(meta.code_offset_bytes)
        .map_err(|_| stage_error(stage_name, "shader-offset-convert"))?;
    let code_alignment = usize::try_from(meta.code_alignment_bytes)
        .map_err(|_| stage_error(stage_name, "shader-align-convert"))?;
    if code_alignment == 0 || code_offset % code_alignment != 0 {
        return Err(stage_error(stage_name, "shader-offset-alignment"));
    }

    let ksp_offset = usize::try_from(meta.ksp_offset_bytes)
        .map_err(|_| stage_error(stage_name, "shader-ksp-convert"))?;
    if ksp_offset % 64 != 0 {
        return Err(stage_error(stage_name, "shader-ksp-alignment"));
    }
    if ksp_offset >= declared_size {
        return Err(stage_error(stage_name, "shader-ksp-range"));
    }

    Ok(StageUploadRange {
        code_offset_bytes: code_offset,
        c
[... truncated in extract ...]

[2026-05-11T01:12:59.675Z] tool-output line=29
------------------------------------------------------------------------------------------------
Chunk ID: e81ca7
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1911
Output:
#[derive(Copy, Clone, Debug, Default)]
struct TriangleStageStats {
    ia_vertices: u64,
    ia_primitives: u64,
    vs_invocations: u64,
    hs_invocations: u64,
    ds_invocations: u64,
    gs_invocations: u64,
    gs_primitives: u64,
    cl_invocations: u64,
    cl_primitives: u64,
    ps_invocations: u64,
    cps_invocations: u64,
    ps_depth: u64,
    so_prims_written_0: u64,
    so_write_offset_0: u64,
}

impl TriangleStageStats {
    fn delta_since(self, before: Self) -> Self {
        Self {
            ia_vertices: self.ia_vertices.saturating_sub(before.ia_vertices),
            ia_primitives: self.ia_primitives.saturating_sub(before.ia_primitives),
            vs_invocations: self.vs_invocations.saturating_sub(before.vs_invocations),
            hs_invocations: self.hs_invocations.saturating_sub(before.hs_invocations),
            ds_invocations: self.ds_invocations.saturating_sub(before.ds_invocations),
            gs_invocations: self.gs_invocations.saturating_sub(before.gs_invocations),
            gs_primitives: self.gs_primitives.saturating_sub(before.gs_primitives),
            cl_invocations: self.cl_invocations.saturating_sub(before.cl_invocations),
            cl_primitives: self.cl_primitives.saturating_sub(before.cl_primitives),
            ps_invocations: self.ps_invocations.saturating_sub(before.ps_invocations),
            cps_invocations: self.cps_invocations.saturating_sub(before.cps_invocations),
            ps_depth: self.ps_depth.saturating_sub(before.ps_depth),
            so_prims_written_0: self
                .so_prims_written_0
                .saturating_sub(before.so_prims_written_0),
            so_write_offset_0: self
                .so_write_offset_0
                .saturating_sub(before.so_write_offset_0),
        }
    }
}

#[derive(Copy, Clone, Debug)]
pub struct RenderWarmState {
    pub device_id: u16,
    pub revision_id: u8,
    pub mmio_base: usize,
    pub mmio_len: usize,
    pub ring_phys: u64,
    pub ring_virt: *mut u8,
    pub ring_len: usize,
    pub context_phys: u64,
    pub context_virt: *mut u8,
    pub context_len: usize,
    pub batch_phys: u64,
    pub batch_virt: *mut u8,
    pub batch_len: usize,
    pub draw_state_phys: u64,
    pub draw_state_virt: *mut u8,
    pub draw_state_len: usize,
    pub vertex_phys: u64,
    pub vertex_virt: *mut u8,
    pub vertex_len: usize,
    pub result_phys: u64,
    pub result_virt: *mut u8,
    pub result_len: usize,
    pub streamout_phys: u64,
    pub streamout_virt: *mut u8,
    pub streamout_len: usize,
    pub gpgpu_arena_phys: u64,
    pub gpgpu_arena_virt: *mut u8,
    pub gpgpu_arena_len: usize,
}

#[derive(Copy, Clone)]
struct TriangleDrawPrep {
    vertex_count: u32,
    vertex_stride: u32,
    vertex_gpu_addr: u64,
    state_gpu_addr: u64,
    rt_gpu_addr: u64,
    rt_pitch: u32,
    target_w: u32,
    target_h: u32,
}

#[derive(Copy, Clone)]
struct TriangleVertexUploadProof {
    vertex_count: u32,
    vertex_stride: u32,
    byte_len: usize,
    gpu_addr: u64,
    signed_area_2x: f32,
    cpu_readback_ok: bool,
}

#[derive(Copy, Clone)]
struct TriangleShaderStageLayout {
    code_offset_bytes: u32,
    code_gpu_addr: u64,
    ksp_offset_bytes: u32,
    ksp_gpu_addr: u64,
    code_size_bytes: u32,
}

#[derive(Copy, Clone)]
struct TriangleShaderLayout {
    vs: TriangleShaderStageLayout,
    ps: TriangleShaderStageLayout,
    state_region_gpu_addr: u64,
    state_region_offset_bytes: u32,
    used_bytes: u32,
}

#[derive(Copy, Clone)]
struct TriangleProbeStateLayout {
    binding_table_offset_bytes: u32,
    surface_state_offset_bytes: u32,
    sampler_state_offset_bytes: u32,
    blend_state_offset_bytes: u32,
    color_calc_state_offset_bytes: u32,
    cc_viewport_offset_bytes: u32,
    sf_clip_viewport_offset_bytes: u32,
    scissor_rect_offset_bytes: u32,
    slice_hash_table_offset_bytes: u32,
}

#[derive(Copy, Clone, Debug, Eq, PartialEq)]
struct TriangleFrontEndContract {
    label: &'static str,
    vs_urb_output_length_override: Option<u8>,
    sbe_read_offset: u8,
    sbe_read_length: u8,
    force_sbe_read_offset: bool,
    force_sbe_read_length: bool,
}

#[derive(Copy, Clone)]
struct VsStreamoutProofConfig {
    pipeline: &'static crate::intel::shader::TrianglePipeline,
    shader_layout: TriangleShaderLayout,
}

#[derive(Copy, Clone)]
enum TriangleBlendProbeMode {
    ExplicitRt0,
    Mesa
[... truncated in extract ...]

[2026-05-11T01:13:05.129Z] tool-call:exec_command line=31
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"prepare_gpgpu|submit_gpgpu|build_gpgpu|GPGPU_STORE|SURFTYPE_BUFFER|GPU_VA_GPGPU|HDC1|STORE_SENTINEL|Mandel|mandel\" src/intel/intel/render/gpgpu.rs src/intel/intel/render/*.rs crates/trueos-eu/src/*.rs src/tst/*mandel* src/gfx/mandelbrot.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":20000}

[2026-05-11T01:13:05.154Z] tool-call:exec_command line=33
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '260,620p' src/intel/intel/render/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":26000}

[2026-05-11T01:13:05.154Z] tool-call:exec_command line=34
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1,220p' src/tst/mandelbrot_gpu_sidequest.rs && sed -n '1,220p' src/tst/ui2_mandelbrot_demo.rs && sed -n '1,220p' src/gfx/mandelbrot.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":28000}

[2026-05-11T01:13:05.171Z] tool-output line=36
------------------------------------------------------------------------------------------------
Chunk ID: 151a79
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 5215
Output:
src/tst/mandelbrot_gpu_sidequest.rs:6:pub(crate) const MANDELBROT_GPU_SIDEQUEST_NAME: &str = "mandelbrot-gpu-sidequest";
src/tst/mandelbrot_gpu_sidequest.rs:8:    ".codex_tmp/mandelbrot_fragment_1440p_parametric.frag";
src/tst/mandelbrot_gpu_sidequest.rs:10:    ".codex_tmp/mandelbrot_fragment_1440p_parametric.spv";
src/tst/mandelbrot_gpu_sidequest.rs:16:pub(crate) enum MandelbrotGpuArtifactStage {
src/tst/mandelbrot_gpu_sidequest.rs:21:impl MandelbrotGpuArtifactStage {
src/tst/mandelbrot_gpu_sidequest.rs:31:pub(crate) enum MandelbrotPresentPath {
src/tst/mandelbrot_gpu_sidequest.rs:37:impl MandelbrotPresentPath {
src/tst/mandelbrot_gpu_sidequest.rs:48:pub(crate) struct MandelbrotGpuSidequestPlan {
src/tst/mandelbrot_gpu_sidequest.rs:50:    pub(crate) stage: MandelbrotGpuArtifactStage,
src/tst/mandelbrot_gpu_sidequest.rs:56:    pub(crate) render_path: MandelbrotPresentPath,
src/tst/mandelbrot_gpu_sidequest.rs:57:    pub(crate) fallback_present_path: MandelbrotPresentPath,
src/tst/mandelbrot_gpu_sidequest.rs:60:impl MandelbrotGpuSidequestPlan {
src/tst/mandelbrot_gpu_sidequest.rs:64:            stage: MandelbrotGpuArtifactStage::Fragment,
src/tst/mandelbrot_gpu_sidequest.rs:70:            render_path: MandelbrotPresentPath::BufferFirst,
src/tst/mandelbrot_gpu_sidequest.rs:71:            fallback_present_path: MandelbrotPresentPath::CopyToIntelOverlay,
src/tst/mandelbrot_gpu_sidequest.rs:82:pub(crate) const fn mandelbrot_gpu_sidequest_plan() -> MandelbrotGpuSidequestPlan {
src/tst/mandelbrot_gpu_sidequest.rs:83:    MandelbrotGpuSidequestPlan::default_fragment_buffer_first()
src/tst/mandelbrot_gpu_sidequest.rs:86:pub(crate) fn mandelbrot_fragment_shader_desc(spirv_bytes: &'static [u8]) -> ShaderDesc<'static> {
src/tst/mandelbrot_gpu_sidequest.rs:94:pub(crate) fn spawn_mandelbrot_gpu_sidequest(spawner: Spawner) -> Result<(), SpawnError> {
src/tst/mandelbrot_gpu_sidequest.rs:95:    let token = mandelbrot_gpu_sidequest_task()?;
src/tst/mandelbrot_gpu_sidequest.rs:101:pub(crate) async fn mandelbrot_gpu_sidequest_task() {
src/tst/mandelbrot_gpu_sidequest.rs:102:    let plan = mandelbrot_gpu_sidequest_plan();
src/tst/mandelbrot_gpu_sidequest.rs:108:        "mandelbrot-gpu-sidequest: scaffold name={} called=1 hot=0 artifact_stage={} source={} spirv={} target={}x{} rgba_bytes=0x{:X} push_constants={} render_path={} fallback_present={} scanout={}x{} upload=deferred action=no-submit-no-present\n",
src/intel/intel/render/constants.rs:95:const GPU_VA_GPGPU_TILE_ARENA_BASE: u64 = 0x0400_0000;
src/intel/intel/render/gpgpu.rs:4:const GPU_PROGRAM_SHARED_RAM_WRITE_EXPECTED: u32 = trueos_eu::gfx12::STORE_SENTINEL_U32;
src/intel/intel/render/gpgpu.rs:51:    let artifact = trueos_eu::gfx12::HDC1_BTI34_STORE_THEN_TS_EOT;
src/intel/intel/render/gpgpu.rs:59:const GPGPU_C_STORE_KERNEL_SEND_DWORD: usize = trueos_eu::gfx12::HDC1_BTI34_STORE_SEND_DWORD;
src/intel/intel/render/gpgpu.rs:60:const GPGPU_C_STORE_KERNEL_IMM_DWORD: usize = trueos_eu::gfx12::HDC1_BTI34_STORE_IMM_DWORD;
src/intel/intel/render/gpgpu.rs:61:const GPGPU_STORE_BINDING_TABLE_OFFSET_BYTES: usize = 0x3400;
src/intel/intel/render/gpgpu.rs:62:const GPGPU_STORE_SURFACE_STATE_OFFSET_BYTES: usize = 0x3500;
src/intel/intel/render/gpgpu.rs:63:const GPGPU_STORE_BINDING_TABLE_INDEX: usize = 0x34;
src/intel/intel/render/gpgpu.rs:64:const GPGPU_STORE_BINDING_TABLE_ENTRIES: usize = GPGPU_STORE_BINDING_TABLE_INDEX + 1;
src/intel/intel/render/gpgpu.rs:65:const GPGPU_STORE_SURFACE_DWORDS: usize = 16;
src/intel/intel/render/gpgpu.rs:66:const SURFTYPE_BUFFER: u32 = 4;
src/intel/intel/render/gpgpu.rs:147:pub(crate) fn submit_gpgpu_preflight_once() {
src/intel/intel/render/gpgpu.rs:172:        let eu_artifact = prepare_gpgpu_program_artifact(warm, false);
src/intel/intel/render/gpgpu.rs:199:    let accepted = submit_gpgpu_preflight(dev, warm);
src/intel/intel/render/gpgpu.rs:203:    let eu_artifact = prepare_gpgpu_program_artifact(warm, accepted);
src/intel/intel/render/gpgpu.rs:206:        let walker = submit_gpgpu_compute_walker_probe(dev, warm);
src/intel/intel/render/gpgpu.rs:218:        GPU_VA_GPGPU_TILE_ARENA_BASE
src/intel/intel/render/gpgpu.rs:272:        GPU_VA_GPGPU_TILE_ARENA_BASE,
src/intel/intel/render/gpgpu.rs:303:fn submit_gpgpu_preflight(dev: crate::intel::Dev, warm: RenderWarmState) -> bool {
src/intel/intel/render/gpgpu.rs:444:fn prepare_gpgpu_program_artifact(
src/intel/intel/r
[... truncated in extract ...]

[2026-05-11T01:13:05.171Z] tool-output line=37
------------------------------------------------------------------------------------------------
Chunk ID: 561cdb
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 2296
Output:
use crate::{EuArtifact, EuArtifactKind, EuIsa};

pub const STORE_SENTINEL_U32: u32 = 0xC0DE_7733;
pub const HDC1_BTI34_STORE_SEND_DWORD: usize = 11;
pub const HDC1_BTI34_STORE_IMM_DWORD: usize = 3;
pub const HDC1_STATELESS_STATIC_DP4A_STORE_SEND_DWORD: usize = 23;
pub const HDC1_STATELESS_STATIC_DP4A_BASE_DWORD: usize = 3;
pub const STATIC_DP4A_DOT_ADDEND_U32: u32 = 10;
pub const STATIC_DP4A_BASE_U32: u32 = STORE_SENTINEL_U32 - STATIC_DP4A_DOT_ADDEND_U32;

#[derive(Copy, Clone, Debug, Eq, PartialEq)]
pub enum Gfx12EotVariant {
    TsR0ToG112,
    TsR0ToG120,
    TsR0ToG126,
    TsR0R1ToG126G127Mlen2,
    TsNopThenR0ToG126,
    TsR0ToG127,
    TsR0ToG127Send1,
    GatewayR0ToG127,
    GatewayR0ToG127Dg2,
    TsR0ToG127AccClear,
    IllegalAllOnes,
}

pub static TS_EOT_R0_TO_G112_WORDS: [u32; 8] = [
    0x80030061, 0x70050220, 0x00460005, 0x00000000, 0x80030131, 0x00000004, 0x7000700C, 0x00000000,
];

pub static TS_EOT_R0_TO_G112: EuArtifact = EuArtifact {
    name: "gfx12-ts-eot-r0-to-g112-assembled",
    isa: EuIsa::Gfx12,
    kind: EuArtifactKind::ThreadSpawnerEot,
    words: &TS_EOT_R0_TO_G112_WORDS,
    expects_store: false,
};

pub static TS_EOT_R0_TO_G120_WORDS: [u32; 8] = [
    0x80030061, 0x78050220, 0x00460005, 0x00000000, 0x80030131, 0x00000004, 0x7000780C, 0x00000000,
];

pub static TS_EOT_R0_TO_G120: EuArtifact = EuArtifact {
    name: "gfx12-ts-eot-r0-to-g120-assembled",
    isa: EuIsa::Gfx12,
    kind: EuArtifactKind::ThreadSpawnerEot,
    words: &TS_EOT_R0_TO_G120_WORDS,
    expects_store: false,
};

pub static TS_EOT_R0_TO_G126_WORDS: [u32; 8] = [
    0x80030061, 0x7E050220, 0x00460005, 0x00000000, 0x80030131, 0x00000004, 0x70007E0C, 0x00000000,
];

pub static TS_EOT_R0_TO_G126: EuArtifact = EuArtifact {
    name: "gfx12-ts-eot-r0-to-g126-assembled",
    isa: EuIsa::Gfx12,
    kind: EuArtifactKind::ThreadSpawnerEot,
    words: &TS_EOT_R0_TO_G126_WORDS,
    expects_store: false,
};

// Deliberate Gfx12.0/Xe-LP EOT payload-width probe. The PRM names a single
// GRF R0-copy payload for TS_EOT, but this variant tests whether this legacy
// GPGPU_WALKER path expects a wider header/token image.
//
// Assembled with Mesa brw_asm for `tgl`:
//
// mov(8)  g126<1>UD  g0<8,8,1>UD
// mov(8)  g127<1>UD  g1<8,8,1>UD
// send(8) nullUD     g126UD nullUD 0x04000000 0x00000000
//         ts/btd MsgDesc: mlen 2 ex_mlen 0 rlen 0 EOT
pub static TS_EOT_R0_R1_TO_G126_G127_MLEN2_WORDS: [u32; 12] = [
    0x80030061, 0x7E050220, 0x00460005, 0x00000000, 0x80030061, 0x7F050220, 0x00460105, 0x00000000,
    0x80030131, 0x00000004, 0x70007E14, 0x00000000,
];

pub static TS_EOT_R0_R1_TO_G126_G127_MLEN2: EuArtifact = EuArtifact {
    name: "gfx12-ts-eot-r0-r1-to-g126-g127-mlen2-assembled",
    isa: EuIsa::Gfx12,
    kind: EuArtifactKind::ThreadSpawnerEot,
    words: &TS_EOT_R0_R1_TO_G126_G127_MLEN2_WORDS,
    expects_store: false,
};

// Gfx12.0/Xe-LP single harmless EU instruction before the canonical TS EOT.
// Assembled with Mesa brw_asm for `tgl`:
//
// sync nop(8) null<0,1,0>UB
// mov(8)      g126<1>UD       g0<8,8,1>UD
// send(8)     nullUD          g126UD nullUD 0x02000000 0x00000000 EOT
pub static TS_NOP_THEN_EOT_R0_TO_G126_WORDS: [u32; 12] = [
    0x80030101, 0x00000000, 0x00000000, 0x00000000, 0x80030061, 0x7E050220, 0x00460005, 0x00000000,
    0x80030131, 0x00000004, 0x70007E0C, 0x00000000,
];

pub static TS_NOP_THEN_EOT_R0_TO_G126: EuArtifact = EuArtifact {
    name: "gfx12-ts-nop-then-eot-r0-to-g126-assembled",
    isa: EuIsa::Gfx12,
    kind: EuArtifactKind::ThreadSpawnerEot,
    words: &TS_NOP_THEN_EOT_R0_TO_G126_WORDS,
    expects_store: false,
};

// Gfx12.0/Xe-LP Thread Spawner EOT, assembled with Mesa brw_asm for `tgl`.
// This is the PRM path for media/GPGPU root threads launched by
// MEDIA_INTERFACE_DESCRIPTOR_LOAD + GPGPU_WALKER on ADL-S 8086:4680.
//
// PRM TS_EOT descriptor contract:
// - SFID_TS = 7 on Gfx12.0 and earlier.
// - desc = 0x02000000: mlen 1, rlen 0, message type End Thread.
// - send EOT control is set in the extended descriptor sideband.
// - payload is one GRF copied from the R0 thread payload.
//
// mov(8)  g127<1>UD  g0<8,8,1>UD
// send(8) nullUD     g127UD nullUD 0x02000000 0x00000000
//         ts/btd MsgDesc: mlen 1 ex_mlen 0 rlen 0 EOT
pub static TS_EOT_R0_TO_G127_WORDS: [u32; 8] = [
    0x80030061, 0x7F050220, 0x00460005, 0x00000000, 0x80030131, 0x00000004, 0x70007F0C, 0x00000000,
];
[... truncated in extract ...]

[2026-05-11T01:13:05.189Z] tool-output line=38
------------------------------------------------------------------------------------------------
Chunk ID: 76c12a
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 3580
Output:
fn ensure_gpgpu_tile_arena_mapped(dev: crate::intel::Dev, warm: RenderWarmState) -> bool {
    if GPGPU_TILE_ARENA_MAPPED.load(Ordering::Acquire) {
        return true;
    }
    if warm.gpgpu_arena_len == 0 {
        return false;
    }

    let mapped = super::map_ggtt(
        dev,
        warm.gpgpu_arena_phys,
        warm.gpgpu_arena_len,
        GPU_VA_GPGPU_TILE_ARENA_BASE,
    );
    if mapped {
        super::ggtt_invalidate(dev);
        GPGPU_TILE_ARENA_MAPPED.store(true, Ordering::Release);
    }
    mapped
}

fn log_gpgpu_tile_arena_status(warm: RenderWarmState, mapped: bool) {
    if GPGPU_TILE_ARENA_STATUS_LOGGED.swap(true, Ordering::AcqRel) {
        return;
    }

    let arena_bytes = warm.gpgpu_arena_len;
    crate::log!(
        "intel/gpgpu: arena mapped={} arena_gpu_base=0x{:X} arena_bytes=0x{:X} tile_rows={} max_tiles={} enough_for_shape={} tile_k={} weight_tile_bytes=0x{:X} x_bytes=0x{:X} output_tile_bytes=0x{:X} target_tiles={} does_not_prove=eu_thread_execution_or_matvec\n",
        mapped as u8,
        gpgpu_arena_gpu_base(arena_bytes),
        arena_bytes,
        GPGPU_TILE_ROWS,
        gpgpu_arena_max_tiles(arena_bytes),
        gpgpu_arena_enough_for_shape(arena_bytes) as u8,
        GPGPU_TILE_K_DIM,
        GPGPU_WEIGHT_TILE_BYTES,
        GPGPU_X_VECTOR_BYTES,
        GPGPU_OUTPUT_TILE_BYTES,
        GPGPU_TILE_TARGET_TILES,
    );
}

fn submit_gpgpu_preflight(dev: crate::intel::Dev, warm: RenderWarmState) -> bool {
    let a = [1u32, 2, 3, 4];
    let b = [10u32, 20, 30, 40];
    let sum_a = a.iter().copied().fold(0u32, u32::wrapping_add);
    let sum_b = b.iter().copied().fold(0u32, u32::wrapping_add);
    let dot = a
        .iter()
        .copied()
        .zip(b.iter().copied())
        .fold(0u32, |acc, (lhs, rhs)| acc.wrapping_add(lhs.wrapping_mul(rhs)));

    unsafe {
        core::ptr::write_bytes(warm.batch_virt, 0, warm.batch_len);
        core::ptr::write_bytes(warm.ring_virt, 0, warm.ring_len);
        core::ptr::write_bytes(warm.result_virt, 0, warm.result_len);

        let input_a = warm.vertex_virt as *mut u32;
        let input_b = warm.streamout_virt as *mut u32;
        for i in 0..GPGPU_PREFLIGHT_LANES {
            core::ptr::write_volatile(input_a.add(i), a[i]);
            core::ptr::write_volatile(input_b.add(i), b[i]);
        }
    }
    seed_result_debug_slots(warm);
    crate::intel::dma_flush(warm.vertex_virt, GPGPU_PREFLIGHT_LANES * core::mem::size_of::<u32>());
    crate::intel::dma_flush(
        warm.streamout_virt,
        GPGPU_PREFLIGHT_LANES * core::mem::size_of::<u32>(),
    );
    crate::intel::dma_flush(warm.result_virt, warm.result_len);

    let total_dwords = warm.batch_len / core::mem::size_of::<u32>();
    let batch =
        unsafe { core::slice::from_raw_parts_mut(warm.batch_virt as *mut u32, total_dwords) };
    let batch_tail_bytes = match encode_gpgpu_preflight_batch(batch, dot, sum_a, sum_b) {
        Ok(bytes) => bytes,
        Err(reason) => {
            crate::log!("intel/gpgpu: preflight accepted=0 reason={}\n", reason);
            return false;
        }
    };
    crate::intel::dma_flush(warm.batch_virt, batch_tail_bytes);

    let completed = submit_warm_render_batch(
        dev,
        warm,
        RCS_EXEC_RESULT_GPGPU_PREFLIGHT_DONE,
        RESULT_SLOT_GPGPU_PREFLIGHT_MARKER_DWORD,
        "gpgpu-preflight",
    );
    crate::intel::dma_flush(warm.result_virt, warm.result_len);

    let marker = read_result_dword(warm, RESULT_SLOT_GPGPU_PREFLIGHT_MARKER_DWORD);
    let gpu_dot = read_result_dword(warm, RESULT_SLOT_GPGPU_PREFLIGHT_DOT_DWORD);
    let gpu_sum_a = read_result_dword(warm, RESULT_SLOT_GPGPU_PREFLIGHT_SUM_A_DWORD);
    let gpu_sum_b = read_result_dword(warm, RESULT_SLOT_GPGPU_PREFLIGHT_SUM_B_DWORD);
    let gpu_lanes = read_result_dword(warm, RESULT_SLOT_GPGPU_PREFLIGHT_LANES_DWORD);
    let guc_status = crate::intel::guc_status(warm);
    let accepted = completed
        && marker == RCS_EXEC_RESULT_GPGPU_PREFLIGHT_DONE
        && gpu_dot == dot
        && gpu_sum_a == sum_a
        && gpu_sum_b == sum_b
        && gpu_lanes == GPGPU_PREFLIGHT_LANES as u32;
    GPGPU_PREFLIGHT_COMPLETED.store(completed, Ordering::Release);
    GPGPU_PREFLIGHT_ACCEPTED.store(accepted, Ordering::Release);
    GPGPU_PREFLIGHT_MARKER.store(marker, Ordering::Release);
    GPGPU_PREFLIGHT_DOT.store(gpu_dot, Ordering::Rel
[... truncated in extract ...]

[2026-05-11T01:13:05.189Z] tool-output line=39
------------------------------------------------------------------------------------------------
Chunk ID: aaedc6
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 3527
Output:
#![allow(dead_code)]

use embassy_executor::{SpawnError, Spawner};
use trueos_gfx_core::{ShaderDesc, ShaderFormat, ShaderStage};

pub(crate) const MANDELBROT_GPU_SIDEQUEST_NAME: &str = "mandelbrot-gpu-sidequest";
pub(crate) const MANDELBROT_FRAGMENT_SOURCE_PATH: &str =
    ".codex_tmp/mandelbrot_fragment_1440p_parametric.frag";
pub(crate) const MANDELBROT_FRAGMENT_SPIRV_PATH: &str =
    ".codex_tmp/mandelbrot_fragment_1440p_parametric.spv";
pub(crate) const MANDELBROT_TARGET_WIDTH: u32 = 2560;
pub(crate) const MANDELBROT_TARGET_HEIGHT: u32 = 1440;
pub(crate) const MANDELBROT_PUSH_CONSTANT_BYTES: u16 = 24;

#[derive(Copy, Clone, Debug, Eq, PartialEq)]
pub(crate) enum MandelbrotGpuArtifactStage {
    Fragment,
    Compute,
}

impl MandelbrotGpuArtifactStage {
    pub(crate) const fn as_str(self) -> &'static str {
        match self {
            Self::Fragment => "fragment",
            Self::Compute => "compute",
        }
    }
}

#[derive(Copy, Clone, Debug, Eq, PartialEq)]
pub(crate) enum MandelbrotPresentPath {
    BufferFirst,
    CopyToIntelOverlay,
    DirectFramebuffer,
}

impl MandelbrotPresentPath {
    pub(crate) const fn as_str(self) -> &'static str {
        match self {
            Self::BufferFirst => "buffer-first",
            Self::CopyToIntelOverlay => "copy-to-intel-overlay",
            Self::DirectFramebuffer => "direct-framebuffer",
        }
    }
}

#[derive(Copy, Clone, Debug, Eq, PartialEq)]
pub(crate) struct MandelbrotGpuSidequestPlan {
    pub(crate) name: &'static str,
    pub(crate) stage: MandelbrotGpuArtifactStage,
    pub(crate) source_path: &'static str,
    pub(crate) spirv_path: &'static str,
    pub(crate) target_width: u32,
    pub(crate) target_height: u32,
    pub(crate) push_constant_bytes: u16,
    pub(crate) render_path: MandelbrotPresentPath,
    pub(crate) fallback_present_path: MandelbrotPresentPath,
}

impl MandelbrotGpuSidequestPlan {
    pub(crate) const fn default_fragment_buffer_first() -> Self {
        Self {
            name: MANDELBROT_GPU_SIDEQUEST_NAME,
            stage: MandelbrotGpuArtifactStage::Fragment,
            source_path: MANDELBROT_FRAGMENT_SOURCE_PATH,
            spirv_path: MANDELBROT_FRAGMENT_SPIRV_PATH,
            target_width: MANDELBROT_TARGET_WIDTH,
            target_height: MANDELBROT_TARGET_HEIGHT,
            push_constant_bytes: MANDELBROT_PUSH_CONSTANT_BYTES,
            render_path: MandelbrotPresentPath::BufferFirst,
            fallback_present_path: MandelbrotPresentPath::CopyToIntelOverlay,
        }
    }

    pub(crate) const fn target_rgba_bytes(self) -> usize {
        (self.target_width as usize)
            .saturating_mul(self.target_height as usize)
            .saturating_mul(4)
    }
}

pub(crate) const fn mandelbrot_gpu_sidequest_plan() -> MandelbrotGpuSidequestPlan {
    MandelbrotGpuSidequestPlan::default_fragment_buffer_first()
}

pub(crate) fn mandelbrot_fragment_shader_desc(spirv_bytes: &'static [u8]) -> ShaderDesc<'static> {
    ShaderDesc {
        stage: ShaderStage::Fragment,
        format: ShaderFormat::SpirV,
        bytes: spirv_bytes,
    }
}

pub(crate) fn spawn_mandelbrot_gpu_sidequest(spawner: Spawner) -> Result<(), SpawnError> {
    let token = mandelbrot_gpu_sidequest_task()?;
    spawner.spawn(token);
    Ok(())
}

#[embassy_executor::task(pool_size = 1)]
pub(crate) async fn mandelbrot_gpu_sidequest_task() {
    let plan = mandelbrot_gpu_sidequest_plan();
    let scanout = crate::intel::active_scanout_dimensions();
    let scanout_w = scanout.map(|dims| dims.0).unwrap_or(0);
    let scanout_h = scanout.map(|dims| dims.1).unwrap_or(0);

    crate::log!(
        "mandelbrot-gpu-sidequest: scaffold name={} called=1 hot=0 artifact_stage={} source={} spirv={} target={}x{} rgba_bytes=0x{:X} push_constants={} render_path={} fallback_present={} scanout={}x{} upload=deferred action=no-submit-no-present\n",
        plan.name,
        plan.stage.as_str(),
        plan.source_path,
        plan.spirv_path,
        plan.target_width,
        plan.target_height,
        plan.target_rgba_bytes(),
        plan.push_constant_bytes,
        plan.render_path.as_str(),
        plan.fallback_present_path.as_str(),
        scanout_w,
        scanout_h
    );
}
use embassy_time::{Duration as EmbassyDuration, Timer};

const UI2_MANDELBROT_TEX_ID: u32 = crate::tst_ui2_ids::Ui2DemoTexId::Mandelbrot.get();
const UI2_MANDE
[... truncated in extract ...]

[2026-05-11T01:13:15.564Z] tool-call:exec_command line=41
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '620,980p' src/intel/intel/render/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":28000}

[2026-05-11T01:13:15.572Z] tool-call:exec_command line=42
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '220,520p' src/gfx/mandelbrot.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":22000}

[2026-05-11T01:13:15.573Z] tool-call:exec_command line=43
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1,180p' src/intel/intel/render/constants.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":12000}

[2026-05-11T01:13:15.588Z] tool-call:exec_command line=44
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"submit_gpgpu_preflight_once|gpgpu_preflight|spawn_mandelbrot|render_mandelbrot|MANDELBROT_GPU\" src crates -S","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":16000}

[2026-05-11T01:13:15.985Z] tool-output line=46
------------------------------------------------------------------------------------------------
Chunk ID: ca5971
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 3884
Output:
            0
        },
        proof.expects_store as u8,
    );
    log_gpgpu_eot_send_contract(proof.program_name, proof.program_uploaded, program);
}

fn log_gpgpu_eot_send_contract(program_name: &'static str, uploaded: bool, program: &'static [u32]) {
    if program.len() < 4 {
        return;
    }
    let send_base = program.len() - 4;
    let send_w0 = program[send_base];
    let send_w1 = program[send_base + 1];
    let send_w2 = program[send_base + 2];
    let send_w3 = program[send_base + 3];
    let eot_bit = (send_w1 >> 2) & 1;
    let desc_is_reg = (send_w1 >> 16) & 1;
    let exdesc_is_reg = (send_w1 >> 17) & 1;
    let dst_reg_file = (send_w1 >> 18) & 1;
    let response_len = (send_w1 >> 19) & 0x1F;
    let dst_reg_num = (send_w1 >> 24) & 0xFF;
    let src0_mlen = (send_w2 >> 3) & 0xF;
    let src0_reg_num = (send_w2 >> 8) & 0xFF;
    let sfid = (send_w2 >> 28) & 0xF;
    let sfid_gateway = sfid == 3;
    let sfid_ts = sfid == 7;
    let dst_null_like = dst_reg_file == 0 && dst_reg_num == 0 && response_len == 0;
    let desc_immediate = desc_is_reg == 0;
    let exdesc_immediate = exdesc_is_reg == 0;
    let src_in_eot_safe_window = (112..=127).contains(&src0_reg_num);
    let target_supports_eot = sfid_gateway || sfid_ts;
    crate::log!(
        "intel/gpgpu: eot-send-contract source={} uploaded={} send_word_off={} send_w0=0x{:08X} send_w1=0x{:08X} send_w2=0x{:08X} send_w3=0x{:08X} eot_bit={} desc_is_reg={} exdesc_is_reg={} desc_immediate={} exdesc_immediate={} dst_reg_file={} dst_reg_num={} response_len={} dst_null_like={} src0_g={} src0_mlen={} src_in_eot_safe_window={} sfid={} sfid_gateway={} sfid_ts={} target_supports_eot={} prm_rules_ok={} probe=selected-pure-eot-artifact expected_good=post_walker_marker-or-eot_retired failure_disproves=selected-eot-payload-shape note=decoded-from-gfx12-send-format\n",
        program_name,
        uploaded as u8,
        send_base,
        send_w0,
        send_w1,
        send_w2,
        send_w3,
        eot_bit,
        desc_is_reg,
        exdesc_is_reg,
        desc_immediate as u8,
        exdesc_immediate as u8,
        dst_reg_file,
        dst_reg_num,
        response_len,
        dst_null_like as u8,
        src0_reg_num,
        src0_mlen,
        src_in_eot_safe_window as u8,
        sfid,
        sfid_gateway as u8,
        sfid_ts as u8,
        target_supports_eot as u8,
        (eot_bit == 1
            && desc_immediate
            && exdesc_immediate
            && dst_null_like
            && src_in_eot_safe_window
            && target_supports_eot) as u8,
    );
}

fn prepare_gpgpu_store_surface_state(warm: RenderWarmState) -> GpgpuStoreSurfaceState {
    let target_gpu = GPU_VA_RESULT_BASE
        + (RESULT_SLOT_GPGPU_EU_C_STORE_DWORD as u64) * core::mem::size_of::<u32>() as u64;
    let binding_table_bytes = GPGPU_STORE_BINDING_TABLE_ENTRIES * core::mem::size_of::<u32>();
    let surface_bytes = GPGPU_STORE_SURFACE_DWORDS * core::mem::size_of::<u32>();
    let binding_end = GPGPU_STORE_BINDING_TABLE_OFFSET_BYTES.saturating_add(binding_table_bytes);
    let surface_end = GPGPU_STORE_SURFACE_STATE_OFFSET_BYTES.saturating_add(surface_bytes);
    let binding_table_aligned = GPGPU_STORE_BINDING_TABLE_OFFSET_BYTES & 0x3F == 0;
    let surface_aligned = GPGPU_STORE_SURFACE_STATE_OFFSET_BYTES & 0x3F == 0;
    let ready = binding_table_aligned
        && surface_aligned
        && binding_end <= warm.draw_state_len
        && surface_end <= warm.draw_state_len;
    if !ready {
        crate::log!(
            "intel/gpgpu: gpu-program-surface-state ready=0 reason=draw-state-bounds bt_off=0x{:X} bt_bytes=0x{:X} surf_off=0x{:X} surf_bytes=0x{:X} draw_state_len=0x{:X}\n",
            GPGPU_STORE_BINDING_TABLE_OFFSET_BYTES,
            binding_table_bytes,
            GPGPU_STORE_SURFACE_STATE_OFFSET_BYTES,
            surface_bytes,
            warm.draw_state_len,
        );
        return GpgpuStoreSurfaceState {
            ready: false,
            binding_table_offset: GPGPU_STORE_BINDING_TABLE_OFFSET_BYTES,
            surface_state_offset: GPGPU_STORE_SURFACE_STATE_OFFSET_BYTES,
            binding_table_index: GPGPU_STORE_BINDING_TABLE_INDEX,
            surface_gpu: GPU_VA_DRAW_STATE_BASE + GPGPU_STORE_SURFACE_STATE_OFFSET_BYTES as u64,
            target_gpu,
            surface_dword0: 0,
            binding_entry: 0,
        };
[... truncated in extract ...]

[2026-05-11T01:13:15.985Z] tool-output line=48
------------------------------------------------------------------------------------------------
Chunk ID: d7ff8e
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 2187
Output:
const FORCEWAKE_RENDER: usize = 0x0A278;
const FORCEWAKE_GT: usize = 0x0A188;
const FORCEWAKE_ACK_RENDER: usize = 0x0D84;
const FORCEWAKE_ACK_GT: usize = 0x130044;
const FORCEWAKE_KERNEL: u32 = 1 << 0;
const FORCEWAKE_FALLBACK: u32 = 1 << 15;
const TBIMR_BATCH_SIZE_OVERRIDE: u32 = 1 << 1;
const TBIMR_OPEN_BATCH_ENABLE: u32 = 1 << 4;
const TBIMR_FAST_CLIP: u32 = 1 << 5;
const FF_DOP_CLOCK_GATE_DISABLE: u32 = 1 << 1;
const FORCEWAKE_POLL_ITERS: usize = 20_000;
const RCS_RING_BASE: usize = 0x0000_2000;
const RCS_CS_GPR_REL_BASE: usize = 0x600;
const RCS_CS_GPR_BASE: usize = RCS_RING_BASE + 0x600;
const RCS_CS_GPR_COUNT: usize = 16;
const RCS_RING_TAIL: usize = RCS_RING_BASE + 0x30;
const RCS_RING_HEAD: usize = RCS_RING_BASE + 0x34;
const RCS_RING_START: usize = RCS_RING_BASE + 0x38;
const RCS_RING_CTL: usize = RCS_RING_BASE + 0x3C;
const RCS_RING_PSMI_CTL: usize = RCS_RING_BASE + 0x50;
const RCS_RING_ACTHD_UDW: usize = RCS_RING_BASE + 0x5C;
const RCS_RING_DMA_FADD_UDW: usize = RCS_RING_BASE + 0x60;
const RCS_RING_ACTHD: usize = RCS_RING_BASE + 0x74;
const RCS_RING_INSTPS: usize = RCS_RING_BASE + 0x70;
const RCS_RING_DMA_FADD: usize = RCS_RING_BASE + 0x78;
const RCS_RING_NOPID: usize = RCS_RING_BASE + 0x94;
const RCS_RING_MI_MODE: usize = RCS_RING_BASE + 0x9C;
const RCS_RING_IMR: usize = RCS_RING_BASE + 0xA8;
const RCS_CS_DEBUG_MODE1: usize = RCS_RING_BASE + 0xEC;
const RCS_CS_DEBUG_MODE2: usize = RCS_RING_BASE + 0xD8;
const RCS_RING_EIR: usize = RCS_RING_BASE + 0xB0;
const RCS_RING_ESR: usize = RCS_RING_BASE + 0xB8;
const RCS_RING_IPEIR: usize = RCS_RING_BASE + 0x64;
const RCS_RING_IPEHR: usize = RCS_RING_BASE + 0x68;
const RCS_RING_INSTDONE: usize = RCS_RING_BASE + 0x6C;
const RCS_RING_INSTPM: usize = RCS_RING_BASE + 0xC0;
const RCS_RING_BBSTATE: usize = RCS_RING_BASE + 0x110;
const RCS_RING_BBADDR: usize = RCS_RING_BASE + 0x140;
const RCS_RING_BBADDR_UDW: usize = RCS_RING_BASE + 0x168;
const GEN8_RING_FAULT_REG: usize = 0x4094;
const GEN8_FAULT_TLB_DATA0: usize = 0x4B10;
const GEN8_FAULT_TLB_DATA1: usize = 0x4B14;
const GEN12_FAULT_TLB_DATA0: usize = 0xCEB8;
const GEN12_FAULT_TLB_DATA1: usize = 0xCEBC;
const GEN12_RING_FAULT_REG: usize = 0xCEC4;
const ERROR_GEN6: usize = 0x40A0;
const GFX_MODE: usize = 0x2520;
const GEN12_RCU_MODE: usize = 0x14800;
const GEN12_RCU_MODE_CCS_ENABLE: u32 = 1 << 0;
const CHICKEN_RASTER_2: usize = 0x6208;
const INSTDONE_GEOM: usize = 0x666C;
const SC_INSTDONE: usize = 0x7100;
const SC_INSTDONE_EXTRA: usize = 0x7104;
const SC_INSTDONE_EXTRA2: usize = 0x7108;
const SAMPLER_INSTDONE: usize = 0xE160;
const ROW_INSTDONE: usize = 0xE164;
const TDL_THR_STATUS0: usize = 0xE4B8;
const TDL_THR_DISP_COUNT: usize = 0xE4BC;
const TDL_THR_STATUS1: usize = 0xE5B8;
const TDL_THR_PF_COUNT: usize = 0xE5BC;
const TDL_THR_PF_STATUS0: usize = 0xE6B8;
const TDL_THR_PF_STATUS1: usize = 0xE7B8;
const RCS_RING_CONTEXT_CONTROL: usize = RCS_RING_BASE + 0x244;
const RCS_RING_CONTEXT_CONTROL_REF: usize = RCS_RING_BASE + 0x5A0;
const RCS_RING_MODE_GEN7: usize = RCS_RING_BASE + 0x29C;
const RCS_RING_EXECLIST_SUBMIT_PORT: usize = RCS_RING_BASE + 0x230;
const RCS_RING_EXECLIST_STATUS_LO: usize = RCS_RING_BASE + 0x234;
const RCS_RING_EXECLIST_STATUS_HI: usize = RCS_RING_BASE + 0x238;
const RCS_RING_EXECLIST_CONTROL: usize = RCS_RING_BASE + 0x550;
const RCS_RING_EXECLIST_SQ_LO: usize = RCS_RING_BASE + 0x510;
const RCS_RING_EXECLIST_SQ_HI: usize = RCS_RING_BASE + 0x514;
const RCS_RING_HWS_PGA: usize = RCS_RING_BASE + 0x80;
const GDRST: usize = 0x0000_941C;
const CURSOR_A_OFFSET: usize = 0x70080;
const CURSOR_B_OFFSET: usize = 0x71080;
const CURSOR_C_OFFSET: usize = 0x72080;
const CURSOR_D_OFFSET: usize = 0x73080;
const WARM_RING_BYTES: usize = 4096;
const WARM_CONTEXT_BYTES: usize = 22 * 4096;
const WARM_BATCH_BYTES: usize = 512 * 4096;
const WARM_DRAW_STATE_BYTES: usize = 16 * 4096;
const WARM_VERTEX_BYTES: usize = 4096;
const WARM_RESULT_BYTES: usize = 4096;
const WARM_STREAMOUT_BYTES: usize = 4096;
const BLT_RING_DWORDS: usize = 4;
const BLT_RING_TAIL_BYTES: usize = BLT_RING_DWORDS * core::mem::size_of::<u32>();
const LRC_STATE_OFFSET_DWORDS: usize = 4096 / core::mem::size_of::<u32>();
const GPU_VA_RING_BASE: u64 = 0x0080_0000;
const GPU_VA_CONTEXT_BASE: u64 = 0x0081_0000;
const GPU_VA_BATCH_BASE: u64 = 0x0180_0000;
const GPU_VA_RESULT_BASE: u64 = 0x0084_0000;
const GPU_VA_DRAW_STATE_BASE: u64 = 0x0086_0000;
const GPU_
[... truncated in extract ...]

[2026-05-11T01:13:15.985Z] tool-output line=49
------------------------------------------------------------------------------------------------
Chunk ID: 865dfa
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 947
Output:
src/tst/ui2_mandelbrot_demo.rs:51:        if !surface.render_mandelbrot(elapsed_ticks, tick_hz, "ui2-mandelbrot-demo") {
crates/trueos-sys/src/vcabi.rs:303:    pub fn trueos_cabi_gfx_queue_render_mandelbrot_to_texture(
src/lumen/gpu_shadow.rs:36:    let gpu = crate::intel::gpgpu_preflight_status();
src/lumen/gpu_shadow.rs:189:    let gpu = crate::intel::gpgpu_preflight_status();
src/lumen/burn_baba.rs:200:    let gpu = crate::intel::gpgpu_preflight_status();
src/tst/mandelbrot_gpu_sidequest.rs:6:pub(crate) const MANDELBROT_GPU_SIDEQUEST_NAME: &str = "mandelbrot-gpu-sidequest";
src/tst/mandelbrot_gpu_sidequest.rs:63:            name: MANDELBROT_GPU_SIDEQUEST_NAME,
src/tst/mandelbrot_gpu_sidequest.rs:94:pub(crate) fn spawn_mandelbrot_gpu_sidequest(spawner: Spawner) -> Result<(), SpawnError> {
src/ui2/ui2_win.rs:1565:    pub fn render_mandelbrot(
src/ui2/ui2_win.rs:1576:        if !crate::r::io::cabi::queue_render_mandelbrot_to_texture(
src/r/gfx_cabi.rs:2507:    pub fn queue_render_mandelbrot_to_texture(
src/r/gfx_cabi.rs:2745:                    let rc = render_mandelbrot_to_texture_now(req.tex_id, req.ticks, req.tick_hz);
src/r/gfx_cabi.rs:4921:    fn render_mandelbrot_to_texture_now(tex_id: u32, ticks: u64, tick_hz: u64) -> i32 {
src/r/gfx_cabi.rs:6138:    pub extern "C" fn trueos_cabi_gfx_queue_render_mandelbrot_to_texture(
src/r/gfx_cabi.rs:6144:        if queue_render_mandelbrot_to_texture(
src/intel/intel/mod.rs:234:    self::render::submit_gpgpu_preflight_once();
src/intel/intel/mod.rs:310:pub(crate) fn gpgpu_preflight_status() -> self::render::GpgpuPreflightStatus {
src/intel/intel/mod.rs:311:    self::render::gpgpu_preflight_status()
src/intel/mod.rs:386:    self::gpgpu::submit_gpgpu_preflight_once();
src/intel/mod.rs:455:pub(crate) fn gpgpu_preflight_status() -> GpgpuPreflightStatus {
src/intel/mod.rs:456:    let status = self::gpgpu::gpgpu_preflight_status();
src/intel/gpgpu.rs:1671:pub(crate) fn gpgpu_preflight_status() -> GpgpuPreflightStatus {
src/intel/gpgpu.rs:1722:pub(crate) fn submit_gpgpu_preflight_once() {
src/intel/gpgpu.rs:1759:    let accepted = submit_gpgpu_preflight(dev, warm);
src/intel/gpgpu.rs:4884:fn submit_gpgpu_preflight(dev: crate::intel::Dev, warm: RenderWarmState) -> bool {
src/intel/gpgpu.rs:4918:    let batch_tail_bytes = match encode_gpgpu_preflight_batch(batch, dot, sum_a, sum_b) {
src/intel/gpgpu.rs:6781:fn encode_gpgpu_preflight_batch(
src/intel/intel/render/gpgpu.rs:114:pub(crate) fn gpgpu_preflight_status() -> GpgpuPreflightStatus {
src/intel/intel/render/gpgpu.rs:147:pub(crate) fn submit_gpgpu_preflight_once() {
src/intel/intel/render/gpgpu.rs:199:    let accepted = submit_gpgpu_preflight(dev, warm);
src/intel/intel/render/gpgpu.rs:303:fn submit_gpgpu_preflight(dev: crate::intel::Dev, warm: RenderWarmState) -> bool {
src/intel/intel/render/gpgpu.rs:337:    let batch_tail_bytes = match encode_gpgpu_preflight_batch(batch, dot, sum_a, sum_b) {
src/intel/intel/render/gpgpu.rs:2019:fn encode_gpgpu_preflight_batch(
crates/trueos-math/src/lib.rs:113:fn render_mandelbrot_bw_view_f128(
crates/trueos-math/src/lib.rs:147:fn render_mandelbrot_rgb32_view_f128(
crates/trueos-math/src/lib.rs:193:pub fn render_mandelbrot(buffer: &mut [u8], width: usize, height: usize, max_iter: u32) {
crates/trueos-math/src/lib.rs:203:    render_mandelbrot_bw_view_f128(
crates/trueos-math/src/lib.rs:211:pub fn render_mandelbrot_rgb32(buffer: &mut [u32], width: usize, height: usize, max_iter: u32) {
crates/trueos-math/src/lib.rs:220:    render_mandelbrot_rgb32_view_f128(
crates/trueos-math/src/lib.rs:228:pub fn render_mandelbrot_rgb32_view(
crates/trueos-math/src/lib.rs:246:    render_mandelbrot_rgb32_view_f128(
crates/trueos-math/src/lib.rs:269:        render_mandelbrot(&mut buffer, WIDTH, HEIGHT, MAX_ITER);

[2026-05-11T01:13:24.183Z] assistant line=51
------------------------------------------------------------------------------------------------
There are two Intel trees with similar GPGPU code (`src/intel/intel/...` and `src/intel/...`). The nested render modules appear to be the current factored path, while the top-level `src/intel/gpgpu.rs` still carries a larger copy; I’m checking call sites so I don’t accidentally report a stale integration point as active.

[2026-05-11T01:13:24.184Z] assistant line=52
------------------------------------------------------------------------------------------------
There are two Intel trees with similar GPGPU code (`src/intel/intel/...` and `src/intel/...`). The nested render modules appear to be the current factored path, while the top-level `src/intel/gpgpu.rs` still carries a larger copy; I’m checking call sites so I don’t accidentally report a stale integration point as active.

[2026-05-11T01:13:24.189Z] tool-call:exec_command line=53
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '180,330p' src/intel/intel/mod.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":12000}

[2026-05-11T01:13:24.189Z] tool-call:exec_command line=54
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '340,470p' src/intel/mod.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":12000}

[2026-05-11T01:13:24.208Z] tool-call:exec_command line=55
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1980,2120p' src/intel/intel/render/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":16000}

[2026-05-11T01:13:24.248Z] tool-output line=58
------------------------------------------------------------------------------------------------
Chunk ID: 62ed9b
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1309
Output:
        dev.device_id,
        dev.revision_id,
        dev.mmio_len
    );
    *CLAIMED_DEVICE.lock() = Some(dev);
    let fw = self::guc::load_fw();
    if fw.len == 0 {
        crate::log!("intel/guc: firmware module missing or invalid\n");
        return;
    }
    crate::log!(
        "intel/guc: firmware found phys=0x{:X} gpu=0x{:X} len=0x{:X} xfer=0x{:X}\n",
        fw.phys,
        fw.gpu,
        fw.len,
        fw.xfer_len
    );
    let ads = self::guc::alloc_ads(fw.private_data_size);
    if ads.len == 0 {
        crate::log!("intel/guc: ads alloc failed private_data=0x{:X}\n", fw.private_data_size);
        return;
    }
    if !map_ggtt(dev, fw.phys, fw.len, fw.gpu) || !map_ggtt(dev, ads.phys, ads.len, ads.gpu) {
        crate::log!("intel/guc: ggtt map failed fw_len=0x{:X} ads_len=0x{:X}\n", fw.len, ads.len);
        return;
    }
    ggtt_invalidate(dev);
    forcewake(dev);
    let ready = self::guc::bootstrap(dev, fw, ads);
    let status = self::guc::status(dev);
    let (bootrom, ukernel, auth) = self::guc::describe_status(status);
    crate::log!(
        "intel/guc: bootstrap ready={} status=0x{:08X} bootrom={} ukernel={} auth=0x{:X}\n",
        ready as u8,
        status,
        bootrom,
        ukernel,
        auth
    );
    if ready {
        self::guc::prove_h2g_mmio_once(dev, "boot-control-ctb-disable");
    }
    let warm = self::render::warm_once(dev);
    self::render::log_cursor_plane_info(warm);
    self::render::log_sprite_plane_info(warm);
    if DISPLAY_PLANE1_BOOT_DEMO_ENABLED {
        self::display::init_primary_boot_surface(dev);
    } else {
        crate::log!("intel/display: plane1 boot demo disabled\n");
    }
    if self::render::forcewake_render_acquire(warm) {
        self::render::forcewake_render_sanity(warm);
    }
    self::render::submit_primary_triangle_once();
    self::render::submit_gpgpu_preflight_once();
    if RENDER_BOOT_PROBE_LOOP_ENABLED {
        crate::log!(
            "intel/render: scheduled periodic probe interval_ms={}\n",
            RENDER_BOOT_PROBE_INTERVAL_MS
        );
        crate::wait::spawn_local_detached(async move {
            loop {
                Timer::after(EmbassyDuration::from_millis(RENDER_BOOT_PROBE_INTERVAL_MS)).await;
                self::render::submit_primary_probe_periodic();
            }
        });
    }
    crate::log!("intel/media: source warmup disabled trigger=trueosfs-root-mounted\n",);
    if MEDIA_BOOT_DEMO_ENABLED {
        crate::log!("intel/media: scheduled boot demo delay_ms={}\n", MEDIA_BOOT_DEMO_DELAY_MS);
        crate::wait::spawn_local_detached(async move {
            Timer::after(EmbassyDuration::from_millis(MEDIA_BOOT_DEMO_DELAY_MS)).await;
            let queued_at_ms = embassy_time::Instant::now().as_millis() as u64;
            if let Some((slot, worker_spawner)) = pick_media_boot_demo_spawner() {
                match media_boot_demo_task(slot, queued_at_ms) {
                    Ok(token) => {
                        crate::log!(
                            "intel/media: boot demo handoff target_slot={} mode=worker\n",
                            slot,
                        );
                        worker_spawner.spawn(token);
                        return;
                    }
                    Err(err) => {
                        crate::log!(
                            "intel/media: boot demo handoff failed target_slot={} err={:?} fallback=local\n",
                            slot,
                            err,
                        );
                    }
                }
            } else {
                crate::log!(
                    "intel/media: boot demo handoff skipped reason=no-worker-ap fallback=local\n"
                );
            }

            log_media_demo_task_profile("local", 0, queued_at_ms);
            crate::log!("intel/media: boot demo begin\n");
            let first_frame = self::run_media2_first_frame_async().await;
            crate::log!(
                "intel/media: boot demo first-frame origin=local returned={}\n",
                first_frame.is_some() as u8,
            );
        });
    } else {
        crate::log!("intel/media: boot demo disabled\n");
    }
}

pub fn guc_ready() -> bool {
    self::guc::ready()
}

pub(crate) fn guc_h2g_mmio_accepted() -> bool {
    self::guc::h2g_mmio_accepted()
}

pub fn has_claimed_device() ->
[... truncated in extract ...]

[2026-05-11T01:13:24.248Z] tool-output line=59
------------------------------------------------------------------------------------------------
Chunk ID: 8546b0
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1169
Output:
    *CLAIMED_DEVICE.lock() = Some(dev);
    let fw = self::guc::load_fw();
    if fw.len == 0 {
        crate::log!("intel/guc: firmware module missing or invalid\n");
        return;
    }
    crate::log!(
        "intel/guc: firmware found phys=0x{:X} gpu=0x{:X} len=0x{:X} xfer=0x{:X}\n",
        fw.phys,
        fw.gpu,
        fw.len,
        fw.xfer_len
    );
    let ads = self::guc::alloc_ads(fw.private_data_size);
    if ads.len == 0 {
        crate::log!("intel/guc: ads alloc failed private_data=0x{:X}\n", fw.private_data_size);
        return;
    }
    if !map_ggtt(dev, fw.phys, fw.len, fw.gpu) || !map_ggtt(dev, ads.phys, ads.len, ads.gpu) {
        crate::log!("intel/guc: ggtt map failed fw_len=0x{:X} ads_len=0x{:X}\n", fw.len, ads.len);
        return;
    }
    ggtt_invalidate(dev);
    forcewake(dev);
    let ready = self::guc::bootstrap(dev, fw, ads);
    let status = self::guc::status(dev);
    let (bootrom, ukernel, auth) = self::guc::describe_status(status);
    crate::log!(
        "intel/guc: bootstrap ready={} status=0x{:08X} bootrom={} ukernel={} auth=0x{:X}\n",
        ready as u8,
        status,
        bootrom,
        ukernel,
        auth
    );
    if ready {
        self::guc::prove_h2g_mmio_once(dev, "boot-control-ctb-disable");
    }
    if DISPLAY_PLANE1_BOOT_DEMO_ENABLED {
        self::display::init_primary_boot_surface(dev);
    } else {
        crate::log!("intel/display: plane1 boot demo disabled\n");
    }
    crate::log!(
        "intel/render: disabled reason=primary-render-module-removed gpgpu_probe=enabled\n"
    );
    self::gpgpu::submit_gpgpu_preflight_once();
    crate::log!("intel/media: source warmup disabled trigger=trueosfs-root-mounted\n",);
    if MEDIA_BOOT_DEMO_ENABLED {
        crate::log!("intel/media: scheduled boot demo delay_ms={}\n", MEDIA_BOOT_DEMO_DELAY_MS);
        crate::wait::spawn_local_detached(async move {
            Timer::after(EmbassyDuration::from_millis(MEDIA_BOOT_DEMO_DELAY_MS)).await;
            let queued_at_ms = embassy_time::Instant::now().as_millis() as u64;
            if let Some((slot, worker_spawner)) = pick_media_boot_demo_spawner() {
                match media_boot_demo_task(slot, queued_at_ms) {
                    Ok(token) => {
                        crate::log!(
                            "intel/media: boot demo handoff target_slot={} mode=worker\n",
                            slot,
                        );
                        worker_spawner.spawn(token);
                        return;
                    }
                    Err(err) => {
                        crate::log!(
                            "intel/media: boot demo handoff failed target_slot={} err={:?} fallback=local\n",
                            slot,
                            err,
                        );
                    }
                }
            } else {
                crate::log!(
                    "intel/media: boot demo handoff skipped reason=no-worker-ap fallback=local\n"
                );
            }

            log_media_demo_task_profile("local", 0, queued_at_ms);
            crate::log!("intel/media: boot demo begin\n");
            let first_frame = self::run_media2_first_frame_async().await;
            crate::log!(
                "intel/media: boot demo first-frame origin=local returned={}\n",
                first_frame.is_some() as u8,
            );
        });
    } else {
        crate::log!("intel/media: boot demo disabled\n");
    }
}

pub fn guc_ready() -> bool {
    self::guc::ready()
}

pub(crate) fn guc_h2g_mmio_accepted() -> bool {
    self::guc::h2g_mmio_accepted()
}

pub fn has_claimed_device() -> bool {
    CLAIMED_DEVICE.lock().is_some()
}

pub(crate) fn claimed_device() -> Option<Dev> {
    *CLAIMED_DEVICE.lock()
}

pub fn warm_state() -> Option<RenderWarmState> {
    claimed_device().map(|dev| RenderWarmState {
        device_id: dev.device_id,
        revision_id: dev.revision_id,
        mmio_base: dev.mmio as usize,
        mmio_len: dev.mmio_len,
    })
}

pub(crate) fn gpgpu_preflight_status() -> GpgpuPreflightStatus {
    let status = self::gpgpu::gpgpu_preflight_status();
    GpgpuPreflightStatus {
        submitted: status.submitted,
        accepted: status.accepted,
        completed: status.completed,
        guc_ready: status.guc_ready,
        marker: status.marker,
        dot: s
[... truncated in extract ...]

[2026-05-11T01:13:24.248Z] tool-output line=60
------------------------------------------------------------------------------------------------
Chunk ID: b9d1bf
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 813
Output:
        eot_only_retired as u8,
        failure_class,
        next,
    );
    crate::log!(
        "intel/gpgpu: started-thread-snapshot started={} command_stream_reached_walker={} threads_started={} worker_done_signal_seen={} command_after_worker_ran={} store_seen={} plain=\"gpu accepted the walker and TS counted launched EU threads; command stream is now waiting for those workers to say done before the post-walker marker can execute\"\n",
        gpu_program_started as u8,
        breadcrumbs_ok as u8,
        proof.dispatch_delta,
        eot_only_retired as u8,
        post_walker_marker_retired as u8,
        store_landed_anywhere as u8,
    );
    crate::log!(
        "intel/gpgpu: gpu-program-proof program_source={} expects_store={} start_submitted={} finished={} finish_marker=0x{:08X} finish_expected=0x{:08X} starts_before={} starts_after={} starts_delta={} start_command_bytes=0x{:X} gpu_program_started={} shared_ram_slot={} shared_ram_value=0x{:08X} shared_ram_expected=0x{:08X} wrote_shared_ram={} store_landed_anywhere={} eot_retired={} command_breadcrumbs_ok={} post_walker_marker={} failure_class={} cpu_reads_c_back=1 backend=gfx12-gpgpu-start-command next={} does_not_prove=matmul\n",
        proof.program_name,
        proof.expects_store as u8,
        proof.submitted as u8,
        proof.retired as u8,
        proof.marker,
        RCS_EXEC_RESULT_COMPUTE_WALKER_DONE,
        proof.dispatch_before,
        proof.dispatch_after,
        proof.dispatch_delta,
        proof.batch_bytes,
        gpu_program_started as u8,
        RESULT_SLOT_GPGPU_EU_C_STORE_DWORD,
        proof.c_value,
        GPU_PROGRAM_SHARED_RAM_WRITE_EXPECTED,
        proof.result_c_changed_by_eu as u8,
        store_landed_anywhere as u8,
        eot_only_retired as u8,
        breadcrumbs_ok as u8,
        post_walker_marker_retired as u8,
        failure_class,
        next,
    );
}

fn encode_gpgpu_preflight_batch(
    batch_dwords: &mut [u32],
    dot: u32,
    sum_a: u32,
    sum_b: u32,
) -> Result<usize, &'static str> {
    const STORES: [(usize, fn(u32, u32, u32) -> u32); 5] = [
        (RESULT_SLOT_GPGPU_PREFLIGHT_MARKER_DWORD, |_, _, _| RCS_EXEC_RESULT_GPGPU_PREFLIGHT_DONE),
        (RESULT_SLOT_GPGPU_PREFLIGHT_DOT_DWORD, |dot, _, _| dot),
        (RESULT_SLOT_GPGPU_PREFLIGHT_SUM_A_DWORD, |_, sum_a, _| sum_a),
        (RESULT_SLOT_GPGPU_PREFLIGHT_SUM_B_DWORD, |_, _, sum_b| sum_b),
        (RESULT_SLOT_GPGPU_PREFLIGHT_LANES_DWORD, |_, _, _| GPGPU_PREFLIGHT_LANES as u32),
    ];
    const STORE_DWORDS: usize = 4;
    const END_DWORDS: usize = 2;

    if batch_dwords.len() < STORES.len() * STORE_DWORDS + END_DWORDS {
        return Err("batch-too-small");
    }

    let mut idx = 0;
    for (slot, value_fn) in STORES {
        let dst = GPU_VA_RESULT_BASE + (slot as u64) * core::mem::size_of::<u32>() as u64;
        batch_dwords[idx] = MI_STORE_DATA_IMM_GGTT_DW1;
        batch_dwords[idx + 1] = dst as u32;
        batch_dwords[idx + 2] = (dst >> 32) as u32;
        batch_dwords[idx + 3] = value_fn(dot, sum_a, sum_b);
        idx += STORE_DWORDS;
    }
    batch_dwords[idx] = MI_BATCH_BUFFER_END;
    batch_dwords[idx + 1] = MI_NOOP;
    idx += END_DWORDS;

    Ok(idx * core::mem::size_of::<u32>())
}

[2026-05-11T01:13:24.248Z] tool-output line=61
------------------------------------------------------------------------------------------------
Chunk ID: c7a694
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1355
Output:

    fn render_mandelbrot_to_texture_now(tex_id: u32, ticks: u64, tick_hz: u64) -> i32 {
        crate::gfx::init(None);

        if tex_id == 0 {
            return -1;
        }
        if reject_unreasonable_tex_id(tex_id, "render-mandelbrot-now") {
            return -8;
        }
        if !crate::gfx::is_virgl_active() {
            return -2;
        }

        let verts = crate::gfx::mandelbrot::fullscreen_quad_rgba_bytes_for_view(ticks, tick_hz);
        let usable = verts.len();
        if usable == 0 {
            return -3;
        }

        let (image, width, height) = {
            let st = GFX_CABI_STATE.lock();
            let idx = tex_id.saturating_sub(1) as usize;
            let Some(entry) = st
                .tex_images
                .as_ref()
                .and_then(|images| images.get(idx))
                .and_then(|entry| entry.as_ref())
            else {
                return -6;
            };
            (entry.image, entry.width.max(1), entry.height.max(1))
        };

        if !image.is_valid() {
            return -7;
        }

        crate::gfx::with_cabi_frame_lock(|| {
            let Some(ret) =
                crate::gfx::with_context_tag(crate::gfx::SystemLockOwner::DrawMandelbrot, |ctx| {
                    let (pipeline, vbuf, _) =
                        match ensure_gfx_resources_tex(ctx, usable, TexPipelineKind::Mandelbrot) {
                            Some(v) => v,
                            None => return -4,
                        };

                    if ctx.write_buffer(vbuf, 0, verts.as_slice()).is_err() {
                        return -5;
                    }

                    let cmds = [
                        Command::SetRenderTarget(Some(image)),
                        Command::SetViewport(Viewport {
                            x: 0,
                            y: 0,
                            width: width as i32,
                            height: height as i32,
                        }),
                        Command::SetScissor(None),
                        Command::SetBlend(trueos_gfx_core::BlendDesc::disabled()),
                        Command::BindPipeline(pipeline),
                        Command::BindVertexBuffer {
                            buffer: vbuf,
                            offset: 0,
                        },
                        Command::Draw {
                            vertex_count: 6,
                            first_vertex: 0,
                        },
                    ];
                    if !check_submit_budget(usable, cmds.len(), "draw_mandelbrot_to_texture") {
                        return -8;
                    }
                    if ctx.submit(CommandBuffer { commands: &cmds }).is_err() {
                        return -9;
                    }
                    0
                })
            else {
                return -10;
            };
            if ret == 0 {
                let mut st = GFX_CABI_STATE.lock();
                st.ring_idx = (st.ring_idx + 1) % GFX_CABI_VBUF_RING_LEN;
                st.viewport_configured = false;
            }
            ret
        })
    }

    fn render_tex_triangles_to_texture_now(
        target_tex_id: u32,
        source_tex_id: u32,
        clear_rgb: u32,
        vtx: &[u8],
        particle_shader: bool,
    ) -> i32 {
        crate::gfx::init(None);

        if target_tex_id == 0 || source_tex_id == 0 {
            return -1;
        }
        if reject_unreasonable_tex_pair(target_tex_id, source_tex_id, "render-tex-now") {
            return -8;
        }
        const VTX_SIZE: usize = 20;
        let usable = vtx.len() - (vtx.len() % VTX_SIZE);
        if usable == 0 {
            return -2;
        }
        let vcount = (usable / VTX_SIZE) as u32;
        if vcount == 0 {
            return 0;
        }

        let (target_image, target_width, target_height) = {
            let st = GFX_CABI_STATE.lock();
            let idx = target_tex_id.saturating_sub(1) as usize;
            let Some(entry) = st
                .tex_images
                .as_ref()
                .and_then(|images| images.get(idx))
                .and_then(|entry| entry.as_ref())
            else {
                return -3;
            };
            (entry.image, entry.width.max(1), entry.height.max(1))
        };

        let (source_image, pi
[... truncated in extract ...]

[2026-05-11T01:13:29.851Z] tool-call:exec_command line=63
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"fn encode_gfx12_gpgpu_walker_probe_batch|MEDIA_INTERFACE_DESCRIPTOR|GPGPU_WALKER|STATE_BASE_ADDRESS|MEDIA_VFE|CURBE|MI_STORE_DATA_IMM_GGTT\" src/intel/intel/render/gpgpu.rs src/intel/intel/render/*.rs src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":24000}

[2026-05-11T01:13:30.090Z] tool-call:exec_command line=64
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1220,1680p' src/intel/intel/render/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":30000}

[2026-05-11T01:13:30.097Z] tool-call:exec_command line=65
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1,120p' src/intel/intel/render/mod.rs && sed -n '1,100p' src/intel/mod.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":10000}

[2026-05-11T01:13:30.189Z] tool-output line=67
------------------------------------------------------------------------------------------------
Chunk ID: ca75ef
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 5040
Output:
src/intel/intel/render/constants.rs:97:const GPGPU_WALKER_SCRATCH_OFFSET_BYTES: usize = 0x3800;
src/intel/intel/render/constants.rs:168:const MI_STORE_DATA_IMM_GGTT_DW1: u32 = 0x1040_0002;
src/intel/intel/render/constants.rs:209:const STATE_BASE_ADDRESS_CMD: u32 = 20 | (1 << 16) | (1 << 24) | (3 << 29);
src/intel/intel/render/gpgpu.rs:5:const GPGPU_LOAD_DUMMY_CURBE: bool = true;
src/intel/intel/render/gpgpu.rs:6:const GPGPU_DUMMY_CURBE_BYTES: usize = 64;
src/intel/intel/render/gpgpu.rs:9:// ADL-S 8086:4680 is Gfx12.0/Xe-LP.  The PRM-length GPGPU_WALKER now launches
src/intel/intel/render/gpgpu.rs:451:    let walker_gpu = GPU_VA_BATCH_BASE + GPGPU_WALKER_SCRATCH_OFFSET_BYTES as u64;
src/intel/intel/render/gpgpu.rs:462:        && GPGPU_WALKER_SCRATCH_OFFSET_BYTES
src/intel/intel/render/gpgpu.rs:546:            warm.batch_virt.add(GPGPU_WALKER_SCRATCH_OFFSET_BYTES),
src/intel/intel/render/gpgpu.rs:551:        unsafe { warm.batch_virt.add(GPGPU_WALKER_SCRATCH_OFFSET_BYTES) },
src/intel/intel/render/gpgpu.rs:616:        GPGPU_LOAD_DUMMY_CURBE as u8,
src/intel/intel/render/gpgpu.rs:617:        if GPGPU_LOAD_DUMMY_CURBE {
src/intel/intel/render/gpgpu.rs:618:            GPGPU_DUMMY_CURBE_BYTES
src/intel/intel/render/gpgpu.rs:1136:        push(batch_dwords, cursor, MI_STORE_DATA_IMM_GGTT_DW1)?;
src/intel/intel/render/gpgpu.rs:1201:    push(batch_dwords, &mut cursor, STATE_BASE_ADDRESS_CMD)?;
src/intel/intel/render/gpgpu.rs:1354:fn encode_gfx12_gpgpu_walker_probe_batch(
src/intel/intel/render/gpgpu.rs:1360:    const MEDIA_VFE_STATE_CMD: u32 = (3 << 29) | (2 << 27) | 7;
src/intel/intel/render/gpgpu.rs:1361:    const MEDIA_CURBE_LOAD_CMD: u32 = (3 << 29) | (2 << 27) | (1 << 16) | 2;
src/intel/intel/render/gpgpu.rs:1362:    const MEDIA_INTERFACE_DESCRIPTOR_LOAD_CMD: u32 = (3 << 29) | (2 << 27) | (2 << 16) | 2;
src/intel/intel/render/gpgpu.rs:1363:    const GPGPU_WALKER_CMD: u32 = (3 << 29) | (2 << 27) | (1 << 24) | (5 << 16) | 13;
src/intel/intel/render/gpgpu.rs:1376:    const IDD_STATE_OFFSET_BYTES: usize = GPGPU_WALKER_SCRATCH_OFFSET_BYTES;
src/intel/intel/render/gpgpu.rs:1377:    const CURBE_STATE_OFFSET_BYTES: usize = GPGPU_WALKER_SCRATCH_OFFSET_BYTES + 0x100;
src/intel/intel/render/gpgpu.rs:1380:    const CURBE_READ_LENGTH_8DW: u32 = if GPGPU_LOAD_DUMMY_CURBE {
src/intel/intel/render/gpgpu.rs:1381:        (GPGPU_DUMMY_CURBE_BYTES / 32) as u32
src/intel/intel/render/gpgpu.rs:1386:    const CURBE_TOTAL_BYTES: usize = if GPGPU_LOAD_DUMMY_CURBE {
src/intel/intel/render/gpgpu.rs:1387:        GPGPU_DUMMY_CURBE_BYTES
src/intel/intel/render/gpgpu.rs:1391:    const VFE_CURBE_ALLOCATION_32B: u32 = if GPGPU_LOAD_DUMMY_CURBE {
src/intel/intel/render/gpgpu.rs:1392:        (GPGPU_DUMMY_CURBE_BYTES / 32) as u32
src/intel/intel/render/gpgpu.rs:1401:    const CURBE_DYNAMIC_OFFSET_BYTES: usize =
src/intel/intel/render/gpgpu.rs:1402:        GPU_VA_DRAW_STATE_BASE as usize + CURBE_STATE_OFFSET_BYTES;
src/intel/intel/render/gpgpu.rs:1411:    // Mesa's minimal legacy GPGPU_WALKER executor leaves both masks full.
src/intel/intel/render/gpgpu.rs:1414:    const GPGPU_WALKER_SIMD8_RIGHT_MASK: u32 = 0xFFFF_FFFF;
src/intel/intel/render/gpgpu.rs:1415:    const GPGPU_WALKER_BOTTOM_MASK: u32 = 0xFFFF_FFFF;
src/intel/intel/render/gpgpu.rs:1419:    // one-thread GPGPU_WALKER case.  Keep exception/preemption policy boring
src/intel/intel/render/gpgpu.rs:1463:        push(batch_dwords, cursor, MI_STORE_DATA_IMM_GGTT_DW1)?;
src/intel/intel/render/gpgpu.rs:1506:    if GPGPU_LOAD_DUMMY_CURBE {
src/intel/intel/render/gpgpu.rs:1507:        let curbe_index = CURBE_STATE_OFFSET_BYTES / core::mem::size_of::<u32>();
src/intel/intel/render/gpgpu.rs:1508:        let curbe_dwords = GPGPU_DUMMY_CURBE_BYTES / core::mem::size_of::<u32>();
src/intel/intel/render/gpgpu.rs:1516:            let curbe = warm.draw_state_virt.add(CURBE_STATE_OFFSET_BYTES) as *mut u32;
src/intel/intel/render/gpgpu.rs:1522:            unsafe { warm.draw_state_virt.add(CURBE_STATE_OFFSET_BYTES) },
src/intel/intel/render/gpgpu.rs:1523:            GPGPU_DUMMY_CURBE_BYTES,
src/intel/intel/render/gpgpu.rs:1550:    // switch back before MEDIA_VFE_STATE and GPGPU_WALKER.
src/intel/intel/render/gpgpu.rs:1591:    idd_words[5] = CURBE_READ_LENGTH_8DW << 16;
src/intel/intel/render/gpgpu.rs:1627:    push(batch_dwords, &mut cursor, STATE_BASE_ADDRESS_CMD)?;
src/intel/intel/render/gpgpu.rs:1691:    push(batch_
[... truncated in extract ...]

[2026-05-11T01:13:30.189Z] tool-output line=68
------------------------------------------------------------------------------------------------
Chunk ID: b19f71
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 5103
Output:
    push(batch_dwords, &mut cursor, 0xFFFF_0000)?;
    push_store_marker(batch_dwords, &mut cursor, 25, 0xC0DE_7903)?;
    push_pipe_control_full(
        batch_dwords,
        &mut cursor,
        PIPE_CONTROL_HDC_PIPELINE_FLUSH_HEADER | PIPE_CONTROL_UNTYPED_DATAPORT_FLUSH_HEADER,
        PIPE_CONTROL_RENDER_TARGET_CACHE_FLUSH | PIPE_CONTROL_CS_STALL,
    )?;

    let cfe_start = cursor;
    push(batch_dwords, &mut cursor, CFE_STATE_CMD)?;
    push(batch_dwords, &mut cursor, 0)?;
    push(batch_dwords, &mut cursor, 0)?;
    push(batch_dwords, &mut cursor, (63 << 16) | (1 << 3))?;
    push(batch_dwords, &mut cursor, 0)?;
    push(batch_dwords, &mut cursor, 0)?;
    push_pipe_control_full(
        batch_dwords,
        &mut cursor,
        PIPE_CONTROL_HDC_PIPELINE_FLUSH_HEADER,
        PIPE_CONTROL_CS_STALL,
    )?;
    push_cs_gpr_stamp(batch_dwords, &mut cursor)?;

    let walker_start = cursor;
    push(batch_dwords, &mut cursor, COMPUTE_WALKER_CMD)?;
    let body_start = cursor;
    for _ in 0..COMPUTE_WALKER_BODY_DWORDS {
        push(batch_dwords, &mut cursor, 0)?;
    }

    let kernel_gpu = GPU_VA_DRAW_STATE_BASE + GPGPU_EU_KERNEL_OFFSET_BYTES as u64;
    batch_dwords[body_start + 4] = 0xFFFF_FFFF;
    batch_dwords[body_start + 6] = 1;
    batch_dwords[body_start + 7] = 1;
    batch_dwords[body_start + 8] = 1;

    let idd = body_start + BODY_INTERFACE_DESCRIPTOR_DWORD;
    batch_dwords[idd] = (kernel_gpu as u32) & 0xFFFF_FFC0;
    batch_dwords[idd + 4] = if program.expects_store && store_surface.ready {
        ((store_surface.binding_table_offset as u32) & 0x001F_FFE0) | 31
    } else {
        0
    };
    batch_dwords[idd + 5] = 1 | (3 << 26);

    let post_sync = body_start + BODY_POSTSYNC_DWORD;
    batch_dwords[post_sync] = RENDER_MOCS << 4;

    push_store_marker(
        batch_dwords,
        &mut cursor,
        RESULT_SLOT_GPGPU_COMPUTE_WALKER_DWORD,
        RCS_EXEC_RESULT_COMPUTE_WALKER_DONE,
    )?;

    push_pipe_control(batch_dwords, &mut cursor, PIPE_CONTROL_FLUSH_BITS)?;
    push(batch_dwords, &mut cursor, MI_BATCH_BUFFER_END)?;
    push(batch_dwords, &mut cursor, MI_NOOP)?;

    let command_bytes = cursor * core::mem::size_of::<u32>();
    crate::log!(
        "intel/gpgpu: compute-walker-layout program_source={} expects_store={} cfe_off=0x{:X} cfe_cmd=0x{:08X} cfe_dw3=0x{:08X} walker_off=0x{:X} walker_cmd=0x{:08X} body0=0x{:08X} exec_mask=0x{:08X} tg_dims={}x{}x{} idd0=0x{:08X} idd4=0x{:08X} idd5=0x{:08X} post_sync0=0x{:08X} surface_base=0x{:X} tail_off=0x{:X} cs_marker=0x{:08X} note=gen125-cfe-compute-walker-embedded-idd-no-post-cfe-mi-store\n",
        program.name,
        program.expects_store as u8,
        cfe_start * core::mem::size_of::<u32>(),
        batch_dwords[cfe_start],
        batch_dwords[cfe_start + 3],
        walker_start * core::mem::size_of::<u32>(),
        batch_dwords[walker_start],
        batch_dwords[body_start],
        batch_dwords[body_start + 4],
        batch_dwords[body_start + 6],
        batch_dwords[body_start + 7],
        batch_dwords[body_start + 8],
        batch_dwords[idd],
        batch_dwords[idd + 4],
        batch_dwords[idd + 5],
        batch_dwords[post_sync],
        GPU_VA_DRAW_STATE_BASE,
        command_bytes,
        RCS_EXEC_RESULT_COMPUTE_WALKER_DONE,
    );
    crate::log!(
        "intel/gpgpu: compute-walker-store-contract program_source={} expects_store={} compact_send_desc_word=0x{:08X} compact_send_exdesc_word=0x{:08X} expected_bti=0x{:02X} binding_ready={} bt_off=0x{:X} bt_entry=0x{:08X} surf_off=0x{:X} surf_gpu=0x{:X} target_gpu=0x{:X} surf0=0x{:08X} note=compact-send-raw-words-not-direct-bti-decode\n",
        program.name,
        program.expects_store as u8,
        gpgpu_store_eot_program().words[GPGPU_C_STORE_KERNEL_SEND_DWORD - 1],
        gpgpu_store_eot_program().words[GPGPU_C_STORE_KERNEL_SEND_DWORD],
        store_surface.binding_table_index,
        store_surface.ready as u8,
        store_surface.binding_table_offset,
        store_surface.binding_entry,
        store_surface.surface_state_offset,
        store_surface.surface_gpu,
        store_surface.target_gpu,
        store_surface.surface_dword0,
    );
    crate::log!(
        "intel/gpgpu: compute-walker-dwords w0=0x{:08X} w1=0x{:08X} w2=0x{:08X} w3=0x{:08X} w4=0x{:08X} w5=0x{:08X} w6=0x{:08X} w7=0x{:08X} w8=0x{:08X} w9=0x{:08X} w10=0x{:08X} w11=0x{:08X
[... truncated in extract ...]

[2026-05-11T01:13:30.189Z] tool-output line=69
------------------------------------------------------------------------------------------------
Chunk ID: 2318f1
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1617
Output:
// Render proof contract.
//
// The goal of this file is not "triangle or nothing."  Each probe should
// move one named boundary and say what it does not prove:
//
// - `batch-submit-proof`: RCS/execlist accepted enough command stream to run
//   markers.  Current captures still show `final_marker=0`, so full retire is
//   not proven.
// - `mi-scanout-store-proof`: RCS command streamer wrote one DWORD into the
//   live scanout surface via MI_STORE_DATA_IMM.  This proves neither 3D stage
//   progress nor PS/color-backend writes.
// - `memory-proof`: warm render buffers were mapped into their fixed GGTT
//   slots, cache-flushed, and CPU-read back in one source-level proof line.
//   This does not prove each 3D stage actually consumed its buffer.
// - `gpgpu-preflight`: RCS submission writes deterministic vector proof results
//   into the warm result buffer. This proves the buffer/result runway we need
//   for GPGPU bring-up, but not EU thread execution or matmul arithmetic yet.
// - `vertex-upload-proof`: CPU wrote/read back the triangle vertex bytes and
//   flushed them.  This does not prove VF consumed them.
// - `vf-proof`: IA/VF counters advance for three vertices.  Current captures
//   prove this with `vf-proof accepted=1`.
// - `vs-proof`: baked/uploaded VS bytes match and VS counters advance.  Current
//   captures prove this with `vs-proof accepted=1 vs_delta=3`.
// - `clip-raster-proof`: clipper counters advance.  Current captures prove this
//   on the VF draw path only; VS-to-clipper handoff is still the frontier.
// - `ps-dispatch-proof` and `ps-rt-proof`: not proven in current captures;
//   `ps_delta=0`, `rt_any_change=0`.
//
// Keep these proof lines conservative.  Packet markers are useful context, but
// stage proofs should only accept on counters or memory changes owned by that
// boundary.

use core::sync::atomic::{AtomicBool, AtomicU32, Ordering};
use spin::Mutex;

macro_rules! intel_render_focus_log {
    ($($arg:tt)*) => {
        if crate::logflag::INTEL_STAGE1_LOGS || crate::logflag::INTEL_RENDER_NGIN_LOGS {
            crate::log!($($arg)*);
        }
    };
}

macro_rules! intel_render_verbose_log {
    ($($arg:tt)*) => {
        if crate::logflag::INTEL_RENDER_NGIN_LOGS && !crate::logflag::INTEL_STAGE1_LOGS {
            crate::log!($($arg)*);
        }
    };
}

macro_rules! intel_render_batch_log {
    ($($arg:tt)*) => {
        if crate::logflag::INTEL_RENDER_NGIN_BATCH_LOGS && !crate::logflag::INTEL_STAGE1_LOGS {
            crate::log!($($arg)*);
        }
    };
}

include!("constants.rs");
include!("state.rs");
include!("warmup.rs");
include!("primary.rs");
include!("pipeline.rs");
include!("resources.rs");
include!("gpgpu.rs");
include!("submit.rs");
include!("lrc.rs");
// Intel bring-up source contracts.
//
// These comments intentionally mirror `may_help/stateoftheart`, but live next
// to the code so the documentary facts do not drift away from the driver.
//
// Current proven hardware claim:
// - PCI claim has reached `00:02.0 device=0x4680 rev=0x0C`.
// - The observed MMIO aperture is `0x1000000` bytes.
//
// This module proves only ownership, MMIO bounds, shared address constants, and
// top-level sequencing.  It does not prove render progress; render/display/GuC
// modules must each emit their own named proof lines.

mod display;
pub(crate) mod format;
mod gpgpu;
mod guc;
pub mod hda;
mod hw_cursor;
pub(crate) mod state;
pub(crate) mod stats;
pub(crate) mod xelp_media2_ngin;
pub(crate) mod xelp_media_h264src;
pub(crate) mod xelp_media_matroska;
pub(crate) mod xelp_media_mp4;
pub(crate) mod xelp_media_source;

use core::sync::atomic::{AtomicBool, Ordering};
use embassy_executor::SendSpawner;
use embassy_time::{Duration as EmbassyDuration, Timer};
use spin::Mutex;

pub(crate) const INTEL_VENDOR_ID: u16 = 0x8086;
pub(crate) const PCI_CLASS_DISPLAY: u8 = 0x03;
pub(crate) const GPU_VA_GUC_FW_BASE: u64 = 0x0085_0000;
pub(crate) const GPU_VA_GUC_ADS_BASE: u64 = 0x0100_0000;
pub(crate) const GPU_VA_DISPLAY_PRIMARY_BASE: u64 = 0x0200_0000;
pub(crate) const GPU_VA_DISPLAY_OVERLAY_BASE: u64 = 0x0300_0000;
pub(crate) const GPU_VA_DISPLAY_CURSOR_BASE: u64 = 0x0600_0000;
pub(crate) const WARM_ALIGN: usize = 4096;
const GGTT_ALIAS_BASE_OFF: usize = 0x0080_0000;
const GGTT_ALIAS_BYTES: usize = 0x0080_0000;
const GGTT_PAGE_BYTES: u64 = 4096;
const GEN8_PAGE_PRESENT: u64 = 1;
const F
[... truncated in extract ...]

[2026-05-11T01:13:37.168Z] tool-call:exec_command line=71
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"mod intel|pub mod intel|crate::intel::intel|intel/intel\" src build.rs Cargo.toml","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":12000}

[2026-05-11T01:13:37.168Z] tool-call:exec_command line=72
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"mod intel|pub mod intel\" src/main.rs src/lib.rs src/*.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":8000}

[2026-05-11T01:13:37.234Z] tool-output line=75
------------------------------------------------------------------------------------------------
Chunk ID: 058702
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 33
Output:
src/main.rs:43:mod intel;
src/main.rs:44:mod intel_hda_probe;
src/intel/intel/may_help/README.md:327:  - Old `src/intel/intel.rs`.

[2026-05-11T01:13:37.234Z] tool-output line=76
------------------------------------------------------------------------------------------------
Chunk ID: cad17a
Wall time: 0.0000 seconds
Process exited with code 2
Original token count: 45
Output:
rg: src/lib.rs: No such file or directory (os error 2)
src/main.rs:43:mod intel;
src/main.rs:44:mod intel_hda_probe;
src/main.rs:43:mod intel;
src/main.rs:44:mod intel_hda_probe;

[2026-05-11T01:13:37.234Z] tool-output line=77
------------------------------------------------------------------------------------------------
Chunk ID: f80bd7
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 335
Output:
#![no_std]
#![no_main]
#![cfg_attr(target_arch = "x86_64", feature(abi_x86_interrupt))]
#![feature(f16)]
#![allow(unsafe_op_in_unsafe_fn)]

const _: f16 = 0.0_f16;

#[macro_use]
pub extern crate alloc;

#[allow(non_snake_case)]
mod Tyche;
mod allcaps;
mod allocators;
pub mod allports;
mod aud;
mod blueprint;
#[cfg(any(target_os = "trueos", target_os = "zkvm"))]
mod blueprint_net_broker;
#[cfg(any(target_os = "trueos", target_os = "zkvm"))]
mod blueprint_net_wire;
#[path = "Chronos.rs"]
mod chronos;
mod cpu;
mod disc;
pub mod dma;
mod efi;
#[cfg(target_arch = "x86_64")]
mod exceptions;
#[cfg(not(target_arch = "x86_64"))]
#[path = "exceptions_disabled.rs"]
mod exceptions;
mod gfx;
mod globalog;
mod host_api;
#[cfg(target_arch = "x86_64")]
mod hv;
#[cfg(not(target_arch = "x86_64"))]
#[path = "hv_disabled.rs"]
mod hv;
mod hyper_probe;
mod intel;
mod intel_hda_probe;
mod iso9660;
mod limine;
mod logflag;
mod lumen;
#[cfg(any(target_os = "trueos", target_os = "zkvm"))]
mod mio_compat;
mod mio_probe;
mod net;
mod pci;
mod percpu;
mod phys;
mod portio;
#[cfg(target_arch = "x86_64")]
mod power;
#[cfg(not(target_arch = "x86_64"))]
#[path = "power_disabled.rs"]
mod power;
mod r;
#[cfg(target_arch = "x86_64")]
mod rapl;
#[cfg(not(target_arch = "x86_64"))]
#[path = "rapl_disabled.rs"]
mod rapl;
mod runtime;
mod shell2;
mod smp;

[2026-05-11T01:13:42.999Z] tool-call:exec_command line=79
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1200,1260p' src/intel/gpgpu.rs && sed -n '4960,5260p' src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":26000}

[2026-05-11T01:13:43.004Z] tool-call:exec_command line=80
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '5260,5605p' src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":30000}

[2026-05-11T01:13:43.022Z] tool-call:exec_command line=81
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '6040,6605p' src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":32000}

[2026-05-11T01:13:44.434Z] tool-output line=83
------------------------------------------------------------------------------------------------
Chunk ID: 7478d0
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 3518
Output:
    state[idx + 98] = 0x2068;
    state[idx + 99] = 0;
    state[idx + 100] = 0x2084;
    state[idx + 101] = 0;
    idx += 102;

    state[idx] = MI_NOOP;
    idx += 1;
    state[idx] = MI_BATCH_BUFFER_END | 1;

    crate::intel::dma_flush(warm.context_virt, warm.context_len);
    true
}

const GPGPU_PREFLIGHT_LANES: usize = 4;
const GPGPU_BURN_MIN_ROWS: usize = 512;
const GPGPU_BURN_MIN_K_DIM: usize = 512;
const GPU_PROGRAM_SHARED_RAM_WRITE_EXPECTED: u32 = trueos_eu::gfx12::STORE_SENTINEL_U32;
const GPGPU_LOAD_DUMMY_CURBE: bool = true;
const GPGPU_DUMMY_CURBE_BYTES: usize = 64;
const GPGPU_CONTIGUOUS_VFE_IDD_WALKER: bool = false;
const GPGPU_MESA_POST_VFE_PIPE_CONTROL: bool = false;
const GPGPU_USE_MINIMAL_PPGTT: bool = true;
const GPGPU_BATCH_START_IN_PPGTT: bool = false;
// ADL-S 8086:4680 is Gfx12.0/Xe-LP.  Keep the active milestone probe as the
// smallest Mesa-shaped root thread: no CURBE payload, one SIMD8 group, TS EOT.
const ACTIVE_GFX12_EOT_VARIANT: trueos_eu::gfx12::Gfx12EotVariant =
    trueos_eu::gfx12::Gfx12EotVariant::TsR0ToG127Send1;
const GPGPU_ENABLE_SIP_EXCEPTIONS: bool = false;
const GPGPU_SIP_HANDLER_OFFSET_BYTES: usize = GPGPU_EU_KERNEL_OFFSET_BYTES + 0x200;
const GPGPU_SIP_HANDLER_VARIANT: trueos_eu::gfx12::Gfx12EotVariant =
    trueos_eu::gfx12::Gfx12EotVariant::TsR0ToG127Send1;
const GPGPU_USE_STATIC_DP4A_STORE_THEN_EOT: bool = true;
const GPGPU_USE_HDC_STORE_THEN_EOT: bool = true;
const GPGPU_USE_GFX125_COMPUTE_WALKER: bool = false;
const GPGPU_WALKER_GROUP_X_DIM_LADDER: &[u32] = &[186];
// T5 live4 proof cap: 4096 groups retires cleanly on the current shell.
// 6144 groups still writes the right value, but does not retire cleanly; leave
// this cap alone until the kernel, queueing model, or retire logic grows.
const GPGPU_T5_LIVE4_GROUP_X_DIM_LADDER: &[u32] = &[4096];
// T6 starts from the same clean retire cap as T5 until live8 has its own logs.
const GPGPU_T6_LIVE8_GROUP_X_DIM_LADDER: &[u32] = &[4096];
// T6.1 live16 uses the same guarded cap for its first runtime proof.
const GPGPU_T61_LIVE16_GROUP_X_DIM_LADDER: &[u32] = &[4096];
const GPGPU_WALKER_GROUP_THREADS: u32 = 1;
const GPGPU_WALKER_SIMD8_LANES: u32 = 8;

#[derive(Copy, Clone)]
struct GpgpuEuProgram {
    name: &'static str,
    kind: trueos_eu::EuArtifactKind,
    words: &'static [u32],
    expects_store: bool,
    expected_store_value: u32,
    store_send_dword: Option<usize>,
    visible_seed_dword: Option<usize>,
}

#[derive(Copy, Clone)]
struct GpgpuOneRowMatvecProfile {
    program: GpgpuEuProgram,
    expects_store: bool,
    program_uploaded: bool,
    walker_encoded: bool,
    result_changed_by_current_backend: bool,
    program_gpu: u64,
    program_bytes: usize,
    program_sig: u64,
    walker_gpu: u64,
    walker_bytes: usize,
}

#[derive(Copy, Clone)]
struct GpgpuStoreSurfaceState {
    ready: bool,
    binding_table_offset: usize,
    surface_state_offset: usize,
    binding_table_index: usize,
    surface_gpu: u64,
    target_gpu: u64,
    surface_dword0: u32,
    surface_dword2: u32,
    surface_dword3: u32,
    binding_entry: u32,
}

fn prepare_gpgpu_program_artifact(
    warm: RenderWarmState,
    result_changed_by_current_backend: bool,
) -> GpgpuProgramArtifactProof {
    let program = selected_gpgpu_eu_program();
    let sip_handler = trueos_eu::gfx12::eot_artifact(GPGPU_SIP_HANDLER_VARIANT);
    let program_bytes = program.words.len() * core::mem::size_of::<u32>();
    let program_gpu = GPU_VA_DRAW_STATE_BASE + GPGPU_EU_KERNEL_OFFSET_BYTES as u64;
    let walker_gpu = GPU_VA_BATCH_BASE + GPGPU_WALKER_SCRATCH_OFFSET_BYTES as u64;

    let primary_uploaded = program_bytes != 0
        && GPGPU_EU_KERNEL_OFFSET_BYTES
            .checked_add(program_bytes)
            .is_some_and(|end| end <= warm.draw_state_len)
        && upload_and_verify_gpu_program_at(warm, GPGPU_EU_KERNEL_OFFSET_BYTES, program.words);
    let sip_bytes = sip_handler.words.len() * core::mem::size_of::<u32>();
    let sip_uploaded = !GPGPU_ENABLE_SIP_EXCEPTIONS
        || (sip_bytes != 0
            && GPGPU_SIP_HANDLER_OFFSET_BYTES
                .checked_add(sip_bytes)
                .is_some_and(|end| end <= warm.draw_state_len)
            && upload_and_verify_gpu_program_at(
                warm,
                GPGPU_SIP_HANDLER_OFFSET_BYTES,
                sip_handler.words,
            ));
    let program_uploaded = primary_
[... truncated in extract ...]

[2026-05-11T01:13:44.434Z] tool-output line=84
------------------------------------------------------------------------------------------------
Chunk ID: 77ee71
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 3434
Output:
    let dst_reg_num = (send_w1 >> 24) & 0xFF;
    let src0_mlen = (send_w2 >> 3) & 0xF;
    let src0_reg_num = (send_w2 >> 8) & 0xFF;
    let sfid = (send_w2 >> 28) & 0xF;
    let sfid_gateway = sfid == 3;
    let sfid_ts = sfid == 7;
    let dst_null_like = dst_reg_file == 0 && dst_reg_num == 0 && response_len == 0;
    let desc_immediate = desc_is_reg == 0;
    let exdesc_immediate = exdesc_is_reg == 0;
    let src_in_eot_safe_window = (112..=127).contains(&src0_reg_num);
    let target_supports_eot = sfid_gateway || sfid_ts;
    crate::log!(
        "intel/gpgpu: eot-send-contract source={} uploaded={} send_word_off={} send_w0=0x{:08X} send_w1=0x{:08X} send_w2=0x{:08X} send_w3=0x{:08X} eot_bit={} desc_is_reg={} exdesc_is_reg={} desc_immediate={} exdesc_immediate={} dst_reg_file={} dst_reg_num={} response_len={} dst_null_like={} src0_g={} src0_mlen={} src_in_eot_safe_window={} sfid={} sfid_gateway={} sfid_ts={} target_supports_eot={} prm_rules_ok={} probe=selected-pure-eot-artifact expected_good=post_walker_marker-or-eot_retired failure_disproves=selected-eot-payload-shape note=decoded-from-gfx12-send-format\n",
        program_name,
        uploaded as u8,
        send_base,
        send_w0,
        send_w1,
        send_w2,
        send_w3,
        eot_bit,
        desc_is_reg,
        exdesc_is_reg,
        desc_immediate as u8,
        exdesc_immediate as u8,
        dst_reg_file,
        dst_reg_num,
        response_len,
        dst_null_like as u8,
        src0_reg_num,
        src0_mlen,
        src_in_eot_safe_window as u8,
        sfid,
        sfid_gateway as u8,
        sfid_ts as u8,
        target_supports_eot as u8,
        (eot_bit == 1
            && desc_immediate
            && exdesc_immediate
            && dst_null_like
            && src_in_eot_safe_window
            && target_supports_eot) as u8,
    );
}

fn prepare_gpgpu_store_surface_state(warm: RenderWarmState) -> GpgpuStoreSurfaceState {
    let target_gpu = GPU_VA_RESULT_BASE
        + (RESULT_SLOT_GPGPU_EU_C_STORE_DWORD as u64) * core::mem::size_of::<u32>() as u64;
    prepare_gpgpu_store_surface_state_for_target(
        warm,
        target_gpu,
        "bind-send-bti-to-result-raw-buffer",
    )
}

fn prepare_gpgpu_store_surface_state_for_target(
    warm: RenderWarmState,
    target_gpu: u64,
    note: &'static str,
) -> GpgpuStoreSurfaceState {
    prepare_gpgpu_store_surface_state_for_target_span(
        warm,
        target_gpu,
        core::mem::size_of::<u32>(),
        note,
    )
}

fn prepare_gpgpu_store_surface_state_for_target_span(
    warm: RenderWarmState,
    target_gpu: u64,
    target_bytes: usize,
    note: &'static str,
) -> GpgpuStoreSurfaceState {
    let binding_table_bytes = GPGPU_STORE_BINDING_TABLE_ENTRIES * core::mem::size_of::<u32>();
    let surface_bytes = GPGPU_STORE_SURFACE_DWORDS * core::mem::size_of::<u32>();
    let binding_end = GPGPU_STORE_BINDING_TABLE_OFFSET_BYTES.saturating_add(binding_table_bytes);
    let surface_end = GPGPU_STORE_SURFACE_STATE_OFFSET_BYTES.saturating_add(surface_bytes);
    let binding_table_aligned = GPGPU_STORE_BINDING_TABLE_OFFSET_BYTES & 0x3F == 0;
    let surface_aligned = GPGPU_STORE_SURFACE_STATE_OFFSET_BYTES & 0x3F == 0;
    let ready = binding_table_aligned
        && surface_aligned
        && binding_end <= warm.draw_state_len
        && surface_end <= warm.draw_state_len;
    if !ready {
        crate::log!(
            "intel/gpgpu: gpu-program-surface-state ready=0 reason=draw-state-bounds bt_off=0x{:X} bt_bytes=0x{:X} surf_off=0x{:X} surf_bytes=0x{:X} draw_state_len=0x{:X}\n",
            GPGPU_STORE_BINDING_TABLE_OFFSET_BYTES,
            binding_table_bytes,
            GPGPU_STORE_SURFACE_STATE_OFFSET_BYTES,
            surface_bytes,
            warm.draw_state_len,
        );
        return GpgpuStoreSurfaceState {
            ready: false,
            binding_table_offset: GPGPU_STORE_BINDING_TABLE_OFFSET_BYTES,
            surface_state_offset: GPGPU_STORE_SURFACE_STATE_OFFSET_BYTES,
            binding_table_index: GPGPU_STORE_BINDING_TABLE_INDEX,
            surface_gpu: GPU_VA_DRAW_STATE_BASE + GPGPU_STORE_SURFACE_STATE_OFFSET_BYTES as u64,
            target_gpu,
            surface_dword0: 0,
            surface_dword2: 0,
            surface_dword3: 0,
            binding_entry: 0,
        };
    }

    let binding_entry = GPGPU_STORE_SURFA
[... truncated in extract ...]

[2026-05-11T01:13:44.434Z] tool-output line=85
------------------------------------------------------------------------------------------------
Chunk ID: 075ad1
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 6720
Output:
        batch_dwords[idd + 6],
        batch_dwords[idd + 7],
    );

    debug_assert_eq!(cursor - walker_start, COMPUTE_WALKER_DWORDS + 4 + 6 + 2);
    Ok(command_bytes)
}

#[allow(dead_code)]
fn encode_gfx12_gpgpu_walker_probe_batch(
    warm: RenderWarmState,
    batch_dwords: &mut [u32],
    store_surface: GpgpuStoreSurfaceState,
    program: GpgpuEuProgram,
    walker_group_x_dim: u32,
) -> Result<usize, &'static str> {
    const MEDIA_VFE_STATE_CMD: u32 = (3 << 29) | (2 << 27) | 7;
    const MEDIA_CURBE_LOAD_CMD: u32 = (3 << 29) | (2 << 27) | (1 << 16) | 2;
    const MEDIA_INTERFACE_DESCRIPTOR_LOAD_CMD: u32 = (3 << 29) | (2 << 27) | (2 << 16) | 2;
    const GPGPU_WALKER_CMD: u32 = (3 << 29) | (2 << 27) | (1 << 24) | (5 << 16) | 13;
    const MEDIA_STATE_FLUSH_CMD: u32 = (3 << 29) | (2 << 27) | (4 << 16);
    const PIPELINE_SELECT_BASE: u32 = (3 << 29) | (1 << 27) | (1 << 24) | (4 << 16);
    const PIPELINE_SELECT_GFX12_MASK: u32 = 0x13 << 8;
    const PIPELINE_SELECT_MEDIA_SAMPLER_DOP_CG_ENABLE: u32 = 1 << 4;
    const PIPELINE_SELECT_3D: u32 = PIPELINE_SELECT_BASE
        | PIPELINE_SELECT_GFX12_MASK
        | PIPELINE_SELECT_MEDIA_SAMPLER_DOP_CG_ENABLE;
    const PIPELINE_SELECT_GPGPU: u32 = PIPELINE_SELECT_3D | 2;
    const COMPUTE_SBA_SPAN_BYTES: usize = 0xFFFF_F000;
    const CS_GPR_STAMP_HI: u32 = 0x0000_0001;
    const CS_GPR0_STAMP_LO: u32 = 0xC5A0_2600;
    const CS_GPR1_STAMP_LO: u32 = 0xC5A0_2608;
    const IDD_STATE_OFFSET_BYTES: usize = GPGPU_WALKER_SCRATCH_OFFSET_BYTES;
    const CURBE_STATE_OFFSET_BYTES: usize = GPGPU_WALKER_SCRATCH_OFFSET_BYTES + 0x100;
    const IDD_PAYLOAD_DWORDS: usize = 8;
    const IDD_LOAD_DWORDS: usize = IDD_PAYLOAD_DWORDS;
    const CURBE_READ_LENGTH_8DW: u32 = if GPGPU_LOAD_DUMMY_CURBE { 1 } else { 0 };
    const GPGPU_THREADS_IN_GROUP: u32 = GPGPU_WALKER_GROUP_THREADS;
    const GPGPU_WALKER_GROUP_Y_DIM: u32 = 1;
    const GPGPU_WALKER_GROUP_Z_DIM: u32 = 1;
    const CURBE_TOTAL_BYTES: usize = if GPGPU_LOAD_DUMMY_CURBE {
        GPGPU_DUMMY_CURBE_BYTES
    } else {
        0
    };
    const VFE_CURBE_ALLOCATION_32B: u32 = if GPGPU_LOAD_DUMMY_CURBE {
        (GPGPU_DUMMY_CURBE_BYTES / 32) as u32
    } else {
        0
    };
    const GPGPU_VFE_MAX_THREADS: u32 = 223;
    const GPGPU_VFE_URB_ENTRIES: u32 = 2;
    const GPGPU_VFE_FUSED_EU_DISPATCH_LEGACY_MODE: u32 = 0;
    const GPGPU_VFE_URB_ENTRY_ALLOCATION_32B: u32 = 2;
    const GPGPU_RELATIVE_STATE_BASES: bool = true;
    const GPGPU_TEMPORARY_3D_FOR_SBA: bool = true;
    const GPGPU_DYNAMIC_STATE_BASE: u64 = if GPGPU_RELATIVE_STATE_BASES {
        GPU_VA_DRAW_STATE_BASE
    } else {
        0
    };
    const IDD_DYNAMIC_OFFSET_BYTES: usize = if GPGPU_RELATIVE_STATE_BASES {
        IDD_STATE_OFFSET_BYTES
    } else {
        GPU_VA_DRAW_STATE_BASE as usize + IDD_STATE_OFFSET_BYTES
    };
    const CURBE_DYNAMIC_OFFSET_BYTES: usize = if GPGPU_RELATIVE_STATE_BASES {
        CURBE_STATE_OFFSET_BYTES
    } else {
        GPU_VA_DRAW_STATE_BASE as usize + CURBE_STATE_OFFSET_BYTES
    };
    const GPGPU_KERNEL_GPU: u64 = GPU_VA_DRAW_STATE_BASE + GPGPU_EU_KERNEL_OFFSET_BYTES as u64;
    // Mesa's runtime dump for an empty compute shader resolves the kernel via
    // InstructionBase + KSP=0.  Keep dynamic state relative, but make the EU
    // fetch address land exactly at the uploaded program base.
    const GPGPU_INSTRUCTION_BASE: u64 = GPGPU_KERNEL_GPU;
    const GPGPU_KSP_NEGATIVE_CONTROL: bool = false;
    const GPGPU_BAD_KERNEL_START_POINTER: u64 = 0x00F0_0000;
    const GPGPU_MIDL_NEGATIVE_CONTROL: bool = false;
    // Gen12 legacy GPGPU_WALKER is a 15-dword packet.  The PRM default length
    // is 0x0D with a length bias of 2; keep the mask shape aligned with Mesa's
    // minimal legacy walker while the low SIMD8 lanes remain the consumed bits.
    const GPGPU_WALKER_SIMD8_RIGHT_MASK: u32 = 0x0000_00FF;
    const GPGPU_WALKER_BOTTOM_MASK: u32 = 0xFFFF_FFFF;
    const STATE_SIP_CMD: u32 = 0x6102_0001;
    // The repeatable stall sits at GPGPU_WALKER with no visible TDL dispatch.
    // Restore the older active path's non-preemptible root thread policy while
    // keeping exception/SIP routing as a separate diagnostic knob.
    const IDD_THREAD_PREEMPTION_DISABLE: u32 = 1 << 20;
    const IDD_ILLEGAL_OPCODE_EXCEPTION_ENABLE: u32 = 1 << 13;
    const IDD_SOFTWARE_EXCEPTION_ENABLE: u32 = 1 << 7;
    const GPGP
[... truncated in extract ...]

[2026-05-11T01:13:48.240Z] tool-call:exec_command line=87
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"fn selected_gpgpu_eu_program|fn gpgpu_store_eot_program|T5_SMALL|T6_SMALL|T61|HDC1\" src/intel/gpgpu.rs crates/trueos-eu/src/gfx12.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":20000}

[2026-05-11T01:13:48.280Z] tool-output line=89
------------------------------------------------------------------------------------------------
Chunk ID: 6f3a0f
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 3151
Output:
crates/trueos-eu/src/gfx12.rs:4:pub const HDC1_BTI34_STORE_SEND_DWORD: usize = 11;
crates/trueos-eu/src/gfx12.rs:5:pub const HDC1_BTI34_STORE_IMM_DWORD: usize = 3;
crates/trueos-eu/src/gfx12.rs:6:pub const HDC1_STATELESS_STATIC_DP4A_STORE_SEND_DWORD: usize = 23;
crates/trueos-eu/src/gfx12.rs:7:pub const HDC1_STATELESS_STATIC_DP4A_BASE_DWORD: usize = 3;
crates/trueos-eu/src/gfx12.rs:227:// send    HDC1 untyped surface write, BTI 0x34, SIMD8
crates/trueos-eu/src/gfx12.rs:230:pub static HDC1_BTI34_STORE_THEN_TS_EOT_WORDS: [u32; 20] = [
crates/trueos-eu/src/gfx12.rs:253:pub static HDC1_BTI34_STORE_THEN_TS_EOT: EuArtifact = EuArtifact {
crates/trueos-eu/src/gfx12.rs:257:    words: &HDC1_BTI34_STORE_THEN_TS_EOT_WORDS,
crates/trueos-eu/src/gfx12.rs:265:// send    HDC1 untyped surface write, stateless/non-coherent BTI 253, SIMD8
crates/trueos-eu/src/gfx12.rs:271:pub static HDC1_STATELESS_STORE_THEN_TS_EOT_WORDS: [u32; 20] = [
crates/trueos-eu/src/gfx12.rs:294:pub static HDC1_STATELESS_STORE_THEN_TS_EOT: EuArtifact = EuArtifact {
crates/trueos-eu/src/gfx12.rs:298:    words: &HDC1_STATELESS_STORE_THEN_TS_EOT_WORDS,
crates/trueos-eu/src/gfx12.rs:309:// send     HDC1 untyped surface write, stateless/non-coherent BTI 253, SIMD8
crates/trueos-eu/src/gfx12.rs:315:pub static STATIC_DP4A_HDC1_STATELESS_STORE_THEN_TS_EOT_WORDS: [u32; 32] = [
crates/trueos-eu/src/gfx12.rs:350:pub static STATIC_DP4A_HDC1_STATELESS_STORE_THEN_TS_EOT: EuArtifact = EuArtifact {
crates/trueos-eu/src/gfx12.rs:354:    words: &STATIC_DP4A_HDC1_STATELESS_STORE_THEN_TS_EOT_WORDS,
crates/trueos-eu/src/gfx12.rs:366:pub static T4_LIVE_X_STATIC_DP4A_REQUIREMENT_HDC1_STORE_THEN_TS_EOT: EuArtifact = EuArtifact {
crates/trueos-eu/src/gfx12.rs:370:    words: &STATIC_DP4A_HDC1_STATELESS_STORE_THEN_TS_EOT_WORDS,
crates/trueos-eu/src/gfx12.rs:382:pub const T5_SMALL_LIVE4_TRUEOS_ARENA_EXPECTED_SENTINEL_U32: u32 = 0xC0DE_7505;
crates/trueos-eu/src/gfx12.rs:383:pub const T5_SMALL_LIVE4_TRUEOS_ARENA_STORE_SEND_DWORD: usize = 73;
crates/trueos-eu/src/gfx12.rs:384:pub const T5_SMALL_LIVE4_TRUEOS_ARENA_SENTINEL_DWORD: usize = 19;
crates/trueos-eu/src/gfx12.rs:385:pub const T5_SMALL_LIVE4_WORD_VIEW_PROGRAM_NAME: &str =
crates/trueos-eu/src/gfx12.rs:387:pub const T5_SMALL_LIVE4_PROVEN_HDC_PROGRAM_NAME: &str =
crates/trueos-eu/src/gfx12.rs:389:pub const T5_SMALL_LIVE4_PROVEN_HDC_STORE_SEND_DWORD: usize = 77;
crates/trueos-eu/src/gfx12.rs:390:pub const T5_SMALL_LIVE4_PROVEN_HDC_SENTINEL_DWORD: usize = 19;
crates/trueos-eu/src/gfx12.rs:408:pub const T6_SMALL_LIVE8_TRUEOS_ARENA_EXPECTED_SENTINEL_U32: u32 = 0xC0DE_7606;
crates/trueos-eu/src/gfx12.rs:409:pub const T6_SMALL_LIVE8_TRUEOS_ARENA_STORE_SEND_DWORD: usize = 97;
crates/trueos-eu/src/gfx12.rs:410:pub const T6_SMALL_LIVE8_TRUEOS_ARENA_SENTINEL_DWORD: usize = 19;
crates/trueos-eu/src/gfx12.rs:411:pub const T61_ONE_ROW_MATVEC_PROGRAM_NAME: &str =
crates/trueos-eu/src/gfx12.rs:413:pub const T61_ONE_ROW_MATVEC_LIVE_K: usize = 16;
crates/trueos-eu/src/gfx12.rs:414:pub const T61_LIVE16_TRUEOS_ARENA_EXPECTED_SENTINEL_U32: u32 = 0xC0DE_7616;
crates/trueos-eu/src/gfx12.rs:415:pub const T61_LIVE16_TRUEOS_ARENA_STORE_SEND_DWORD: usize = 145;
crates/trueos-eu/src/gfx12.rs:416:pub const T61_LIVE16_TRUEOS_ARENA_SENTINEL_DWORD: usize = 19;
crates/trueos-eu/src/gfx12.rs:433:// T5 diagnostic control: preserve the T5 arena payload shape and final HDC1
crates/trueos-eu/src/gfx12.rs:438:pub static T5_STORE_ONLY_ARENA_OFFSET_HDC1_STORE_THEN_TS_EOT_WORDS: [u32; 42] = [
crates/trueos-eu/src/gfx12.rs:458:    T5_SMALL_LIVE4_TRUEOS_ARENA_EXPECTED_SENTINEL_U32,
crates/trueos-eu/src/gfx12.rs:483:pub static T5_STORE_ONLY_ARENA_OFFSET_HDC1_STORE_THEN_TS_EOT: EuArtifact = EuArtifact {
crates/trueos-eu/src/gfx12.rs:487:    words: &T5_STORE_ONLY_ARENA_OFFSET_HDC1_STORE_THEN_TS_EOT_WORDS,
crates/trueos-eu/src/gfx12.rs:535:pub static T5_LOAD_ECHO_TRUEOS_ARENA_RAW_OPERANDS_HDC1_STORE_THEN_TS_EOT_WORDS: [u32; 88] = [
crates/trueos-eu/src/gfx12.rs:549:pub static T5_LOAD_ECHO_TRUEOS_ARENA_RAW_OPERANDS_HDC1_STORE_THEN_TS_EOT: EuArtifact = EuArtifact {
crates/trueos-eu/src/gfx12.rs:553:    words: &T5_LOAD_ECHO_TRUEOS_ARENA_RAW_OPERANDS_HDC1_STORE_THEN_TS_EOT_WORDS,
crates/trueos-eu/src/gfx12.rs:563:pub static T5_SMALL_LIVE4_TRUEOS_ARENA_BF16_DOT_WORD_VIEW_HDC1_STATELESS_STORE_THEN_TS_EOT_WORDS:
crates/trueos-eu/src/gfx12.rs:578:pub static T5_SMALL_LIVE4_TRUEOS_ARENA_B
[... truncated in extract ...]

[2026-05-11T01:13:50.255Z] tool-call:exec_command line=90
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1300,1432p' src/intel/gpgpu.rs && sed -n '1470,1530p' src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":18000}

[2026-05-11T01:13:50.297Z] tool-output line=92
------------------------------------------------------------------------------------------------
Chunk ID: 7c809a
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1923
Output:
    0xA07C0061,
    0x3F810000,
    0x00040132,
    0x00000004,
    0x50007E14,
    0x00C47834,
];

fn selected_gpgpu_eu_program() -> GpgpuEuProgram {
    if GPGPU_USE_STATIC_DP4A_STORE_THEN_EOT {
        return gpgpu_static_dp4a_store_eot_program();
    }
    if GPGPU_USE_HDC_STORE_THEN_EOT {
        return gpgpu_store_eot_program();
    }
    let artifact = trueos_eu::gfx12::eot_artifact(ACTIVE_GFX12_EOT_VARIANT);
    GpgpuEuProgram {
        name: artifact.name,
        kind: artifact.kind,
        words: artifact.words,
        expects_store: artifact.expects_store,
        expected_store_value: GPU_PROGRAM_SHARED_RAM_WRITE_EXPECTED,
        store_send_dword: None,
        visible_seed_dword: None,
    }
}

fn is_raw_awake_program_name(name: &str) -> bool {
    name == trueos_eu::gfx12::STATIC_DP4A_HDC1_STATELESS_STORE_THEN_TS_EOT.name
}

fn should_log_gpgpu_program_shape(name: &str) -> bool {
    !is_raw_awake_program_name(name)
}

fn should_log_gpgpu_surface_state(note: &str) -> bool {
    note != "bind-send-bti-to-result-raw-buffer"
}

fn gpgpu_store_eot_program() -> GpgpuEuProgram {
    let artifact = trueos_eu::gfx12::HDC1_STATELESS_STORE_THEN_TS_EOT;
    GpgpuEuProgram {
        name: artifact.name,
        kind: artifact.kind,
        words: artifact.words,
        expects_store: artifact.expects_store,
        expected_store_value: GPU_PROGRAM_SHARED_RAM_WRITE_EXPECTED,
        store_send_dword: Some(trueos_eu::gfx12::HDC1_BTI34_STORE_SEND_DWORD),
        visible_seed_dword: Some(trueos_eu::gfx12::HDC1_BTI34_STORE_IMM_DWORD),
    }
}

fn gpgpu_static_dp4a_store_eot_program() -> GpgpuEuProgram {
    let artifact = trueos_eu::gfx12::STATIC_DP4A_HDC1_STATELESS_STORE_THEN_TS_EOT;
    GpgpuEuProgram {
        name: artifact.name,
        kind: artifact.kind,
        words: artifact.words,
        expects_store: artifact.expects_store,
        expected_store_value: GPU_PROGRAM_SHARED_RAM_WRITE_EXPECTED,
        store_send_dword: Some(trueos_eu::gfx12::HDC1_STATELESS_STATIC_DP4A_STORE_SEND_DWORD),
        visible_seed_dword: Some(trueos_eu::gfx12::HDC1_STATELESS_STATIC_DP4A_BASE_DWORD),
    }
}

fn gpgpu_one_tile_output_sentinel_program() -> GpgpuEuProgram {
    let artifact = trueos_eu::gfx12::HDC1_STATELESS_STORE_THEN_TS_EOT;
    GpgpuEuProgram {
        name: "gfx12-t47-one-tile-output-sentinel-hdc1-stateless-store-then-ts-eot",
        kind: artifact.kind,
        words: artifact.words,
        expects_store: artifact.expects_store,
        expected_store_value: GPGPU_ONE_TILE_OUTPUT_SENTINEL,
        store_send_dword: Some(trueos_eu::gfx12::HDC1_BTI34_STORE_SEND_DWORD),
        visible_seed_dword: Some(trueos_eu::gfx12::HDC1_BTI34_STORE_IMM_DWORD),
    }
}

fn gpgpu_one_tile_output_compare_program() -> GpgpuEuProgram {
    let artifact = trueos_eu::gfx12::STATIC_DP4A_HDC1_STATELESS_STORE_THEN_TS_EOT;
    GpgpuEuProgram {
        name: "gfx12-t48-one-tile-output-compare-dp4a-echo-hdc1-stateless-store-then-ts-eot",
        kind: artifact.kind,
        words: artifact.words,
        expects_store: artifact.expects_store,
        expected_store_value: 0,
        store_send_dword: Some(trueos_eu::gfx12::HDC1_STATELESS_STATIC_DP4A_STORE_SEND_DWORD),
        visible_seed_dword: Some(trueos_eu::gfx12::HDC1_STATELESS_STATIC_DP4A_BASE_DWORD),
    }
}

fn gpgpu_t5_one_row_matvec_program() -> GpgpuEuProgram {
    let artifact =
        trueos_eu::gfx12::T5_SMALL_LIVE4_TRUEOS_ARENA_BF16_DOT_HDC1_STATELESS_STORE_THEN_TS_EOT;
    GpgpuEuProgram {
        name: artifact.name,
        kind: artifact.kind,
        words: artifact.words,
        expects_store: artifact.expects_store,
        expected_store_value: 0,
        store_send_dword: Some(trueos_eu::gfx12::T5_SMALL_LIVE4_TRUEOS_ARENA_STORE_SEND_DWORD),
        visible_seed_dword: Some(trueos_eu::gfx12::T5_SMALL_LIVE4_TRUEOS_ARENA_SENTINEL_DWORD),
    }
}

fn gpgpu_t6_one_row_matvec_program() -> GpgpuEuProgram {
    let artifact =
        trueos_eu::gfx12::T6_SMALL_LIVE8_TRUEOS_ARENA_BF16_DOT_HDC1_STATELESS_STORE_THEN_TS_EOT;
    GpgpuEuProgram {
        name: artifact.name,
        kind: artifact.kind,
        words: artifact.words,
        expects_store: artifact.expects_store,
        expected_store_value: 0,
        store_send_dword: Some(trueos_eu::gfx12::T6_SMALL_LIVE8_TRUEOS_ARENA_STORE_SEND_DWORD),
        visible_seed_dword: Some(trueos_eu::gfx12::T6_SMALL_
[... truncated in extract ...]

[2026-05-11T01:13:56.261Z] tool-output line=96
------------------------------------------------------------------------------------------------
Chunk ID: 415b22
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1865
Output:
    0x00000000,
];

pub static HDC1_BTI34_STORE_THEN_TS_EOT: EuArtifact = EuArtifact {
    name: "gfx12-hdc1-bti34-store-then-ts-eot",
    isa: EuIsa::Gfx12,
    kind: EuArtifactKind::Hdc1BtiStoreThenThreadSpawnerEot,
    words: &HDC1_BTI34_STORE_THEN_TS_EOT_WORDS,
    expects_store: true,
};

// Mesa brw_asm source:
//
// mov(8)  g4<1>UD    0xC0DE7733UD
// mov(8)  g127<1>UD  0x00840058UD
// send    HDC1 untyped surface write, stateless/non-coherent BTI 253, SIMD8
// mov(8)  g127<1>UD  g0<8,8,1>UD
// send    Thread Spawner EOT from g127
//
// This is a diagnostic "EU executed a visible send" canary.  The address is
// TRUEOS' current result slot 22 GPU VA in the minimal GPGPU probe.
pub static HDC1_STATELESS_STORE_THEN_TS_EOT_WORDS: [u32; 20] = [
    0x80030061,
    0x04054660,
    0x00000000,
    STORE_SENTINEL_U32,
    0x80030061,
    0x7F054220,
    0x00000000,
    0x00840058,
    0x00030131,
    0x00000000,
    0xCDFA7F0C,
    0x009A040C,
    0x80030061,
    0x7F050220,
    0x00460005,
    0x00000000,
    0x80030131,
    0x00000004,
    0x70007F0C,
    0x00000000,
];

pub static HDC1_STATELESS_STORE_THEN_TS_EOT: EuArtifact = EuArtifact {
    name: "gfx12-hdc1-stateless-store-then-ts-eot",
    isa: EuIsa::Gfx12,
    kind: EuArtifactKind::Hdc1BtiStoreThenThreadSpawnerEot,
    words: &HDC1_STATELESS_STORE_THEN_TS_EOT_WORDS,
    expects_store: true,
};

// Mesa brw_asm source, dependency-shaped tiny ALU rung:
//
// mov(8)   g2<1>D      0xC0DE7729D
// mov(8)   g6<1>D      0x01020304D
// mov(8)   g7<1>D      0x01010101D
// dp4a(8)  g4<1>D      g2<8,8,1>D  g6<8,8,1>D  g7<1,1,1>D
// mov(8)   g127<1>UD   0x00840058UD
// send     HDC1 untyped surface write, stateless/non-coherent BTI 253, SIMD8
// mov(8)   g127<1>UD   g0<8,8,1>UD
// send     Thread Spawner EOT from g127
//
// The dot is 1+2+3+4 = 10, so the value stored by the HDC send is not a
// directly moved canary. It is STATIC_DP4A_BASE_U32 + 10 = STORE_SENTINEL_U32.
pub static STATIC_DP4A_HDC1_STATELESS_STORE_THEN_TS_EOT_WORDS: [u32; 32] = [
    0x80030061,
    0x02054660,
    0x00000000,
    STATIC_DP4A_BASE_U32,
    0x80030061,
    0x06054660,
    0x00000000,
    0x01020304,
    0x80030061,
    0x07054660,
    0x00000000,
    0x01010101,
    0x00030158,
    0x04040E68,
    0x0E0E0205,
    0x07050605,
    0x80030061,
    0x7F054220,
    0x00000000,
    0x00840058,
    0x00030131,
    0x00000000,
    0xCDFA7F0C,
    0x009A040C,
    0x80030061,
    0x7F050220,
    0x00460005,
    0x00000000,
    0x80030131,
    0x00000004,
    0x70007F0C,
    0x00000000,
];

pub static STATIC_DP4A_HDC1_STATELESS_STORE_THEN_TS_EOT: EuArtifact = EuArtifact {
    name: "gfx12-static-dp4a-hdc1-stateless-store-then-ts-eot",
    isa: EuIsa::Gfx12,
    kind: EuArtifactKind::StaticDp4aThenHdc1StoreThenThreadSpawnerEot,
    words: &STATIC_DP4A_HDC1_STATELESS_STORE_THEN_TS_EOT_WORDS,
    expects_store: true,
};

// T4 catalog rung: this intentionally reuses the proven T3 shell until the
// live activation load/send encoding is filled in.  The artifact kind and name
// let the boot harness and Lumen sidepath describe the next exact contract
// without risking the known-good static DP4A proof.
pub const T4_LIVE_X_STATIC_DP4A_LANES: usize = 4;
pub const T4_LIVE_X_STATIC_DP4A_WEIGHTS_U8: [u8; T4_LIVE_X_STATIC_DP4A_LANES] = [1, 2, 3, 4];
pub const T4_LIVE_X_STATIC_DP4A_EXPECTS_GPU_LOAD: bool = true;

pub static T4_LIVE_X_STATIC_DP4A_REQUIREMENT_HDC1_STORE_THEN_TS_EOT: EuArtifact = EuArtifact {
    name: "gfx12-t4-live-x-static-dp4a-requirement-hdc1-store-then-ts-eot",
    isa: EuIsa::Gfx12,
    kind: EuArtifactKind::LiveXStaticDp4aRequirementThenHdc1StoreThenThreadSpawnerEot,
    words: &STATIC_DP4A_HDC1_STATELESS_STORE_THEN_TS_EOT_WORDS,
    expects_store: true,
};

// T5 is the first rung whose success condition is a real one-row matvec:
// load the staged live f32 activation vector, load one staged BF16 model row,
// multiply/reduce, and store the computed row output.  Older T47/T48 artifacts
// remain preserved as sentinel/echo controls and must not satisfy this rung.
pub const T5_ONE_ROW_MATVEC_PROGRAM_NAME: &str =
    "gfx12-t5-small-live4-packed-bf16-dot-hdc1-stateless-store-then-ts-eot";
pub const T5_ONE_ROW_MATVEC_LIVE_K: usize = 4;
pub const T5_ONE_ROW_MATVEC_REQUIRES_LIVE_GPU_LOAD: bool = true;
pub const T5_SMALL_LIVE4_TRUEOS_ARENA_EXPECTED_SENTINEL_U32: u32 = 0xC0DE_7505;
pub const T
[... truncated in extract ...]

[2026-05-11T01:14:11.753Z] assistant line=98
------------------------------------------------------------------------------------------------
No files edited.

**Mandelbrot Status**
I did not find a real Intel EU/GPGPU Mandelbrot shader artifact or Intel one-shot test-render path. Existing Mandelbrot code is:

- [src/tst/mandelbrot_gpu_sidequest.rs](/home/t4ce/REPOS/TRUEOS/src/tst/mandelbrot_gpu_sidequest.rs:6): scaffold only, logs `action=no-submit-no-present`.
- [src/tst/ui2_mandelbrot_demo.rs](/home/t4ce/REPOS/TRUEOS/src/tst/ui2_mandelbrot_demo.rs:51): UI2 demo using `surface.render_mandelbrot`.
- [src/r/gfx_cabi.rs](/home/t4ce/REPOS/TRUEOS/src/r/gfx_cabi.rs:4921): VirGL/GFX path, draws a textured full-screen quad with `TexPipelineKind::Mandelbrot`.
- [src/gfx/mandelbrot.rs](/home/t4ce/REPOS/TRUEOS/src/gfx/mandelbrot.rs:1): TGSI/string shader + quad vertex generation, not Intel EU bytes.

**Existing EU Artifacts**
The active compiled Intel GPGPU path is [src/intel/gpgpu.rs](/home/t4ce/REPOS/TRUEOS/src/intel/gpgpu.rs:1208). The nested [src/intel/intel/render/gpgpu.rs](/home/t4ce/REPOS/TRUEOS/src/intel/intel/render/gpgpu.rs:1) is a factored mirror/reference, but `src/main.rs` loads `mod intel;`, so [src/intel/mod.rs](/home/t4ce/REPOS/TRUEOS/src/intel/mod.rs:43) is the active module.

Important artifacts/constants:

- Active selected artifact: `gfx12-static-dp4a-hdc1-stateless-store-then-ts-eot`, 32 dwords / 128 bytes, because `GPGPU_USE_STATIC_DP4A_STORE_THEN_EOT = true`.
- Store sentinel: `STORE_SENTINEL_U32 = 0xC0DE_7733`.
- Static DP4A seed base: `STATIC_DP4A_BASE_U32 = STORE_SENTINEL_U32 - 10`.
- Stateless store artifact: `gfx12-hdc1-stateless-store-then-ts-eot`, 20 dwords / 80 bytes.
- EOT fallback selected by constant if stores disabled: `Gfx12EotVariant::TsR0ToG127Send1`, 8 dwords / 32 bytes.
- GPGPU program upload VA: `GPU_VA_DRAW_STATE_BASE + GPGPU_EU_KERNEL_OFFSET_BYTES = 0x0086_3000`.
- Walker scratch: `GPU_VA_BATCH_BASE + GPGPU_WALKER_SCRATCH_OFFSET_BYTES = 0x0180_3800`.
- Store surface state: binding table offset `0x3400`, surface state offset `0x3500`, BTI index `0x34`, raw buffer format `SURFACE_FORMAT_RAW = 0x1FF`.

**Functions To Reuse**
- Upload EU bytes: `upload_and_verify_gpu_program_at(warm, offset, words)` in [src/intel/gpgpu.rs](/home/t4ce/REPOS/TRUEOS/src/intel/gpgpu.rs:5029).
- Prepare/log active artifact: `prepare_gpgpu_program_artifact`.
- Encode/submit current walker: `encode_gfx12_gpgpu_walker_probe_batch(...)` and `submit_gpgpu_compute_walker_probe(...)`.
- Submit through RCS/execlist: `submit_warm_render_batch(...)`.
- GGTT map helpers are in `src/intel/mod.rs`: `map_ggtt`, `ggtt_invalidate`, `dma_flush`.

**Arbitrary Store Target**
Yes, mostly. Active code already has:

- `prepare_gpgpu_store_surface_state_for_target(warm, target_gpu, note)`
- `prepare_gpgpu_store_surface_state_for_target_span(warm, target_gpu, target_bytes, note)`

So the HDC/raw-buffer surface can be pointed at an arbitrary GPU VA/span. Caveat: that buffer must already be mapped into the GPU address space, and the current EU artifacts may still contain hard-coded stateless addresses like `0x00840058`; a Mandelbrot kernel should either use the BTI surface path consistently or have generated EU bytes patched for the target address.

**Smallest Integration Point**
Smallest clean one-shot Mandelbrot hook: add a new private one-shot function next to `submit_gpgpu_compute_walker_probe` in [src/intel/gpgpu.rs](/home/t4ce/REPOS/TRUEOS/src/intel/gpgpu.rs:5528).

Reuse the existing sequence:

1. `warm_once` / existing warm buffers.
2. Map a target RGBA buffer to a chosen GPU VA.
3. Upload Mandelbrot EU words with `upload_and_verify_gpu_program_at`.
4. Call `prepare_gpgpu_store_surface_state_for_target_span(warm, target_gpu, width * height * 4, "mandelbrot")`.
5. Encode a GPGPU walker with `group_x_dim` covering pixels/lanes.
6. Submit via `submit_warm_render_batch`.

For plumbing, the dormant entry is `mandelbrot_gpu_sidequest_task`; replace its `upload=deferred action=no-submit-no-present` behavior with a call into that new Intel one-shot helper.

[2026-05-11T01:14:11.754Z] assistant line=99
------------------------------------------------------------------------------------------------
No files edited.

**Mandelbrot Status**
I did not find a real Intel EU/GPGPU Mandelbrot shader artifact or Intel one-shot test-render path. Existing Mandelbrot code is:

- [src/tst/mandelbrot_gpu_sidequest.rs](/home/t4ce/REPOS/TRUEOS/src/tst/mandelbrot_gpu_sidequest.rs:6): scaffold only, logs `action=no-submit-no-present`.
- [src/tst/ui2_mandelbrot_demo.rs](/home/t4ce/REPOS/TRUEOS/src/tst/ui2_mandelbrot_demo.rs:51): UI2 demo using `surface.render_mandelbrot`.
- [src/r/gfx_cabi.rs](/home/t4ce/REPOS/TRUEOS/src/r/gfx_cabi.rs:4921): VirGL/GFX path, draws a textured full-screen quad with `TexPipelineKind::Mandelbrot`.
- [src/gfx/mandelbrot.rs](/home/t4ce/REPOS/TRUEOS/src/gfx/mandelbrot.rs:1): TGSI/string shader + quad vertex generation, not Intel EU bytes.

**Existing EU Artifacts**
The active compiled Intel GPGPU path is [src/intel/gpgpu.rs](/home/t4ce/REPOS/TRUEOS/src/intel/gpgpu.rs:1208). The nested [src/intel/intel/render/gpgpu.rs](/home/t4ce/REPOS/TRUEOS/src/intel/intel/render/gpgpu.rs:1) is a factored mirror/reference, but `src/main.rs` loads `mod intel;`, so [src/intel/mod.rs](/home/t4ce/REPOS/TRUEOS/src/intel/mod.rs:43) is the active module.

Important artifacts/constants:

- Active selected artifact: `gfx12-static-dp4a-hdc1-stateless-store-then-ts-eot`, 32 dwords / 128 bytes, because `GPGPU_USE_STATIC_DP4A_STORE_THEN_EOT = true`.
- Store sentinel: `STORE_SENTINEL_U32 = 0xC0DE_7733`.
- Static DP4A seed base: `STATIC_DP4A_BASE_U32 = STORE_SENTINEL_U32 - 10`.
- Stateless store artifact: `gfx12-hdc1-stateless-store-then-ts-eot`, 20 dwords / 80 bytes.
- EOT fallback selected by constant if stores disabled: `Gfx12EotVariant::TsR0ToG127Send1`, 8 dwords / 32 bytes.
- GPGPU program upload VA: `GPU_VA_DRAW_STATE_BASE + GPGPU_EU_KERNEL_OFFSET_BYTES = 0x0086_3000`.
- Walker scratch: `GPU_VA_BATCH_BASE + GPGPU_WALKER_SCRATCH_OFFSET_BYTES = 0x0180_3800`.
- Store surface state: binding table offset `0x3400`, surface state offset `0x3500`, BTI index `0x34`, raw buffer format `SURFACE_FORMAT_RAW = 0x1FF`.

**Functions To Reuse**
- Upload EU bytes: `upload_and_verify_gpu_program_at(warm, offset, words)` in [src/intel/gpgpu.rs](/home/t4ce/REPOS/TRUEOS/src/intel/gpgpu.rs:5029).
- Prepare/log active artifact: `prepare_gpgpu_program_artifact`.
- Encode/submit current walker: `encode_gfx12_gpgpu_walker_probe_batch(...)` and `submit_gpgpu_compute_walker_probe(...)`.
- Submit through RCS/execlist: `submit_warm_render_batch(...)`.
- GGTT map helpers are in `src/intel/mod.rs`: `map_ggtt`, `ggtt_invalidate`, `dma_flush`.

**Arbitrary Store Target**
Yes, mostly. Active code already has:

- `prepare_gpgpu_store_surface_state_for_target(warm, target_gpu, note)`
- `prepare_gpgpu_store_surface_state_for_target_span(warm, target_gpu, target_bytes, note)`

So the HDC/raw-buffer surface can be pointed at an arbitrary GPU VA/span. Caveat: that buffer must already be mapped into the GPU address space, and the current EU artifacts may still contain hard-coded stateless addresses like `0x00840058`; a Mandelbrot kernel should either use the BTI surface path consistently or have generated EU bytes patched for the target address.

**Smallest Integration Point**
Smallest clean one-shot Mandelbrot hook: add a new private one-shot function next to `submit_gpgpu_compute_walker_probe` in [src/intel/gpgpu.rs](/home/t4ce/REPOS/TRUEOS/src/intel/gpgpu.rs:5528).

Reuse the existing sequence:

1. `warm_once` / existing warm buffers.
2. Map a target RGBA buffer to a chosen GPU VA.
3. Upload Mandelbrot EU words with `upload_and_verify_gpu_program_at`.
4. Call `prepare_gpgpu_store_surface_state_for_target_span(warm, target_gpu, width * height * 4, "mandelbrot")`.
5. Encode a GPGPU walker with `group_x_dim` covering pixels/lanes.
6. Submit via `submit_warm_render_batch`.

For plumbing, the dormant entry is `mandelbrot_gpu_sidequest_task`; replace its `upload=deferred action=no-submit-no-present` behavior with a call into that new Intel one-shot helper.

[extracted_items=64]

================================================================================================
OLDER: visible Intel scanout/render path for sidequest pixels
source: /home/t4ce/.codex/sessions/2026/05/11/rollout-2026-05-11T03-30-48-019e14a8-84c7-7ee2-a00a-ffc1e18a587e.jsonl
================================================================================================

[2026-05-11T01:30:49.584Z] meta line=1
------------------------------------------------------------------------------------------------
id=019e14a8-84c7-7ee2-a00a-ffc1e18a587e | cwd=/home/t4ce/REPOS/TRUEOS | source={'subagent': {'thread_spawn': {'parent_thread_id': '019e1270-c5b3-7552-b990-18526e80915b', 'depth': 1, 'agent_path': None, 'agent_nickname': 'Faraday', 'agent_role': 'explorer'}}} | git=33320ed4e7d5

[2026-05-11T01:30:49.586Z] user line=6
------------------------------------------------------------------------------------------------
Explore the TRUEOS repo for the Intel visible render path needed to make the Mandelbrot sidequest actually show pixels. Focus on existing display/scanout/primary surface code, any render/3D pipeline code, framebuffer/logo painting, copy/present paths, and what is missing between a GPU-written buffer and visible output. Do not edit files. Return concise findings with file paths and concrete next steps/risks.

[2026-05-11T01:30:51.410Z] assistant line=9
------------------------------------------------------------------------------------------------
I’ll inspect the repo read-only and trace the graphics path from “pixels exist somewhere” to “scanout shows them.” I’ll keep the pass focused on Intel display, framebuffer/logo, and any present/copy hooks, then come back with concrete gaps rather than a broad tour.

[2026-05-11T01:30:51.410Z] assistant line=10
------------------------------------------------------------------------------------------------
I’ll inspect the repo read-only and trace the graphics path from “pixels exist somewhere” to “scanout shows them.” I’ll keep the pass focused on Intel display, framebuffer/logo, and any present/copy hooks, then come back with concrete gaps rather than a broad tour.

[2026-05-11T01:30:52.275Z] tool-call:update_plan line=11
------------------------------------------------------------------------------------------------
{"plan":[{"step":"Map repository graphics-related files","status":"in_progress"},{"step":"Inspect Intel display and framebuffer paths","status":"pending"},{"step":"Inspect render/3D and Mandelbrot sidequest code","status":"pending"},{"step":"Synthesize missing link, next steps, risks","status":"pending"}]}

[2026-05-11T01:30:55.978Z] tool-call:exec_command line=14
------------------------------------------------------------------------------------------------
{"cmd":"rg --files | rg -i '(intel|i915|gpu|drm|display|scan|framebuffer|fb|logo|render|mandel|mesa|3d|surface|present|blit|copy)'","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":20000}

[2026-05-11T01:30:55.988Z] tool-call:exec_command line=15
------------------------------------------------------------------------------------------------
{"cmd":"find . -maxdepth 3 -type d | sed 's#^./##' | sort | rg -i '(kernel|drivers|display|graphics|video|gpu|arch|apps|user|boot)'","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":12000}

[2026-05-11T01:30:56.031Z] tool-output line=18
------------------------------------------------------------------------------------------------
Chunk ID: a9055a
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1792
Output:
docs/intel-uhd770-cpu-reference.html
tools/intel_userland_oracle/README.md
tools/intel_userland_oracle/hw_mmio_sampler.c
tools/intel_userland_oracle/run_oracle.sh
tools/intel_userland_oracle/sentinel.comp
tools/intel_userland_oracle/vk_compute_sentinel.c
tools/intel_userland_oracle/ioctl_trace.c
tools/intel_vk_empty_compute.c
src/tst/ui2_mandelbrot_demo.rs
src/tst/mandelbrot_gpu_sidequest.rs
src/intel_hda_probe.rs
src/intel/intel/shader/mod.rs
src/intel/intel/shader/generated.rs
src/intel/intel/shader/triangle.frag
src/intel/intel/shader/triangle.vert
src/intel/intel/shader/bake_format.md
src/intel/intel/mod.rs
src/intel/intel/c3D.rs
src/intel/intel/guc.rs
src/intel/intel/format.rs
src/intel/intel/hda.rs
src/intel/intel/may_help/XeLp_copy_ngin.rs_first_try
src/intel/intel/may_help/README.md
src/intel/intel/may_help/i915-adls-tc3-power-map.md
src/intel/intel/may_help/gfx_intel_disp.rs
src/intel/intel/may_help/intel_guc.rs
src/intel/intel/may_help/XeLp_media_ngin.rs_first_try
src/intel/intel/may_help/intelpain_intel.rs
src/intel/intel/may_help/gfx_backend_intel.rs
src/intel/intel/may_help/pci.txt
src/intel/intel/may_help/drm-tree.txt
src/intel/intel/may_help/intel_igpu770.rs
src/intel/intel/may_help/intel_old.rs
src/intel/intel/may_help/XeLp_display_ngin.rs_first_try
logo.jpg
src/intel/intel/may_help/XeLp_render_ngin.rs_first_try
src/intel/intel/may_help/intel_igpu770_rcs.rs
src/intel/intel/may_help/connectors.txt
src/intel/intel/may_help/journal-i915.txt
src/intel/intel/may_help/gfx_intel.rs
src/intel/intel/may_help/fulldmp
src/intel/intel/may_help/intel_770_registers.rs
src/intel/intel/stateoftheart
src/intel/intel/xelp_media_mp4.rs
src/intel/intel/mem.rs
src/intel/intel/xelp_media_h264src.rs
src/intel/intel/display.rs
src/intel/intel/hw_cursor.rs
src/intel/intel/xelp_media_matroska.rs
src/intel/intel/xelp_media2_ngin.rs
src/intel/intel/render/constants.rs
src/intel/intel/render/mod.rs
src/intel/intel/render/warmup.rs
src/intel/intel/render/gpgpu.rs
src/intel/intel/render/resources.rs
src/intel/intel/render/primary.rs
src/intel/intel/render/submit.rs
src/intel/intel/render/pipeline.rs
src/intel/intel/render/state.rs
src/intel/intel/render/lrc.rs
src/intel/intel/xelp_media_source.rs
src/intel/intel/stats.rs
src/intel/intel/state.rs
src/intel/intel/reference/barmap
src/intel/mem.rs
src/intel/xelp_media_h264src.rs
src/intel/display.rs
src/intel/hw_cursor.rs
src/intel/xelp_media_matroska.rs
src/intel/tlb (2).txt
src/intel/xelp_media2_ngin.rs
src/intel/xelp_media_source.rs
src/intel/stats.rs
src/intel/state.rs
src/intel/intel/reference/hvv/README.md
src/intel/intel/reference/hvv/mod.rs
src/intel/intel/reference/hvv/caps.rs
src/intel/intel/reference/hvv/lineage.rs
src/intel/intel/reference/registers
src/intel/intel/reference/forcewake
src/pci/reference/virtio_gpu.rs
src/intel/format.rs
src/intel/hda.rs
src/intel/gpgpu.rs
src/intel/xelp_media_mp4.rs
src/lumen/gpu_shadow.rs
src/intel/c3D.rs
src/lumen/gpgpu_ladder.md
src/intel/guc.rs
src/intel/mod.rs
src/intel/intel/reference/amvpipedump
src/intel/intel/reference/G12TL_intel_prm/intel-gfx-prm-osrc-tgl-vol06-memory_views-rev2.txt
src/intel/intel/reference/G12TL_intel_prm/intel-gfx-prm-osrc-tgl-vol-02-b-command-reference-enumerations.txt
src/intel/intel/reference/G12TL_intel_prm/intel-gfx-prm-osrc-tgl-vol-02-a-command-reference-instructions.txt
src/intel/intel/reference/G12TL_intel_prm/intel-gfx-prm-osrc-tgl-vol-05-memory-data-formats.pdf
src/intel/intel/reference/G12TL_intel_prm/intel-gfx-prm-osrc-tgl-vol-02-d-command-reference-structures.txt
src/intel/intel/reference/G12TL_intel_prm/intel-gfx-prm-osrc-tgl-vol-03-gpu-overview.txt
src/intel/intel/reference/G12TL_intel_prm/intel-gfx-prm-osrc-tgl-vol-01-preface-2.pdf
src/intel/intel/reference/G12TL_intel_prm/intel-gfx-prm-osrc-tgl-vol-02-b-command-reference-enumerations.pdf
src/intel/intel/reference/G12TL_intel_prm/intel-gfx-prm-osrc-tgl-vol-14-work-arounds.txt
src/intel/intel/reference/G12TL_intel_prm/intel-gfx-prm-osrc-tgl-vol-02-c-command-reference-registers-part-1.txt
src/intel/intel/reference/G12TL_intel_prm/intel-gfx-prm-osrc-tgl-vol-07-memory-cache.txt
src/intel/intel/reference/G12TL_intel_prm/intel-gfx-prm-osrc-tgl-vol-04-configurations.txt
src/intel/intel/reference/G12TL_intel_prm/intel-gfx-prm-osrc-tgl-vol-02-d-command-reference-structures.pdf
src/intel/intel/reference/G12TL_intel_prm/intel-gfx-prm-osrc-tgl-vol-02-c-
[... truncated in extract ...]

[2026-05-11T01:30:56.031Z] tool-output line=19
------------------------------------------------------------------------------------------------
Chunk ID: 87ca2a
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 283
Output:
.codex_tmp/intel_userland_oracle
.codex_tmp/intel_userland_oracle/latest
.codex_tmp/intel_userland_oracle/mandatory-hw-check
.codex_tmp/intel_userland_oracle/sentinel-regression
.codex_tmp/intel_userland_oracle/smoke-userland
.codex_tmp/intel_userland_oracle/t5-small-live4
.codex_tmp/intel_userland_oracle/t5-small-live4-trueos-arena
.codex_tmp/intel_userland_oracle/t5-small-live4-trueos-arena-bf16-unpack
.codex_tmp/intel_userland_oracle/t5-small-live4-trueos-arena-native-redump
.codex_tmp/intel_userland_oracle/t5-small-live4-trueos-arena-native-redump-nocache
.codex_tmp/intel_userland_oracle/t6-1-live16-trueos-arena-bf16-unpack
.codex_tmp/intel_userland_oracle/t6-2-lane-indexed-live16-trueos-arena-bf16-unpack
.codex_tmp/intel_userland_oracle/t6-2-row-indexed-live16-trueos-arena-bf16-unpack
.codex_tmp/intel_userland_oracle/t6-3-accum16-hi-live32-trueos-arena-bf16-unpack
.codex_tmp/intel_userland_oracle/t6-3-lane-indexed-live32-trueos-arena-bf16-unpack
.codex_tmp/intel_userland_oracle/t6-small-live8-trueos-arena-bf16-unpack
bld/EFI/BOOT
bld/iso-bootroot
bld/iso-bootroot/EFI
src/usb2/video
tools/intel_userland_oracle

[2026-05-11T01:30:56.042Z] tool-output line=20
------------------------------------------------------------------------------------------------
Chunk ID: 43cb81
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 67
Output:
A  .codex_tmp/mandelbrot_fragment_1440p_parametric.spv
M  .codex_tmp/mandelbrot_fragment_1440p_parametric_memo.md
A  retro_sun_shader.html
M  src/intel/gpgpu.rs
M  src/intel/mod.rs
M  src/r/readiness.rs
M  src/r/spawn_service.rs
MM src/tst/mandelbrot_gpu_sidequest.rs

[2026-05-11T01:30:58.284Z] assistant line=22
------------------------------------------------------------------------------------------------
There are already local modifications, including the sidequest and Intel modules, so I’m treating the tree as read-only and will avoid assuming those changes are mine. The graphics code is clustered in `src/intel`, `src/intel/intel/render`, `src/gfx`, and the test sidequest.

[2026-05-11T01:30:58.284Z] assistant line=23
------------------------------------------------------------------------------------------------
There are already local modifications, including the sidequest and Intel modules, so I’m treating the tree as read-only and will avoid assuming those changes are mine. The graphics code is clustered in `src/intel`, `src/intel/intel/render`, `src/gfx`, and the test sidequest.

[2026-05-11T01:30:59.106Z] tool-call:update_plan line=24
------------------------------------------------------------------------------------------------
{"plan":[{"step":"Map repository graphics-related files","status":"completed"},{"step":"Inspect Intel display and framebuffer paths","status":"in_progress"},{"step":"Inspect render/3D and Mandelbrot sidequest code","status":"pending"},{"step":"Synthesize missing link, next steps, risks","status":"pending"}]}

[2026-05-11T01:31:02.618Z] tool-call:exec_command line=27
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1,260p' src/intel/display.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":22000}

[2026-05-11T01:31:02.626Z] tool-call:exec_command line=28
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1,260p' src/intel/intel/display.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":22000}

[2026-05-11T01:31:02.641Z] tool-call:exec_command line=29
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"framebuffer|Framebuffer|fb_|fb\\.|logo|scanout|plane|PLANE|SURF|stride|pitch|primary|cursor|present|blit|copy|PIPE|DPLL|transcoder|vblank\" src -g '*.rs'","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":32000}

[2026-05-11T01:31:02.690Z] tool-output line=31
------------------------------------------------------------------------------------------------
Chunk ID: 519055
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 2347
Output:
// Display proof contract.
//
// Current evidence from the bring-up transcript:
// - `primary-boot-surface` programs pipe-a at `2560x1440`, pitch `0x2800`.
// - The surface GPU address is `0x02000000`.
// - `surf_live` matches `surf`, and the boot logo path reports `ok=1`.
//
// This proves scanout handoff to known memory.  It does not prove the 3D
// pipeline rendered that memory; render must separately produce `ps-rt-proof
// accepted=1` before a displayed pixel can be attributed to GPU rendering.

use core::sync::atomic::{AtomicBool, AtomicU32, Ordering};
use spin::Mutex;

macro_rules! intel_display_verbose_log {
    ($($arg:tt)*) => {
        if crate::logflag::INTEL_DISPLAY_NGIN_LOGS && !crate::logflag::INTEL_STAGE1_LOGS {
            crate::log!($($arg)*);
        }
    };
}

const PIPE_A_SRC: usize = 0x7001C;
const PIPE_B_SRC: usize = 0x7101C;
const PIPE_C_SRC: usize = 0x7201C;
const PIPE_D_SRC: usize = 0x7301C;
const PIPECONF_A: usize = 0x70008;
const TRANS_HTOTAL_A: usize = 0x60000;
const TRANS_HSYNC_A: usize = 0x60008;
const TRANS_VTOTAL_A: usize = 0x6000C;
const TRANS_VSYNC_A: usize = 0x60014;
const TRANS_DDI_FUNC_CTL_A: usize = 0x60400;
const TRANS_PSR_CTL_A: usize = 0x60800;
const TRANS_PSR_STATUS_A: usize = 0x60840;
const TRANS_PSR2_CTL_A: usize = 0x60900;
const TRANS_PSR2_STATUS_A: usize = 0x60940;
const CUR_SURFLIVE_A: usize = 0x700AC;
const PIPE_FRMCOUNT_A: usize = 0x70040;
const PIPE_MMIO_STRIDE: usize = 0x1000;
const SKL_BOTTOM_COLOR_A: usize = 0x70034;
const SKL_BOTTOM_COLOR_PIPE_STRIDE: usize = 0x1000;
const UNI_PLANE_BASE: usize = 0x70180;
const UNI_PLANE_PIPE_STRIDE: usize = 0x1000;
const UNI_PLANE_SLOT_STRIDE: usize = 0x100;
const UNI_PLANE_CTL_OFF: usize = 0x00;
const UNI_PLANE_STRIDE_OFF: usize = 0x08;
const UNI_PLANE_POS_OFF: usize = 0x0C;
const UNI_PLANE_SIZE_OFF: usize = 0x10;
const UNI_PLANE_KEYVAL_OFF: usize = 0x14;
const UNI_PLANE_KEYMSK_OFF: usize = 0x18;
const UNI_PLANE_SURF_OFF: usize = 0x1C;
const UNI_PLANE_KEYMAX_OFF: usize = 0x20;
const UNI_PLANE_OFFSET_OFF: usize = 0x24;
const UNI_PLANE_SURFLIVE_OFF: usize = 0x2C;
const UNI_PLANE_AUX_DIST_OFF: usize = 0x40;
const UNI_PLANE_AUX_OFFSET_OFF: usize = 0x44;
const UNI_PLANE_COLOR_CTL_OFF: usize = 0x4C;
const UNI_PLANE_BUF_CFG_OFF: usize = 0xFC;
const PLANE_CTL_ENABLE: u32 = 1 << 31;
const PLANE_CTL_FORMAT_MASK_SKL: u32 = 0x0F << 24;
const PLANE_CTL_ORDER_RGBX: u32 = 1 << 20;
const PLANE_CTL_TILED_MASK: u32 = 0x07 << 10;
const PLANE_CTL_ROTATE_MASK: u32 = 0x03;
const PLANE_CTL_FORMAT_XRGB_8888: u32 = 4 << 24;
const PLANE_CTL_TILED_LINEAR: u32 = 0 << 10;
const PLANE_COLOR_ALPHA_MASK: u32 = 0x03 << 4;
const PIPE_BOTTOM_COLOR_RGB: u32 = 0x00FF_37FF;
const PRIMARY_FORMAT_PROBE_XRGB: u32 = 0;
const PRIMARY_FORMAT_PROBE_XBGR: u32 = 1;
const PRIMARY_FORMAT_PROBE_MODE: u32 = PRIMARY_FORMAT_PROBE_XBGR;
const PRIMARY_PRESENT_DISABLE_PSR_PROBE: bool = true;
const PRIMARY_BYTES_PER_PIXEL: u32 = 4;
const PRIMARY_BASELINE_COLOR: u32 = 0x00FF_37FF;
const PRIMARY_BOOT_LOGO_JPEG: &[u8] = include_bytes!("../../logo.jpg");
const PRIMARY_BOOT_LOGO_ENABLED: bool = true;
const CPU_SCANOUT_PROOF_ENABLED: bool = true;
const CPU_SCANOUT_PROOF_COLOR: u32 = 0x00FF_00FF;
const CPU_SCANOUT_PROOF_SIZE: u32 = 8;
const OVERLAY_PLANE_SLOT: usize = 1;
const OVERLAY_MARGIN_X: u32 = 0;
const OVERLAY_MARGIN_Y: u32 = 0;

static PRIMARY_BOOT_SURFACE_INIT: AtomicBool = AtomicBool::new(false);
static PRIMARY_PRESENT_SEQ: AtomicU32 = AtomicU32::new(0);
static PRIMARY_SURFACE: Mutex<Option<PrimarySurface>> = Mutex::new(None);
static OVERLAY_PRESENT_SEQ: AtomicU32 = AtomicU32::new(0);
static OVERLAY_SURFACE: Mutex<Option<OverlaySurface>> = Mutex::new(None);

#[derive(Copy, Clone)]
pub(super) struct PipeInfo {
    pub(super) name: &'static str,
    pub(super) slot: usize,
    pub(super) pipe_src_off: usize,
    pub(super) plane_ctl_off: usize,
    pub(super) plane_stride_off: usize,
    pub(super) plane_surf_off: usize,
    pub(super) plane_surf_live_off: usize,
}

#[derive(Copy, Clone)]
struct PrimarySurface {
    width: u32,
    height: u32,
    pitch_bytes: u32,
    phys: u64,
    virt: *mut u8,
    pipe: PipeInfo,
}

unsafe impl Send for PrimarySurface {}
unsafe impl Sync for PrimarySurface {}

#[derive(Copy, Clone)]
struct OverlaySurface {
    width: u32,
    height: u32,
    pitch_bytes: u32,
    phys: u64,
    virt: *mut u8,
    pipe: PipeInfo,
    plane_slot: usize,
}
[... truncated in extract ...]

[2026-05-11T01:31:02.690Z] tool-output line=32
------------------------------------------------------------------------------------------------
Chunk ID: 18eced
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 2206
Output:
// Display proof contract.
//
// Current evidence from the bring-up transcript:
// - `primary-boot-surface` programs pipe-a at `2560x1440`, pitch `0x2800`.
// - The surface GPU address is `0x02000000`.
// - `surf_live` matches `surf`, and the boot logo path reports `ok=1`.
//
// This proves scanout handoff to known memory.  It does not prove the 3D
// pipeline rendered that memory; render must separately produce `ps-rt-proof
// accepted=1` before a displayed pixel can be attributed to GPU rendering.

use core::sync::atomic::{AtomicBool, AtomicU32, Ordering};
use spin::Mutex;

macro_rules! intel_display_focus_log {
    ($($arg:tt)*) => {
        if crate::logflag::INTEL_STAGE1_LOGS || crate::logflag::INTEL_DISPLAY_NGIN_LOGS {
            crate::log!($($arg)*);
        }
    };
}

macro_rules! intel_display_verbose_log {
    ($($arg:tt)*) => {
        if crate::logflag::INTEL_DISPLAY_NGIN_LOGS && !crate::logflag::INTEL_STAGE1_LOGS {
            crate::log!($($arg)*);
        }
    };
}

const PIPE_A_SRC: usize = 0x7001C;
const PIPE_B_SRC: usize = 0x7101C;
const PIPE_C_SRC: usize = 0x7201C;
const PIPE_D_SRC: usize = 0x7301C;
const PIPECONF_A: usize = 0x70008;
const TRANS_HTOTAL_A: usize = 0x60000;
const TRANS_HSYNC_A: usize = 0x60008;
const TRANS_VTOTAL_A: usize = 0x6000C;
const TRANS_VSYNC_A: usize = 0x60014;
const TRANS_DDI_FUNC_CTL_A: usize = 0x60400;
const TRANS_PSR_CTL_A: usize = 0x60800;
const TRANS_PSR_STATUS_A: usize = 0x60840;
const TRANS_PSR2_CTL_A: usize = 0x60900;
const TRANS_PSR2_STATUS_A: usize = 0x60940;
const CUR_SURFLIVE_A: usize = 0x700AC;
const PIPE_FRMCOUNT_A: usize = 0x70040;
const PIPE_MMIO_STRIDE: usize = 0x1000;
const SKL_BOTTOM_COLOR_A: usize = 0x70034;
const SKL_BOTTOM_COLOR_PIPE_STRIDE: usize = 0x1000;
const UNI_PLANE_BASE: usize = 0x70180;
const UNI_PLANE_PIPE_STRIDE: usize = 0x1000;
const UNI_PLANE_SLOT_STRIDE: usize = 0x100;
const UNI_PLANE_CTL_OFF: usize = 0x00;
const UNI_PLANE_STRIDE_OFF: usize = 0x08;
const UNI_PLANE_POS_OFF: usize = 0x0C;
const UNI_PLANE_SIZE_OFF: usize = 0x10;
const UNI_PLANE_KEYVAL_OFF: usize = 0x14;
const UNI_PLANE_KEYMSK_OFF: usize = 0x18;
const UNI_PLANE_SURF_OFF: usize = 0x1C;
const UNI_PLANE_KEYMAX_OFF: usize = 0x20;
const UNI_PLANE_OFFSET_OFF: usize = 0x24;
const UNI_PLANE_SURFLIVE_OFF: usize = 0x2C;
const UNI_PLANE_AUX_DIST_OFF: usize = 0x40;
const UNI_PLANE_AUX_OFFSET_OFF: usize = 0x44;
const UNI_PLANE_COLOR_CTL_OFF: usize = 0x4C;
const UNI_PLANE_BUF_CFG_OFF: usize = 0xFC;
const PLANE_CTL_ENABLE: u32 = 1 << 31;
const PLANE_CTL_FORMAT_MASK_SKL: u32 = 0x0F << 24;
const PLANE_CTL_ORDER_RGBX: u32 = 1 << 20;
const PLANE_CTL_TILED_MASK: u32 = 0x07 << 10;
const PLANE_CTL_ROTATE_MASK: u32 = 0x03;
const PLANE_CTL_FORMAT_XRGB_8888: u32 = 4 << 24;
const PLANE_CTL_TILED_LINEAR: u32 = 0 << 10;
const PLANE_COLOR_ALPHA_MASK: u32 = 0x03 << 4;
const PIPE_BOTTOM_COLOR_RGB: u32 = 0x00FF_37FF;
const PRIMARY_FORMAT_PROBE_XRGB: u32 = 0;
const PRIMARY_FORMAT_PROBE_XBGR: u32 = 1;
const PRIMARY_FORMAT_PROBE_MODE: u32 = PRIMARY_FORMAT_PROBE_XBGR;
const PRIMARY_PRESENT_DISABLE_PSR_PROBE: bool = true;
const PRIMARY_BYTES_PER_PIXEL: u32 = 4;
const PRIMARY_BASELINE_COLOR: u32 = 0x00FF_37FF;
const PRIMARY_BOOT_LOGO_JPEG: &[u8] = include_bytes!("../../logo.jpg");
const PRIMARY_BOOT_LOGO_ENABLED: bool = true;
const CPU_SCANOUT_PROOF_ENABLED: bool = true;
const CPU_SCANOUT_PROOF_COLOR: u32 = 0x00FF_00FF;
const CPU_SCANOUT_PROOF_SIZE: u32 = 8;
const OVERLAY_PLANE_SLOT: usize = 1;
const OVERLAY_MARGIN_X: u32 = 0;
const OVERLAY_MARGIN_Y: u32 = 0;

static PRIMARY_BOOT_SURFACE_INIT: AtomicBool = AtomicBool::new(false);
static PRIMARY_PRESENT_SEQ: AtomicU32 = AtomicU32::new(0);
static PRIMARY_SURFACE: Mutex<Option<PrimarySurface>> = Mutex::new(None);
static OVERLAY_PRESENT_SEQ: AtomicU32 = AtomicU32::new(0);
static OVERLAY_SURFACE: Mutex<Option<OverlaySurface>> = Mutex::new(None);

#[derive(Copy, Clone, Debug, Default)]
pub(crate) struct PrimarySurfaceSampleSet {
    pub(crate) tl: u32,
    pub(crate) center: u32,
    pub(crate) br: u32,
    pub(crate) apex: u32,
    pub(crate) centroid: u32,
    pub(crate) left: u32,
    pub(crate) right: u32,
}

impl PrimarySurfaceSampleSet {
    pub(crate) fn any_changed_since(self, before: Self) -> bool {
        self.tl != before.tl
            || self.center != before.center
            || self.br != before.br
            || self.apex != before.apex
[... truncated in extract ...]

[2026-05-11T01:31:02.692Z] tool-output line=33
------------------------------------------------------------------------------------------------
Chunk ID: 886302
Wall time: 0.0004 seconds
Process exited with code 0
Original token count: 152314
Output:
Total output lines: 6455

src/pci/vrng.rs:339:                dest[filled..filled + take].copy_from_slice(&src[..take]);
src/pci/vrng.rs:354:/// Initialize/attach virtio-rng if present.
src/pci/vrng.rs:373:                crate::log!("pci/vrng: not present\n");
src/mio_compat.rs:68:                raw.addr[..4].copy_from_slice(&addr);
src/mio_compat.rs:320:            self.net = VNetBridge::open_primary();
src/mio_compat.rs:393:            if !crate::net::adapter::service_tick_primary_once() {
src/mio_compat.rs:1187:        core::ptr::copy_nonoverlapping(bytes.as_ptr(), out_ptr, got);
src/mio_compat.rs:1375:        req[..MIO_ADDR_BYTES].copy_from_slice(addr_bytes(&addr));
src/mio_compat.rs:1378:        req[MIO_ADDR_BYTES..MIO_ADDR_BYTES + n].copy_from_slice(&data[..n]);
src/mio_compat.rs:1412:            core::ptr::copy_nonoverlapping(data.as_ptr(), out_ptr, len);
src/mio_compat.rs:1450:        core::ptr::copy_nonoverlapping(bytes[MIO_ADDR_BYTES..].as_ptr(), out_ptr, got);
src/mio_compat.rs:1575:        req.copy_from_slice(&(token as u64).to_le_bytes());
src/mio_compat.rs:1666:            core::ptr::copy_nonoverlapping(
src/limine.rs:4:pub type FramebufferResponse = request::FramebufferResponse;
src/limine.rs:35:pub static FRAMEBUFFER_REQUEST: request::FramebufferRequest = request::FramebufferRequest::new();
src/limine.rs:85:pub fn framebuffer_response() -> Option<&'static FramebufferResponse> {
src/limine.rs:225:/// - A raw physical address present in the Limine memory map
src/limine.rs:226:/// - An HHDM address (HHDM+phys) where `phys` is present in the Limine memory map
src/shell2/mod.rs:985:fn redraw_status_preserving_cursor(
src/shell2/mod.rs:1010:fn redraw_clock_preserving_cursor(out: &AlignedWriter<'_>, output_mask: u8, time_text: &str) {
src/shell2/mod.rs:1075:fn cycle_live_history(up: bool, cursor: &mut Option<usize>) -> Option<AllocString> {
src/shell2/mod.rs:1078:        *cursor = None;
src/shell2/mod.rs:1083:    let next = match (*cursor, up) {
src/shell2/mod.rs:1095:    *cursor = Some(next);
src/shell2/mod.rs:1140:    let mut live_history_cursor: Option<usize> = None;
src/shell2/mod.rs:1154:            redraw_status_preserving_cursor(
src/shell2/mod.rs:1176:            redraw_clock_preserving_cursor(&out, output_mask, minute_text.as_str());
src/shell2/mod.rs:1209:                            if let Some(entry) = cycle_live_history(true, &mut live_history_cursor)
src/shell2/mod.rs:1217:                            if let Some(entry) = cycle_live_history(false, &mut live_history_cursor)
src/shell2/mod.rs:1386:                    live_history_cursor = None;
src/pci/nvme_backend.rs:5:        NonNull, copy_nonoverlapping, read_unaligned, read_volatile, write_bytes, write_volatile,
src/pci/nvme_backend.rs:200:    fn copy_from_slice(&self, src: &[u8]) {
src/pci/nvme_backend.rs:203:            copy_nonoverlapping(src.as_ptr(), self.virt.as_ptr(), span);
src/pci/nvme_backend.rs:329:    doorbell_stride_bytes: u32,
src/pci/nvme_backend.rs:343:        let offset = NVME_REG_DBS + (2 * qid as usize) * (self.doorbell_stride_bytes as usize);
src/pci/nvme_backend.rs:348:        let offset = NVME_REG_DBS + (2 * qid as usize + 1) * (self.doorbell_stride_bytes as usize);
src/pci/nvme_backend.rs:632:    doorbell_stride_bytes: u32,
src/pci/nvme_backend.rs:662:            doorbell_stride_bytes: self.doorbell_stride_bytes,
src/pci/nvme_backend.rs:989:        let doorbell_stride_bytes = 4u32 << dstrd;
src/pci/nvme_backend.rs:994:            doorbell_stride_bytes,
src/pci/nvme_backend.rs:1129:                copy_nonoverlapping(
src/pci/nvme_backend.rs:1173:            dma_buf.copy_from_slice(&buf[offset..offset + bytes_here]);
src/tst/netbench_boot.rs:8:// This version does NOT pull TCP payload bytes through vnet events (which copy
src/tst/netbench_boot.rs:134:        let nic_index = crate::net::primary_device_index();
src/pci/pciids.rs:97:    // older root still works if the primary root switched later in boot.
src/pci/pciids.rs:134:    out.copy_from_slice(bytes);
src/shell2/cmds/net.rs:125:                crate::t::net::dns::resolve_ipv4_primary(
src/shell2/cmds/net.rs:154:            None => VNet::open_primary(),
src/shell2/backends/ui2.rs:36:    pub cursor_col: u32,
src/shell2/backends/ui2.rs:37:    pub cursor_row: u32,
src/shell2/backends/ui2.rs:38:    pub cursor_visible: bool,
src/shell2/backends/ui2.rs:74:    cursor_co
[... truncated in extract ...]

[2026-05-11T01:31:08.992Z] tool-call:exec_command line=35
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1,260p' src/intel/mod.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":22000}

[2026-05-11T01:31:08.995Z] tool-call:exec_command line=36
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '260,620p' src/intel/display.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":30000}

[2026-05-11T01:31:09.013Z] tool-call:exec_command line=37
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"init_primary_boot_surface|present_|present|primary_|overlay|scanout|sample_primary|primary_surface|map_display|ggtt|GTT|GGTT\" src/intel src/gfx src/tst/mandelbrot_gpu_sidequest.rs src/tst/ui2_mandelbrot_demo.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":30000}

[2026-05-11T01:31:09.079Z] tool-output line=39
------------------------------------------------------------------------------------------------
Chunk ID: 6612c8
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 2289
Output:
// Intel bring-up source contracts.
//
// These comments intentionally mirror `may_help/stateoftheart`, but live next
// to the code so the documentary facts do not drift away from the driver.
//
// Current proven hardware claim:
// - PCI claim has reached `00:02.0 device=0x4680 rev=0x0C`.
// - The observed MMIO aperture is `0x1000000` bytes.
//
// This module proves only ownership, MMIO bounds, shared address constants, and
// top-level sequencing.  It does not prove render progress; render/display/GuC
// modules must each emit their own named proof lines.

mod display;
pub(crate) mod format;
mod gpgpu;
mod guc;
pub mod hda;
mod hw_cursor;
pub(crate) mod state;
pub(crate) mod stats;
pub(crate) mod xelp_media2_ngin;
pub(crate) mod xelp_media_h264src;
pub(crate) mod xelp_media_matroska;
pub(crate) mod xelp_media_mp4;
pub(crate) mod xelp_media_source;

use core::sync::atomic::{AtomicBool, Ordering};
use embassy_executor::SendSpawner;
use embassy_time::{Duration as EmbassyDuration, Timer};
use spin::Mutex;

pub(crate) const INTEL_VENDOR_ID: u16 = 0x8086;
pub(crate) const PCI_CLASS_DISPLAY: u8 = 0x03;
pub(crate) const GPU_VA_GUC_FW_BASE: u64 = 0x0085_0000;
pub(crate) const GPU_VA_GUC_ADS_BASE: u64 = 0x0100_0000;
pub(crate) const GPU_VA_DISPLAY_PRIMARY_BASE: u64 = 0x0200_0000;
pub(crate) const GPU_VA_DISPLAY_OVERLAY_BASE: u64 = 0x0300_0000;
pub(crate) const GPU_VA_DISPLAY_CURSOR_BASE: u64 = 0x0600_0000;
pub(crate) const WARM_ALIGN: usize = 4096;
const GGTT_ALIAS_BASE_OFF: usize = 0x0080_0000;
const GGTT_ALIAS_BYTES: usize = 0x0080_0000;
const GGTT_PAGE_BYTES: u64 = 4096;
const GEN8_PAGE_PRESENT: u64 = 1;
const FORCEWAKE_RENDER: usize = 0x0A278;
const FORCEWAKE_MEDIA: usize = 0x0A184;
const FORCEWAKE_GT: usize = 0x0A188;
const FORCEWAKE_ACK_RENDER: usize = 0x0D84;
const FORCEWAKE_ACK_MEDIA: usize = 0x0D88;
const FORCEWAKE_ACK_GT: usize = 0x130044;
const FORCEWAKE_KERNEL: u32 = 1 << 0;
const FORCEWAKE_FALLBACK: u32 = 1 << 15;
const FORCEWAKE_POLL_ITERS: usize = 20_000;
const GFX_FLSH_CNTL_GEN6: usize = 0x101008;
const GFX_FLSH_CNTL_EN: u32 = 1 << 0;
const GUC_WOPCM_OFFSET_SHIFT: u32 = 14;
const GUC_WOPCM_SIZE_MASK: u32 = 0xFFFFF << 12;
const GEN11_WOPCM_SIZE: u32 = 0x0020_0000;
const WOPCM_RESERVED_SIZE: u32 = 0x0000_4000;
const GUC_WOPCM_RESERVED_SIZE: u32 = 0x0000_4000;
const GUC_WOPCM_STACK_RESERVED_SIZE: u32 = 0x0000_2000;
const WOPCM_HW_CTX_RESERVED_SIZE: u32 = 0x0000_9000;
const GUC_WOPCM_OFFSET_ALIGNMENT: u32 = 1 << GUC_WOPCM_OFFSET_SHIFT;
pub(crate) const GS_BOOTROM_MASK: u32 = 0x7F << 1;
pub(crate) const GS_UKERNEL_MASK: u32 = 0xFF << 8;
pub(crate) const GS_AUTH_STATUS_MASK: u32 = 0x03 << 30;
const DISPLAY_PLANE1_BOOT_DEMO_ENABLED: bool = true;
const MEDIA_BOOT_DEMO_ENABLED: bool = false;
const MEDIA_BOOT_DEMO_DELAY_MS: u64 = 5_000;
const MEDIA_BOOT_DEMO_PREFERRED_AP_SLOT: u32 = 3;
const GPGPU_BURN_MIN_ROWS: usize = 512;
const GPGPU_BURN_MIN_K_DIM: usize = 512;
const GPGPU_TILE_ROWS: usize = 256;

static INIT: AtomicBool = AtomicBool::new(false);
static CLAIMED_DEVICE: Mutex<Option<Dev>> = Mutex::new(None);

#[derive(Copy, Clone, Debug)]
pub struct RenderWarmState {
    pub device_id: u16,
    pub revision_id: u8,
    pub mmio_base: usize,
    pub mmio_len: usize,
}

#[derive(Copy, Clone, Debug)]
pub(crate) struct GpgpuPreflightStatus {
    pub(crate) submitted: bool,
    pub(crate) accepted: bool,
    pub(crate) completed: bool,
    pub(crate) guc_ready: bool,
    pub(crate) marker: u32,
    pub(crate) dot: u32,
    pub(crate) sum_a: u32,
    pub(crate) sum_b: u32,
    pub(crate) lanes: u32,
    pub(crate) min_burn_rows: usize,
    pub(crate) min_burn_k_dim: usize,
    pub(crate) arena_gpu_base: u64,
    pub(crate) arena_bytes: usize,
    pub(crate) tile_rows: usize,
    pub(crate) max_tiles: usize,
    pub(crate) enough_for_shape: bool,
    pub(crate) eu_kernel_uploaded: bool,
    pub(crate) eu_walker_encoded: bool,
    pub(crate) eu_walker_submitted: bool,
    pub(crate) eu_walker_retired: bool,
    pub(crate) eu_dispatch_delta: u32,
    pub(crate) eu_c_store_value: u32,
    pub(crate) eu_expected_store_value: u32,
    pub(crate) eu_program_name: &'static str,
    pub(crate) result_c_changed_by_eu: bool,
}

#[derive(Copy, Clone, Debug)]
pub(crate) struct GpgpuOneTileStageProof {
    pub(crate) staged: bool,
    pub(crate) reason: &'static str,
    pub(crate) readback_ok: bool,
    pub(crate) output_zeroed: bo
[... truncated in extract ...]

[2026-05-11T01:31:09.079Z] tool-output line=40
------------------------------------------------------------------------------------------------
Chunk ID: d826a1
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 3067
Output:
        phys,
        byte_len,
        crate::intel::GPU_VA_DISPLAY_PRIMARY_BASE,
    ) {
        crate::log!(
            "intel/display: primary-boot-surface ggtt map failed bytes=0x{:X} gpu=0x{:X}\n",
            byte_len,
            crate::intel::GPU_VA_DISPLAY_PRIMARY_BASE
        );
        return;
    }
    crate::intel::ggtt_invalidate(dev);

    let Some(_stride_reg) = plane_stride_reg_value(pitch_bytes) else {
        crate::log!(
            "intel/display: primary-boot-surface stride encode failed pitch=0x{:X}\n",
            pitch_bytes
        );
        return;
    };
    let Some(surface_reg) = u32::try_from(crate::intel::GPU_VA_DISPLAY_PRIMARY_BASE).ok() else {
        crate::log!("intel/display: primary-boot-surface gpu addr out of range\n");
        return;
    };

    log_primary_plane_probe(dev, pipe, "before-arm");
    let ctl_before = crate::intel::mmio_read(dev, pipe.plane_ctl_off);
    let surf_before = crate::intel::mmio_read(dev, pipe.plane_surf_off);
    let (_, _, surf_live, _) = program_primary_plane_and_wait(
        dev,
        pipe,
        width,
        height,
        pitch_bytes,
        surface_reg,
        "init-arm",
    );
    log_primary_plane_probe(dev, pipe, "after-arm");
    log_pipe_scanout_probe(dev, "after-primary-init");
    let surf_armed = crate::intel::mmio_read(dev, pipe.plane_surf_off);
    let ctl_after = crate::intel::mmio_read(dev, pipe.plane_ctl_off);
    let ok = surf_live == surface_reg || surf_armed == surface_reg;

    let primary_surface = PrimarySurface {
        width,
        height,
        pitch_bytes,
        phys,
        virt,
        pipe,
    };
    *PRIMARY_SURFACE.lock() = Some(primary_surface);
    log_primary_scanout_pte_window(dev, "after-primary-init", byte_len);
    let _ = notify_primary_surface_present(primary_surface, "scanout-demo", byte_len);

    let logo_ok = if PRIMARY_BOOT_LOGO_ENABLED {
        match crate::gfx::jpeg_codec::decode_jpeg_rgba(PRIMARY_BOOT_LOGO_JPEG) {
            Ok(decoded) => present_rgba_surface_center(
                decoded.rgba.as_slice(),
                decoded.width,
                decoded.height,
                (decoded.width as usize).saturating_mul(4),
            ),
            Err(err) => {
                crate::log!(
                    "intel/display: primary-logo decode failed code={} bytes=0x{:X}\n",
                    err.code(),
                    PRIMARY_BOOT_LOGO_JPEG.len()
                );
                false
            }
        }
    } else {
        false
    };

    if CPU_SCANOUT_PROOF_ENABLED {
        log_cpu_scanout_proof(dev, primary_surface);
    }

    crate::log!(
        "intel/display: primary-boot-surface pipe={} size={}x{} pitch=0x{:X} gpu=0x{:X} phys=0x{:X} plane_enabled={} ctl_before=0x{:08X} ctl_after=0x{:08X} surf_before=0x{:08X} surf=0x{:08X} surf_live=0x{:08X} ok={} logo={}\n",
        pipe.name,
        width,
        height,
        pitch_bytes,
        crate::intel::GPU_VA_DISPLAY_PRIMARY_BASE,
        phys,
        ((ctl_after & PLANE_CTL_ENABLE) != 0) as u8,
        ctl_before,
        ctl_after,
        surf_before,
        surf_armed,
        surf_live,
        ok as u8,
        logo_ok as u8
    );
}

fn log_cpu_scanout_proof(dev: crate::intel::Dev, surface: PrimarySurface) {
    if surface.width == 0
        || surface.height == 0
        || surface.pitch_bytes < PRIMARY_BYTES_PER_PIXEL
        || surface.virt.is_null()
    {
        crate::log!("intel/display: cpu-scanout-proof accepted=0 reason=bad-surface\n");
        return;
    }

    let marker_w = CPU_SCANOUT_PROOF_SIZE.min(surface.width);
    let marker_h = CPU_SCANOUT_PROOF_SIZE.min(surface.height);
    let x = surface.width.saturating_sub(marker_w).saturating_sub(16);
    let y = surface.height.saturating_sub(marker_h).saturating_sub(16);
    let pitch_bytes = surface.pitch_bytes as usize;
    let pixel_offset = (y as usize)
        .saturating_mul(pitch_bytes)
        .saturating_add((x as usize).saturating_mul(PRIMARY_BYTES_PER_PIXEL as usize));
    let sample_ptr = unsafe { surface.virt.add(pixel_offset) };
    let before = unsafe { core::ptr::read_volatile(sample_ptr as *const u32) };

    for row in 0..marker_h as usize {
        let row_ptr = unsafe {
            surface
                .virt
                .add((y as usize + row).saturating_mul(pitch_bytes))
                .a
[... truncated in extract ...]

[2026-05-11T01:31:09.080Z] tool-output line=41
------------------------------------------------------------------------------------------------
Chunk ID: 5cec34
Wall time: 0.0001 seconds
Process exited with code 0
Original token count: 100608
Output:
Total output lines: 2598

src/tst/ui2_mandelbrot_demo.rs:41:    // Let the blank window present first so the window frame lands before shader work is queued.
src/tst/mandelbrot_gpu_sidequest.rs:45:            Self::CopyToIntelOverlay => "copy-to-intel-overlay",
src/tst/mandelbrot_gpu_sidequest.rs:61:    pub(crate) fallback_present_path: MandelbrotPresentPath,
src/tst/mandelbrot_gpu_sidequest.rs:75:            fallback_present_path: MandelbrotPresentPath::CopyToIntelOverlay,
src/tst/mandelbrot_gpu_sidequest.rs:116:    let scanout = crate::intel::active_scanout_dimensions();
src/tst/mandelbrot_gpu_sidequest.rs:117:    let scanout_w = scanout.map(|dims| dims.0).unwrap_or(0);
src/tst/mandelbrot_gpu_sidequest.rs:118:    let scanout_h = scanout.map(|dims| dims.1).unwrap_or(0);
src/tst/mandelbrot_gpu_sidequest.rs:119:    let primary_gpu = crate::intel::primary_surface_gpu_addr().unwrap_or(0);
src/tst/mandelbrot_gpu_sidequest.rs:126:        "mandelbrot-gpu-sidequest: attempted name={} called=1 hot=0 artifact_stage={} source={} spirv={} spirv_bytes={} spirv_sig=0x{:016X} target={}x{} rgba_bytes=0x{:X} push_constants={} render_path={} fallback_present={} scanout={}x{} primary_gpu=0x{:X} shader_helper_ready={} upload=replaced-by-gpgpu-target-buffer action=hold-lumen-load next=proven-gpgpu-target-buffer-render\n",
src/tst/mandelbrot_gpu_sidequest.rs:138:        plan.fallback_present_path.as_str(),
src/tst/mandelbrot_gpu_sidequest.rs:139:        scanout_w,
src/tst/mandelbrot_gpu_sidequest.rs:140:        scanout_h,
src/tst/mandelbrot_gpu_sidequest.rs:141:        primary_gpu,
src/tst/mandelbrot_gpu_sidequest.rs:174:                "wire-real-mandelbrot-pixel-kernel-or-scanout-copy"
src/intel/display.rs:8:// This proves scanout handoff to known memory.  It does not prove the 3D
src/intel/display.rs:214:pub(crate) fn init_primary_boot_surface(dev: crate::intel::Dev) {
src/intel/display.rs:219:    log_pipe_scanout_probe(dev, "before-primary-init");
src/intel/display.rs:239:    log_primary_dimensions_probe(pipe.name, pipe_src_raw, pipe_src_dims, fb_dims, chosen_from);
src/intel/display.rs:258:    if !crate::intel::map_display_scanout_ggtt(
src/intel/display.rs:265:            "intel/display: primary-boot-surface ggtt map failed bytes=0x{:X} gpu=0x{:X}\n",
src/intel/display.rs:271:    crate::intel::ggtt_invalidate(dev);
src/intel/display.rs:285:    log_primary_plane_probe(dev, pipe, "before-arm");
src/intel/display.rs:288:    let (_, _, surf_live, _) = program_primary_plane_and_wait(
src/intel/display.rs:297:    log_primary_plane_probe(dev, pipe, "after-arm");
src/intel/display.rs:298:    log_pipe_scanout_probe(dev, "after-primary-init");
src/intel/display.rs:303:    let primary_surface = PrimarySurface {
src/intel/display.rs:311:    *PRIMARY_SURFACE.lock() = Some(primary_surface);
src/intel/display.rs:312:    log_primary_scanout_pte_window(dev, "after-primary-init", byte_len);
src/intel/display.rs:313:    let _ = notify_primary_surface_present(primary_surface, "scanout-demo", byte_len);
src/intel/display.rs:317:            Ok(decoded) => present_rgba_surface_center(
src/intel/display.rs:337:        log_cpu_scanout_proof(dev, primary_surface);
src/intel/display.rs:359:fn log_cpu_scanout_proof(dev: crate::intel::Dev, surface: PrimarySurface) {
src/intel/display.rs:365:        crate::log!("intel/display: cpu-scanout-proof accepted=0 reason=bad-surface\n");
src/intel/display.rs:406:        "intel/display: cpu-scanout-proof accepted={} pipe={} gpu=0x{:X} phys=0x{:X} xy={}x{} size={}x{} pitch=0x{:X} stride_reg=0x{:08X} before=0x{:08X} after=0x{:08X} color=0x{:08X} flush=1 surf=0x{:08X} surf_live=0x{:08X} does_not_prove=render_backend_write\n",
src/intel/display.rs:510:pub(crate) fn active_scanout_dimensions() -> Option<(u32, u32)> {
src/intel/display.rs:517:pub(crate) fn primary_surface_gpu_addr() -> Option<u64> {
src/intel/display.rs:524:pub(crate) fn present_rgba_surface_center(
src/intel/display.rs:588:    notify_primary_surface_present(surface, "rgba-center", byte_len);
src/intel/display.rs:592:pub(crate) fn present_rgba_overlay_top_right(
src/intel/display.rs:605:    let Some(surface) = ensure_overlay_surface(dev, src_width, src_height) else {
src/intel/display.rs:609:    if !copy_rgba_into_overlay(surface, src, src_width, src_height, src_pitch_bytes) {
src/intel/display.rs:616:    if overlay_plane_needs_rearm(dev, surface) {
src/i
[... truncated in extract ...]

[2026-05-11T01:31:13.659Z] tool-call:exec_command line=43
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '620,980p' src/intel/display.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":32000}

[2026-05-11T01:31:13.659Z] tool-call:exec_command line=44
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '980,1320p' src/intel/display.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":32000}

[2026-05-11T01:31:13.678Z] tool-call:exec_command line=45
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1320,1535p' src/intel/display.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":26000}

[2026-05-11T01:31:13.678Z] tool-call:exec_command line=46
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '420,500p' src/intel/mod.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":12000}

[2026-05-11T01:31:13.706Z] tool-output line=48
------------------------------------------------------------------------------------------------
Chunk ID: 39b070
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 3619
Output:
    }

    let seq = OVERLAY_PRESENT_SEQ.fetch_add(1, Ordering::AcqRel) + 1;
    if seq <= 8 || seq.is_multiple_of(60) {
        let plane_base = overlay_plane_base(surface.pipe, surface.plane_slot);
        crate::log!(
            "intel/display: overlay-present seq={} pipe={} slot={} size={}x{} pitch=0x{:X} gpu=0x{:X} phys=0x{:X} surf=0x{:08X} surf_live=0x{:08X}\n",
            seq,
            surface.pipe.name,
            surface.plane_slot,
            surface.width,
            surface.height,
            surface.pitch_bytes,
            crate::intel::GPU_VA_DISPLAY_OVERLAY_BASE,
            surface.phys,
            crate::intel::mmio_read(dev, plane_base + UNI_PLANE_SURF_OFF),
            crate::intel::mmio_read(dev, plane_base + UNI_PLANE_SURFLIVE_OFF)
        );
    }

    true
}

#[inline]
fn clamp_u8_i32(value: i32) -> u8 {
    value.clamp(0, 255) as u8
}

pub(crate) fn present_nv12_surface_center(
    src: &[u8],
    coded_width: u32,
    coded_height: u32,
    visible_x: u32,
    visible_y: u32,
    visible_width: u32,
    visible_height: u32,
    src_pitch_bytes: usize,
) -> bool {
    let Some(surface) = *PRIMARY_SURFACE.lock() else {
        return false;
    };
    if surface.virt.is_null() || coded_width == 0 || coded_height == 0 {
        return false;
    }

    let coded_width = coded_width as usize;
    let coded_height = coded_height as usize;
    let visible_x = visible_x as usize;
    let visible_y = visible_y as usize;
    let visible_width = visible_width as usize;
    let visible_height = visible_height as usize;
    if src_pitch_bytes < coded_width || visible_width == 0 || visible_height == 0 {
        return false;
    }
    if visible_x.saturating_add(visible_width) > coded_width
        || visible_y.saturating_add(visible_height) > coded_height
    {
        return false;
    }

    // Y-tile: 128 bytes wide × 32 rows tall = 4096 bytes per tile.
    // In NV12 tiled, both Y and UV planes share the same tiled surface.
    // UV plane starts at row chroma_y_offset = align(src_height, 32) for
    // tile boundary alignment (must match MFX_SURFACE_STATE programming).
    const YTILE_W: usize = 128;
    const YTILE_H: usize = 32;
    let tiles_per_row = src_pitch_bytes / YTILE_W;
    let chroma_y_offset = (coded_height + YTILE_H - 1) & !(YTILE_H - 1);
    let total_height = chroma_y_offset + (coded_height + 1) / 2;
    let total_tile_rows = (total_height + YTILE_H - 1) / YTILE_H;
    let needed = total_tile_rows * tiles_per_row * 4096;
    if src.len() < needed {
        return false;
    }

    let dst_width = surface.width as usize;
    let dst_height = surface.height as usize;
    let dst_pitch = surface.pitch_bytes as usize;
    if dst_pitch < dst_width.saturating_mul(4) {
        return false;
    }

    // Repaint the whole destination every frame. The NV12 copy does not always
    // touch every pixel of the scanout surface, so reusing prior contents leaves
    // deterministic stale bands at the bottom/edges when the copied region is
    // even slightly smaller than the destination.
    fill_surface_color(
        surface.virt,
        dst_pitch,
        surface.width,
        surface.height,
        PRIMARY_BASELINE_COLOR,
    );

    // Fit the visible video into the destination while preserving aspect ratio.
    // The previous code only downscaled and never upscaled, so a 1080p video on
    // a taller panel would leave whole green tile rows unused at the bottom.
    let (copy_w, copy_h) =
        if dst_width.saturating_mul(visible_height) <= dst_height.saturating_mul(visible_width) {
            let copy_w = dst_width.max(1);
            let copy_h = visible_height
                .saturating_mul(copy_w)
                .checked_div(visible_width.max(1))
                .unwrap_or(1)
                .max(1)
                .min(dst_height);
            (copy_w, copy_h)
        } else {
            let copy_h = dst_height.max(1);
            let copy_w = visible_width
                .saturating_mul(copy_h)
                .checked_div(visible_height.max(1))
                .unwrap_or(1)
                .max(1)
                .min(dst_width);
            (copy_w, copy_h)
        };
    let dst_x = dst_width.saturating_sub(copy_w) / 2;
    let dst_y = dst_height.saturating_sub(copy_h) / 2;

    // Detile helper: given (byte_x, row_y) return byte offset in the tiled surfa
[... truncated in extract ...]

[2026-05-11T01:31:13.706Z] tool-output line=49
------------------------------------------------------------------------------------------------
Chunk ID: 8afc3f
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 3076
Output:
    let mut live = crate::intel::mmio_read(dev, plane_base + UNI_PLANE_SURFLIVE_OFF);
    let mut iter = 0usize;
    while iter < max_iters && live != want_live {
        core::hint::spin_loop();
        live = crate::intel::mmio_read(dev, plane_base + UNI_PLANE_SURFLIVE_OFF);
        iter += 1;
    }
    (live, iter)
}

fn primary_plane_ctl_enabled(ctl_before: u32) -> u32 {
    let order_bits = match PRIMARY_FORMAT_PROBE_MODE {
        PRIMARY_FORMAT_PROBE_XRGB => 0,
        PRIMARY_FORMAT_PROBE_XBGR => PLANE_CTL_ORDER_RGBX,
        _ => 0,
    };
    (ctl_before
        & !(PLANE_CTL_ENABLE
            | PLANE_CTL_FORMAT_MASK_SKL
            | PLANE_CTL_TILED_MASK
            | PLANE_CTL_ORDER_RGBX))
        | PLANE_CTL_ENABLE
        | PLANE_CTL_FORMAT_XRGB_8888
        | PLANE_CTL_TILED_LINEAR
        | order_bits
}

fn overlay_plane_ctl_enabled(ctl_before: u32) -> u32 {
    primary_plane_ctl_enabled(ctl_before)
}

fn primary_format_probe_name() -> &'static str {
    match PRIMARY_FORMAT_PROBE_MODE {
        PRIMARY_FORMAT_PROBE_XRGB => "xrgb8888",
        PRIMARY_FORMAT_PROBE_XBGR => "xbgr8888-order-rgbx",
        _ => "unknown",
    }
}

fn program_primary_plane_and_wait(
    dev: crate::intel::Dev,
    pipe: PipeInfo,
    width: u32,
    height: u32,
    pitch_bytes: u32,
    surface_reg: u32,
    reason: &str,
) -> (u32, u32, u32, usize) {
    let Some(stride_reg) = plane_stride_reg_value(pitch_bytes) else {
        return (0, 0, 0, 0);
    };
    let ctl_before = crate::intel::mmio_read(dev, pipe.plane_ctl_off);
    let ctl_disabled = ctl_before & !PLANE_CTL_ENABLE;
    let ctl_enabled = primary_plane_ctl_enabled(ctl_before);

    crate::intel::mmio_write(dev, pipe.plane_ctl_off, ctl_disabled);
    crate::intel::mmio_write(dev, pipe.plane_surf_off, 0);
    let (disable_frame_before, disable_frame_after, disable_frame_iters) =
        wait_for_pipe_next_frame(dev, pipe);
    let (live_cleared, clear_iters) = wait_for_primary_plane_live(dev, pipe, 0, 20_000);

    crate::intel::mmio_write(dev, pipe.plane_stride_off, stride_reg);
    crate::intel::mmio_write(
        dev,
        pipe.plane_ctl_off + UNI_PLANE_POS_OFF,
        plane_pos_reg_value(0, 0),
    );
    crate::intel::mmio_write(
        dev,
        pipe.plane_ctl_off + UNI_PLANE_SIZE_OFF,
        plane_size_reg_value(width, height),
    );
    crate::intel::mmio_write(
        dev,
        pipe.plane_ctl_off + UNI_PLANE_OFFSET_OFF,
        plane_pos_reg_value(0, 0),
    );
    crate::intel::mmio_write(dev, pipe.plane_ctl_off, ctl_enabled);
    crate::intel::mmio_write(dev, pipe.plane_surf_off, surface_reg);

    let (arm_frame_before, arm_frame_after, arm_frame_iters) = wait_for_pipe_next_frame(dev, pipe);
    let (surf_live_after, live_iters) = wait_for_primary_plane_live(dev, pipe, surface_reg, 20_000);

    intel_display_verbose_log!(
        "intel/display: primary-rearm reason={} pipe={} format_probe={} ctl_before=0x{:08X} ctl_disabled=0x{:08X} ctl_enabled=0x{:08X} disable_frame={}=>{} disable_wait={} clear_live=0x{:08X} clear_iters={} arm_frame={}=>{} arm_wait={} surf=0x{:08X} surf_live=0x{:08X} live_iters={}\n",
        reason,
        pipe.name,
        primary_format_probe_name(),
        ctl_before,
        ctl_disabled,
        ctl_enabled,
        disable_frame_before,
        disable_frame_after,
        disable_frame_iters,
        live_cleared,
        clear_iters,
        arm_frame_before,
        arm_frame_after,
        arm_frame_iters,
        crate::intel::mmio_read(dev, pipe.plane_surf_off),
        surf_live_after,
        live_iters
    );

    (ctl_before, ctl_enabled, surf_live_after, live_iters)
}

fn log_primary_scanout_pte_window(dev: crate::intel::Dev, label: &str, byte_len: usize) {
    let page_count = byte_len.div_ceil(crate::intel::WARM_ALIGN);
    let mut entries = [0u64; 4];
    let count = page_count.min(entries.len());
    let mut idx = 0usize;
    while idx < count {
        let gpu = crate::intel::GPU_VA_DISPLAY_PRIMARY_BASE
            + (idx as u64) * crate::intel::WARM_ALIGN as u64;
        entries[idx] = crate::intel::read_ggtt_pte(dev, gpu).unwrap_or(0);
        idx += 1;
    }
    intel_display_verbose_log!(
        "intel/display: primary-ggtt label={} gpu=0x{:X} bytes=0x{:X} pages={} pte0=0x{:016X} pte1=0x{:016X} pte2=0x{:016X} pte3=0x{:016X}\n",
        label,
        crate::intel::GPU_VA_DISPLAY_PR
[... truncated in extract ...]

[2026-05-11T01:31:13.711Z] tool-output line=50
------------------------------------------------------------------------------------------------
Chunk ID: 2ea57e
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1820
Output:
        let plane_ctl = crate::intel::mmio_read(dev, pipe.plane_ctl_off);
        let plane_surf = crate::intel::mmio_read(dev, pipe.plane_surf_off);
        let plane_surf_live = crate::intel::mmio_read(dev, pipe.plane_surf_live_off);
        if enabled_plane.is_none()
            && (plane_ctl & PLANE_CTL_ENABLE) != 0
            && (plane_surf != 0 || plane_surf_live != 0)
        {
            enabled_plane = Some(pipe);
        }
        if observed.is_none() && (plane_ctl != 0 || plane_surf != 0 || plane_surf_live != 0) {
            observed = Some(pipe);
        }
    }
    enabled_plane.or(observed)
}

fn log_pipe_scanout_probe(dev: crate::intel::Dev, label: &str) {
    for pipe in PIPES {
        let pipe_src_raw = crate::intel::mmio_read(dev, pipe.pipe_src_off);
        let pipe_src_dims = decode_pipe_src(pipe_src_raw);
        let plane_ctl = crate::intel::mmio_read(dev, pipe.plane_ctl_off);
        let plane_surf = crate::intel::mmio_read(dev, pipe.plane_surf_off);
        let plane_surf_live = crate::intel::mmio_read(dev, pipe.plane_surf_live_off);
        let (width, height) = pipe_src_dims.unwrap_or((0, 0));
        intel_display_verbose_log!(
            "intel/display: pipe-probe label={} pipe={} pipe_src=0x{:08X} dims={}x{} plane_enabled={} surf=0x{:08X} surf_live=0x{:08X}\n",
            label,
            pipe.name,
            pipe_src_raw,
            width,
            height,
            ((plane_ctl & PLANE_CTL_ENABLE) != 0) as u8,
            plane_surf,
            plane_surf_live
        );
    }
}

fn log_primary_plane_probe(dev: crate::intel::Dev, pipe: PipeInfo, label: &str) {
    let ctl = crate::intel::mmio_read(dev, pipe.plane_ctl_off);
    let stride = crate::intel::mmio_read(dev, pipe.plane_stride_off);
    let pos = crate::intel::mmio_read(dev, pipe.plane_ctl_off + UNI_PLANE_POS_OFF);
    let size = crate::intel::mmio_read(dev, pipe.plane_ctl_off + UNI_PLANE_SIZE_OFF);
    let keyval = crate::intel::mmio_read(dev, pipe.plane_ctl_off + UNI_PLANE_KEYVAL_OFF);
    let keymsk = crate::intel::mmio_read(dev, pipe.plane_ctl_off + UNI_PLANE_KEYMSK_OFF);
    let surf = crate::intel::mmio_read(dev, pipe.plane_surf_off);
    let keymax = crate::intel::mmio_read(dev, pipe.plane_ctl_off + UNI_PLANE_KEYMAX_OFF);
    let offset = crate::intel::mmio_read(dev, pipe.plane_ctl_off + UNI_PLANE_OFFSET_OFF);
    let surf_live = crate::intel::mmio_read(dev, pipe.plane_surf_live_off);
    let aux_dist = crate::intel::mmio_read(dev, pipe.plane_ctl_off + UNI_PLANE_AUX_DIST_OFF);
    let aux_offset = crate::intel::mmio_read(dev, pipe.plane_ctl_off + UNI_PLANE_AUX_OFFSET_OFF);
    let color_ctl = crate::intel::mmio_read(dev, pipe.plane_ctl_off + UNI_PLANE_COLOR_CTL_OFF);
    let buf_cfg = crate::intel::mmio_read(dev, pipe.plane_ctl_off + UNI_PLANE_BUF_CFG_OFF);

    intel_display_verbose_log!(
        "intel/display: primary-probe label={} pipe={} ctl=0x{:08X} enabled={} format={} tiled={} rot={} rgbx={} stride=0x{:08X} pos={}x{} size={}x{} offset={}x{} surf=0x{:08X} surf_live=0x{:08X} color_ctl=0x{:08X} color_alpha={} key=0x{:08X}/0x{:08X}/0x{:08X} aux=0x{:08X}/0x{:08X} buf_cfg=0x{:08X}\n",
        label,
        pipe.name,
        ctl,
        ((ctl & PLANE_CTL_ENABLE) != 0) as u8,
        decode_plane_format(ctl),
        decode_plane_tiling(ctl),
        decode_plane_rotation(ctl),
        ((ctl & PLANE_CTL_ORDER_RGBX) != 0) as u8,
        stride,
        decode_xy_x(pos),
        decode_xy_y(pos),
        decode_xy_x(size).saturating_add(1),
        decode_xy_y(size).saturating_add(1),
        decode_xy_x(offset),
        decode_xy_y(offset),
        surf,
        surf_live,
        color_ctl,
        decode_plane_color_alpha(color_ctl),
        keyval,
        keymsk,
        keymax,
        aux_dist,
        aux_offset,
        buf_cfg
    );
}

fn decode_plane_format(ctl: u32) -> &'static str {
    match ctl & PLANE_CTL_FORMAT_MASK_SKL {
        0x0000_0000 => "YUV422",
        0x0100_0000 => "NV12",
        0x0200_0000 => "XRGB2101010",
        0x0300_0000 => "P010",
        0x0400_0000 => "XRGB8888/ARGB8888",
        0x0500_0000 => "P012",
        0x0600_0000 => "XRGB16161616F",
        0x0700_0000 => "P016",
        0x0800_0000 => "XYUV",
        0x0C00_0000 => "INDEXED",
        0x0E00_0000 => "RGB565",
        _ => "unknown",
    }
}

fn decode_plane_color_alpha(color_ctl: u32) -> &'static st
[... truncated in extract ...]

[2026-05-11T01:31:13.711Z] tool-output line=51
------------------------------------------------------------------------------------------------
Chunk ID: 9fc7cd
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 604
Output:
            crate::log!(
                "intel/media: boot demo first-frame origin=local returned={}\n",
                first_frame.is_some() as u8,
            );
        });
    } else {
        crate::log!("intel/media: boot demo disabled\n");
    }
}

pub fn guc_ready() -> bool {
    self::guc::ready()
}

pub(crate) fn guc_h2g_mmio_accepted() -> bool {
    self::guc::h2g_mmio_accepted()
}

pub fn has_claimed_device() -> bool {
    CLAIMED_DEVICE.lock().is_some()
}

pub(crate) fn claimed_device() -> Option<Dev> {
    *CLAIMED_DEVICE.lock()
}

pub fn warm_state() -> Option<RenderWarmState> {
    claimed_device().map(|dev| RenderWarmState {
        device_id: dev.device_id,
        revision_id: dev.revision_id,
        mmio_base: dev.mmio as usize,
        mmio_len: dev.mmio_len,
    })
}

pub(crate) fn gpgpu_preflight_status() -> GpgpuPreflightStatus {
    let status = self::gpgpu::gpgpu_preflight_status();
    GpgpuPreflightStatus {
        submitted: status.submitted,
        accepted: status.accepted,
        completed: status.completed,
        guc_ready: status.guc_ready,
        marker: status.marker,
        dot: status.dot,
        sum_a: status.sum_a,
        sum_b: status.sum_b,
        lanes: status.lanes,
        min_burn_rows: status.min_burn_rows,
        min_burn_k_dim: status.min_burn_k_dim,
        arena_gpu_base: status.arena_gpu_base,
        arena_bytes: status.arena_bytes,
        tile_rows: status.tile_rows,
        max_tiles: status.max_tiles,
        enough_for_shape: status.enough_for_shape,
        eu_kernel_uploaded: status.eu_kernel_uploaded,
        eu_walker_encoded: status.eu_walker_encoded,
        eu_walker_submitted: status.eu_walker_submitted,
        eu_walker_retired: status.eu_walker_retired,
        eu_dispatch_delta: status.eu_dispatch_delta,
        eu_c_store_value: status.eu_c_store_value,
        eu_expected_store_value: status.eu_expected_store_value,
        eu_program_name: status.eu_program_name,
        result_c_changed_by_eu: status.result_c_changed_by_eu,
    }
}

pub(crate) fn stage_gpgpu_one_tile_record_probe(
    x: &[f32],
    row_bf16: &[u8],
    k_dim: usize,
    row_index: usize,
    x_checksum: u64,
    row_checksum: u64,
    cpu_expected_bits: u32,
) -> GpgpuOneTileStageProof {
    self::gpgpu::stage_gpgpu_one_tile_record_probe(
        x,
        row_bf16,
        k_dim,
        row_index,
        x_checksum,

[2026-05-11T01:31:18.512Z] tool-call:exec_command line=53
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '180,260p' src/intel/mod.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":12000}

[2026-05-11T01:31:18.519Z] tool-call:exec_command line=54
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"pub fn active_scanout_dimensions|primary_surface_gpu_addr|present_rgba_overlay|present_rgba_surface|render|submit_primary|probe_periodic|mi-scanout|scanout-store\" src/intel/mod.rs src/intel/gpgpu.rs src/intel/c3D.rs src/intel/intel/render/*.rs src/gfx/*.rs src/tst/mandelbrot_gpu_sidequest.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":32000}

[2026-05-11T01:31:18.534Z] tool-call:exec_command line=55
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1,240p' src/tst/mandelbrot_gpu_sidequest.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":22000}

[2026-05-11T01:31:18.606Z] tool-output line=57
------------------------------------------------------------------------------------------------
Chunk ID: b97c8f
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 710
Output:
    pub(crate) gpu_value: u32,
    pub(crate) cpu_expected_bits: u32,
    pub(crate) output_first_before: u32,
    pub(crate) output_first_after: u32,
    pub(crate) output_hits_lo64: u64,
    pub(crate) dispatch_delta: u64,
    pub(crate) finish_marker: u32,
    pub(crate) expected_finish_marker: u32,
    pub(crate) batch_bytes: usize,
}

#[derive(Copy, Clone, Debug)]
pub(crate) struct GpgpuT5OneRowMatvecProof {
    pub(crate) submitted: bool,
    pub(crate) finished: bool,
    pub(crate) readback_ok: bool,
    pub(crate) compare_ok: bool,
    pub(crate) reason: &'static str,
    pub(crate) program_name: &'static str,
    pub(crate) output_gpu: u64,
    pub(crate) gpu_value: u32,
    pub(crate) cpu_expected_bits: u32,
    pub(crate) output_first_before: u32,
    pub(crate) output_first_after: u32,
    pub(crate) output_hits_lo64: u64,
    pub(crate) dispatch_delta: u64,
    pub(crate) finish_marker: u32,
    pub(crate) expected_finish_marker: u32,
    pub(crate) batch_bytes: usize,
    pub(crate) live_k_dim: usize,
    pub(crate) requires_live_gpu_load: bool,
}

#[derive(Copy, Clone, Debug)]
pub(crate) struct GpgpuT62PartialMatvecProof {
    pub(crate) submitted: bool,
    pub(crate) finished: bool,
    pub(crate) readback_ok: bool,
    pub(crate) compare_ok: bool,
    pub(crate) reason: &'static str,
    pub(crate) program_name: &'static str,
    pub(crate) output_gpu: u64,
    pub(crate) output_words: [u32; 8],
    pub(crate) expected_words: [u32; 8],
    pub(crate) compare_mask: u32,
    pub(crate) expected_mask: u32,
    pub(crate) dispatch_delta: u64,
    pub(crate) finish_marker: u32,
    pub(crate) expected_finish_marker: u32,
    pub(crate) batch_bytes: usize,
    pub(crate) row_count: usize,
    pub(crate) live_k_dim: usize,
}

fn pick_media_boot_demo_spawner() -> Option<(u32, SendSpawner)> {
    let background_slots = crate::workers::background_worker_slots();

    let pick_slot = |predicate: fn(u32) -> bool| {
        background_slots.iter().copied().find(|slot| {
            predicate(*slot)
                && crate::cpu::CpuProfile::for_slot(*slot)
                    .map(|profile| profile.is_perf())
                    .unwrap_or(false)
        })
    };

    let selected_slot = pick_slot(|slot| slot >= MEDIA_BOOT_DEMO_PREFERRED_AP_SLOT)
        .or_else(|| {
            background_slots
                .iter()
                .copied()
                .find(|slot| *slot >= MEDIA_BOOT_DEMO_PREFERRED_AP_SLOT)
        })
        .or_else(|| pick_slot(|slot| slot > 2))
        .or_else(|| background_slots.iter().copied().find(|slot| *slot > 2))?;

    crate::workers::spawner_for_slot(selected_slot).map(|spawner| (selected_slot, spawner))
}

fn log_media_demo_task_profile(origin: &str, requested_slot: u32, queued_at_ms: u64) {
    let queued_ms = embassy_time::Instant::now()

[2026-05-11T01:31:18.606Z] tool-output line=58
------------------------------------------------------------------------------------------------
Chunk ID: 822a82
Wall time: 0.0001 seconds
Process exited with code 0
Original token count: 15691
Output:
Total output lines: 462

src/intel/intel/render/warmup.rs:39:        crate::log!("intel/render: warm alloc failed part=ring size=0x{:X}\n", WARM_RING_BYTES);
src/intel/intel/render/warmup.rs:77:            "intel/render: warm alloc failed part=context size=0x{:X}\n",
src/intel/intel/render/warmup.rs:116:        crate::log!("intel/render: warm alloc failed part=batch size=0x{:X}\n", WARM_BATCH_BYTES);
src/intel/intel/render/warmup.rs:154:            "intel/render: warm alloc failed part=draw-state size=0x{:X}\n",
src/intel/intel/render/warmup.rs:193:        crate::log!("intel/render: warm alloc failed part=vertex size=0x{:X}\n", WARM_VERTEX_BYTES);
src/intel/intel/render/warmup.rs:230:        crate::log!("intel/render: warm alloc failed part=result size=0x{:X}\n", WARM_RESULT_BYTES);
src/intel/intel/render/warmup.rs:268:            "intel/render: warm alloc failed part=streamout size=0x{:X}\n",
src/intel/intel/render/warmup.rs:339:    intel_render_verbose_log!(
src/intel/intel/render/warmup.rs:356:    intel_render_verbose_log!(
src/intel/intel/render/warmup.rs:370:pub fn forcewake_render_acquire(warm: RenderWarmState) -> bool {
src/intel/intel/render/warmup.rs:386:    let render_cleared = wait_eq(
src/intel/intel/render/warmup.rs:395:    let render_ok = wait_eq(
src/intel/intel/render/warmup.rs:416:            "intel/render: forcewake render_cleared={} render_ack=0x{:08X} gt_ack=0x{:08X} cs_debug_mode1=0x{:08X} ff_dop_cg_disable={} ok={}\n",
src/intel/intel/render/warmup.rs:417:            render_cleared as u8,
src/intel/intel/render/warmup.rs:422:            (render_ok && gt_ok) as u8
src/intel/intel/render/warmup.rs:426:    render_ok && gt_ok
src/intel/intel/render/warmup.rs:443:            "intel/render: gfx125-raster-wa chicken_raster_2 before=0x{:08X} after=0x{:08X} tbimr_batch_override={} tbimr_open_batch={} tbimr_fast_clip={}\n",
src/intel/intel/render/warmup.rs:641:    // Keep RHWO disabled for bring-up so the first render proof does not depend
src/intel/intel/render/warmup.rs:646:pub fn forcewake_render_sanity(warm: RenderWarmState) {
src/intel/intel/render/warmup.rs:662:    intel_render_verbose_log!(
src/intel/intel/render/warmup.rs:663:        "intel/render: sanity reg=RCS_IMR before=0x{:08X} wrote=0x{:08X} after=0x{:08X} restored=0x{:08X}\n",
src/intel/mod.rs:11:// top-level sequencing.  It does not prove render progress; render/display/GuC
src/intel/mod.rs:384:        "intel/render: disabled reason=primary-render-module-removed gpgpu_probe=enabled\n"
src/intel/mod.rs:672:pub fn active_scanout_dimensions() -> Option<(u32, u32)> {
src/intel/mod.rs:676:pub fn primary_surface_gpu_addr() -> Option<u64> {
src/intel/mod.rs:677:    self::display::primary_surface_gpu_addr()
src/intel/mod.rs:680:pub fn present_rgba_overlay_top_right(
src/intel/mod.rs:686:    self::display::present_rgba_overlay_top_right(src, src_width, src_height, src_pitch_bytes)
src/intel/intel/render/mod.rs:9:// - `mi-scanout-store-proof`: RCS command streamer wrote one DWORD into the
src/intel/intel/render/mod.rs:12:// - `memory-proof`: warm render buffers were mapped into their fixed GGTT
src/intel/intel/render/mod.rs:36:macro_rules! intel_render_focus_log {
src/intel/intel/render/mod.rs:44:macro_rules! intel_render_verbose_log {
src/intel/intel/render/mod.rs:52:macro_rules! intel_render_batch_log {
src/intel/intel/render/gpgpu.rs:175:            "intel/gpgpu: preflight skipped reason=render-bringup-disabled artifact_only=1 gpu_program_uploaded={} start_command_encoded={}\n",
src/intel/intel/render/gpgpu.rs:183:            "intel/gpgpu: primary-render-disabled-but-gpgpu-submit-enabled artifact_only=0\n"
src/intel/intel/render/gpgpu.rs:187:    if !forcewake_render_acquire(warm) {
src/intel/intel/render/gpgpu.rs:201:        recover_render_engine_after_nonretired_submit(dev, warm, "gpgpu-preflight");
src/intel/intel/render/gpgpu.rs:208:            recover_render_engine_after_nonretired_submit(dev, warm, "gpgpu-compute-walker");
src/intel/intel/render/gpgpu.rs:346:    let completed = submit_warm_render_batch(
src/intel/intel/render/gpgpu.rs:925:    let retired = submit_warm_render_batch(
src/intel/intel/render/resources.rs:180:        intel_render_focus_log!(
src/intel/intel/render/resources.rs:181:            "intel/render: {} shader-upload-verify note={} vs_match={} vs_baked_sig=0x{:016X} vs_uploaded_sig=0x{:016X} vs_first=0x{:08X}/0x{:08X}
[... truncated in extract ...]

[2026-05-11T01:31:18.606Z] tool-output line=59
------------------------------------------------------------------------------------------------
Chunk ID: 48c4cc
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1706
Output:
#![allow(dead_code)]

use embassy_executor::{SpawnError, Spawner};
use embassy_time::{Duration as EmbassyDuration, Timer};
use trueos_gfx_core::{ShaderDesc, ShaderFormat, ShaderStage};

pub(crate) const MANDELBROT_GPU_SIDEQUEST_NAME: &str = "mandelbrot-gpu-sidequest";
pub(crate) const MANDELBROT_FRAGMENT_SOURCE_PATH: &str =
    ".codex_tmp/mandelbrot_fragment_1440p_parametric.frag";
pub(crate) const MANDELBROT_FRAGMENT_SPIRV_PATH: &str =
    ".codex_tmp/mandelbrot_fragment_1440p_parametric.spv";
pub(crate) const MANDELBROT_FRAGMENT_SPIRV_BYTES: &[u8] =
    include_bytes!("../../.codex_tmp/mandelbrot_fragment_1440p_parametric.spv");
pub(crate) const MANDELBROT_TARGET_WIDTH: u32 = 2560;
pub(crate) const MANDELBROT_TARGET_HEIGHT: u32 = 1440;
pub(crate) const MANDELBROT_PUSH_CONSTANT_BYTES: u16 = 24;
pub(crate) const MANDELBROT_GPGPU_LOOP_MS: u64 = 1000;

#[derive(Copy, Clone, Debug, Eq, PartialEq)]
pub(crate) enum MandelbrotGpuArtifactStage {
    Fragment,
    Compute,
}

impl MandelbrotGpuArtifactStage {
    pub(crate) const fn as_str(self) -> &'static str {
        match self {
            Self::Fragment => "fragment",
            Self::Compute => "compute",
        }
    }
}

#[derive(Copy, Clone, Debug, Eq, PartialEq)]
pub(crate) enum MandelbrotPresentPath {
    BufferFirst,
    CopyToIntelOverlay,
    DirectFramebuffer,
}

impl MandelbrotPresentPath {
    pub(crate) const fn as_str(self) -> &'static str {
        match self {
            Self::BufferFirst => "buffer-first",
            Self::CopyToIntelOverlay => "copy-to-intel-overlay",
            Self::DirectFramebuffer => "direct-framebuffer",
        }
    }
}

#[derive(Copy, Clone, Debug, Eq, PartialEq)]
pub(crate) struct MandelbrotGpuSidequestPlan {
    pub(crate) name: &'static str,
    pub(crate) stage: MandelbrotGpuArtifactStage,
    pub(crate) source_path: &'static str,
    pub(crate) spirv_path: &'static str,
    pub(crate) target_width: u32,
    pub(crate) target_height: u32,
    pub(crate) push_constant_bytes: u16,
    pub(crate) render_path: MandelbrotPresentPath,
    pub(crate) fallback_present_path: MandelbrotPresentPath,
}

impl MandelbrotGpuSidequestPlan {
    pub(crate) const fn default_fragment_buffer_first() -> Self {
        Self {
            name: MANDELBROT_GPU_SIDEQUEST_NAME,
            stage: MandelbrotGpuArtifactStage::Fragment,
            source_path: MANDELBROT_FRAGMENT_SOURCE_PATH,
            spirv_path: MANDELBROT_FRAGMENT_SPIRV_PATH,
            target_width: MANDELBROT_TARGET_WIDTH,
            target_height: MANDELBROT_TARGET_HEIGHT,
            push_constant_bytes: MANDELBROT_PUSH_CONSTANT_BYTES,
            render_path: MandelbrotPresentPath::BufferFirst,
            fallback_present_path: MandelbrotPresentPath::CopyToIntelOverlay,
        }
    }

    pub(crate) const fn target_rgba_bytes(self) -> usize {
        (self.target_width as usize)
            .saturating_mul(self.target_height as usize)
            .saturating_mul(4)
    }
}

pub(crate) const fn mandelbrot_gpu_sidequest_plan() -> MandelbrotGpuSidequestPlan {
    MandelbrotGpuSidequestPlan::default_fragment_buffer_first()
}

pub(crate) fn mandelbrot_fragment_shader_desc(spirv_bytes: &'static [u8]) -> ShaderDesc<'static> {
    ShaderDesc {
        stage: ShaderStage::Fragment,
        format: ShaderFormat::SpirV,
        bytes: spirv_bytes,
    }
}

fn byte_signature(bytes: &[u8]) -> u64 {
    let mut sig = 0xCBF2_9CE4_8422_2325u64;
    for &byte in bytes {
        sig ^= byte as u64;
        sig = sig.wrapping_mul(0x0000_0100_0000_01B3);
    }
    sig
}

pub(crate) fn spawn_mandelbrot_gpu_sidequest(spawner: Spawner) -> Result<(), SpawnError> {
    let token = mandelbrot_gpu_sidequest_task()?;
    spawner.spawn(token);
    Ok(())
}

#[embassy_executor::task(pool_size = 1)]
pub(crate) async fn mandelbrot_gpu_sidequest_task() {
    let plan = mandelbrot_gpu_sidequest_plan();
    let scanout = crate::intel::active_scanout_dimensions();
    let scanout_w = scanout.map(|dims| dims.0).unwrap_or(0);
    let scanout_h = scanout.map(|dims| dims.1).unwrap_or(0);
    let primary_gpu = crate::intel::primary_surface_gpu_addr().unwrap_or(0);
    let shader_helper_ready = true;
    let shader_desc = mandelbrot_fragment_shader_desc(MANDELBROT_FRAGMENT_SPIRV_BYTES);
    let spirv_bytes = shader_desc.bytes.len();
    let spirv_sig = byte_signature(shader_desc.bytes);

    crate::
[... truncated in extract ...]

[2026-05-11T01:31:24.871Z] assistant line=61
------------------------------------------------------------------------------------------------
A useful split is emerging: the compiled `src/intel/display.rs` path already owns a live linear scanout surface and CPU copy/present helpers, while the rich 3D render code under `src/intel/intel/render` looks like reference/parked code rather than the active module graph. I’m checking the active `src/intel/gpgpu.rs` path now because the sidequest currently loops through that, not the old 3D triangle path.

[2026-05-11T01:31:24.872Z] assistant line=62
------------------------------------------------------------------------------------------------
A useful split is emerging: the compiled `src/intel/display.rs` path already owns a live linear scanout surface and CPU copy/present helpers, while the rich 3D render code under `src/intel/intel/render` looks like reference/parked code rather than the active module graph. I’m checking the active `src/intel/gpgpu.rs` path now because the sidequest currently loops through that, not the old 3D triangle path.

[2026-05-11T01:31:24.874Z] tool-call:exec_command line=63
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '340,392p' src/intel/mod.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":10000}

[2026-05-11T01:31:24.894Z] tool-call:exec_command line=64
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '660,690p' src/intel/mod.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":10000}

[2026-05-11T01:31:24.894Z] tool-call:exec_command line=65
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '2320,2410p' src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":18000}

[2026-05-11T01:31:24.894Z] tool-call:exec_command line=66
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '2160,2265p' src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":18000}

[2026-05-11T01:31:24.932Z] tool-output line=68
------------------------------------------------------------------------------------------------
Chunk ID: 71517e
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 519
Output:
    *CLAIMED_DEVICE.lock() = Some(dev);
    let fw = self::guc::load_fw();
    if fw.len == 0 {
        crate::log!("intel/guc: firmware module missing or invalid\n");
        return;
    }
    crate::log!(
        "intel/guc: firmware found phys=0x{:X} gpu=0x{:X} len=0x{:X} xfer=0x{:X}\n",
        fw.phys,
        fw.gpu,
        fw.len,
        fw.xfer_len
    );
    let ads = self::guc::alloc_ads(fw.private_data_size);
    if ads.len == 0 {
        crate::log!("intel/guc: ads alloc failed private_data=0x{:X}\n", fw.private_data_size);
        return;
    }
    if !map_ggtt(dev, fw.phys, fw.len, fw.gpu) || !map_ggtt(dev, ads.phys, ads.len, ads.gpu) {
        crate::log!("intel/guc: ggtt map failed fw_len=0x{:X} ads_len=0x{:X}\n", fw.len, ads.len);
        return;
    }
    ggtt_invalidate(dev);
    forcewake(dev);
    let ready = self::guc::bootstrap(dev, fw, ads);
    let status = self::guc::status(dev);
    let (bootrom, ukernel, auth) = self::guc::describe_status(status);
    crate::log!(
        "intel/guc: bootstrap ready={} status=0x{:08X} bootrom={} ukernel={} auth=0x{:X}\n",
        ready as u8,
        status,
        bootrom,
        ukernel,
        auth
    );
    if ready {
        self::guc::prove_h2g_mmio_once(dev, "boot-control-ctb-disable");
    }
    if DISPLAY_PLANE1_BOOT_DEMO_ENABLED {
        self::display::init_primary_boot_surface(dev);
    } else {
        crate::log!("intel/display: plane1 boot demo disabled\n");
    }
    crate::log!(
        "intel/render: disabled reason=primary-render-module-removed gpgpu_probe=enabled\n"
    );
    self::gpgpu::submit_gpgpu_preflight_once();
    crate::log!("intel/media: source warmup disabled trigger=trueosfs-root-mounted\n",);
    if MEDIA_BOOT_DEMO_ENABLED {
        crate::log!("intel/media: scheduled boot demo delay_ms={}\n", MEDIA_BOOT_DEMO_DELAY_MS);
        crate::wait::spawn_local_detached(async move {
            Timer::after(EmbassyDuration::from_millis(MEDIA_BOOT_DEMO_DELAY_MS)).await;
            let queued_at_ms = embassy_time::Instant::now().as_millis() as u64;

[2026-05-11T01:31:24.932Z] tool-output line=69
------------------------------------------------------------------------------------------------
Chunk ID: 33efa5
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 213
Output:
        function: 0,
        device_id: warm.device_id,
        revision_id: warm.revision_id,
        mmio: warm.mmio_base as *mut u8,
        mmio_len: warm.mmio_len,
    })
}

pub(crate) fn log_guc_submission_contract(dev: Dev, label: &'static str) {
    self::guc::log_submission_contract(dev, label);
}

pub fn active_scanout_dimensions() -> Option<(u32, u32)> {
    self::display::active_scanout_dimensions()
}

pub fn primary_surface_gpu_addr() -> Option<u64> {
    self::display::primary_surface_gpu_addr()
}

pub fn present_rgba_overlay_top_right(
    src: &[u8],
    src_width: u32,
    src_height: u32,
    src_pitch_bytes: usize,
) -> bool {
    self::display::present_rgba_overlay_top_right(src, src_width, src_height, src_pitch_bytes)
}

pub fn update_kernel_hw_cursor() -> Option<u32> {
    self::hw_cursor::update_kernel_hw_cursor()

[2026-05-11T01:31:24.932Z] tool-output line=70
------------------------------------------------------------------------------------------------
Chunk ID: 90a04c
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 874
Output:
        finish_marker,
        expected_finish_marker: RCS_EXEC_RESULT_COMPUTE_WALKER_DONE,
        batch_bytes,
    }
}

pub(crate) fn submit_gpgpu_sidequest_target_buffer_probe() -> crate::intel::GpgpuOneTileSentinelProof
{
    const SIDEQUEST_TARGET_BYTES: usize = 64;
    const SIDEQUEST_CPU_EXPECTED_BITS: u32 = 0;

    let output_gpu = GPU_VA_GPGPU_TILE_ARENA_BASE + GPGPU_TILE_OUTPUT_OFFSET_BYTES as u64;
    if let Some(warm) = warm_state() {
        let output_offset = GPGPU_TILE_OUTPUT_OFFSET_BYTES;
        if !warm.gpgpu_arena_virt.is_null()
            && output_offset
                .checked_add(SIDEQUEST_TARGET_BYTES)
                .is_some_and(|end| end <= warm.gpgpu_arena_len)
        {
            let output_virt = unsafe { warm.gpgpu_arena_virt.add(output_offset) };
            unsafe {
                core::ptr::write_bytes(output_virt, 0, SIDEQUEST_TARGET_BYTES);
            }
            crate::intel::dma_flush(output_virt, SIDEQUEST_TARGET_BYTES);
            crate::log!(
                "intel/gpgpu: sidequest-target-buffer-clear target_gpu=0x{:X} target_off=0x{:X} target_bytes=0x{:X} action=prepare-proven-gpgpu-store\n",
                output_gpu,
                output_offset,
                SIDEQUEST_TARGET_BYTES,
            );
        } else {
            crate::log!(
                "intel/gpgpu: sidequest-target-buffer-clear target_gpu=0x{:X} target_off=0x{:X} target_bytes=0x{:X} action=skip reason=arena-not-ready\n",
                output_gpu,
                output_offset,
                SIDEQUEST_TARGET_BYTES,
            );
        }
    }

    let proof = submit_gpgpu_one_tile_output_sentinel_probe(
        output_gpu,
        SIDEQUEST_TARGET_BYTES,
        SIDEQUEST_CPU_EXPECTED_BITS,
    );
    crate::log!(
        "intel/gpgpu: sidequest-target-buffer-render submitted={} finished={} readback_ok={} reason={} program_source={} target_gpu=0x{:X} target_bytes=0x{:X} sentinel=0x{:08X} before=0x{:08X} after=0x{:08X} hits=0x{:016X} lane_dispatch={} finish_marker=0x{:08X} finish_expected=0x{:08X} batch_bytes=0x{:X} action={} next={} does_not_prove=fragment_shader_or_full_framebuffer\n",
        proof.submitted as u8,
        proof.finished as u8,
        proof.readback_ok as u8,
        proof.reason,
        proof.program_name,
        proof.output_gpu,
        SIDEQUEST_TARGET_BYTES,
        proof.sentinel,
        proof.output_first_before,
        proof.output_first_after,
        proof.output_hits_lo64,
        proof.dispatch_delta,
        proof.finish_marker,
        proof.expected_finish_marker,
        proof.batch_bytes,
        if proof.readback_ok {
            "release-lumen-load"
        } else {
            "hold-lumen-load"
        },
        if proof.readback_ok {
            "optional-visible-scanout-target"
        } else {
            "fix-gpgpu-target-buffer-render"
        },
    );
    proof
}

pub(crate) fn submit_gpgpu_one_tile_output_compare_probe(
    output_gpu: u64,
    output_bytes: usize,
    cpu_expected_bits: u32,
) -> crate::intel::GpgpuOneTileCompareProof {
    let program = gpgpu_one_tile_output_compare_program();
    let Some(dev) = crate::intel::claimed_device() else {
        return gpgpu_one_tile_compare_failure("no-device", program, output_gpu, cpu_expected_bits);
    };
    let Some(warm) = warm_state() else {
        return gpgpu_one_tile_compare_failure(
            "no-warm-state",
            program,
            output_gpu,
            cpu_expected_bits,

[2026-05-11T01:31:24.932Z] tool-output line=71
------------------------------------------------------------------------------------------------
Chunk ID: 6ee89f
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1171
Output:
        return gpgpu_one_tile_sentinel_failure("no-arena", program, output_gpu);
    }
    if output_bytes < core::mem::size_of::<u32>() {
        return gpgpu_one_tile_sentinel_failure("bad-output-bytes", program, output_gpu);
    }
    if output_gpu < GPU_VA_GPGPU_TILE_ARENA_BASE {
        return gpgpu_one_tile_sentinel_failure("output-gpu-before-arena", program, output_gpu);
    }
    let output_offset = (output_gpu - GPU_VA_GPGPU_TILE_ARENA_BASE) as usize;
    let Some(output_end) = output_offset.checked_add(output_bytes) else {
        return gpgpu_one_tile_sentinel_failure("output-range-overflow", program, output_gpu);
    };
    if output_end > warm.gpgpu_arena_len {
        return gpgpu_one_tile_sentinel_failure("output-range-outside-arena", program, output_gpu);
    }
    if output_gpu >> 32 != 0 {
        return gpgpu_one_tile_sentinel_failure(
            "output-gpu-high32-unsupported",
            program,
            output_gpu,
        );
    }
    if !forcewake_render_acquire(warm) {
        return gpgpu_one_tile_sentinel_failure("forcewake", program, output_gpu);
    }
    if !ensure_gpgpu_warm_buffers_mapped(dev, warm) || !ensure_gpgpu_tile_arena_mapped(dev, warm) {
        return gpgpu_one_tile_sentinel_failure("ppgtt-map", program, output_gpu);
    }

    let output_virt = unsafe { warm.gpgpu_arena_virt.add(output_offset) };
    let output_count = output_bytes / core::mem::size_of::<u32>();
    let output_first_before = unsafe { core::ptr::read_volatile(output_virt as *const u32) };
    let output_nonzero_before =
        unsafe { gpgpu_stage_nonzero_dwords(output_virt as *const u32, output_count) };

    let mut sentinel_words = trueos_eu::gfx12::HDC1_STATELESS_STORE_THEN_TS_EOT_WORDS;
    sentinel_words[trueos_eu::gfx12::HDC1_BTI34_STORE_IMM_DWORD] = GPGPU_ONE_TILE_OUTPUT_SENTINEL;
    sentinel_words[7] = output_gpu as u32;
    let program_uploaded =
        upload_and_verify_gpu_program_at(warm, GPGPU_EU_KERNEL_OFFSET_BYTES, &sentinel_words);
    if !program_uploaded {
        return gpgpu_one_tile_sentinel_failure("program-upload", program, output_gpu);
    }

    unsafe {
        core::ptr::write_volatile(
            warm.result_virt
                .add(RESULT_SLOT_GPGPU_COMPUTE_WALKER_DWORD * core::mem::size_of::<u32>())
                as *mut u32,
            0,
        );
        for breadcrumb_slot in 23..=28 {
            core::ptr::write_volatile(
                warm.result_virt
                    .add(breadcrumb_slot * core::mem::size_of::<u32>()) as *mut u32,
                0,
            );
        }
        core::ptr::write_bytes(warm.batch_virt, 0, warm.batch_len);
        core::ptr::write_bytes(warm.ring_virt, 0, warm.ring_len);
    }
    crate::intel::dma_flush(warm.result_virt, warm.result_len);

    let batch_dwords = warm.batch_len / core::mem::size_of::<u32>();
    let batch =
        unsafe { core::slice::from_raw_parts_mut(warm.batch_virt as *mut u32, batch_dwords) };
    let store_surface = prepare_gpgpu_store_surface_state_for_target(
        warm,
        output_gpu,
        "bind-send-bti-to-one-tile-output-sentinel",
    );
    let batch_result =
        encode_gfx12_gpgpu_walker_probe_batch(warm, batch, store_surface, program, 1);
    let batch_bytes = match batch_result {
        Ok(bytes) => bytes,
        Err(reason) => return gpgpu_one_tile_sentinel_failure(reason, program, output_gpu),
    };
    crate::intel::dma_flush(warm.batch_virt, batch_bytes);

    let dispatch_before = read_gpgpu_threads_dispatched(dev);
    let finished = submit_warm_render_batch(
        dev,
        warm,
        RCS_EXEC_RESULT_COMPUTE_WALKER_DONE,
        RESULT_SLOT_GPGPU_COMPUTE_WALKER_DWORD,
        "gpgpu-one-tile-sentinel",
    );
    crate::intel::dma_flush(warm.result_virt, warm.result_len);
    crate::intel::dma_flush(output_virt, output_bytes);
    let dispatch_after = read_gpgpu_threads_dispatched(dev);
    let dispatch_delta = dispatch_after.saturating_sub(dispatch_before);
    let finish_marker = read_result_dword(warm, RESULT_SLOT_GPGPU_COMPUTE_WALKER_DWORD);
    let output_first_after = unsafe { core::ptr::read_volatile(output_virt as *const u32) };
    let output_nonzero_after =
        unsafe { gpgpu_stage_nonzero_dwords(output_virt as *const u32, output_count) };
    let output_hits_lo64 = unsafe {
        gpgpu_stage_dword_hits_mask_lo64(
            output
[... truncated in extract ...]

[2026-05-11T01:31:28.449Z] tool-call:exec_command line=72
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '2265,2328p' src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":12000}

[2026-05-11T01:31:28.492Z] tool-output line=74
------------------------------------------------------------------------------------------------
Chunk ID: b7f2a8
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 610
Output:
    let readback_ok =
        sentinel_written && finished && finish_marker == RCS_EXEC_RESULT_COMPUTE_WALKER_DONE;
    let submitted = batch_bytes != 0;
    let reason = if output_first_before != 0 {
        "output-not-zero-before-submit"
    } else if readback_ok && dispatch_delta == 0 {
        "sentinel-written-no-ts-delta"
    } else if readback_ok {
        "sentinel-written"
    } else if !finished {
        "submit-not-finished"
    } else if dispatch_delta == 0 {
        "no-dispatch-delta"
    } else if output_first_after != GPGPU_ONE_TILE_OUTPUT_SENTINEL {
        "sentinel-missing"
    } else {
        "sentinel-not-at-slot0"
    };
    crate::log!(
        "intel/gpgpu: one-tile-output-sentinel submitted={} finished={} readback_ok={} reason={} program_source={} groups=1 expected_lane_dispatch=8 observed_lane_dispatch={} output_gpu=0x{:X} output_first_before=0x{:08X} output_first_after=0x{:08X} sentinel=0x{:08X} output_nonzero_before={} output_nonzero_after={} output_hits_lo64=0x{:016X} finish_marker=0x{:08X} finish_expected=0x{:08X} batch_bytes=0x{:X} cpu_expected_bits=0x{:08X} output_owner=cpu-ap next=one-tile-output-compare does_not_prove=model_matvec\n",
        submitted as u8,
        finished as u8,
        readback_ok as u8,
        reason,
        program.name,
        dispatch_delta,
        output_gpu,
        output_first_before,
        output_first_after,
        GPGPU_ONE_TILE_OUTPUT_SENTINEL,
        output_nonzero_before,
        output_nonzero_after,
        output_hits_lo64,
        finish_marker,
        RCS_EXEC_RESULT_COMPUTE_WALKER_DONE,
        batch_bytes,
        cpu_expected_bits,
    );
    if !finished {
        recover_render_engine_after_nonretired_submit(dev, warm, "gpgpu-one-tile-sentinel");
    }
    crate::intel::GpgpuOneTileSentinelProof {
        submitted,
        finished,
        readback_ok,
        reason,
        program_name: program.name,
        output_gpu,
        sentinel: GPGPU_ONE_TILE_OUTPUT_SENTINEL,
        output_first_before,
        output_first_after,
        output_nonzero_before,
        output_nonzero_after,
        output_hits_lo64,
        dispatch_delta,
        finish_marker,
        expected_finish_marker: RCS_EXEC_RESULT_COMPUTE_WALKER_DONE,
        batch_bytes,
    }
}

pub(crate) fn submit_gpgpu_sidequest_target_buffer_probe() -> crate::intel::GpgpuOneTileSentinelProof
{
    const SIDEQUEST_TARGET_BYTES: usize = 64;

[2026-05-11T01:31:33.635Z] tool-call:exec_command line=76
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1,150p' src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":15000}

[2026-05-11T01:31:33.642Z] tool-call:exec_command line=77
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"GPGPU_TILE_ARENA|TILE_OUTPUT|ARENA_BASE|prepare_gpgpu_store_surface|surface state|BTI|store_surface|HDC\" src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":24000}

[2026-05-11T01:31:33.656Z] tool-call:exec_command line=78
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1,120p' src/intel/intel/render/mod.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":12000}

[2026-05-11T01:31:33.703Z] tool-output line=80
------------------------------------------------------------------------------------------------
Chunk ID: 6af48e
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1839
Output:
use core::sync::atomic::{AtomicBool, AtomicU32, Ordering};

use spin::Mutex;

const FORCEWAKE_RENDER: usize = 0x0A278;
const FORCEWAKE_GT: usize = 0x0A188;
const FORCEWAKE_ACK_RENDER: usize = 0x0D84;
const FORCEWAKE_ACK_GT: usize = 0x130044;
const FORCEWAKE_KERNEL: u32 = 1 << 0;
const FORCEWAKE_FALLBACK: u32 = 1 << 15;
const FORCEWAKE_POLL_ITERS: usize = 20_000;
const FF_DOP_CLOCK_GATE_DISABLE: u32 = 1 << 1;
const RCS_RING_BASE: usize = 0x0000_2000;
const RCS_CS_GPR_REL_BASE: usize = 0x600;
const RCS_CS_GPR_BASE: usize = RCS_RING_BASE + RCS_CS_GPR_REL_BASE;
const RCS_CS_GPR_COUNT: usize = 16;
const RCS_RING_TAIL: usize = RCS_RING_BASE + 0x30;
const RCS_RING_HEAD: usize = RCS_RING_BASE + 0x34;
const RCS_RING_ACTHD_UDW: usize = RCS_RING_BASE + 0x5C;
const RCS_RING_DMA_FADD_UDW: usize = RCS_RING_BASE + 0x60;
const RCS_RING_ACTHD: usize = RCS_RING_BASE + 0x74;
const RCS_RING_INSTPS: usize = RCS_RING_BASE + 0x70;
const RCS_RING_DMA_FADD: usize = RCS_RING_BASE + 0x78;
const RCS_RING_NOPID: usize = RCS_RING_BASE + 0x94;
const RCS_RING_PSMI_CTL: usize = RCS_RING_BASE + 0x50;
const RCS_RING_MI_MODE: usize = RCS_RING_BASE + 0x9C;
const RCS_CS_DEBUG_MODE1: usize = RCS_RING_BASE + 0xEC;
const RCS_CS_DEBUG_MODE2: usize = RCS_RING_BASE + 0xD8;
const RCS_RING_EIR: usize = RCS_RING_BASE + 0xB0;
const RCS_RING_ESR: usize = RCS_RING_BASE + 0xB8;
const RCS_RING_IPEIR: usize = RCS_RING_BASE + 0x64;
const RCS_RING_IPEHR: usize = RCS_RING_BASE + 0x68;
const RCS_RING_INSTDONE: usize = RCS_RING_BASE + 0x6C;
const RCS_RING_INSTPM: usize = RCS_RING_BASE + 0xC0;
const RCS_RING_BBSTATE: usize = RCS_RING_BASE + 0x110;
const RCS_RING_BBADDR: usize = RCS_RING_BASE + 0x140;
const RCS_RING_BBADDR_UDW: usize = RCS_RING_BASE + 0x168;
const GEN8_RING_FAULT_REG: usize = 0x4094;
const GEN8_FAULT_TLB_DATA0: usize = 0x4B10;
const GEN8_FAULT_TLB_DATA1: usize = 0x4B14;
const GEN12_FAULT_TLB_DATA0: usize = 0xCEB8;
const GEN12_FAULT_TLB_DATA1: usize = 0xCEBC;
const GEN12_RING_FAULT_REG: usize = 0xCEC4;
const ERROR_GEN6: usize = 0x40A0;
const GFX_MODE: usize = 0x2520;
const GEN12_RCU_MODE: usize = 0x14800;
const GEN12_RCU_MODE_CCS_ENABLE: u32 = 1 << 0;
const SC_INSTDONE: usize = 0x7100;
const SC_INSTDONE_EXTRA: usize = 0x7104;
const SC_INSTDONE_EXTRA2: usize = 0x7108;
const SAMPLER_INSTDONE: usize = 0xE160;
const ROW_INSTDONE: usize = 0xE164;
const TDL_THR_STATUS0: usize = 0xE4B8;
const TDL_THR_DISP_COUNT: usize = 0xE4BC;
const TDL_THR_STATUS1: usize = 0xE5B8;
const TDL_THR_PF_COUNT: usize = 0xE5BC;
const TDL_THR_PF_STATUS0: usize = 0xE6B8;
const TDL_THR_PF_STATUS1: usize = 0xE7B8;
const RCS_RING_CONTEXT_CONTROL: usize = RCS_RING_BASE + 0x244;
const RCS_RING_CONTEXT_CONTROL_REF: usize = RCS_RING_BASE + 0x5A0;
const RCS_RING_MODE_GEN7: usize = RCS_RING_BASE + 0x29C;
const RCS_RING_EXECLIST_STATUS_LO: usize = RCS_RING_BASE + 0x234;
const RCS_RING_EXECLIST_STATUS_HI: usize = RCS_RING_BASE + 0x238;
const RCS_RING_EXECLIST_CONTROL: usize = RCS_RING_BASE + 0x550;
const RCS_RING_EXECLIST_SQ_LO: usize = RCS_RING_BASE + 0x510;
const RCS_RING_EXECLIST_SQ_HI: usize = RCS_RING_BASE + 0x514;
const RCS_RING_HWS_PGA: usize = RCS_RING_BASE + 0x80;
const GDRST: usize = 0x0000_941C;
const WARM_RING_BYTES: usize = 4096;
const WARM_CONTEXT_BYTES: usize = 22 * 4096;
const WARM_BATCH_BYTES: usize = 512 * 4096;
const WARM_DRAW_STATE_BYTES: usize = 16 * 4096;
const WARM_VERTEX_BYTES: usize = 4096;
const WARM_RESULT_BYTES: usize = 4096;
const WARM_STREAMOUT_BYTES: usize = 4096;
const PPGTT_PT_COUNT: usize = 64;
const WARM_PPGTT_BYTES: usize = (3 + PPGTT_PT_COUNT) * 4096;
const BLT_RING_DWORDS: usize = 4;
const BLT_RING_TAIL_BYTES: usize = BLT_RING_DWORDS * core::mem::size_of::<u32>();
const LRC_STATE_OFFSET_DWORDS: usize = 4096 / core::mem::size_of::<u32>();
const GPU_VA_RING_BASE: u64 = 0x0080_0000;
const GPU_VA_CONTEXT_BASE: u64 = 0x0081_0000;
const GPU_VA_BATCH_BASE: u64 = 0x0180_0000;
const GPU_VA_RESULT_BASE: u64 = 0x0084_0000;
const GPU_VA_DRAW_STATE_BASE: u64 = 0x0086_0000;
const GPU_VA_VERTEX_BASE: u64 = 0x0087_0000;
const GPU_VA_STREAMOUT_BASE: u64 = 0x0088_0000;
const GPU_VA_GPGPU_TILE_ARENA_BASE: u64 = 0x0400_0000;
const GPGPU_EU_KERNEL_OFFSET_BYTES: usize = 0x3000;
const GPGPU_WALKER_SCRATCH_OFFSET_BYTES: usize = 0x3800;
const GPGPU_TILE_ROWS: usize = 256;
const GPGPU_TILE_K_DIM: usize = 2048;
const GPGPU_TILE_WEIGHT_BYTES_PER_ELEM: usize = 2;
const GPGPU_TILE_X_BYTES_PE
[... truncated in extract ...]

[2026-05-11T01:31:33.704Z] tool-output line=81
------------------------------------------------------------------------------------------------
Chunk ID: 0ebed9
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 2593
Output:
88:const GPU_VA_GPGPU_TILE_ARENA_BASE: u64 = 0x0400_0000;
95:const GPGPU_TILE_OUTPUT_BYTES_PER_ELEM: usize = 4;
100:const GPGPU_OUTPUT_TILE_BYTES: usize = GPGPU_TILE_ROWS * GPGPU_TILE_OUTPUT_BYTES_PER_ELEM;
101:const GPGPU_TILE_OUTPUT_OFFSET_BYTES: usize = GPGPU_X_VECTOR_BYTES + GPGPU_WEIGHT_TILE_BYTES;
103:    GPGPU_TILE_OUTPUT_OFFSET_BYTES + GPGPU_OUTPUT_TILE_BYTES;
105:const GPGPU_TILE_ARENA_REQUIRED_BYTES: usize = GPGPU_TILE_TARGET_TILES * GPGPU_TILE_RECORD_BYTES;
106:const GPGPU_TILE_ARENA_BYTES: usize = (GPGPU_TILE_ARENA_REQUIRED_BYTES + 4095) & !4095;
137:const GPGPU_ONE_TILE_OUTPUT_SENTINEL: u32 = 0xC0DE_7747;
150:const PIPE_CONTROL_FLUSH_HDC: u32 = 1 << 26;
156:    | PIPE_CONTROL_FLUSH_HDC;
209:static T63_FIRST_TILE_OUTPUT_DETAIL_LOGGED: AtomicBool = AtomicBool::new(false);
289:    match crate::dma::alloc(GPGPU_TILE_ARENA_BYTES, crate::intel::WARM_ALIGN) {
293:            warm.gpgpu_arena_len = GPGPU_TILE_ARENA_BYTES;
298:                GPGPU_TILE_ARENA_BYTES,
406:                GPU_VA_GPGPU_TILE_ARENA_BASE,
1233:const GPGPU_USE_HDC_STORE_THEN_EOT: bool = true;
1312:    if GPGPU_USE_HDC_STORE_THEN_EOT {
1328:    name == trueos_eu::gfx12::STATIC_DP4A_HDC1_STATELESS_STORE_THEN_TS_EOT.name
1340:    let artifact = trueos_eu::gfx12::HDC1_STATELESS_STORE_THEN_TS_EOT;
1347:        store_send_dword: Some(trueos_eu::gfx12::HDC1_BTI34_STORE_SEND_DWORD),
1348:        visible_seed_dword: Some(trueos_eu::gfx12::HDC1_BTI34_STORE_IMM_DWORD),
1353:    let artifact = trueos_eu::gfx12::STATIC_DP4A_HDC1_STATELESS_STORE_THEN_TS_EOT;
1360:        store_send_dword: Some(trueos_eu::gfx12::HDC1_STATELESS_STATIC_DP4A_STORE_SEND_DWORD),
1361:        visible_seed_dword: Some(trueos_eu::gfx12::HDC1_STATELESS_STATIC_DP4A_BASE_DWORD),
1366:    let artifact = trueos_eu::gfx12::HDC1_STATELESS_STORE_THEN_TS_EOT;
1372:        expected_store_value: GPGPU_ONE_TILE_OUTPUT_SENTINEL,
1373:        store_send_dword: Some(trueos_eu::gfx12::HDC1_BTI34_STORE_SEND_DWORD),
1374:        visible_seed_dword: Some(trueos_eu::gfx12::HDC1_BTI34_STORE_IMM_DWORD),
1379:    let artifact = trueos_eu::gfx12::STATIC_DP4A_HDC1_STATELESS_STORE_THEN_TS_EOT;
1386:        store_send_dword: Some(trueos_eu::gfx12::HDC1_STATELESS_STATIC_DP4A_STORE_SEND_DWORD),
1387:        visible_seed_dword: Some(trueos_eu::gfx12::HDC1_STATELESS_STATIC_DP4A_BASE_DWORD),
1393:        trueos_eu::gfx12::T5_SMALL_LIVE4_TRUEOS_ARENA_BF16_DOT_HDC1_STATELESS_STORE_THEN_TS_EOT;
1407:        trueos_eu::gfx12::T6_SMALL_LIVE8_TRUEOS_ARENA_BF16_DOT_HDC1_STATELESS_STORE_THEN_TS_EOT;
1421:        trueos_eu::gfx12::T61_LIVE16_TRUEOS_ARENA_BF16_DOT_HDC1_STATELESS_STORE_THEN_TS_EOT;
1435:        trueos_eu::gfx12::T62_ROW_INDEXED_LIVE16_TRUEOS_ARENA_BF16_DOT_HDC1_STATELESS_STORE_THEN_TS_EOT;
1449:        trueos_eu::gfx12::T63_LANE_INDEXED_LIVE32_TRUEOS_ARENA_BF16_DOT_HDC1_STATELESS_STORE_THEN_TS_EOT;
1463:        trueos_eu::gfx12::T63_ACCUM16_HI_LIVE32_TRUEOS_ARENA_BF16_DOT_HDC1_STATELESS_STORE_THEN_TS_EOT;
1575:    let artifact = trueos_eu::gfx12::T5_STORE_ONLY_ARENA_OFFSET_HDC1_STORE_THEN_TS_EOT;
1588:    let artifact = trueos_eu::gfx12::T5_LOAD_ECHO_TRUEOS_ARENA_RAW_OPERANDS_HDC1_STORE_THEN_TS_EOT;
1630:static GPGPU_TILE_ARENA_MAPPED: AtomicBool = AtomicBool::new(false);
1631:static GPGPU_TILE_ARENA_STATUS_LOGGED: AtomicBool = AtomicBool::new(false);
1877:    let output_offset = tile_base_offset + GPGPU_TILE_OUTPUT_OFFSET_BYTES;
1949:        arena_gpu_base: GPU_VA_GPGPU_TILE_ARENA_BASE,
1950:        x_gpu: GPU_VA_GPGPU_TILE_ARENA_BASE + x_offset as u64,
1951:        row_gpu: GPU_VA_GPGPU_TILE_ARENA_BASE + row_offset as u64,
1952:        output_gpu: GPU_VA_GPGPU_TILE_ARENA_BASE + output_offset as u64,
2027:    if output_gpu < GPU_VA_GPGPU_TILE_ARENA_BASE {
2057:    let output_offset = (output_gpu - GPU_VA_GPGPU_TILE_ARENA_BASE) as usize;
2058:    let Some(tile_base_offset) = output_offset.checked_sub(GPGPU_TILE_OUTPUT_OFFSET_BYTES) else {
2165:    if output_gpu < GPU_VA_GPGPU_TILE_ARENA_BASE {
2168:    let output_offset = (output_gpu - GPU_VA_GPGPU_TILE_ARENA_BASE) as usize;
2195:    let mut sentinel_words = trueos_eu::gfx12::HDC1_STATELESS_STORE_THEN_TS_EOT_WORDS;
2196:    sentinel_words[trueos_eu::gfx12::HDC1_BTI34_STORE_IMM_DWORD] = GPGPU_ONE_TILE_OUTPUT_SENTINEL;
2226:    let store_surface = prepare_gpgpu_store_surface_state_for_target(
2232:        encode_gfx12_gpgpu_walker_probe_batch(warm, ba
[... truncated in extract ...]

[2026-05-11T01:31:33.704Z] tool-output line=82
------------------------------------------------------------------------------------------------
Chunk ID: afeebc
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 691
Output:
// Render proof contract.
//
// The goal of this file is not "triangle or nothing."  Each probe should
// move one named boundary and say what it does not prove:
//
// - `batch-submit-proof`: RCS/execlist accepted enough command stream to run
//   markers.  Current captures still show `final_marker=0`, so full retire is
//   not proven.
// - `mi-scanout-store-proof`: RCS command streamer wrote one DWORD into the
//   live scanout surface via MI_STORE_DATA_IMM.  This proves neither 3D stage
//   progress nor PS/color-backend writes.
// - `memory-proof`: warm render buffers were mapped into their fixed GGTT
//   slots, cache-flushed, and CPU-read back in one source-level proof line.
//   This does not prove each 3D stage actually consumed its buffer.
// - `gpgpu-preflight`: RCS submission writes deterministic vector proof results
//   into the warm result buffer. This proves the buffer/result runway we need
//   for GPGPU bring-up, but not EU thread execution or matmul arithmetic yet.
// - `vertex-upload-proof`: CPU wrote/read back the triangle vertex bytes and
//   flushed them.  This does not prove VF consumed them.
// - `vf-proof`: IA/VF counters advance for three vertices.  Current captures
//   prove this with `vf-proof accepted=1`.
// - `vs-proof`: baked/uploaded VS bytes match and VS counters advance.  Current
//   captures prove this with `vs-proof accepted=1 vs_delta=3`.
// - `clip-raster-proof`: clipper counters advance.  Current captures prove this
//   on the VF draw path only; VS-to-clipper handoff is still the frontier.
// - `ps-dispatch-proof` and `ps-rt-proof`: not proven in current captures;
//   `ps_delta=0`, `rt_any_change=0`.
//
// Keep these proof lines conservative.  Packet markers are useful context, but
// stage proofs should only accept on counters or memory changes owned by that
// boundary.

use core::sync::atomic::{AtomicBool, AtomicU32, Ordering};
use spin::Mutex;

macro_rules! intel_render_focus_log {
    ($($arg:tt)*) => {
        if crate::logflag::INTEL_STAGE1_LOGS || crate::logflag::INTEL_RENDER_NGIN_LOGS {
            crate::log!($($arg)*);
        }
    };
}

macro_rules! intel_render_verbose_log {
    ($($arg:tt)*) => {
        if crate::logflag::INTEL_RENDER_NGIN_LOGS && !crate::logflag::INTEL_STAGE1_LOGS {
            crate::log!($($arg)*);
        }
    };
}

macro_rules! intel_render_batch_log {
    ($($arg:tt)*) => {
        if crate::logflag::INTEL_RENDER_NGIN_BATCH_LOGS && !crate::logflag::INTEL_STAGE1_LOGS {
            crate::log!($($arg)*);
        }
    };
}

include!("constants.rs");
include!("state.rs");
include!("warmup.rs");
include!("primary.rs");
include!("pipeline.rs");
include!("resources.rs");
include!("gpgpu.rs");
include!("submit.rs");
include!("lrc.rs");

[2026-05-11T01:31:36.715Z] tool-call:exec_command line=84
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '5370,5435p' src/intel/gpgpu.rs && sed -n '5435,5525p' src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":22000}

[2026-05-11T01:31:36.765Z] tool-output line=86
------------------------------------------------------------------------------------------------
Chunk ID: 0365ff
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1639
Output:
            && src_in_eot_safe_window
            && target_supports_eot) as u8,
    );
}

fn prepare_gpgpu_store_surface_state(warm: RenderWarmState) -> GpgpuStoreSurfaceState {
    let target_gpu = GPU_VA_RESULT_BASE
        + (RESULT_SLOT_GPGPU_EU_C_STORE_DWORD as u64) * core::mem::size_of::<u32>() as u64;
    prepare_gpgpu_store_surface_state_for_target(
        warm,
        target_gpu,
        "bind-send-bti-to-result-raw-buffer",
    )
}

fn prepare_gpgpu_store_surface_state_for_target(
    warm: RenderWarmState,
    target_gpu: u64,
    note: &'static str,
) -> GpgpuStoreSurfaceState {
    prepare_gpgpu_store_surface_state_for_target_span(
        warm,
        target_gpu,
        core::mem::size_of::<u32>(),
        note,
    )
}

fn prepare_gpgpu_store_surface_state_for_target_span(
    warm: RenderWarmState,
    target_gpu: u64,
    target_bytes: usize,
    note: &'static str,
) -> GpgpuStoreSurfaceState {
    let binding_table_bytes = GPGPU_STORE_BINDING_TABLE_ENTRIES * core::mem::size_of::<u32>();
    let surface_bytes = GPGPU_STORE_SURFACE_DWORDS * core::mem::size_of::<u32>();
    let binding_end = GPGPU_STORE_BINDING_TABLE_OFFSET_BYTES.saturating_add(binding_table_bytes);
    let surface_end = GPGPU_STORE_SURFACE_STATE_OFFSET_BYTES.saturating_add(surface_bytes);
    let binding_table_aligned = GPGPU_STORE_BINDING_TABLE_OFFSET_BYTES & 0x3F == 0;
    let surface_aligned = GPGPU_STORE_SURFACE_STATE_OFFSET_BYTES & 0x3F == 0;
    let ready = binding_table_aligned
        && surface_aligned
        && binding_end <= warm.draw_state_len
        && surface_end <= warm.draw_state_len;
    if !ready {
        crate::log!(
            "intel/gpgpu: gpu-program-surface-state ready=0 reason=draw-state-bounds bt_off=0x{:X} bt_bytes=0x{:X} surf_off=0x{:X} surf_bytes=0x{:X} draw_state_len=0x{:X}\n",
            GPGPU_STORE_BINDING_TABLE_OFFSET_BYTES,
            binding_table_bytes,
            GPGPU_STORE_SURFACE_STATE_OFFSET_BYTES,
            surface_bytes,
            warm.draw_state_len,
        );
        return GpgpuStoreSurfaceState {
            ready: false,
            binding_table_offset: GPGPU_STORE_BINDING_TABLE_OFFSET_BYTES,
            surface_state_offset: GPGPU_STORE_SURFACE_STATE_OFFSET_BYTES,
            binding_table_index: GPGPU_STORE_BINDING_TABLE_INDEX,
            surface_gpu: GPU_VA_DRAW_STATE_BASE + GPGPU_STORE_SURFACE_STATE_OFFSET_BYTES as u64,
            target_gpu,
            surface_dword0: 0,
            surface_dword2: 0,
            surface_dword3: 0,
            binding_entry: 0,
        };
    }
    }

    let binding_entry = GPGPU_STORE_SURFACE_STATE_OFFSET_BYTES as u32;
    let surface_dword0 = (SURFTYPE_BUFFER << 29) | (SURFACE_FORMAT_RAW << 18);
    let surface_span_bytes = target_bytes.max(1);
    let surface_extent = surface_span_bytes.saturating_sub(1);
    let surface_width_minus1 = (surface_extent & 0x7F) as u32;
    let surface_height_minus1 = ((surface_extent >> 7) & 0x3FFF) as u32;
    let surface_depth_minus1 = ((surface_extent >> 21) & 0x7FF) as u32;
    let surface_dword2 = (surface_height_minus1 << 16) | surface_width_minus1;
    let surface_dword3 = surface_depth_minus1 << 21;
    unsafe {
        let binding_table = warm
            .draw_state_virt
            .add(GPGPU_STORE_BINDING_TABLE_OFFSET_BYTES) as *mut u32;
        for index in 0..GPGPU_STORE_BINDING_TABLE_ENTRIES {
            core::ptr::write_volatile(binding_table.add(index), binding_entry);
        }

        let surface = warm
            .draw_state_virt
            .add(GPGPU_STORE_SURFACE_STATE_OFFSET_BYTES) as *mut u32;
        for index in 0..GPGPU_STORE_SURFACE_DWORDS {
            core::ptr::write_volatile(surface.add(index), 0);
        }
        core::ptr::write_volatile(surface.add(0), surface_dword0);
        core::ptr::write_volatile(surface.add(1), RENDER_MOCS << 24);
        core::ptr::write_volatile(surface.add(2), surface_dword2);
        core::ptr::write_volatile(surface.add(3), surface_dword3);
        core::ptr::write_volatile(surface.add(8), target_gpu as u32);
        core::ptr::write_volatile(surface.add(9), (target_gpu >> 32) as u32);
    }
    crate::intel::dma_flush(
        unsafe {
            warm.draw_state_virt
                .add(GPGPU_STORE_BINDING_TABLE_OFFSET_BYTES)
        },
        binding_table_bytes,
    );
    crate::intel::dma_flush(
        unsafe {
[... truncated in extract ...]

[2026-05-11T01:31:42.129Z] tool-call:exec_command line=88
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '260,325p' src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":12000}

[2026-05-11T01:31:42.129Z] tool-call:exec_command line=89
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1620,1668p' src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":10000}

[2026-05-11T01:31:42.148Z] tool-call:exec_command line=90
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '4920,4965p' src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":12000}

[2026-05-11T01:31:42.148Z] tool-call:exec_command line=91
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"map_ppgtt|ppgtt|PML4|PDP|ensure_gpgpu_warm_buffers_mapped|ensure_gpgpu_tile_arena_mapped|map_ggtt\\(\" src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":24000}

[2026-05-11T01:31:42.192Z] tool-output line=93
------------------------------------------------------------------------------------------------
Chunk ID: d6a6d1
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 624
Output:
                }
                None => {
                    *WARM_STATE.lock() = Some(warm);
                    crate::log!(
                        "intel/gpgpu: warm alloc failed part={} size=0x{:X}\n",
                        $part,
                        $size,
                    );
                    return warm;
                }
            }
        };
    }

    alloc_part!(ring_phys, ring_virt, ring_len, WARM_RING_BYTES, "ring");
    alloc_part!(context_phys, context_virt, context_len, WARM_CONTEXT_BYTES, "context");
    alloc_part!(batch_phys, batch_virt, batch_len, WARM_BATCH_BYTES, "batch");
    alloc_part!(
        draw_state_phys,
        draw_state_virt,
        draw_state_len,
        WARM_DRAW_STATE_BYTES,
        "draw-state"
    );
    alloc_part!(vertex_phys, vertex_virt, vertex_len, WARM_VERTEX_BYTES, "vertex");
    alloc_part!(result_phys, result_virt, result_len, WARM_RESULT_BYTES, "result");
    alloc_part!(streamout_phys, streamout_virt, streamout_len, WARM_STREAMOUT_BYTES, "streamout");
    alloc_part!(ppgtt_phys, ppgtt_virt, ppgtt_len, WARM_PPGTT_BYTES, "ppgtt");

    match crate::dma::alloc(GPGPU_TILE_ARENA_BYTES, crate::intel::WARM_ALIGN) {
        Some((phys, virt)) => {
            warm.gpgpu_arena_phys = phys;
            warm.gpgpu_arena_virt = virt;
            warm.gpgpu_arena_len = GPGPU_TILE_ARENA_BYTES;
        }
        None => {
            crate::log!(
                "intel/gpgpu: arena alloc failed arena_bytes=0x{:X} tile_rows={} max_tiles=0 enough_for_shape=0\n",
                GPGPU_TILE_ARENA_BYTES,
                GPGPU_TILE_ROWS,
            );
        }
    }

    unsafe {
        core::ptr::write_bytes(warm.ring_virt, 0, warm.ring_len);
        core::ptr::write_bytes(warm.context_virt, 0, warm.context_len);
        core::ptr::write_bytes(warm.batch_virt, 0, warm.batch_len);
        core::ptr::write_bytes(warm.draw_state_virt, 0, warm.draw_state_len);
        core::ptr::write_bytes(warm.vertex_virt, 0, warm.vertex_len);
        core::ptr::write_bytes(warm.result_virt, 0, warm.result_len);
        core::ptr::write_bytes(warm.streamout_virt, 0, warm.streamout_len);
        core::ptr::write_bytes(warm.ppgtt_virt, 0, warm.ppgtt_len);
        if !warm.gpgpu_arena_virt.is_null() {
            core::ptr::write_bytes(warm.gpgpu_arena_virt, 0, warm.gpgpu_arena_len);
        }
    }

    *WARM_STATE.lock() = Some(warm);
    warm
}

fn warm_state() -> Option<RenderWarmState> {
    *WARM_STATE.lock()
}

[2026-05-11T01:31:42.192Z] tool-output line=94
------------------------------------------------------------------------------------------------
Chunk ID: 175f9b
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 581
Output:

static GPGPU_PREFLIGHT_SUBMITTED: AtomicBool = AtomicBool::new(false);
static GPGPU_PREFLIGHT_ACCEPTED: AtomicBool = AtomicBool::new(false);
static GPGPU_PREFLIGHT_COMPLETED: AtomicBool = AtomicBool::new(false);
static GPGPU_PREFLIGHT_MARKER: AtomicU32 = AtomicU32::new(0);
static GPGPU_PREFLIGHT_DOT: AtomicU32 = AtomicU32::new(0);
static GPGPU_PREFLIGHT_SUM_A: AtomicU32 = AtomicU32::new(0);
static GPGPU_PREFLIGHT_SUM_B: AtomicU32 = AtomicU32::new(0);
static GPGPU_PREFLIGHT_LANES_OBSERVED: AtomicU32 = AtomicU32::new(0);
static GPGPU_WARM_BUFFERS_MAPPED: AtomicBool = AtomicBool::new(false);
static GPGPU_TILE_ARENA_MAPPED: AtomicBool = AtomicBool::new(false);
static GPGPU_TILE_ARENA_STATUS_LOGGED: AtomicBool = AtomicBool::new(false);
static GPGPU_EU_KERNEL_UPLOADED: AtomicBool = AtomicBool::new(false);
static GPGPU_EU_WALKER_ENCODED: AtomicBool = AtomicBool::new(false);
static GPGPU_EU_WALKER_SUBMITTED: AtomicBool = AtomicBool::new(false);
static GPGPU_EU_WALKER_RETIRED: AtomicBool = AtomicBool::new(false);
static GPGPU_EU_DISPATCH_DELTA: AtomicU32 = AtomicU32::new(0);
static GPGPU_EU_C_STORE_VALUE: AtomicU32 = AtomicU32::new(0);
static GPGPU_EU_PROVEN_WALKER_RETIRED: AtomicBool = AtomicBool::new(false);
static GPGPU_EU_PROVEN_DISPATCH_DELTA: AtomicU32 = AtomicU32::new(0);
static GPGPU_EU_PROVEN_C_STORE_VALUE: AtomicU32 = AtomicU32::new(0);

#[derive(Copy, Clone, Debug)]
pub(crate) struct GpgpuPreflightStatus {
    pub(crate) submitted: bool,
    pub(crate) accepted: bool,
    pub(crate) completed: bool,
    pub(crate) guc_ready: bool,
    pub(crate) marker: u32,
    pub(crate) dot: u32,
    pub(crate) sum_a: u32,
    pub(crate) sum_b: u32,
    pub(crate) lanes: u32,
    pub(crate) min_burn_rows: usize,
    pub(crate) min_burn_k_dim: usize,
    pub(crate) arena_gpu_base: u64,
    pub(crate) arena_bytes: usize,
    pub(crate) tile_rows: usize,
    pub(crate) max_tiles: usize,
    pub(crate) enough_for_shape: bool,
    pub(crate) eu_kernel_uploaded: bool,
    pub(crate) eu_walker_encoded: bool,
    pub(crate) eu_walker_submitted: bool,
    pub(crate) eu_walker_retired: bool,
    pub(crate) eu_dispatch_delta: u32,
    pub(crate) eu_c_store_value: u32,
    pub(crate) eu_expected_store_value: u32,
    pub(crate) eu_program_name: &'static str,
    pub(crate) result_c_changed_by_eu: bool,

[2026-05-11T01:31:42.192Z] tool-output line=95
------------------------------------------------------------------------------------------------
Chunk ID: 4763a6
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 363
Output:
        && super::map_ggtt(dev, warm.result_phys, warm.result_len, GPU_VA_RESULT_BASE)
        && super::map_ggtt(dev, warm.streamout_phys, warm.streamout_len, GPU_VA_STREAMOUT_BASE);
    if mapped {
        super::ggtt_invalidate(dev);
        GPGPU_WARM_BUFFERS_MAPPED.store(true, Ordering::Release);
    }
    mapped
}

fn ensure_gpgpu_tile_arena_mapped(dev: crate::intel::Dev, warm: RenderWarmState) -> bool {
    if GPGPU_TILE_ARENA_MAPPED.load(Ordering::Acquire) {
        return true;
    }
    if warm.gpgpu_arena_len == 0 {
        return false;
    }

    let mapped = super::map_ggtt(
        dev,
        warm.gpgpu_arena_phys,
        warm.gpgpu_arena_len,
        GPU_VA_GPGPU_TILE_ARENA_BASE,
    );
    if mapped {
        super::ggtt_invalidate(dev);
        GPGPU_TILE_ARENA_MAPPED.store(true, Ordering::Release);
    }
    mapped
}

fn log_gpgpu_tile_arena_status(_warm: RenderWarmState, _mapped: bool) {
    let _ = GPGPU_TILE_ARENA_STATUS_LOGGED.swap(true, Ordering::AcqRel);
}

fn submit_gpgpu_preflight(dev: crate::intel::Dev, warm: RenderWarmState) -> bool {
    let a = [1u32, 2, 3, 4];
    let b = [10u32, 20, 30, 40];
    let sum_a = a.iter().copied().fold(0u32, u32::wrapping_add);
    let sum_b = b.iter().copied().fold(0u32, u32::wrapping_add);
    let dot = a
        .iter()
        .copied()
        .zip(b.iter().copied())
        .fold(0u32, |acc, (lhs, rhs)| acc.wrapping_add(lhs.wrapping_mul(rhs)));

    unsafe {

[2026-05-11T01:31:42.192Z] tool-output line=96
------------------------------------------------------------------------------------------------
Chunk ID: 464ab2
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 855
Output:
197:    ppgtt_phys: u64,
198:    ppgtt_virt: *mut u8,
199:    ppgtt_len: usize,
238:        ppgtt_phys: 0,
239:        ppgtt_virt: core::ptr::null_mut(),
240:        ppgtt_len: 0,
287:    alloc_part!(ppgtt_phys, ppgtt_virt, ppgtt_len, WARM_PPGTT_BYTES, "ppgtt");
312:        core::ptr::write_bytes(warm.ppgtt_virt, 0, warm.ppgtt_len);
326:fn init_minimal_ppgtt(warm: RenderWarmState) -> bool {
327:    if warm.ppgtt_virt.is_null() || warm.ppgtt_len < WARM_PPGTT_BYTES {
339:        core::ptr::write_bytes(warm.ppgtt_virt, 0, warm.ppgtt_len);
340:        let pml4 = warm.ppgtt_virt.add(pml4_off) as *mut u64;
341:        let pdp = warm.ppgtt_virt.add(pdp_off) as *mut u64;
342:        let pd = warm.ppgtt_virt.add(pd_off) as *mut u64;
343:        core::ptr::write_volatile(pml4, (warm.ppgtt_phys + pdp_off as u64) | pde_present_rw_uc);
344:        core::ptr::write_volatile(pdp, (warm.ppgtt_phys + pd_off as u64) | pde_present_rw_uc);
346:            let pt_phys = warm.ppgtt_phys + pt_off as u64 + (index as u64) * 4096;
370:                    warm.ppgtt_virt.add(pte_off) as *mut u64,
412:    crate::intel::dma_flush(warm.ppgtt_virt, warm.ppgtt_len);
493:    let ppgtt_ok = !GPGPU_USE_MINIMAL_PPGTT || init_minimal_ppgtt(warm);
496:            "intel/gpgpu: ppgtt-map enabled={} ok={} pml4_phys=0x{:X} bytes=0x{:X} batch_start_space={}\n",
498:            ppgtt_ok as u8,
499:            warm.ppgtt_phys,
500:            warm.ppgtt_len,
502:                "ppgtt"
508:    if !ppgtt_ok {
1068:        (warm.ppgtt_phys >> 32) as u32
1074:        warm.ppgtt_phys as u32
1743:        let arena_mapped = ensure_gpgpu_tile_arena_mapped(dev, warm);
1754:    if !ensure_gpgpu_warm_buffers_mapped(dev, warm) {
1757:    let arena_mapped = ensure_gpgpu_tile_arena_mapped(dev, warm);
1899:    let arena_mapped = ensure_gpgpu_tile_arena_mapped(dev, warm);
2185:    if !ensure_gpgpu_warm_buffers_mapped(dev, warm) || !ensure_gpgpu_tile_arena_mapped(dev, warm) {
2186:        return gpgpu_one_tile_sentinel_failure("ppgtt-map", program, output_gpu);
2460:    if !ensure_gpgpu_warm_buffers_mapped(dev, warm) || !ensure_gpgpu_tile_arena_mapped(dev, warm) {
2461:        return gpgpu_one_tile_compare_failure("ppgtt-map", program, output_gpu, cpu_expected_bits);
3494:    if !ensure_gpgpu_warm_buffers_mapped(dev, warm) || !ensure_gpgpu_tile_arena_mapped(dev, warm) {
3497:            "ppgtt-map",
3835:    if !ensure_gpgpu_warm_buffers_mapped(dev, warm) || !ensure_gpgpu_tile_arena_mapped(dev, warm) {
3837:            "ppgtt-map",
4910:fn ensure_gpgpu_warm_buffers_mapped(dev: crate::intel::Dev, warm: RenderWarmState) -> bool {
4915:    let mapped = super::map_ggtt(dev, warm.ring_phys, warm.ring_len, GPU_VA_RING_BASE)
4916:        && super::map_ggtt(dev, warm.context_phys, warm.context_len, GPU_VA_CONTEXT_BASE)
4917:        && super::map_ggtt(dev, warm.batch_phys, warm.batch_len, GPU_VA_BATCH_BASE)
4918:        && super::map_ggtt(dev, warm.draw_state_phys, warm.draw_state_len, GPU_VA_DRAW_STATE_BASE)
4919:        && super::map_ggtt(dev, warm.vertex_phys, warm.vertex_len, GPU_VA_VERTEX_BASE)
4920:        && super::map_ggtt(dev, warm.result_phys, warm.result_len, GPU_VA_RESULT_BASE)
4921:        && super::map_ggtt(dev, warm.streamout_phys, warm.streamout_len, GPU_VA_STREAMOUT_BASE);
4929:fn ensure_gpgpu_tile_arena_mapped(dev: crate::intel::Dev, warm: RenderWarmState) -> bool {
4937:    let mapped = super::map_ggtt(

[2026-05-11T01:31:45.931Z] tool-call:exec_command line=97
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '326,416p' src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":16000}

[2026-05-11T01:31:45.931Z] tool-call:exec_command line=98
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '480,515p' src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":8000}

[2026-05-11T01:31:45.947Z] tool-call:exec_command line=99
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"GPGPU_USE_MINIMAL_PPGTT|MI_BATCH_PPGTT|MI_BATCH_GTT|BATCH_BUFFER_START|CTX_DESC_PPGTT\" src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":14000}

[2026-05-11T01:31:45.970Z] tool-output line=101
------------------------------------------------------------------------------------------------
Chunk ID: 80116f
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 821
Output:
fn init_minimal_ppgtt(warm: RenderWarmState) -> bool {
    if warm.ppgtt_virt.is_null() || warm.ppgtt_len < WARM_PPGTT_BYTES {
        return false;
    }

    let pml4_off = 0usize;
    let pdp_off = 4096usize;
    let pd_off = 8192usize;
    let pt_off = 12288usize;
    let pte_present_rw = crate::intel::GEN8_PAGE_PRESENT | GEN8_PAGE_RW;
    let pde_present_rw_uc = pte_present_rw | GEN8_PAGE_PWT | GEN8_PAGE_PCD;

    unsafe {
        core::ptr::write_bytes(warm.ppgtt_virt, 0, warm.ppgtt_len);
        let pml4 = warm.ppgtt_virt.add(pml4_off) as *mut u64;
        let pdp = warm.ppgtt_virt.add(pdp_off) as *mut u64;
        let pd = warm.ppgtt_virt.add(pd_off) as *mut u64;
        core::ptr::write_volatile(pml4, (warm.ppgtt_phys + pdp_off as u64) | pde_present_rw_uc);
        core::ptr::write_volatile(pdp, (warm.ppgtt_phys + pd_off as u64) | pde_present_rw_uc);
        for index in 0..PPGTT_PT_COUNT {
            let pt_phys = warm.ppgtt_phys + pt_off as u64 + (index as u64) * 4096;
            core::ptr::write_volatile(pd.add(index), pt_phys | pde_present_rw_uc);
        }
    }

    fn map_region(
        warm: RenderWarmState,
        gpu: u64,
        phys: u64,
        len: usize,
        entry_flags: u64,
    ) -> bool {
        let pt_off = 12288usize;
        for page in 0..len.div_ceil(4096) {
            let va_page = (gpu >> 12) + page as u64;
            let pd_index = ((va_page >> 9) & 0x1FF) as usize;
            let pt_index = (va_page & 0x1FF) as usize;
            if pd_index >= PPGTT_PT_COUNT {
                return false;
            }
            let pte_off = pt_off + pd_index * 4096 + pt_index * core::mem::size_of::<u64>();
            let pte = (phys + (page as u64) * 4096) & !0xFFF;
            unsafe {
                core::ptr::write_volatile(
                    warm.ppgtt_virt.add(pte_off) as *mut u64,
                    pte | entry_flags,
                );
            }
        }
        true
    }

    let ok = map_region(warm, GPU_VA_RING_BASE, warm.ring_phys, warm.ring_len, pte_present_rw)
        && map_region(
            warm,
            GPU_VA_CONTEXT_BASE,
            warm.context_phys,
            warm.context_len,
            pte_present_rw,
        )
        && map_region(warm, GPU_VA_BATCH_BASE, warm.batch_phys, warm.batch_len, pte_present_rw)
        && map_region(
            warm,
            GPU_VA_DRAW_STATE_BASE,
            warm.draw_state_phys,
            warm.draw_state_len,
            pte_present_rw,
        )
        && map_region(warm, GPU_VA_VERTEX_BASE, warm.vertex_phys, warm.vertex_len, pte_present_rw)
        && map_region(warm, GPU_VA_RESULT_BASE, warm.result_phys, warm.result_len, pte_present_rw)
        && map_region(
            warm,
            GPU_VA_STREAMOUT_BASE,
            warm.streamout_phys,
            warm.streamout_len,
            pte_present_rw,
        )
        && (warm.gpgpu_arena_virt.is_null()
            || map_region(
                warm,
                GPU_VA_GPGPU_TILE_ARENA_BASE,
                warm.gpgpu_arena_phys,
                warm.gpgpu_arena_len,
                pte_present_rw,
            ));

    crate::intel::dma_flush(warm.ppgtt_virt, warm.ppgtt_len);
    ok
}

fn forcewake_render_acquire(warm: RenderWarmState) -> bool {

[2026-05-11T01:31:45.970Z] tool-output line=102
------------------------------------------------------------------------------------------------
Chunk ID: 4d455d
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 301
Output:
fn submit_warm_render_batch(
    dev: crate::intel::Dev,
    warm: RenderWarmState,
    expected_result: u32,
    expected_result_slot_dword: usize,
    submit_name: &'static str,
) -> bool {
    let log_submit = should_log_gpgpu_submit_name(submit_name);
    if is_gpgpu_submit_name(submit_name) {
        recover_render_engine_after_nonretired_submit(dev, warm, "gpgpu-pre-submit");
        crate::intel::mmio_write(dev, GEN12_RCU_MODE, masked_bit_enable(GEN12_RCU_MODE_CCS_ENABLE));
    }

    let ppgtt_ok = !GPGPU_USE_MINIMAL_PPGTT || init_minimal_ppgtt(warm);
    if log_submit {
        crate::log!(
            "intel/gpgpu: ppgtt-map enabled={} ok={} pml4_phys=0x{:X} bytes=0x{:X} batch_start_space={}\n",
            GPGPU_USE_MINIMAL_PPGTT as u8,
            ppgtt_ok as u8,
            warm.ppgtt_phys,
            warm.ppgtt_len,
            if GPGPU_BATCH_START_IN_PPGTT {
                "ppgtt"
            } else {
                "ggtt"
            },
        );
    }
    if !ppgtt_ok {
        return false;
    }

    let ring_tail_bytes = build_ring_batch_start(warm, GPU_VA_BATCH_BASE);
    let Some(ring_ctl) = ring_ctl_value(warm.ring_len) else {
        return false;
    };

[2026-05-11T01:31:45.978Z] tool-output line=103
------------------------------------------------------------------------------------------------
Chunk ID: 0fc2dd
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 144
Output:
117:const MI_BATCH_BUFFER_START_GEN8: u32 = (0x31 << 23) | 1;
118:const MI_BATCH_GTT: u32 = 2 << 6;
119:const MI_BATCH_PPGTT: u32 = 1 << 8;
493:    let ppgtt_ok = !GPGPU_USE_MINIMAL_PPGTT || init_minimal_ppgtt(warm);
497:            GPGPU_USE_MINIMAL_PPGTT as u8,
884:    dwords[0] = MI_BATCH_BUFFER_START_GEN8
886:            MI_BATCH_PPGTT
888:            MI_BATCH_GTT
921:        | if GPGPU_USE_MINIMAL_PPGTT {
1067:    state[idx + 15] = if GPGPU_USE_MINIMAL_PPGTT {
1073:    state[idx + 17] = if GPGPU_USE_MINIMAL_PPGTT {
1222:const GPGPU_USE_MINIMAL_PPGTT: bool = true;

[2026-05-11T01:31:49.320Z] tool-call:exec_command line=104
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '860,930p' src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":10000}

[2026-05-11T01:31:49.330Z] tool-call:exec_command line=105
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1048,1080p' src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":8000}

[2026-05-11T01:31:49.341Z] tool-call:exec_command line=106
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1216,1228p' src/intel/gpgpu.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":6000}

[2026-05-11T01:31:49.395Z] tool-output line=108
------------------------------------------------------------------------------------------------
Chunk ID: 08528b
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 576
Output:
    super::ggtt_invalidate(dev);
    crate::intel::mmio_write(
        dev,
        RCS_RING_MODE_GEN7,
        masked_bit_enable(GFX_RUN_LIST_ENABLE | GEN11_GFX_DISABLE_LEGACY_MODE),
    );
    let forcewake_ok = forcewake_render_acquire(warm);

    if log_recovery {
        crate::log!(
            "intel/gpgpu: {} recovery end gdrst=0x{:08X} execlist_lo=0x{:08X} mi_mode=0x{:08X} mode=0x{:08X} forcewake_ok={}\n",
            submit_name,
            crate::intel::mmio_read(dev, GDRST),
            crate::intel::mmio_read(dev, RCS_RING_EXECLIST_STATUS_LO),
            crate::intel::mmio_read(dev, RCS_RING_MI_MODE),
            crate::intel::mmio_read(dev, RCS_RING_MODE_GEN7),
            forcewake_ok as u8,
        );
    }
}

fn build_ring_batch_start(warm: RenderWarmState, batch_gpu_addr: u64) -> usize {
    let dwords =
        unsafe { core::slice::from_raw_parts_mut(warm.ring_virt as *mut u32, BLT_RING_DWORDS) };
    dwords[0] = MI_BATCH_BUFFER_START_GEN8
        | if GPGPU_BATCH_START_IN_PPGTT {
            MI_BATCH_PPGTT
        } else {
            MI_BATCH_GTT
        };
    dwords[1] = batch_gpu_addr as u32;
    dwords[2] = (batch_gpu_addr >> 32) as u32;
    dwords[3] = MI_NOOP;
    crate::intel::dma_flush(warm.ring_virt, BLT_RING_TAIL_BYTES);
    BLT_RING_TAIL_BYTES
}

fn ring_ctl_value(size: usize) -> Option<u32> {
    let size = u32::try_from(size).ok()?;
    Some(size.checked_sub(4096)? | RING_VALID)
}

fn masked_bit_enable(bit: u32) -> u32 {
    bit | (bit << 16)
}

fn masked_bit_disable(bit: u32) -> u32 {
    bit << 16
}

fn masked_bits_update(set_bits: u32, clear_bits: u32) -> u32 {
    let update = set_bits | clear_bits;
    set_bits | (update << 16)
}

fn build_execlist_context_descriptor(context_gpu_addr: u64) -> (u32, u32) {
    static RCS_SUBMIT_COUNTER: AtomicU32 = AtomicU32::new(0);

    let base = (context_gpu_addr as u32) & 0xFFFF_F000;
    let desc = base
        | GEN8_CTX_VALID
        | if GPGPU_USE_MINIMAL_PPGTT {
            GEN8_CTX_PPGTT_ENABLE
        } else {
            0
        }
        | CTX_DESC_FORCE_RESTORE
        | GEN8_CTX_PRIVILEGE
        | GEN12_CTX_PRIORITY_NORMAL
        | (INTEL_LEGACY_64B_CONTEXT << GEN8_CTX_ADDRESSING_MODE_SHIFT);
    let sw_context_id = (((context_gpu_addr >> 12) as u32) & 0x7FF).max(1);

[2026-05-11T01:31:49.395Z] tool-output line=109
------------------------------------------------------------------------------------------------
Chunk ID: 2e33c6
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 210
Output:
    push_mi_nops(state, &mut idx, 5);

    state[idx] = mi_lri_cmd(9, MI_LRI_FORCE_POSTED);
    idx += 1;
    state[idx] = 0x23A8;
    state[idx + 1] = 0;
    state[idx + 2] = 0x228C;
    state[idx + 3] = 0;
    state[idx + 4] = 0x2288;
    state[idx + 5] = 0;
    state[idx + 6] = 0x2284;
    state[idx + 7] = 0;
    state[idx + 8] = 0x2280;
    state[idx + 9] = 0;
    state[idx + 10] = 0x227C;
    state[idx + 11] = 0;
    state[idx + 12] = 0x2278;
    state[idx + 13] = 0;
    state[idx + 14] = 0x2274;
    state[idx + 15] = if GPGPU_USE_MINIMAL_PPGTT {
        (warm.ppgtt_phys >> 32) as u32
    } else {
        0
    };
    state[idx + 16] = 0x2270;
    state[idx + 17] = if GPGPU_USE_MINIMAL_PPGTT {
        warm.ppgtt_phys as u32
    } else {
        0
    };
    idx += 18;

    state[idx] = mi_lri_cmd(3, MI_LRI_FORCE_POSTED);

[2026-05-11T01:31:49.395Z] tool-output line=110
------------------------------------------------------------------------------------------------
Chunk ID: 86a11d
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 187
Output:
const GPGPU_BURN_MIN_K_DIM: usize = 512;
const GPU_PROGRAM_SHARED_RAM_WRITE_EXPECTED: u32 = trueos_eu::gfx12::STORE_SENTINEL_U32;
const GPGPU_LOAD_DUMMY_CURBE: bool = true;
const GPGPU_DUMMY_CURBE_BYTES: usize = 64;
const GPGPU_CONTIGUOUS_VFE_IDD_WALKER: bool = false;
const GPGPU_MESA_POST_VFE_PIPE_CONTROL: bool = false;
const GPGPU_USE_MINIMAL_PPGTT: bool = true;
const GPGPU_BATCH_START_IN_PPGTT: bool = false;
// ADL-S 8086:4680 is Gfx12.0/Xe-LP.  Keep the active milestone probe as the
// smallest Mesa-shaped root thread: no CURBE payload, one SIMD8 group, TS EOT.
const ACTIVE_GFX12_EOT_VARIANT: trueos_eu::gfx12::Gfx12EotVariant =
    trueos_eu::gfx12::Gfx12EotVariant::TsR0ToG127Send1;
const GPGPU_ENABLE_SIP_EXCEPTIONS: bool = false;

[2026-05-11T01:32:00.563Z] tool-call:exec_command line=112
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1,200p' src/tst/ui2_mandelbrot_demo.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":16000}

[2026-05-11T01:32:00.563Z] tool-call:exec_command line=113
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"mandelbrot_gpu_sidequest|MANDELBROT|spawn_mandelbrot|UI2.*mandel|queue_texture|present|overlay\" src/r src/tst src/lib.rs src/main.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":24000}

[2026-05-11T01:32:00.582Z] tool-call:exec_command line=114
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1,240p' src/gfx/mandelbrot.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":20000}

[2026-05-11T01:32:00.602Z] tool-output line=116
------------------------------------------------------------------------------------------------
Chunk ID: 3d5ecb
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 533
Output:
use embassy_time::{Duration as EmbassyDuration, Timer};

const UI2_MANDELBROT_TEX_ID: u32 = crate::tst_ui2_ids::Ui2DemoTexId::Mandelbrot.get();
const UI2_MANDELBROT_RT_W: u32 = 768;
const UI2_MANDELBROT_RT_H: u32 = 512;
const UI2_MANDELBROT_WINDOW_Z: i16 = 31;

#[embassy_executor::task]
pub async fn ui2_mandelbrot_demo_task() {
    let _task_guard = crate::r::spawn_service::task_run_guard("ui2-mandelbrot-demo");
    let Some(surface) = crate::r::ui2::Ui2SurfaceWindow::new(
        "Demo Mandelbrot",
        crate::r::ui2::Ui2Rect {
            x: 10.0,
            y: 10.0,
            w: UI2_MANDELBROT_RT_W as f32,
            h: UI2_MANDELBROT_RT_H as f32,
        },
        UI2_MANDELBROT_WINDOW_Z,
        128,
        UI2_MANDELBROT_TEX_ID,
        false,
        [0x08, 0x0B, 0x10, 0xFF],
    ) else {
        return;
    };
    let _ = surface.bind_spawn_task("ui2-mandelbrot-demo");

    let window_id = surface.window_id();
    let (surface_w, surface_h) = surface.size();
    crate::log!(
        "ui2-mandelbrot-demo: window={} tex={} size={}x{} start\n",
        window_id,
        surface.tex_id(),
        surface_w,
        surface_h
    );

    let _ = crate::r::ui2::set_window_title(window_id, "Seahorse Valley");

    // Let the blank window present first so the window frame lands before shader work is queued.
    Timer::after(EmbassyDuration::from_millis(1)).await;
    let start_ticks = embassy_time_driver::now();
    let tick_hz = embassy_time_driver::TICK_HZ;

    loop {
        if crate::r::spawn_service::task_stop_requested("ui2-mandelbrot-demo") {
            break;
        }
        let elapsed_ticks = embassy_time_driver::now().saturating_sub(start_ticks);
        if !surface.render_mandelbrot(elapsed_ticks, tick_hz, "ui2-mandelbrot-demo") {
            let _ = crate::r::ui2::set_window_title(window_id, "Seahorse Valley (unavailable)");
            crate::log!("ui2-mandelbrot-demo: window={} queue failed\n", window_id);
            break;
        }

        if crate::r::spawn_service::wait_task_or_timeout_ms("ui2-mandelbrot-demo", 33).await {
            break;
        }
    }
}

[2026-05-11T01:32:00.609Z] tool-output line=117
------------------------------------------------------------------------------------------------
Chunk ID: b976d4
Wall time: 0.0000 seconds
Process exited with code 2
Original token count: 3919
Output:
rg: src/lib.rs: No such file or directory (os error 2)
src/main.rs:94:#[path = "tst/mandelbrot_gpu_sidequest.rs"]
src/main.rs:95:mod tst_mandelbrot_gpu_sidequest;
src/r/fs/trueosfs.rs:579:/// state for `disk` when TRUEOSFS is present so stale indexes and file-record
src/tst/ui2_coreticks_demo.rs:241:            if !surface.upload_rgba_owned(pixels, "ui2-coreticks-demo-present") {
src/tst/ui2_coreticks_demo.rs:244:            let _ = crate::r::ui2::request_window_content_present(
src/tst/ui2_coreticks_demo.rs:246:                "ui2-coreticks-demo-present",
src/tst/ui2_player_demo.rs:379:            if !crate::r::io::cabi::queue_texture_rgba_image_upload_owned(
src/tst/ui2_player_demo.rs:385:                "ui2-player-demo-present",
src/tst/ui2_mandelbrot_demo.rs:3:const UI2_MANDELBROT_TEX_ID: u32 = crate::tst_ui2_ids::Ui2DemoTexId::Mandelbrot.get();
src/tst/ui2_mandelbrot_demo.rs:4:const UI2_MANDELBROT_RT_W: u32 = 768;
src/tst/ui2_mandelbrot_demo.rs:5:const UI2_MANDELBROT_RT_H: u32 = 512;
src/tst/ui2_mandelbrot_demo.rs:6:const UI2_MANDELBROT_WINDOW_Z: i16 = 31;
src/tst/ui2_mandelbrot_demo.rs:16:            w: UI2_MANDELBROT_RT_W as f32,
src/tst/ui2_mandelbrot_demo.rs:17:            h: UI2_MANDELBROT_RT_H as f32,
src/tst/ui2_mandelbrot_demo.rs:19:        UI2_MANDELBROT_WINDOW_Z,
src/tst/ui2_mandelbrot_demo.rs:21:        UI2_MANDELBROT_TEX_ID,
src/tst/ui2_mandelbrot_demo.rs:41:    // Let the blank window present first so the window frame lands before shader work is queued.
src/tst/ui2_raple_demo.rs:433:fn present_raple(
src/tst/ui2_raple_demo.rs:443:    let _ = crate::r::io::cabi::queue_texture_rgba_image_upload_copy(
src/tst/ui2_raple_demo.rs:449:        "ui2-raple-present",
src/tst/ui2_raple_demo.rs:492:    present_raple(&surface, &atlases, &current, previous.as_ref(), history.as_slice());
src/tst/ui2_raple_demo.rs:514:        present_raple(&surface, &atlases, &current, previous.as_ref(), history.as_slice());
src/r/spawn_service.rs:62:    MANDELBROT_GPU_SIDEQUEST_STARTED,
src/r/spawn_service.rs:82:    UI2_MANDELBROT_DEMO_STARTED,
src/r/spawn_service.rs:118:    STOP_UI2_MANDELBROT_DEMO,
src/r/spawn_service.rs:133:        "ui2-mandelbrot-demo" => Some(&STOP_UI2_MANDELBROT_DEMO),
src/r/spawn_service.rs:424:fn spawn_mandelbrot_gpu_sidequest(spawner: Spawner) -> SpawnAttempt {
src/r/spawn_service.rs:425:    match crate::tst_mandelbrot_gpu_sidequest::spawn_mandelbrot_gpu_sidequest(spawner) {
src/r/spawn_service.rs:540:        "gfx-virgl-backend-ready: timeout virgl_active={} virgl_present_cached={} ready_mask=0x{:08X}\n",
src/r/spawn_service.rs:542:        crate::gfx::is_virgl_present_cached() as u8,
src/r/spawn_service.rs:552:async fn gfx_virgl_cursor_overlay_task() {
src/r/spawn_service.rs:555:        "boot-probe: gfx-cursor-overlay task start ms={}\n",
src/r/spawn_service.rs:559:        let _ = crate::r::io::cabi::kernel_cursor_overlay_tick();
src/r/spawn_service.rs:564:fn spawn_gfx_virgl_cursor_overlay_task(spawner: Spawner) -> SpawnAttempt {
src/r/spawn_service.rs:565:    spawn_on_ap1(spawner, |_ap1_spawner| gfx_virgl_cursor_overlay_task())
src/r/spawn_service.rs:611:    if crate::gfx::is_virgl_present_cached() {
src/r/spawn_service.rs:1292:        "gfx-virgl-cursor-overlay",
src/r/spawn_service.rs:1295:        spawn_gfx_virgl_cursor_overlay_task,
src/r/spawn_service.rs:1321:        &MANDELBROT_GPU_SIDEQUEST_STARTED,
src/r/spawn_service.rs:1322:        spawn_mandelbrot_gpu_sidequest,
src/r/spawn_service.rs:1411:        &UI2_MANDELBROT_DEMO_STARTED,
src/r/spawn_service.rs:1455:            | crate::r::readiness::MANDELBROT_GPU_SIDEQUEST_READY,
src/tst/ui2_swarm.rs:685:    if !crate::r::io::cabi::queue_texture_rgba_image_upload_copy(
src/tst/ui2_swarm.rs:880:            if !surface.upload_rgba_owned(pixels, "ui2-swarm-present") {
src/r/readiness.rs:51:pub const MANDELBROT_GPU_SIDEQUEST_READY: u32 = 1 << 28;
src/r/readiness.rs:77:    (MANDELBROT_GPU_SIDEQUEST_READY, "MANDELBROT_GPU_SIDEQUEST_READY"),
src/tst/ui2_cursorpicker_demo.rs:152:            if !crate::r::io::cabi::queue_texture_rgba_image_upload_owned(
src/tst/ui2_cursorpicker_demo.rs:158:                "ui2-cursorpicker-demo-present",
src/tst/ws_time.rs:280:            "ws-time: handshake reject handle={} line='{}' path={:?} expect='{}' upgrade_ok={} connection_ok={} ws_upgrade_ok={} key_present={}\n",
src/r/net/cli/irc.rs:54:    /// Trailing parameter (aft
[... truncated in extract ...]

[2026-05-11T01:32:00.613Z] tool-output line=118
------------------------------------------------------------------------------------------------
Chunk ID: d17437
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1935
Output:
extern crate alloc;

use alloc::string::String;
use alloc::vec::Vec;
use core::fmt::Write;
use trueos_gfx_core::{Rgba8, push_tex_quad_ndc, tex_vertices_byte_len};

pub const MANDELBROT_PIPELINE_FS_TAG_RAW: u32 = 0x4D44_4C42;
const FULL_CENTER_X: f32 = -0.5;
const FULL_CENTER_Y: f32 = 0.0;
const FULL_X_SPAN: f32 = 1.5;
const FULL_Y_SPAN: f32 = 1.0;
const SEA_HORSE_CENTER_X: f32 = -0.743_643_9;
const SEA_HORSE_CENTER_Y: f32 = 0.131_825_91;
const SEA_HORSE_X_SPAN_X8: f32 = 0.00075;
const SEA_HORSE_Y_SPAN_X8: f32 = SEA_HORSE_X_SPAN_X8 * (2.0 / 3.0);
const ZOOM_DURATION_SECS: u64 = 36;
const MANDELBROT_PALETTE: [([u8; 3], f32); 15] = [
    ([0xFF, 0xF0, 0xFF], 4.0),  // pink-ice
    ([0xFF, 0xD6, 0xFF], 8.0),  // pink-cloud
    ([0xFF, 0xB8, 0xFF], 12.0), // pink-cotton
    ([0xFF, 0x8C, 0xFF], 16.0), // bubblegum
    ([0xFF, 0x63, 0xFF], 20.0), // candy-pink
    ([0xFF, 0x37, 0xFF], 24.0), // neon-pink
    ([0xFF, 0x00, 0xF7], 28.0), // laser-magenta
    ([0xF0, 0x00, 0xE6], 32.0), // hot-fuchsia
    ([0xD6, 0x00, 0xD6], 36.0), // vivid-fuchsia
    ([0xBF, 0x00, 0xBF], 40.0), // orchid
    ([0xA0, 0x00, 0xC8], 44.0), // electric-purple
    ([0x8A, 0x00, 0xB8], 48.0), // violet-magenta
    ([0x70, 0x00, 0x8F], 52.0), // deep-violet
    ([0x52, 0x00, 0x66], 58.0), // dark-plum
    ([0x33, 0x00, 0x33], 64.0), // midnight-plum
];

fn emit_palette_immediates(shader: &mut String) {
    for (idx, (rgb, threshold)) in MANDELBROT_PALETTE.iter().enumerate() {
        let r = rgb[0] as f32 / 255.0;
        let g = rgb[1] as f32 / 255.0;
        let b = rgb[2] as f32 / 255.0;
        let _ = writeln!(
            shader,
            "IMM[{}] FLT32 {{ {:.6}, {:.6}, {:.6}, {:.6} }}",
            idx, r, g, b, threshold
        );
    }
}

pub fn build_fragment_shader_tgsi_unrolled(iterations: u32) -> String {
    let mut shader = String::new();
    shader.push_str("FRAG\n");
    shader.push_str("DCL IN[0], TEXCOORD[0], LINEAR\n");
    shader.push_str("DCL IN[1], COLOR, LINEAR\n");
    shader.push_str("DCL OUT[0], COLOR\n");
    shader.push_str("DCL TEMP[0]\n");
    shader.push_str("DCL TEMP[1]\n");
    shader.push_str("DCL TEMP[2]\n");
    shader.push_str("DCL TEMP[3]\n");
    shader.push_str("DCL TEMP[4]\n");
    shader.push_str("DCL TEMP[5]\n");
    emit_palette_immediates(&mut shader);

    let mut line = 0u32;
    let push = |text: &str, out: &mut String, line_no: &mut u32| {
        let _ = writeln!(out, "    {}: {}", *line_no, text);
        *line_no = line_no.wrapping_add(1);
    };

    // Derive constants from the incoming white vertex color so we do not need TGSI immediates.
    push("MOV TEMP[0].x, IN[1].xxxx", &mut shader, &mut line); // 1
    push("ADD TEMP[0].y, TEMP[0].xxxx, TEMP[0].xxxx", &mut shader, &mut line); // 2
    push("ADD TEMP[0].z, TEMP[0].yyyy, TEMP[0].xxxx", &mut shader, &mut line); // 3
    push("ADD TEMP[0].w, TEMP[0].yyyy, TEMP[0].yyyy", &mut shader, &mut line); // 4
    push("SUB TEMP[1].w, TEMP[0].xxxx, TEMP[0].xxxx", &mut shader, &mut line); // 0

    // Map uv -> complex plane: x=-2..1, y=1..-1.
    push("MUL TEMP[1].x, IN[0].xxxx, TEMP[0].zzzz", &mut shader, &mut line); // u*3
    push("SUB TEMP[1].x, TEMP[1].xxxx, TEMP[0].yyyy", &mut shader, &mut line); // -2 + u*3
    push("MUL TEMP[1].y, IN[0].yyyy, TEMP[0].yyyy", &mut shader, &mut line); // v*2
    push("SUB TEMP[1].y, TEMP[0].xxxx, TEMP[1].yyyy", &mut shader, &mut line); // 1 - v*2

    push("MOV TEMP[2].x, TEMP[1].wwww", &mut shader, &mut line); // zr = 0
    push("MOV TEMP[2].y, TEMP[1].wwww", &mut shader, &mut line); // zi = 0
    push("MOV TEMP[2].z, TEMP[0].xxxx", &mut shader, &mut line); // alive = 1
    push("MOV TEMP[2].w, TEMP[1].wwww", &mut shader, &mut line); // iter sum = 0

    for _ in 0..iterations {
        push("MUL TEMP[3].x, TEMP[2].xxxx, TEMP[2].xxxx", &mut shader, &mut line);
        push("MUL TEMP[3].y, TEMP[2].yyyy, TEMP[2].yyyy", &mut shader, &mut line);
        push("MUL TEMP[3].z, TEMP[2].xxxx, TEMP[2].yyyy", &mut shader, &mut line);
        push("ADD TEMP[3].w, TEMP[3].xxxx, TEMP[3].yyyy", &mut shader, &mut line);
        push("SLT TEMP[4].x, TEMP[3].wwww, TEMP[0].wwww", &mut shader, &mut line); // mag2 < 4
        push("MUL TEMP[4].x, TEMP[4].xxxx, TEMP[2].zzzz", &mut shader, &mut line); // alive mask
        push("SUB TEMP[4].y, TEMP[0].xxxx, TEMP[4].xxxx", &mut shader, &mut line); // inverse mask
        push("
[... truncated in extract ...]

[2026-05-11T01:32:05.221Z] tool-call:exec_command line=120
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"render_mandelbrot|queue_texture_draw_mandelbrot|TextureDrawMandelbrot|MANDELBROT_PIPELINE_FS_TAG_RAW|draw_mandelbrot\" src/r src/gfx","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":24000}

[2026-05-11T01:32:05.264Z] tool-output line=125
------------------------------------------------------------------------------------------------
Chunk ID: bfc737
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 354
Output:
src/gfx/virtio_gpu_3d.rs:2906:                        == Some(crate::gfx::mandelbrot::MANDELBROT_PIPELINE_FS_TAG_RAW)
src/gfx/mod.rs:143:            Self::DrawMandelbrot => "draw_mandelbrot",
src/gfx/backends/intel.rs:678:                Some(crate::gfx::mandelbrot::MANDELBROT_PIPELINE_FS_TAG_RAW) => {
src/gfx/mandelbrot.rs:8:pub const MANDELBROT_PIPELINE_FS_TAG_RAW: u32 = 0x4D44_4C42;
src/r/gfx_cabi.rs:1785:    struct TextureDrawMandelbrotReq {
src/r/gfx_cabi.rs:1806:        DrawMandelbrot(TextureDrawMandelbrotReq),
src/r/gfx_cabi.rs:2211:    fn enqueue_texture_draw_mandelbrot(req: TextureDrawMandelbrotReq) {
src/r/gfx_cabi.rs:2507:    pub fn queue_render_mandelbrot_to_texture(
src/r/gfx_cabi.rs:2528:        enqueue_texture_draw_mandelbrot(TextureDrawMandelbrotReq {
src/r/gfx_cabi.rs:2745:                    let rc = render_mandelbrot_to_texture_now(req.tex_id, req.ticks, req.tick_hz);
src/r/gfx_cabi.rs:4629:                    ShaderId::from_raw(crate::gfx::mandelbrot::MANDELBROT_PIPELINE_FS_TAG_RAW)
src/r/gfx_cabi.rs:4921:    fn render_mandelbrot_to_texture_now(tex_id: u32, ticks: u64, tick_hz: u64) -> i32 {
src/r/gfx_cabi.rs:4991:                    if !check_submit_budget(usable, cmds.len(), "draw_mandelbrot_to_texture") {
src/r/gfx_cabi.rs:6138:    pub extern "C" fn trueos_cabi_gfx_queue_render_mandelbrot_to_texture(
src/r/gfx_cabi.rs:6144:        if queue_render_mandelbrot_to_texture(

[2026-05-11T01:32:05.264Z] tool-output line=126
------------------------------------------------------------------------------------------------
Chunk ID: fa2307
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 585
Output:
        }
        if !claim_current_vm_texture_id(tex_id, "queue-draw-rgb-owner") {
            return false;
        }
        let host_tex_id = host_texture_id_for_current_context(tex_id);
        if host_tex_id == 0 || reject_unreasonable_tex_id(host_tex_id, "queue-draw-rgb-host") {
            return false;
        }
        if vtx.is_empty() {
            return true;
        }
        const VTX_SIZE: usize = 12;
        let usable = vtx.len() - (vtx.len() % VTX_SIZE);
        if usable == 0 {
            return false;
        }

        enqueue_texture_draw_rgb(TextureDrawRgbReq {
            tex_id: host_tex_id,
            clear_rgb,
            verts: vtx[..usable].to_vec(),
            repaint_window_id,
            repaint_reason,
        });
        true
    }

    pub fn queue_render_mandelbrot_to_texture(
        tex_id: u32,
        ticks: u64,
        tick_hz: u64,
        repaint_window_id: u32,
        repaint_reason: &'static str,
    ) -> bool {
        if tex_id == 0 {
            return false;
        }
        if reject_unreasonable_tex_id(tex_id, "queue-draw-mandelbrot") {
            return false;
        }
        if !claim_current_vm_texture_id(tex_id, "queue-draw-mandelbrot-owner") {
            return false;
        }
        let host_tex_id = host_texture_id_for_current_context(tex_id);
        if host_tex_id == 0 || reject_unreasonable_tex_id(host_tex_id, "queue-draw-mandelbrot-host")
        {
            return false;
        }
        enqueue_texture_draw_mandelbrot(TextureDrawMandelbrotReq {
            tex_id: host_tex_id,
            ticks,
            tick_hz,
            repaint_window_id,
            repaint_reason,
        });
        true
    }

    pub fn queue_render_tex_triangles_to_texture_copy(
        target_tex_id: u32,
        source_tex_id: u32,
        clear_rgb: u32,
        vtx: &[u8],
        repaint_window_id: u32,
        repaint_reason: &'static str,
    ) -> bool {
        if target_tex_id == 0 || source_tex_id == 0 {
            return false;
        }
        if reject_unreasonable_tex_pair(target_tex_id, source_tex_id, "queue-draw-tex") {
            return false;
        }
        if !claim_current_vm_texture_id(target_tex_id, "queue-draw-tex-target-owner") {
            return false;
        }
        if source_tex_id != target_tex_id

[2026-05-11T01:32:05.282Z] tool-output line=127
------------------------------------------------------------------------------------------------
Chunk ID: 8e1f6a
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 694
Output:
        }
        if !st.swapchain_configured || st.swapchain_desc != desired_swap {
            ctx.configure_swapchain(desired_swap).ok()?;
            st.swapchain_desc = desired_swap;
            st.swapchain_configured = true;
            st.viewport_configured = false;
        }

        let mut pipeline_id = match pipeline_kind {
            TexPipelineKind::Mask => st.tex_pipeline_mask,
            TexPipelineKind::Rgba => st.tex_pipeline_rgba,
            TexPipelineKind::Particle => st.tex_pipeline_particle,
            TexPipelineKind::Mandelbrot => st.tex_pipeline_mandelbrot,
        };

        if !pipeline_id.is_valid() {
            let layout = VertexLayout {
                stride: 20, // f32 x,y, f32 u,v, u8 r,g,b,a
                pos_offset: 0,
                color_offset: 16,
                color_format: ColorFormat::RgbaU8,
                texcoord_offset: 8,
                texcoord_format: TexCoordFormat::UvF32,
            };
            let fs_tag = match pipeline_kind {
                TexPipelineKind::Mask => ShaderId::from_raw(TEX_PIPELINE_FS_MASK_TAG_RAW),
                TexPipelineKind::Rgba => ShaderId::from_raw(TEX_PIPELINE_FS_RGBA_TAG_RAW),
                TexPipelineKind::Particle => ShaderId::from_raw(TEX_PIPELINE_FS_PARTICLE_TAG_RAW),
                TexPipelineKind::Mandelbrot => {
                    ShaderId::from_raw(crate::gfx::mandelbrot::MANDELBROT_PIPELINE_FS_TAG_RAW)
                }
            };
            let p = ctx
                .create_pipeline(PipelineDesc {
                    vertex_layout: layout,
                    vs: None,
                    fs: Some(fs_tag),
                })
                .ok()?;
            pipeline_id = p;
            match pipeline_kind {
                TexPipelineKind::Mask => st.tex_pipeline_mask = p,
                TexPipelineKind::Rgba => st.tex_pipeline_rgba = p,
                TexPipelineKind::Particle => st.tex_pipeline_particle = p,
                TexPipelineKind::Mandelbrot => st.tex_pipeline_mandelbrot = p,
            }
        }

        let idx = st.ring_idx % GFX_CABI_VBUF_RING_LEN;
        let cur = st.tex_vbuf[idx];
        let cur_cap = st.tex_capacity[idx];
        if !cur.is_valid() || cur_cap < need_bytes {
            if cur.is_valid() {
                ctx.destroy_buffer(cur);
                st.tex_vbuf[idx] = BufferId::invalid();
                st.tex_capacity[idx] = 0;
            }
            let cap = need_bytes.max(256);
            let b = ctx
                .create_buffer(BufferDesc {
                    size: cap as u64,
                    usage: BufferUsage::Vertex,
                    memory: MemoryType::HostVisible,
                })
                .ok()?;
            st.tex_vbuf[idx] = b;

[2026-05-11T01:32:05.287Z] tool-output line=128
------------------------------------------------------------------------------------------------
Chunk ID: f18bf6
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 835
Output:
                        req.height,
                        req.region,
                        req.rgba,
                        req.sample_kind,
                    );
                    if req.update_async_status {
                        if rc == 0 {
                            set_async_tex_status(req.tex_id, ASYNC_TEX_STATUS_READY);
                        } else {
                            set_async_tex_status(req.tex_id, rc);
                        }
                    }
                    if rc == 0 {
                        request_texture_work_present(req.repaint_window_id, req.repaint_reason);
                    } else {
                        log_texture_work_failed(
                            "upload",
                            rc,
                            req.tex_id,
                            0,
                            req.repaint_window_id,
                            req.repaint_reason,
                        );
                    }
                }
                TextureWorkReq::DrawRgb(req) => {
                    let rc = render_rgb_triangles_to_texture_now(
                        req.tex_id,
                        req.clear_rgb,
                        req.verts.as_slice(),
                    );
                    if rc == 0 {
                        request_texture_work_present(req.repaint_window_id, req.repaint_reason);
                    } else {
                        log_texture_work_failed(
                            "draw-rgb",
                            rc,
                            req.tex_id,
                            0,
                            req.repaint_window_id,
                            req.repaint_reason,
                        );
                    }
                }
                TextureWorkReq::DrawMandelbrot(req) => {
                    let rc = render_mandelbrot_to_texture_now(req.tex_id, req.ticks, req.tick_hz);
                    if rc == 0 {
                        request_texture_work_present(req.repaint_window_id, req.repaint_reason);
                    } else {
                        log_texture_work_failed(
                            "draw-mandelbrot",
                            rc,
                            req.tex_id,
                            0,
                            req.repaint_window_id,
                            req.repaint_reason,
                        );
                    }
                }
                TextureWorkReq::DrawTex(req) => {
                    let rc = render_tex_triangles_to_texture_now(
                        req.target_tex_id,
                        req.source_tex_id,
                        req.clear_rgb,
                        req.verts.as_slice(),
                        req.particle_shader,
                    );
                    if rc == 0 {
                        request_texture_work_present(req.repaint_window_id, req.repaint_reason);
                    } else {
                        log_texture_work_failed(
                            "draw-tex",
                            rc,
                            req.target_tex_id,
                            req.source_tex_id,
                            req.repaint_window_id,
                            req.repaint_reason,
                        );
                    }

[2026-05-11T01:32:08.217Z] tool-output line=131
------------------------------------------------------------------------------------------------
Chunk ID: 186e32
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1005
Output:
                            entry.height,
                            None,
                            trueos_gfx_core::BlendDesc::disabled(),
                            v0,
                            v1,
                            v2,
                        );
                        off += 3 * RGB_VERTEX_SIZE;
                    }
                }
                st.ring_idx = (st.ring_idx + 1) % GFX_CABI_VBUF_RING_LEN;
                st.viewport_configured = false;
            }
            ret
        })
    }

    pub fn render_rgb_triangles_to_texture(tex_id: u32, clear_rgb: u32, vtx: &[u8]) -> i32 {
        render_rgb_triangles_to_texture_now(tex_id, clear_rgb, vtx)
    }

    fn render_mandelbrot_to_texture_now(tex_id: u32, ticks: u64, tick_hz: u64) -> i32 {
        crate::gfx::init(None);

        if tex_id == 0 {
            return -1;
        }
        if reject_unreasonable_tex_id(tex_id, "render-mandelbrot-now") {
            return -8;
        }
        if !crate::gfx::is_virgl_active() {
            return -2;
        }

        let verts = crate::gfx::mandelbrot::fullscreen_quad_rgba_bytes_for_view(ticks, tick_hz);
        let usable = verts.len();
        if usable == 0 {
            return -3;
        }

        let (image, width, height) = {
            let st = GFX_CABI_STATE.lock();
            let idx = tex_id.saturating_sub(1) as usize;
            let Some(entry) = st
                .tex_images
                .as_ref()
                .and_then(|images| images.get(idx))
                .and_then(|entry| entry.as_ref())
            else {
                return -6;
            };
            (entry.image, entry.width.max(1), entry.height.max(1))
        };

        if !image.is_valid() {
            return -7;
        }

        crate::gfx::with_cabi_frame_lock(|| {
            let Some(ret) =
                crate::gfx::with_context_tag(crate::gfx::SystemLockOwner::DrawMandelbrot, |ctx| {
                    let (pipeline, vbuf, _) =
                        match ensure_gfx_resources_tex(ctx, usable, TexPipelineKind::Mandelbrot) {
                            Some(v) => v,
                            None => return -4,
                        };

                    if ctx.write_buffer(vbuf, 0, verts.as_slice()).is_err() {
                        return -5;
                    }

                    let cmds = [
                        Command::SetRenderTarget(Some(image)),
                        Command::SetViewport(Viewport {
                            x: 0,
                            y: 0,
                            width: width as i32,
                            height: height as i32,
                        }),
                        Command::SetScissor(None),
                        Command::SetBlend(trueos_gfx_core::BlendDesc::disabled()),
                        Command::BindPipeline(pipeline),
                        Command::BindVertexBuffer {
                            buffer: vbuf,
                            offset: 0,
                        },
                        Command::Draw {
                            vertex_count: 6,
                            first_vertex: 0,
                        },
                    ];
                    if !check_submit_budget(usable, cmds.len(), "draw_mandelbrot_to_texture") {
                        return -8;
                    }
                    if ctx.submit(CommandBuffer { commands: &cmds }).is_err() {
                        return -9;
                    }
                    0
                })
            else {
                return -10;
            };
            if ret == 0 {
                let mut st = GFX_CABI_STATE.lock();
                st.ring_idx = (st.ring_idx + 1) % GFX_CABI_VBUF_RING_LEN;
                st.viewport_configured = false;
            }
            ret
        })
    }

    fn render_tex_triangles_to_texture_now(
        target_tex_id: u32,
        source_tex_id: u32,
        clear_rgb: u32,
        vtx: &[u8],

[2026-05-11T01:32:12.340Z] tool-call:exec_command line=133
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1,220p' src/gfx/backends/intel.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":18000}

[2026-05-11T01:32:12.347Z] tool-call:exec_command line=134
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '620,720p' src/gfx/backends/intel.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":16000}

[2026-05-11T01:32:12.362Z] tool-call:exec_command line=135
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '720,860p' src/gfx/backends/intel.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":18000}

[2026-05-11T01:32:12.378Z] tool-output line=138
------------------------------------------------------------------------------------------------
Chunk ID: d6e655
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1706
Output:
extern crate alloc;

use alloc::vec::Vec;
use core::sync::atomic::{AtomicU32, Ordering};
use libm::{ceilf, floorf};
use trueos_gfx_core::{
    BlendDesc, BlendFactor, BufferDesc, BufferId, BufferUsage, Command, CommandBuffer, DeviceCaps,
    Error, FenceId, GfxDevice, GfxPresent, ImageDesc, ImageFormat, ImageId, ImageRegion,
    MemoryType, PipelineDesc, PipelineId, Result, SamplerDesc, SamplerFilter, SamplerWrap,
    ScissorRect, ShaderDesc, ShaderId, SwapchainDesc, TexCoordFormat, read_rgb_vertex_f32_bytes,
    read_tex_vertex_f32_bytes,
};

const TEX_PIPELINE_FS_MASK_TAG_RAW: u32 = 0x4D41_534B;
const TEX_PIPELINE_FS_RGBA_TAG_RAW: u32 = 0x5247_4241;
const TEX_PIPELINE_FS_PARTICLE_TAG_RAW: u32 = 0x5052_5443;
const RCS_PRESENT_RETRY_COOLDOWN_PRESENTS: u32 = 600;
const IMAGE_GPU_VA_BASE: u64 = 0x0400_0000;
const IMAGE_GPU_VA_ALIGN: u64 = 0x0040_0000;
const MAX_BACKEND_IMAGE_DIM: u32 = 8192;
const MAX_BACKEND_IMAGE_BYTES: usize = 256 * 1024 * 1024;

static PRESENT_COMPLETED_SEQ: AtomicU32 = AtomicU32::new(0);

#[inline]
pub(crate) fn present_completed_seq() -> u32 {
    PRESENT_COMPLETED_SEQ.load(Ordering::Acquire)
}

#[inline]
fn mark_present_completed(seq: u32) {
    PRESENT_COMPLETED_SEQ.store(seq, Ordering::Release);
}

#[derive(Clone)]
struct BufferEntry {
    bytes: Vec<u8>,
    usage: BufferUsage,
    memory: MemoryType,
}

#[derive(Clone)]
struct ImageEntry {
    width: u32,
    height: u32,
    format: ImageFormat,
    gpu_addr: u64,
    gpu_dirty: bool,
    rgba: Vec<u8>,
}

#[derive(Clone, Copy, Debug, Eq, PartialEq)]
enum PipelineKind {
    Rgb,
    TexMask,
    TexRgba,
    TexParticle,
    Mandelbrot,
}

#[derive(Clone)]
struct PipelineEntry {
    desc: PipelineDesc,
    kind: PipelineKind,
}

#[derive(Clone, Copy)]
enum RenderTarget {
    Screen,
    Image(ImageId),
}

pub struct IntelGfxBackend {
    swapchain_desc: SwapchainDesc,
    framebuffer_ptr: *mut u8,
    framebuffer_pitch: usize,
    framebuffer_width: u32,
    framebuffer_height: u32,
    screen_rgba: Vec<u8>,
    screen_rgba_gpu_dirty: bool,
    screen_scanout_rgba: Vec<u8>,
    screen_scanout_gpu_dirty: bool,
    buffers: Vec<Option<BufferEntry>>,
    images: Vec<Option<ImageEntry>>,
    pipelines: Vec<Option<PipelineEntry>>,
    next_shader_raw: u32,
    next_fence_raw: u64,
    submit_seq: u32,
    present_seq: u32,
    rcs_retry_after_present_seq: u32,
    rcs_present_failures: u32,
    next_image_gpu_addr: u64,
}

unsafe impl Send for IntelGfxBackend {}

impl IntelGfxBackend {
    pub fn init(framebuffers: Option<&'static crate::limine::FramebufferResponse>) -> Option<Self> {
        if !crate::intel::has_claimed_device() {
            return None;
        }

        let fb = *framebuffers?.framebuffers().first()?;
        if fb.memory_model != ::limine::framebuffer::FRAMEBUFFER_RGB
            || fb.bpp != 32
            || fb.address().is_null()
        {
            return None;
        }

        let framebuffer_width = fb.width() as u32;
        let framebuffer_height = fb.height() as u32;
        let (width, height) = crate::intel::active_scanout_dimensions()
            .unwrap_or((framebuffer_width, framebuffer_height));
        let pitch = fb.pitch() as usize;
        if framebuffer_width == 0
            || framebuffer_height == 0
            || pitch < framebuffer_width as usize * 4
        {
            return None;
        }

        let swapchain_desc = SwapchainDesc {
            format: ImageFormat::Rgbx8888,
            extent: trueos_gfx_core::Extent2D { width, height },
        };
        let screen_len = rgba_len(width, height)?;
        Some(Self {
            swapchain_desc,
            framebuffer_ptr: fb.addr() as *mut u8,
            framebuffer_pitch: pitch,
            framebuffer_width,
            framebuffer_height,
            screen_rgba: alloc::vec![0; screen_len],
            screen_rgba_gpu_dirty: false,
            screen_scanout_rgba: alloc::vec![0; screen_len],
            screen_scanout_gpu_dirty: false,
            buffers: Vec::new(),
            images: Vec::new(),
            pipelines: Vec::new(),
            next_shader_raw: 1,
            next_fence_raw: 1,
            submit_seq: 0,
            present_seq: 0,
            rcs_retry_after_present_seq: 0,
            rcs_present_failures: 0,
            next_image_gpu_addr: IMAGE_GPU_VA_BASE,
        })
    }

    fn ensure_screen_rgba(&mu
[... truncated in extract ...]

[2026-05-11T01:32:12.384Z] tool-output line=139
------------------------------------------------------------------------------------------------
Chunk ID: 467e02
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 848
Output:
                    v1,
                    v2,
                );
                off += 3 * trueos_gfx_core::TEX_VERTEX_SIZE;
            }
        })?;
        Ok(())
    }
}

impl GfxDevice for IntelGfxBackend {
    fn caps(&self) -> DeviceCaps {
        DeviceCaps {
            supports_rgbx8888: true,
            supports_host_visible_buffers: true,
            supports_scissor: true,
        }
    }

    fn create_buffer(&mut self, desc: BufferDesc) -> Result<BufferId> {
        let size = usize::try_from(desc.size).map_err(|_| Error::Invalid)?;
        let id = Self::alloc_slot(
            &mut self.buffers,
            BufferEntry {
                bytes: alloc::vec![0; size],
                usage: desc.usage,
                memory: desc.memory,
            },
        );
        Ok(BufferId::from_raw(id))
    }

    fn destroy_buffer(&mut self, id: BufferId) {
        if let Some(slot) = id
            .raw()
            .checked_sub(1)
            .and_then(|idx| self.buffers.get_mut(idx as usize))
        {
            *slot = None;
        }
    }

    fn create_shader(&mut self, _desc: ShaderDesc<'_>) -> Result<ShaderId> {
        let id = self.next_shader_raw;
        self.next_shader_raw = self.next_shader_raw.wrapping_add(1).max(1);
        Ok(ShaderId::from_raw(id))
    }

    fn destroy_shader(&mut self, _id: ShaderId) {}

    fn create_pipeline(&mut self, desc: PipelineDesc) -> Result<PipelineId> {
        let kind = if desc.vertex_layout.texcoord_format == TexCoordFormat::None {
            PipelineKind::Rgb
        } else {
            match desc.fs.map(|id| id.raw()) {
                Some(TEX_PIPELINE_FS_MASK_TAG_RAW) => PipelineKind::TexMask,
                Some(TEX_PIPELINE_FS_RGBA_TAG_RAW) => PipelineKind::TexRgba,
                Some(TEX_PIPELINE_FS_PARTICLE_TAG_RAW) => PipelineKind::TexParticle,
                Some(crate::gfx::mandelbrot::MANDELBROT_PIPELINE_FS_TAG_RAW) => {
                    PipelineKind::Mandelbrot
                }
                _ => PipelineKind::TexRgba,
            }
        };
        let id = Self::alloc_slot(&mut self.pipelines, PipelineEntry { desc, kind });
        Ok(PipelineId::from_raw(id))
    }

    fn destroy_pipeline(&mut self, id: PipelineId) {
        if let Some(slot) = id
            .raw()
            .checked_sub(1)
            .and_then(|idx| self.pipelines.get_mut(idx as usize))
        {
            *slot = None;
        }
    }

    fn create_image(&mut self, desc: ImageDesc) -> Result<ImageId> {
        let len = rgba_len(desc.width, desc.height).ok_or(Error::Invalid)?;
        if desc.width > MAX_BACKEND_IMAGE_DIM
            || desc.height > MAX_BACKEND_IMAGE_DIM
            || len > MAX_BACKEND_IMAGE_BYTES
        {
            crate::log!(
                "intel/gfx-backend: reject image create size={}x{} bytes={} format={:?}\n",
                desc.width,
                desc.height,
                len,
                desc.format
            );
            return Err(Error::Invalid);
        }
        let gpu_addr = self.next_image_gpu_addr;
        let alloc_span = u64::try_from(len).map_err(|_| Error::Invalid)?;
        self.next_image_gpu_addr = align_up_u64(
            gpu_addr
                .saturating_add(alloc_span)
                .saturating_add(IMAGE_GPU_VA_ALIGN.saturating_sub(1)),
            IMAGE_GPU_VA_ALIGN,
        );

[2026-05-11T01:32:12.396Z] tool-output line=140
------------------------------------------------------------------------------------------------
Chunk ID: 7c81f4
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1433
Output:
        );
        let id = Self::alloc_slot(
            &mut self.images,
            ImageEntry {
                width: desc.width,
                height: desc.height,
                format: desc.format,
                gpu_addr,
                gpu_dirty: false,
                rgba: alloc::vec![0; len],
            },
        );
        Ok(ImageId::from_raw(id))
    }

    fn destroy_image(&mut self, id: ImageId) {
        if let Some(slot) = id
            .raw()
            .checked_sub(1)
            .and_then(|idx| self.images.get_mut(idx as usize))
        {
            *slot = None;
        }
    }

    fn write_image(&mut self, id: ImageId, data: &[u8]) -> Result<()> {
        let image = self.image_mut(id).ok_or(Error::NotFound)?;
        let len = rgba_len(image.width, image.height).ok_or(Error::Invalid)?;
        if data.len() < len {
            return Err(Error::Invalid);
        }
        image.rgba[..len].copy_from_slice(&data[..len]);
        if image.format == ImageFormat::Rgbx8888 {
            force_opaque_alpha(image.rgba.as_mut_slice());
        }
        image.gpu_dirty = false;
        Ok(())
    }

    fn write_image_region(&mut self, id: ImageId, region: ImageRegion, data: &[u8]) -> Result<()> {
        let image = self.image_mut(id).ok_or(Error::NotFound)?;
        if region.width == 0
            || region.height == 0
            || region.x.saturating_add(region.width) > image.width
            || region.y.saturating_add(region.height) > image.height
        {
            return Err(Error::Invalid);
        }
        let need = rgba_len(region.width, region.height).ok_or(Error::Invalid)?;
        if data.len() < need {
            return Err(Error::Invalid);
        }
        for row in 0..region.height as usize {
            let src_off = row.saturating_mul(region.width as usize).saturating_mul(4);
            let dst_off = ((region.y as usize + row)
                .saturating_mul(image.width as usize)
                .saturating_add(region.x as usize))
            .saturating_mul(4);
            let row_len = region.width as usize * 4;
            image.rgba[dst_off..dst_off + row_len]
                .copy_from_slice(&data[src_off..src_off + row_len]);
        }
        if image.format == ImageFormat::Rgbx8888 {
            force_opaque_alpha(image.rgba.as_mut_slice());
        }
        image.gpu_dirty = false;
        Ok(())
    }

    fn write_buffer(&mut self, id: BufferId, offset: u64, data: &[u8]) -> Result<()> {
        let buffer = self.buffer_mut(id).ok_or(Error::NotFound)?;
        if buffer.memory != MemoryType::HostVisible {
            return Err(Error::Unsupported);
        }
        let offset = usize::try_from(offset).map_err(|_| Error::Invalid)?;
        let end = offset.saturating_add(data.len());
        if end > buffer.bytes.len() {
            return Err(Error::Invalid);
        }
        buffer.bytes[offset..end].copy_from_slice(data);
        Ok(())
    }

    fn submit(&mut self, cmds: CommandBuffer<'_>) -> Result<FenceId> {
        self.ensure_screen_rgba()?;

        let mut target = RenderTarget::Screen;
        let mut scissor: Option<ScissorRect> = None;
        let mut blend = BlendDesc::disabled();
        let mut sampler = SamplerDesc::default_2d();
        let mut pipeline = PipelineKind::Rgb;
        let mut bound_buffer = BufferId::invalid();
        let mut bound_offset = 0u64;
        let mut bound_image = ImageId::invalid();
        let mut unsupported = 0u32;

        for cmd in cmds.commands {
            match *cmd {
                Command::ClearColor { rgb } => {
                    let fast_ok = match target {
                        RenderTarget::Screen => {
                            let screen_surface_gpu =
                                crate::intel::primary_present_surface_gpu_addr()
                                    .unwrap_or(0x0200_0000);
                            crate::intel::rcs_clear_rgba_surface(
                                self.screen_rgba.as_slice(),
                                self.swapchain_desc.extent.width,
                                self.swapchain_desc.extent.height,
                                screen_surface_gpu,
                                rgb,
                            )
                            .then_some((
                                self.swapchain_desc.extent.width,
[... truncated in extract ...]

[2026-05-11T01:32:12.396Z] tool-output line=141
------------------------------------------------------------------------------------------------
Chunk ID: 92a311
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 481
Output:
    UploadTexture = 2,
    EndFrame = 3,
    CursorQueryViewport = 4,
    CursorEndFrame = 5,
    DrawMandelbrot = 6,
    FinalizeBackendInit = 7,
    WithFramebuffers = 8,
    IsVirglActive = 9,
    IsIntelActive = 10,
    SwitchToVirgl = 11,
    BackendKind = 12,
}

impl SystemLockOwner {
    #[inline]
    fn as_str(self) -> &'static str {
        match self {
            Self::Unknown => "unknown",
            Self::DrawRgbTriangles => "draw_rgb_triangles",
            Self::UploadTexture => "upload_texture",
            Self::EndFrame => "end_frame",
            Self::CursorQueryViewport => "cursor_query_viewport",
            Self::CursorEndFrame => "cursor_end_frame",
            Self::DrawMandelbrot => "draw_mandelbrot",
            Self::FinalizeBackendInit => "finalize_backend_init",
            Self::WithFramebuffers => "with_framebuffers",
            Self::IsVirglActive => "is_virgl_active",
            Self::IsIntelActive => "is_intel_active",
            Self::SwitchToVirgl => "switch_to_virgl",
            Self::BackendKind => "backend_kind",
        }
    }

    #[inline]
    fn from_raw(raw: u32) -> Self {
        match raw {
            x if x == Self::DrawRgbTriangles as u32 => Self::DrawRgbTriangles,
            x if x == Self::UploadTexture as u32 => Self::UploadTexture,
            x if x == Self::EndFrame as u32 => Self::EndFrame,
            x if x == Self::CursorQueryViewport as u32 => Self::CursorQueryViewport,
            x if x == Self::CursorEndFrame as u32 => Self::CursorEndFrame,
            x if x == Self::DrawMandelbrot as u32 => Self::DrawMandelbrot,
            x if x == Self::FinalizeBackendInit as u32 => Self::FinalizeBackendInit,
            x if x == Self::WithFramebuffers as u32 => Self::WithFramebuffers,
            x if x == Self::IsVirglActive as u32 => Self::IsVirglActive,
            x if x == Self::IsIntelActive as u32 => Self::IsIntelActive,

[2026-05-11T01:32:14.163Z] tool-call:exec_command line=142
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '860,1140p' src/gfx/backends/intel.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":30000}

[2026-05-11T01:32:14.207Z] tool-output line=144
------------------------------------------------------------------------------------------------
Chunk ID: 2c21c6
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 2340
Output:
                        }
                        if self.submit_seq <= 8 || self.submit_seq.is_multiple_of(120) {
                            crate::log!(
                                "intel/gfx-backend: clear mode=rcs-store size={}x{} rgb=0x{:06X} gpu=0x{:X}\n",
                                target_w,
                                target_h,
                                rgb & 0x00FF_FFFF,
                                target_gpu_addr
                            );
                        }
                    } else {
                        if matches!(target, RenderTarget::Screen) {
                            self.sync_screen_rgba_from_gpu();
                        }
                        self.with_target_mut(target, |rgba, width, height| {
                            clear_rgba_buffer(rgba, width, height, rgb);
                        })?;
                    }
                }
                Command::ClearRect {
                    rgb,
                    x,
                    y,
                    width,
                    height,
                } => {
                    if matches!(target, RenderTarget::Screen) {
                        self.sync_screen_rgba_from_gpu();
                    }
                    self.with_target_mut(target, |rgba, target_w, target_h| {
                        let mut x0 = x.min(target_w);
                        let mut y0 = y.min(target_h);
                        let mut x1 = x.saturating_add(width).min(target_w);
                        let mut y1 = y.saturating_add(height).min(target_h);
                        if let Some(scissor) = scissor {
                            x0 = x0.max(scissor.x.min(target_w));
                            y0 = y0.max(scissor.y.min(target_h));
                            x1 = x1.min(scissor.x.saturating_add(scissor.width).min(target_w));
                            y1 = y1.min(scissor.y.saturating_add(scissor.height).min(target_h));
                        }
                        if x0 < x1 && y0 < y1 {
                            clear_rgba_rect(
                                rgba,
                                target_w,
                                target_h,
                                x0,
                                y0,
                                x1 - x0,
                                y1 - y0,
                                rgb,
                            );
                        }
                    })?;
                }
                Command::BindPipeline(id) => {
                    if let Some(entry) = self.pipeline(id) {
                        let _layout = entry.desc.vertex_layout;
                        pipeline = entry.kind;
                    } else {
                        unsupported = unsupported.saturating_add(1);
                    }
                }
                Command::BindVertexBuffer { buffer, offset } => {
                    bound_buffer = buffer;
                    bound_offset = offset;
                }
                Command::BindImage(image) => {
                    bound_image = image;
                }
                Command::SetRenderTarget(render_target) => {
                    target = match render_target {
                        Some(image) => {
                            if self.image(image).is_some() {
                                RenderTarget::Image(image)
                            } else {
                                unsupported = unsupported.saturating_add(1);
                                RenderTarget::Screen
                            }
                        }
                        None => RenderTarget::Screen,
                    };
                }
                Command::SetSampler(next) => sampler = next,
                Command::SetBlend(next) => blend = next,
                Command::SetViewport(_viewport) => {}
                Command::SetScissor(next) => scissor = next,
                Command::Draw {
                    vertex_count,
                    first_vertex,
                } => {
                    if bound_buffer.is_valid() {
                        let draw_res = if bound_image.is_valid() || pipeline != PipelineKind::Rgb {
                            self.draw_tex(
                                target,
                                bound_buffer,
                                bound_offs
[... truncated in extract ...]

[2026-05-11T01:32:18.295Z] tool-call:exec_command line=145
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"fn draw_tex|fn draw_rgb|fn draw_mandelbrot|Mandelbrot|present_screen|present_image_target|primary_present_surface_gpu_addr|rcs_.*rgba|copy.*scanout|gpu_dirty\" src/gfx/backends/intel.rs src/intel/mod.rs src/intel/gpgpu.rs src/intel/display.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":32000}

[2026-05-11T01:32:18.307Z] tool-call:exec_command line=146
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '420,620p' src/gfx/backends/intel.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":20000}

[2026-05-11T01:32:18.316Z] tool-call:exec_command line=147
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '220,420p' src/gfx/backends/intel.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":22000}

[2026-05-11T01:32:18.343Z] tool-output line=149
------------------------------------------------------------------------------------------------
Chunk ID: 804f53
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 953
Output:
src/gfx/backends/intel.rs:48:    gpu_dirty: bool,
src/gfx/backends/intel.rs:58:    Mandelbrot,
src/gfx/backends/intel.rs:80:    screen_rgba_gpu_dirty: bool,
src/gfx/backends/intel.rs:82:    screen_scanout_gpu_dirty: bool,
src/gfx/backends/intel.rs:135:            screen_rgba_gpu_dirty: false,
src/gfx/backends/intel.rs:137:            screen_scanout_gpu_dirty: false,
src/gfx/backends/intel.rs:156:            self.screen_rgba_gpu_dirty = false;
src/gfx/backends/intel.rs:158:            self.screen_scanout_gpu_dirty = false;
src/gfx/backends/intel.rs:164:        if !self.screen_rgba_gpu_dirty || self.screen_rgba.is_empty() {
src/gfx/backends/intel.rs:168:        self.screen_rgba_gpu_dirty = false;
src/gfx/backends/intel.rs:173:        core::mem::swap(&mut self.screen_rgba_gpu_dirty, &mut self.screen_scanout_gpu_dirty);
src/gfx/backends/intel.rs:176:                .copy_from_slice(self.screen_scanout_rgba.as_slice());
src/gfx/backends/intel.rs:178:        self.screen_rgba_gpu_dirty = false;
src/gfx/backends/intel.rs:179:        self.screen_scanout_gpu_dirty = false;
src/gfx/backends/intel.rs:186:        if !image.gpu_dirty || image.rgba.is_empty() {
src/gfx/backends/intel.rs:190:        image.gpu_dirty = false;
src/gfx/backends/intel.rs:250:    fn present_screen_cpu_fallback(&mut self) -> Result<()> {
src/gfx/backends/intel.rs:286:    fn present_screen(&mut self) -> Result<()> {
src/gfx/backends/intel.rs:297:        if let Some(surface_gpu_addr) = crate::intel::primary_present_surface_gpu_addr() {
src/gfx/backends/intel.rs:331:            if crate::intel::rcs_present_rgba_frame(self.screen_rgba.as_slice(), copy_w, copy_h) {
src/gfx/backends/intel.rs:373:        let fallback = self.present_screen_cpu_fallback();
src/gfx/backends/intel.rs:390:    fn present_image_target(&mut self, id: ImageId) -> Result<()> {
src/gfx/backends/intel.rs:434:    fn draw_rgb(
src/gfx/backends/intel.rs:454:                    crate::intel::primary_present_surface_gpu_addr().unwrap_or(0x0200_0000);
src/gfx/backends/intel.rs:455:                crate::intel::rcs_draw_rgba_rgb_triangles(
src/gfx/backends/intel.rs:471:                crate::intel::rcs_draw_rgba_rgb_triangles(
src/gfx/backends/intel.rs:485:                RenderTarget::Screen => self.screen_rgba_gpu_dirty = true,
src/gfx/backends/intel.rs:488:                        image.gpu_dirty = true;
src/gfx/backends/intel.rs:530:    fn draw_tex(
src/gfx/backends/intel.rs:558:            PipelineKind::Mandelbrot => return Err(Error::Unsupported),
src/gfx/backends/intel.rs:562:            crate::intel::primary_present_surface_gpu_addr().unwrap_or(0x0200_0000);
src/gfx/backends/intel.rs:582:            self.screen_rgba_gpu_dirty = true;
src/gfx/backends/intel.rs:679:                    PipelineKind::Mandelbrot
src/gfx/backends/intel.rs:728:                gpu_dirty: false,
src/gfx/backends/intel.rs:755:        image.gpu_dirty = false;
src/gfx/backends/intel.rs:785:        image.gpu_dirty = false;
src/gfx/backends/intel.rs:822:                                crate::intel::primary_present_surface_gpu_addr()
src/gfx/backends/intel.rs:824:                            crate::intel::rcs_clear_rgba_surface(
src/gfx/backends/intel.rs:838:                            crate::intel::rcs_clear_rgba_surface(
src/gfx/backends/intel.rs:854:                            RenderTarget::Screen => self.screen_rgba_gpu_dirty = true,
src/gfx/backends/intel.rs:857:                                    image.gpu_dirty = true;
src/gfx/backends/intel.rs:984:                        RenderTarget::Screen => self.present_screen(),
src/gfx/backends/intel.rs:985:                        RenderTarget::Image(id) => self.present_image_target(id),
src/gfx/backends/intel.rs:1270:fn draw_rgb_triangle_rgba(
src/gfx/backends/intel.rs:1340:fn draw_tex_triangle_rgba(

[2026-05-11T01:32:18.343Z] tool-output line=150
------------------------------------------------------------------------------------------------
Chunk ID: 7d85cf
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1983
Output:
                        present_completed_seq(),
                        image.width,
                        image.height,
                        image.gpu_addr,
                        surface_gpu_addr
                    );
                }
                return Ok(());
            }
        }

        Err(Error::Unsupported)
    }

    fn draw_rgb(
        &mut self,
        target: RenderTarget,
        buffer: BufferId,
        byte_offset: u64,
        vertex_count: u32,
        first_vertex: u32,
        scissor: Option<ScissorRect>,
        blend: BlendDesc,
    ) -> Result<()> {
        let buffer = self.buffer(buffer).ok_or(Error::NotFound)?;
        let start = byte_offset as usize + first_vertex as usize * trueos_gfx_core::RGB_VERTEX_SIZE;
        let need = vertex_count as usize * trueos_gfx_core::RGB_VERTEX_SIZE;
        if start > buffer.bytes.len() || start.saturating_add(need) > buffer.bytes.len() {
            return Err(Error::Invalid);
        }
        let verts = buffer.bytes[start..start + need].to_vec();
        let fast_ok = match target {
            RenderTarget::Screen => {
                let screen_surface_gpu =
                    crate::intel::primary_present_surface_gpu_addr().unwrap_or(0x0200_0000);
                crate::intel::rcs_draw_rgba_rgb_triangles(
                    self.screen_rgba.as_slice(),
                    verts.as_slice(),
                    self.swapchain_desc.extent.width,
                    self.swapchain_desc.extent.height,
                    screen_surface_gpu,
                    scissor,
                    blend,
                )
                .then_some((
                    self.swapchain_desc.extent.width,
                    self.swapchain_desc.extent.height,
                    screen_surface_gpu,
                ))
            }
            RenderTarget::Image(id) => self.image(id).and_then(|image| {
                crate::intel::rcs_draw_rgba_rgb_triangles(
                    image.rgba.as_slice(),
                    verts.as_slice(),
                    image.width,
                    image.height,
                    image.gpu_addr,
                    scissor,
                    blend,
                )
                .then_some((image.width, image.height, image.gpu_addr))
            }),
        };
        if let Some((target_w, target_h, target_gpu_addr)) = fast_ok {
            match target {
                RenderTarget::Screen => self.screen_rgba_gpu_dirty = true,
                RenderTarget::Image(id) => {
                    if let Some(image) = self.image_mut(id) {
                        image.gpu_dirty = true;
                    }
                }
            }
            if self.submit_seq <= 8 || self.submit_seq.is_multiple_of(120) {
                crate::log!(
                    "intel/gfx-backend: draw-rgb mode=rcs-store triangles={} size={}x{} gpu=0x{:X}\n",
                    verts.len() / (3 * trueos_gfx_core::RGB_VERTEX_SIZE),
                    target_w,
                    target_h,
                    target_gpu_addr
                );
            }
            return Ok(());
        }

        if matches!(target, RenderTarget::Screen) {
            self.sync_screen_rgba_from_gpu();
        }
        self.with_target_mut(target, |rgba, width, height| {
            let mut off = 0usize;
            while off + (3 * trueos_gfx_core::RGB_VERTEX_SIZE) <= verts.len() {
                let Some(v0) = read_rgb_vertex_f32_bytes(&verts, off) else {
                    break;
                };
                let Some(v1) =
                    read_rgb_vertex_f32_bytes(&verts, off + trueos_gfx_core::RGB_VERTEX_SIZE)
                else {
                    break;
                };
                let Some(v2) =
                    read_rgb_vertex_f32_bytes(&verts, off + 2 * trueos_gfx_core::RGB_VERTEX_SIZE)
                else {
                    break;
                };
                draw_rgb_triangle_rgba(rgba, width, height, scissor, blend, v0, v1, v2);
                off += 3 * trueos_gfx_core::RGB_VERTEX_SIZE;
            }
        })?;
        Ok(())
    }

    fn draw_tex(
        &mut self,
        target: RenderTarget,
        buffer: BufferId,
        byte_offset: u64,
        vertex_count: u32,
        first_vertex: u32,
        source: ImageId,
        pipeline_kind: PipelineKind,
[... truncated in extract ...]

[2026-05-11T01:32:18.349Z] tool-output line=151
------------------------------------------------------------------------------------------------
Chunk ID: 7beeab
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 2030
Output:

    fn image_mut(&mut self, id: ImageId) -> Option<&mut ImageEntry> {
        let idx = id.raw().checked_sub(1)? as usize;
        self.images.get_mut(idx)?.as_mut()
    }

    fn pipeline(&self, id: PipelineId) -> Option<&PipelineEntry> {
        let idx = id.raw().checked_sub(1)? as usize;
        self.pipelines.get(idx)?.as_ref()
    }

    fn with_target_mut<R>(
        &mut self,
        target: RenderTarget,
        f: impl FnOnce(&mut [u8], u32, u32) -> R,
    ) -> Result<R> {
        match target {
            RenderTarget::Screen => {
                self.ensure_screen_rgba()?;
                let width = self.swapchain_desc.extent.width;
                let height = self.swapchain_desc.extent.height;
                Ok(f(self.screen_rgba.as_mut_slice(), width, height))
            }
            RenderTarget::Image(id) => {
                let image = self.image_mut(id).ok_or(Error::NotFound)?;
                Ok(f(image.rgba.as_mut_slice(), image.width, image.height))
            }
        }
    }

    fn present_screen_cpu_fallback(&mut self) -> Result<()> {
        self.ensure_screen_rgba()?;
        if self.framebuffer_ptr.is_null() || self.framebuffer_pitch == 0 {
            return Err(Error::Invalid);
        }

        let copy_w = self.swapchain_desc.extent.width.min(self.framebuffer_width) as usize;
        let copy_h = self
            .swapchain_desc
            .extent
            .height
            .min(self.framebuffer_height) as usize;
        for y in 0..copy_h {
            let src_row = y
                .saturating_mul(self.swapchain_desc.extent.width as usize)
                .saturating_mul(4);
            let dst_row = y.saturating_mul(self.framebuffer_pitch);
            for x in 0..copy_w {
                let src_off = src_row + x.saturating_mul(4);
                if src_off + 4 > self.screen_rgba.len() {
                    break;
                }
                let pixel = pack_xrgb(
                    self.screen_rgba[src_off],
                    self.screen_rgba[src_off + 1],
                    self.screen_rgba[src_off + 2],
                );
                let dst_ptr =
                    unsafe { self.framebuffer_ptr.add(dst_row + x.saturating_mul(4)) as *mut u32 };
                unsafe { core::ptr::write_volatile(dst_ptr, pixel) };
            }
        }

        Ok(())
    }

    fn present_screen(&mut self) -> Result<()> {
        self.ensure_screen_rgba()?;
        self.sync_screen_rgba_from_gpu();
        let copy_w = self.swapchain_desc.extent.width.min(self.framebuffer_width) as usize;
        let copy_h = self
            .swapchain_desc
            .extent
            .height
            .min(self.framebuffer_height) as usize;
        self.present_seq = self.present_seq.wrapping_add(1);

        if let Some(surface_gpu_addr) = crate::intel::primary_present_surface_gpu_addr() {
            crate::intel::dma_cache_flush_range(self.screen_rgba.as_ptr(), self.screen_rgba.len());
            let mapped = crate::intel::ggtt_map_screen_rgba_surface(
                self.screen_rgba.as_slice(),
                self.swapchain_desc.extent.width,
                self.swapchain_desc.extent.height,
                surface_gpu_addr,
            );
            if mapped
                && crate::intel::plane_rebind_present_surface(
                    surface_gpu_addr,
                    self.swapchain_desc.extent.width,
                    self.swapchain_desc.extent.height,
                    self.swapchain_desc.extent.width.saturating_mul(4),
                )
            {
                mark_present_completed(self.present_seq);
                self.rotate_screen_present_buffers();
                if self.present_seq <= 8 || self.present_seq.is_multiple_of(120) {
                    crate::log!(
                        "intel/gfx-backend: present seq={} mode=plane-rebind-backbuffer size={}x{} gpu=0x{:X}\n",
                        self.present_seq,
                        copy_w,
                        copy_h,
                        surface_gpu_addr
                    );
                }
                return Ok(());
            }
        }

        let guc_ready = crate::intel::guc_ready();
        let allow_rcs_retry = guc_ready && self.present_seq >= self.rcs_retry_after_present_seq;
        if allow_rcs_retry {
            if crate::intel::rcs_present
[... truncated in extract ...]

[2026-05-11T01:32:22.744Z] tool-call:exec_command line=153
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"primary_present_surface_gpu_addr|primary_present_shadow_surface|ggtt_map_screen_rgba_surface|plane_rebind_present_surface|rcs_present_rgba_frame|rcs_clear_rgba_surface|rcs_draw_rgba|rcs_draw_screen_tex|TextureStoreSampleKind\" src/intel","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":32000}

[2026-05-11T01:32:22.746Z] tool-call:exec_command line=154
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '390,432p' src/gfx/backends/intel.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":8000}

[2026-05-11T01:32:22.766Z] tool-call:exec_command line=155
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '620,700p' src/gfx/backends/intel.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":12000}

[2026-05-11T01:32:22.802Z] tool-output line=157
------------------------------------------------------------------------------------------------
Chunk ID: e515c6
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 222
Output:
src/intel/intel/may_help/XeLp_display_ngin.rs_first_try:1547:pub(crate) fn primary_present_surface_gpu_addr() -> Option<u64> {
src/intel/intel/may_help/XeLp_display_ngin.rs_first_try:1569:pub(crate) fn primary_present_shadow_surface_gpu_addr() -> Option<u64> {
src/intel/intel/may_help/XeLp_display_ngin.rs_first_try:1718:pub(crate) fn plane_rebind_present_surface(
src/intel/intel/may_help/XeLp_render_ngin.rs_first_try:181:pub(crate) enum TextureStoreSampleKind {
src/intel/intel/may_help/XeLp_render_ngin.rs_first_try:892:    sample_kind: TextureStoreSampleKind,
src/intel/intel/may_help/XeLp_render_ngin.rs_first_try:982:                TextureStoreSampleKind::Mask => [
src/intel/intel/may_help/XeLp_render_ngin.rs_first_try:988:                TextureStoreSampleKind::Rgba => [
src/intel/intel/may_help/XeLp_render_ngin.rs_first_try:1040:    sample_kind: TextureStoreSampleKind,

[2026-05-11T01:32:22.802Z] tool-output line=158
------------------------------------------------------------------------------------------------
Chunk ID: 807664
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 433
Output:
    fn present_image_target(&mut self, id: ImageId) -> Result<()> {
        self.sync_image_rgba_from_gpu(id);
        let image = self.image(id).ok_or(Error::NotFound)?.clone();
        if image.width == 0 || image.height == 0 {
            return Err(Error::Invalid);
        }

        self.present_seq = self.present_seq.wrapping_add(1);
        crate::intel::dma_cache_flush_range(image.rgba.as_ptr(), image.rgba.len());

        if let Some(surface_gpu_addr) = crate::intel::primary_present_shadow_surface_gpu_addr() {
            let mapped = crate::intel::ggtt_map_screen_rgba_surface(
                image.rgba.as_slice(),
                image.width,
                image.height,
                surface_gpu_addr,
            );
            if mapped
                && crate::intel::plane_rebind_present_surface(
                    surface_gpu_addr,
                    image.width,
                    image.height,
                    image.width.saturating_mul(4),
                )
            {
                mark_present_completed(self.present_seq);
                if self.present_seq <= 8 || self.present_seq.is_multiple_of(120) {
                    crate::log!(
                        "intel/gfx-backend: present seq={} complete_seq={} mode=plane-rebind-image-target-fixed-surface size={}x{} src_gpu=0x{:X} gpu=0x{:X}\n",
                        self.present_seq,
                        present_completed_seq(),
                        image.width,
                        image.height,
                        image.gpu_addr,
                        surface_gpu_addr
                    );
                }
                return Ok(());
            }
        }

        Err(Error::Unsupported)
    }

[2026-05-11T01:32:22.802Z] tool-output line=159
------------------------------------------------------------------------------------------------
Chunk ID: 1c86e8
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 662
Output:
                    v1,
                    v2,
                );
                off += 3 * trueos_gfx_core::TEX_VERTEX_SIZE;
            }
        })?;
        Ok(())
    }
}

impl GfxDevice for IntelGfxBackend {
    fn caps(&self) -> DeviceCaps {
        DeviceCaps {
            supports_rgbx8888: true,
            supports_host_visible_buffers: true,
            supports_scissor: true,
        }
    }

    fn create_buffer(&mut self, desc: BufferDesc) -> Result<BufferId> {
        let size = usize::try_from(desc.size).map_err(|_| Error::Invalid)?;
        let id = Self::alloc_slot(
            &mut self.buffers,
            BufferEntry {
                bytes: alloc::vec![0; size],
                usage: desc.usage,
                memory: desc.memory,
            },
        );
        Ok(BufferId::from_raw(id))
    }

    fn destroy_buffer(&mut self, id: BufferId) {
        if let Some(slot) = id
            .raw()
            .checked_sub(1)
            .and_then(|idx| self.buffers.get_mut(idx as usize))
        {
            *slot = None;
        }
    }

    fn create_shader(&mut self, _desc: ShaderDesc<'_>) -> Result<ShaderId> {
        let id = self.next_shader_raw;
        self.next_shader_raw = self.next_shader_raw.wrapping_add(1).max(1);
        Ok(ShaderId::from_raw(id))
    }

    fn destroy_shader(&mut self, _id: ShaderId) {}

    fn create_pipeline(&mut self, desc: PipelineDesc) -> Result<PipelineId> {
        let kind = if desc.vertex_layout.texcoord_format == TexCoordFormat::None {
            PipelineKind::Rgb
        } else {
            match desc.fs.map(|id| id.raw()) {
                Some(TEX_PIPELINE_FS_MASK_TAG_RAW) => PipelineKind::TexMask,
                Some(TEX_PIPELINE_FS_RGBA_TAG_RAW) => PipelineKind::TexRgba,
                Some(TEX_PIPELINE_FS_PARTICLE_TAG_RAW) => PipelineKind::TexParticle,
                Some(crate::gfx::mandelbrot::MANDELBROT_PIPELINE_FS_TAG_RAW) => {
                    PipelineKind::Mandelbrot
                }
                _ => PipelineKind::TexRgba,
            }
        };
        let id = Self::alloc_slot(&mut self.pipelines, PipelineEntry { desc, kind });
        Ok(PipelineId::from_raw(id))
    }

    fn destroy_pipeline(&mut self, id: PipelineId) {
        if let Some(slot) = id
            .raw()
            .checked_sub(1)
            .and_then(|idx| self.pipelines.get_mut(idx as usize))
        {
            *slot = None;
        }
    }

    fn create_image(&mut self, desc: ImageDesc) -> Result<ImageId> {
        let len = rgba_len(desc.width, desc.height).ok_or(Error::Invalid)?;
        if desc.width > MAX_BACKEND_IMAGE_DIM

[2026-05-11T01:32:29.208Z] assistant line=161
------------------------------------------------------------------------------------------------
The active Intel gfx backend is more revealing than expected: it has a software/vector RGBA model and a couple of intended fast paths, but several fast-path functions it calls only exist in `may_help`, not in the compiled `src/intel` module. That is likely one of the “pixels exist but don’t show” breaks.

[2026-05-11T01:32:29.208Z] assistant line=162
------------------------------------------------------------------------------------------------
The active Intel gfx backend is more revealing than expected: it has a software/vector RGBA model and a couple of intended fast paths, but several fast-path functions it calls only exist in `may_help`, not in the compiled `src/intel` module. That is likely one of the “pixels exist but don’t show” breaks.

[2026-05-11T01:32:29.229Z] tool-call:exec_command line=164
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"IntelGfxBackend|backends::intel|is_virgl_active|is_intel_active|BackendKind|Backend\" src/gfx/mod.rs src/r/gfx_cabi.rs src/main.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":24000}

[2026-05-11T01:32:29.229Z] tool-call:exec_command line=165
------------------------------------------------------------------------------------------------
{"cmd":"sed -n '1520,1585p' src/intel/intel/may_help/XeLp_display_ngin.rs_first_try && sed -n '1710,1755p' src/intel/intel/may_help/XeLp_display_ngin.rs_first_try","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":16000}

[2026-05-11T01:32:29.247Z] tool-output line=167
------------------------------------------------------------------------------------------------
Chunk ID: f52288
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 965
Output:
pub mod althlasfont;
pub mod backends;
pub mod jpeg_codec;
pub mod lyon;
pub mod mandelbrot;
pub mod png_codec;
pub mod screenshot;
pub mod svg;
pub mod virtio_gpu_3d;

use spin::{Mutex, Once};

use core::sync::atomic::{AtomicBool, AtomicU8, AtomicU32, AtomicU64, Ordering};
use embassy_time_driver::{TICK_HZ, now};
use trueos_gfx_core::GfxContext;

pub(crate) use screenshot::{publish_screenshot_rgba_buffer, screenshot_capture_armed};

static SYSTEM: Once<Mutex<System>> = Once::new();
static CABI_FRAME_LOCK: AtomicBool = AtomicBool::new(false);
static BACKEND_EPOCH: AtomicU64 = AtomicU64::new(1);
static SYSTEM_LOCK_OWNER: AtomicU32 = AtomicU32::new(SystemLockOwner::Unknown as u32);
static SYSTEM_LOCK_OWNER_CPU: AtomicU32 = AtomicU32::new(u32::MAX);
static SYSTEM_LOCK_OWNER_SINCE: AtomicU64 = AtomicU64::new(0);
static BACKEND_READY_PUBLISHED: AtomicBool = AtomicBool::new(false);
static BACKEND_KIND_ATOMIC: AtomicU8 = AtomicU8::new(0);

// Frame completion register.
//
// The command stream owner (e.g. QJS WebGL shim) knows when a logical frame is complete.
// Multiple producers can OR-in their "done" bit; once all required bits are present, the
// frame boundary can be consumed and the register resets for the next frame.
static FRAME_DONE_REQUIRED: AtomicU32 = AtomicU32::new(1);
static FRAME_DONE_BITS: AtomicU32 = AtomicU32::new(0);
static FRAME_DONE_SEQ: AtomicU32 = AtomicU32::new(0);

#[inline]
pub fn frame_done_set_required(mask: u32) {
    // Require at least one bit by default to avoid accidental always-ready if a caller passes 0.
    let req = if mask == 0 { 1 } else { mask };
    FRAME_DONE_REQUIRED.store(req, Ordering::Release);
}

#[inline]
pub fn frame_done_signal(bits: u32) {
    if bits != 0 {
        let _ = FRAME_DONE_BITS.fetch_or(bits, Ordering::AcqRel);
    }
}

#[inline]
pub fn frame_done_is_ready() -> bool {
    let req = FRAME_DONE_REQUIRED.load(Ordering::Acquire);
    let done = FRAME_DONE_BITS.load(Ordering::Acquire);
    (done & req) == req
}

#[inline]
pub fn frame_done_consume_if_ready() -> Option<u32> {
    if !frame_done_is_ready() {
        return None;
    }

    // Consume the boundary: clear done bits and bump sequence.
    FRAME_DONE_BITS.store(0, Ordering::Release);
    let seq = FRAME_DONE_SEQ
        .fetch_add(1, Ordering::AcqRel)
        .wrapping_add(1);
    Some(seq)
}

#[unsafe(no_mangle)]
pub extern "C" fn trueos_cabi_gfx_frame_done_set_required(mask: u32) {
    frame_done_set_required(mask);
}

#[unsafe(no_mangle)]
pub extern "C" fn trueos_cabi_gfx_frame_done_signal(bits: u32) {
    frame_done_signal(bits);
}

#[unsafe(no_mangle)]
pub extern "C" fn trueos_cabi_gfx_frame_done_is_ready() -> u32 {
    if frame_done_is_ready() { 1 } else { 0 }
}

/// Returns a monotonically increasing sequence when a ready frame boundary is consumed, or 0.
#[unsafe(no_mangle)]
pub extern "C" fn trueos_cabi_gfx_frame_done_consume_if_ready() -> u32 {
    frame_done_consume_if_ready().unwrap_or(0)
}

#[inline]
pub fn backend_epoch() -> u64 {
    BACKEND_EPOCH.load(Ordering::Relaxed)
}

#[inline]
fn bump_backend_epoch() {
    let _ = BACKEND_EPOCH.fetch_add(1, Ordering::Relaxed);
}

pub struct System {
    backend: backends::Backend,
    framebuffers: Option<&'static crate::limine::FramebufferResponse>,
}

fn finalize_backend_init() {
    if BACKEND_READY_PUBLISHED.swap(true, Ordering::AcqRel) {
        return;
    }
    crate::r::readiness::set(crate::r::readiness::GFX_BACKEND_READY);
}

#[derive(Copy, Clone, Debug, Eq, PartialEq)]
#[repr(u32)]
pub enum SystemLockOwner {
    Unknown = 0,
    DrawRgbTriangles = 1,
    UploadTexture = 2,
    EndFrame = 3,
    CursorQueryViewport = 4,
    CursorEndFrame = 5,
    DrawMandelbrot = 6,
    FinalizeBackendInit = 7,
    WithFramebuffers = 8,
    IsVirglActive = 9,
    IsIntelActive = 10,
    SwitchToVirgl = 11,
    BackendKind = 12,

[2026-05-11T01:32:29.267Z] tool-output line=168
------------------------------------------------------------------------------------------------
Chunk ID: f21b53
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 574
Output:
src/gfx/mod.rs:104:    backend: backends::Backend,
src/gfx/mod.rs:125:    FinalizeBackendInit = 7,
src/gfx/mod.rs:130:    BackendKind = 12,
src/gfx/mod.rs:144:            Self::FinalizeBackendInit => "finalize_backend_init",
src/gfx/mod.rs:146:            Self::IsVirglActive => "is_virgl_active",
src/gfx/mod.rs:147:            Self::IsIntelActive => "is_intel_active",
src/gfx/mod.rs:149:            Self::BackendKind => "backend_kind",
src/gfx/mod.rs:162:            x if x == Self::FinalizeBackendInit as u32 => Self::FinalizeBackendInit,
src/gfx/mod.rs:167:            x if x == Self::BackendKind as u32 => Self::BackendKind,
src/gfx/mod.rs:175:        backend: backends::Backend,
src/gfx/mod.rs:190:fn backend_kind_raw(backend: &backends::Backend) -> u8 {
src/gfx/mod.rs:192:        backends::Backend::Virgl(_) => 1,
src/gfx/mod.rs:193:        backends::Backend::None(_) => 2,
src/gfx/mod.rs:198:fn publish_backend_kind(backend: &backends::Backend) {
src/gfx/mod.rs:203:fn backend_kind_cached() -> Option<BackendKind> {
src/gfx/mod.rs:205:        1 => Some(BackendKind::Virgl),
src/gfx/mod.rs:206:        2 => Some(BackendKind::None),
src/gfx/mod.rs:216:        let backend = backends::Backend::init_auto(framebuffers);
src/gfx/mod.rs:218:            backends::Backend::Virgl(_) => "virgl",
src/gfx/mod.rs:219:            backends::Backend::None(_) => "none",
src/gfx/mod.rs:223:        if !matches!(backend, backends::Backend::None(_)) {
src/gfx/mod.rs:280:    // `spin::Mutex::lock()` is an unbounded spin. Backend switches can be invoked from the shell;
src/gfx/mod.rs:360:pub fn is_virgl_active() -> bool {
src/gfx/mod.rs:361:    matches!(backend_kind_cached(), Some(BackendKind::Virgl))
src/gfx/mod.rs:364:pub fn is_intel_active() -> bool {
src/gfx/mod.rs:382:    let Some(b) = backends::Backend::init_virgl(fbs) else {
src/gfx/mod.rs:404:pub enum BackendKind {
src/r/gfx_cabi.rs:4448:            // Backend changed; cached IDs belong to a different backend.
src/r/gfx_cabi.rs:4930:        if !crate::gfx::is_virgl_active() {
src/r/gfx_cabi.rs:5315:                    // Backend changed (or first use). Drop cached IDs so future draws don't
src/r/gfx_cabi.rs:6927:            if crate::gfx::is_virgl_active() {
src/r/gfx_cabi.rs:6970:        } else if crate::gfx::is_virgl_active() {

[2026-05-11T01:32:29.267Z] tool-output line=169
------------------------------------------------------------------------------------------------
Chunk ID: a25deb
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 938
Output:
    let _ = mmio_write32(info, draft.descriptor.plane_surf_off, surface_reg);

    let mut success = false;
    let mut iter = 0usize;
    let mut live = mmio_read32(info, draft.descriptor.plane_surf_live_off);
    while iter < 4096 {
        if live == surface_reg {
            record_primary_present_visible_surface(surface_gpu_addr);
            success = true;
            break;
        }
        core::hint::spin_loop();
        live = mmio_read32(info, draft.descriptor.plane_surf_live_off);
        iter += 1;
    }

    if !success {
        let armed = mmio_read32(info, draft.descriptor.plane_surf_off);
        success = live == surface_reg || armed == surface_reg;
    }
    if success {
        record_primary_present_visible_surface(surface_gpu_addr);
    }
    log_primary_present_proof(label, info, draft, surface_gpu_addr, stride_reg, iter, success);
    success
}

pub(crate) fn primary_present_surface_gpu_addr() -> Option<u64> {
    let draft = draft_workload(DisplayWorkloadKind::PrimaryPresent)?;
    let windows = draft.surface.windows;
    let visible_slot = PRIMARY_PRESENT_VISIBLE_SURFACE_SLOT.load(Ordering::Acquire);
    Some(if visible_slot == PRIMARY_PRESENT_SLOT_SHADOW {
        windows.staging_gpu_addr
    } else {
        windows.shadow_state_gpu_addr
    })
}

pub(crate) fn primary_present_visible_surface_gpu_addr() -> Option<u64> {
    let draft = draft_workload(DisplayWorkloadKind::PrimaryPresent)?;
    let windows = draft.surface.windows;
    let visible_slot = PRIMARY_PRESENT_VISIBLE_SURFACE_SLOT.load(Ordering::Acquire);
    Some(if visible_slot == PRIMARY_PRESENT_SLOT_SHADOW {
        windows.shadow_state_gpu_addr
    } else {
        windows.staging_gpu_addr
    })
}

pub(crate) fn primary_present_shadow_surface_gpu_addr() -> Option<u64> {
    let draft = draft_workload(DisplayWorkloadKind::PrimaryPresent)?;
    Some(draft.surface.windows.shadow_state_gpu_addr)
}

pub(crate) fn primary_present_surface(
    surface_gpu_addr: u64,
    width: u32,
    height: u32,
    pitch_bytes: u32,
) -> bool {
    let draft = match draft_workload(DisplayWorkloadKind::PrimaryPresent) {
        Some(v) => v,
        None => return false,
    };
    let info = match super::intel::first_claimed_device() {
        Some(v) => v,
    let windows = draft.surface.windows;
    if surface_gpu_addr == windows.staging_gpu_addr {
        PRIMARY_PRESENT_VISIBLE_SURFACE_SLOT.store(PRIMARY_PRESENT_SLOT_STAGING, Ordering::Release);
    } else if surface_gpu_addr == windows.shadow_state_gpu_addr {
        PRIMARY_PRESENT_VISIBLE_SURFACE_SLOT.store(PRIMARY_PRESENT_SLOT_SHADOW, Ordering::Release);
    }
}

pub(crate) fn plane_rebind_present_surface(
    surface_gpu_addr: u64,
    width: u32,
    height: u32,
    pitch_bytes: u32,
) -> bool {
    let draft = match draft_workload(DisplayWorkloadKind::PlaneRebind) {
        Some(v) => v,
        None => return false,
    };
    let info = match super::intel::first_claimed_device() {
        Some(v) => v,
        None => return false,
    };
    commit_plane_surface(
        "flip-proof",
        info,
        draft,
        surface_gpu_addr,
        width,
        height,
        pitch_bytes,
        None,
        None,
    )
}

fn log_workload_draft(label: &str, draft: DisplayWorkloadDraft) {
    crate::log!(
        "intel/display-ngin: {} pipe={} workload={} staging={} commit={} async={} surface={}x{} pitch=0x{:X} format={} layout={} next_stage={} staging_gpu=0x{:X}\n",
        label,
        draft.descriptor.id.as_str(),
        draft.workload.as_str(),
        draft.programming.staging.as_str(),
        draft.programming.commit.as_str(),
        draft.programming.async_model.as_str(),
        draft.surface.width,
        draft.surface.height,

[2026-05-11T01:32:31.572Z] tool-output line=173
------------------------------------------------------------------------------------------------
Chunk ID: a8cb51
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1408
Output:
use trueos_gfx_core::{
    BufferDesc, BufferId, CommandBuffer, DeviceCaps, Error, Extent2D, FenceId, GfxContext,
    GfxDevice, GfxPresent, ImageDesc, ImageFormat, ImageId, ImageRegion, PipelineDesc, PipelineId,
    Result, ShaderDesc, ShaderId, SwapchainDesc,
};

use crate::gfx::virtio_gpu_3d;

pub enum Backend {
    Virgl(virtio_gpu_3d::VirglGfxBackend),
    None(NullBackend),
}

pub struct NullBackend;

impl GfxDevice for NullBackend {
    fn caps(&self) -> DeviceCaps {
        DeviceCaps {
            supports_rgbx8888: false,
            supports_host_visible_buffers: false,
            supports_scissor: false,
        }
    }

    fn create_buffer(&mut self, _desc: BufferDesc) -> Result<BufferId> {
        Err(Error::Unsupported)
    }

    fn destroy_buffer(&mut self, _id: BufferId) {}

    fn create_shader(&mut self, _desc: ShaderDesc<'_>) -> Result<ShaderId> {
        Err(Error::Unsupported)
    }

    fn destroy_shader(&mut self, _id: ShaderId) {}

    fn create_pipeline(&mut self, _desc: PipelineDesc) -> Result<PipelineId> {
        Err(Error::Unsupported)
    }

    fn destroy_pipeline(&mut self, _id: PipelineId) {}

    fn create_image(&mut self, _desc: ImageDesc) -> Result<ImageId> {
        Err(Error::Unsupported)
    }

    fn destroy_image(&mut self, _id: ImageId) {}

    fn write_image(&mut self, _id: ImageId, _data: &[u8]) -> Result<()> {
        Err(Error::Unsupported)
    }

    fn write_image_region(
        &mut self,
        _id: ImageId,
        _region: ImageRegion,
        _data: &[u8],
    ) -> Result<()> {
        Err(Error::Unsupported)
    }

    fn write_buffer(&mut self, _id: BufferId, _offset: u64, _data: &[u8]) -> Result<()> {
        Err(Error::Unsupported)
    }

    fn submit(&mut self, _cmds: CommandBuffer<'_>) -> Result<FenceId> {
        Err(Error::Unsupported)
    }

    fn poll(&mut self, _fence: FenceId) -> bool {
        false
    }

    fn device_idle(&mut self) {}
}

impl GfxPresent for NullBackend {
    fn configure_swapchain(&mut self, _desc: SwapchainDesc) -> Result<()> {
        Err(Error::Unsupported)
    }

    fn swapchain_desc(&self) -> SwapchainDesc {
        SwapchainDesc {
            format: ImageFormat::Rgbx8888,
            extent: Extent2D {
                width: 0,
                height: 0,
            },
        }
    }
}

fn ensure_pci_enumerated_if_empty() {
    let mut count: usize = 0;
    crate::pci::with_devices(|list| {
        count = list.len();
    });
    if count == 0 {
        crate::log!("gfx: pci device list empty; enumerating before backend init\n");
        crate::pci::enumerate_impl();
    }
}

impl Backend {
    pub fn init_auto(framebuffers: Option<&'static crate::limine::FramebufferResponse>) -> Self {
        if let Some(v) = Self::init_virgl(framebuffers) {
            crate::log_info!(target: "gfx"; "gfx: using virgl backend (auto)\n");
            return v;
        }
        crate::log_info!(target: "gfx"; "gfx: virgl auto init failed\n");

        crate::log_info!(target: "gfx"; "gfx: no accelerated backend available; gfx backend inactive\n");
        Backend::None(NullBackend)
    }

    pub fn init_virgl(
        framebuffers: Option<&'static crate::limine::FramebufferResponse>,
    ) -> Option<Self> {
        ensure_pci_enumerated_if_empty();
        virtio_gpu_3d::VirglGfxBackend::init(framebuffers).map(Backend::Virgl)
    }
    pub fn context_mut(&mut self) -> &mut dyn GfxContext {
        match self {
            Backend::Virgl(b) => b,
            Backend::None(b) => b,
        }
    }
}
            framebuffers,
        }
    }

    pub fn context_mut(&mut self) -> &mut dyn GfxContext {
        self.backend.context_mut()
    }
}

#[inline]
fn backend_kind_raw(backend: &backends::Backend) -> u8 {
    match backend {
        backends::Backend::Virgl(_) => 1,
        backends::Backend::None(_) => 2,
    }
}

#[inline]
fn publish_backend_kind(backend: &backends::Backend) {
    BACKEND_KIND_ATOMIC.store(backend_kind_raw(backend), Ordering::Release);
}

#[inline]
fn backend_kind_cached() -> Option<BackendKind> {
    match BACKEND_KIND_ATOMIC.load(Ordering::Acquire) {
        1 => Some(BackendKind::Virgl),
        2 => Some(BackendKind::None),
        _ => None,
    }
}

pub fn init(framebuffers: Option<&'static crate::limine::FramebufferResponse>) {
    let _ = SYSTEM.call_once(|| {
        // if we use this qemu will do
[... truncated in extract ...]

[2026-05-11T01:32:37.016Z] tool-call:exec_command line=175
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"log_primary_surface_samples|primary_present|shadow_surface|plane_rebind|ggtt_map_screen_rgba|sample_primary|surface_samples|rcs_present\" src/intel/display.rs src/intel/mod.rs src/intel/gpgpu.rs src/intel/intel/display.rs src/intel/intel/mod.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":30000}

[2026-05-11T01:32:37.051Z] tool-output line=177
------------------------------------------------------------------------------------------------
Chunk ID: 1ef1fd
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 339
Output:
src/intel/display.rs:860:            if should_log_primary_present(seq) {
src/intel/display.rs:874:        if should_log_primary_present(seq) {
src/intel/display.rs:908:    if should_log_primary_present(seq) {
src/intel/display.rs:938:fn should_log_primary_present(seq: u32) -> bool {
src/intel/intel/display.rs:846:pub(crate) fn log_primary_surface_samples(label: &str) {
src/intel/intel/display.rs:850:    log_surface_samples(surface, label);
src/intel/intel/display.rs:853:pub(crate) fn capture_primary_surface_samples() -> Option<PrimarySurfaceSampleSet> {
src/intel/intel/display.rs:855:    capture_surface_samples(surface)
src/intel/intel/display.rs:858:pub(crate) fn sample_primary_surface_pixel(x: u32, y: u32) -> Option<u32> {
src/intel/intel/display.rs:863:fn log_surface_samples(surface: PrimarySurface, label: &str) {
src/intel/intel/display.rs:864:    let Some(samples) = capture_surface_samples(surface) else {
src/intel/intel/display.rs:884:fn capture_surface_samples(surface: PrimarySurface) -> Option<PrimarySurfaceSampleSet> {
src/intel/intel/display.rs:985:            if should_log_primary_present(seq) {
src/intel/intel/display.rs:999:        if should_log_primary_present(seq) {
src/intel/intel/display.rs:1033:    if should_log_primary_present(seq) {
src/intel/intel/display.rs:1063:fn should_log_primary_present(seq: u32) -> bool {

[2026-05-11T01:32:42.190Z] tool-call:exec_command line=180
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"mod intel|path = \\\"intel\" src/main.rs src/**/*.rs","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":10000}

[2026-05-11T01:32:42.207Z] tool-call:exec_command line=181
------------------------------------------------------------------------------------------------
{"cmd":"rg -n \"pub mod backends|mod backends|backends::intel|mod intel;\" src/gfx -g '*.rs'","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":8000}

[2026-05-11T01:32:42.223Z] tool-output line=183
------------------------------------------------------------------------------------------------
Chunk ID: fbaf83
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 269
Output:
mod smp;
#[cfg(any(target_os = "trueos", target_os = "zkvm"))]
mod stackkeeper;
#[cfg(any(target_os = "trueos", target_os = "zkvm"))]
mod std_abi_shim;
mod t;
mod tga;
#[cfg(any(target_os = "trueos", target_os = "zkvm"))]
mod th;
mod tokio_probe;
#[cfg(any(target_os = "trueos", target_os = "zkvm"))]
mod trueos_tokio_worker;
#[path = "tst/boot_factory_ram_probe.rs"]
mod tst_boot_factory_ram_probe;
#[path = "tst/chatserver/server.rs"]
mod tst_chatserver;
#[path = "tst/fileexplorer/axum_server.rs"]
mod tst_fileexplorer_axum;
#[path = "tst/html_demo.rs"]
mod tst_html_demo;
#[path = "tst/html_shack.rs"]
mod tst_html_shack;
#[path = "tst/http_trueosfs.rs"]
mod tst_http_trueosfs;
#[path = "tst/mandelbrot_gpu_sidequest.rs"]
mod tst_mandelbrot_gpu_sidequest;
#[path = "tst/net_tcp_shell.rs"]
mod tst_net_tcp_shell;
#[path = "tst/uas_skhynix_route_probe.rs"]
mod tst_uas_skhynix_route_probe;
#[path = "tst/ui2_analog_clock_demo.rs"]
mod tst_ui2_analog_clock_demo;
#[path = "tst/ui2_bgrt.rs"]
mod tst_ui2_bgrt;
#[path = "tst/ui2_coreticks_demo.rs"]
mod tst_ui2_coreticks_demo;

[2026-05-11T01:32:42.224Z] tool-output line=184
------------------------------------------------------------------------------------------------
Chunk ID: 8e0b4b
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 16
Output:
src/main.rs:43:mod intel;
src/main.rs:44:mod intel_hda_probe;

[2026-05-11T01:32:44.159Z] tool-output line=189
------------------------------------------------------------------------------------------------
Chunk ID: 60494b
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 1437
Output:
        crate::r::readiness::NET_ANY_CONFIGURED,
        &TGA_TASK_STARTED,
        spawn_tga_task,
    ),
    TaskSpec::enabled_gated(
        "gfx-virgl-backend-ready",
        0,
        gfx_backend_boot_gate,
        &GFX_VIRGL_READY_TASK_STARTED,
        spawn_gfx_virgl_ready_task,
    ),
    TaskSpec::enabled(
        "gfx-virgl-cursor-overlay",
        crate::r::readiness::GFX_BACKEND_READY,
        &GFX_VIRGL_CURSOR_OVERLAY_STARTED,
        spawn_gfx_virgl_cursor_overlay_task,
    ),
    TaskSpec::enabled_gated(
        "intel-cursor-service",
        0,
        intel_cursor_service_gate,
        &INTEL_CURSOR_SERVICE_STARTED,
        spawn_intel_cursor_service_task,
    ),
    TaskSpec::disabled("intel-hda-probe", 0, &INTEL_HDA_PROBE_STARTED, spawn_intel_hda_probe_task),
    TaskSpec::enabled("raple-service", 0, &RAPLE_SERVICE_STARTED, spawn_raple_service),
    TaskSpec::disabled(
        "html_fetch_service",
        crate::r::readiness::NET_V4_CONFIGURED | crate::r::readiness::TRUEOSFS_ROOT_MOUNTED,
        &HTML_SHACK_SERVICE_STARTED,
        html_fetch_service,
    ),
    TaskSpec::enabled(
        "gfx-texture-upload-service",
        crate::r::readiness::GFX_BACKEND_READY,
        &GFX_TEXTURE_UPLOAD_SERVICE_STARTED,
        spawn_gfx_texture_upload_service,
    ),
    TaskSpec::enabled(
        "mandelbrot-gpu-sidequest",
        0,
        &MANDELBROT_GPU_SIDEQUEST_STARTED,
        spawn_mandelbrot_gpu_sidequest,
    ),
    TaskSpec::enabled_gated(
        "ui2",
        crate::r::readiness::GFX_BACKEND_READY,
        ui2_core_task_gate,
        &UI2_STARTED,
        spawn_ui2,
    ),
    TaskSpec::enabled_gated(
        "ui2-hosted",
        crate::r::readiness::GFX_BACKEND_READY,
        ui2_core_task_gate,
        &UI2_HOSTED_SYNC_TASK_STARTED,
        spawn_ui2_hosted,
    ),
    TaskSpec::enabled_gated(
        "ui2-hit",
        crate::r::readiness::GFX_BACKEND_READY,
        ui2_core_task_gate,
        &UI2_HIT_TASK_STARTED,
        spawn_ui2_hit,
    ),
    TaskSpec::enabled("truesurfer-factory", 0, &SURFER_FACTORY_STARTED, spawn_truesurfer_factory),
    TaskSpec::enabled(
        "ui2-athlas-third-demo",
        UI2_DEMO_READY,
        &UI2_ATHLAS_THIRD_DEMO_STARTED,
        spawn_ui2_athlas_third_demo,
    ),
    TaskSpec::enabled(
        "ui2-athlas-half-demo",
        UI2_DEMO_READY,
        &UI2_ATHLAS_HALF_DEMO_STARTED,
        spawn_ui2_athlas_half_demo,
    ),
    TaskSpec::enabled(
        "ui2-athlas-1x-demo",
        UI2_DEMO_READY,
        &UI2_ATHLAS_1X_DEMO_STARTED,
        spawn_ui2_athlas_1x_demo,
    ),
    TaskSpec::enabled(
        "ui2-athlas-2x-demo",
        UI2_DEMO_READY,
        &UI2_ATHLAS_2X_DEMO_STARTED,
        spawn_ui2_athlas_2x_demo,
    ),
    TaskSpec::enabled(
        "ui2-palatino-1x-demo",
        UI2_DEMO_READY,
        &UI2_PALATINO_1X_DEMO_STARTED,
        spawn_ui2_palatino_1x_demo,
    ),
    TaskSpec::enabled(
        "ui2-twemoji-1x",
        crate::r::readiness::GFX_TEXTURE_UPLOAD_SERVICE_READY,
        &UI2_TWEMOJI_1X_STARTED,
        spawn_ui2_twemoji_1x,
    ),
    TaskSpec::enabled_gated(
        "ui2-text-input-demo",
        UI2_DEMO_READY,
        ui2_demo_task_gate,
        &UI2_TEXT_INPUT_DEMO_STARTED,
        spawn_ui2_text_input_demo,
    ),
    TaskSpec::enabled(
        "ui2-analog-clock-demo",
        UI2_DEMO_READY,
        &UI2_ANALOG_CLOCK_DEMO_STARTED,
        spawn_ui2_analog_clock_demo,
    ),
    TaskSpec::enabled("ui2-bgrt-demo", UI2_DEMO_READY, &UI2_BGRT_DEMO_STARTED, spawn_ui2_bgrt_demo),
    TaskSpec::enabled(
        "ui2-coreticks-demo",
        UI2_DEMO_READY,
        &UI2_CORETICKS_DEMO_STARTED,
        spawn_ui2_coreticks_demo,
    ),
    TaskSpec::enabled(
        "ui2-cursorpicker-demo",
        UI2_DEMO_READY,
        &UI2_CURSORPICKER_DEMO_STARTED,
        spawn_ui2_cursorpicker_demo,
    ),
    TaskSpec::disabled(
        "ui2-mandelbrot-demo",
        UI2_DEMO_READY,
        &UI2_MANDELBROT_DEMO_STARTED,
        spawn_ui2_mandelbrot_demo,
    ),
    // Keep the player demo opt-in because it opens the audio player on boot.
    // The old hosted-surface window-spam issue was fixed by content-id keyed reuse.
    TaskSpec::enabled(
        "ui2-player-demo",
        UI2_DEMO_READY,
        &UI2_PLAYER_DEMO_STARTED,
        spawn_ui2_player_demo,
    ),
    TaskSpec::enabled(
        "ui2-raple-demo",
        UI2_
[... truncated in extract ...]

[2026-05-11T01:32:50.637Z] tool-call:exec_command line=191
------------------------------------------------------------------------------------------------
{"cmd":"nl -ba src/intel/display.rs | sed -n '214,340p;510,620p;808,930p;1131,1305p'","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":32000}

[2026-05-11T01:32:50.637Z] tool-call:exec_command line=192
------------------------------------------------------------------------------------------------
{"cmd":"nl -ba src/intel/gpgpu.rs | sed -n '88,106p;326,416p;1216,1228p;2160,2392p;4910,4965p'","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":36000}

[2026-05-11T01:32:50.658Z] tool-call:exec_command line=193
------------------------------------------------------------------------------------------------
{"cmd":"nl -ba src/tst/mandelbrot_gpu_sidequest.rs | sed -n '1,185p'","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":22000}

[2026-05-11T01:32:50.658Z] tool-call:exec_command line=194
------------------------------------------------------------------------------------------------
{"cmd":"nl -ba src/gfx/backends/mod.rs | sed -n '1,125p'; nl -ba src/gfx/backends/intel.rs | sed -n '286,420p;530,590p'; nl -ba src/r/gfx_cabi.rs | sed -n '4921,4935p'; nl -ba src/r/spawn_service.rs | sed -n '1314,1420p'","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":36000}

[2026-05-11T01:32:50.677Z] tool-output line=196
------------------------------------------------------------------------------------------------
Chunk ID: 9ecc38
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 6105
Output:
   214	pub(crate) fn init_primary_boot_surface(dev: crate::intel::Dev) {
   215	    if PRIMARY_BOOT_SURFACE_INIT.swap(true, Ordering::AcqRel) {
   216	        return;
   217	    }
   218	
   219	    log_pipe_scanout_probe(dev, "before-primary-init");
   220	    log_transcoder_a_state(dev, "before-primary-init");
   221	
   222	    let Some(pipe) = active_pipe(dev) else {
   223	        crate::log!("intel/display: primary-boot-surface skipped no active pipe discovered\n");
   224	        return;
   225	    };
   226	    let pipe_src_raw = crate::intel::mmio_read(dev, pipe.pipe_src_off);
   227	    let pipe_src_dims = decode_pipe_src(pipe_src_raw);
   228	    let fb_dims = framebuffer_hint();
   229	    let chosen = pipe_src_dims
   230	        .map(|(width, height)| (width, height, "pipe-src"))
   231	        .or_else(|| fb_dims.map(|(width, height)| (width, height, "fb-hint")));
   232	    let Some((width, height, chosen_from)) = chosen else {
   233	        crate::log!(
   234	            "intel/display: primary-boot-surface skipped no dimensions pipe={}\n",
   235	            pipe.name
   236	        );
   237	        return;
   238	    };
   239	    log_primary_dimensions_probe(pipe.name, pipe_src_raw, pipe_src_dims, fb_dims, chosen_from);
   240	    program_pipe_bottom_color(dev, pipe, PIPE_BOTTOM_COLOR_RGB);
   241	
   242	    let Some(pitch_bytes) = aligned_pitch_bytes(width, PRIMARY_BYTES_PER_PIXEL) else {
   243	        crate::log!("intel/display: primary-boot-surface skipped bad pitch width={}\n", width);
   244	        return;
   245	    };
   246	    let Some(byte_len) = usize::try_from(u64::from(pitch_bytes) * u64::from(height)).ok() else {
   247	        crate::log!("intel/display: primary-boot-surface skipped surface too large\n");
   248	        return;
   249	    };
   250	    let Some((phys, virt)) = crate::dma::alloc(byte_len, crate::intel::WARM_ALIGN) else {
   251	        crate::log!("intel/display: primary-boot-surface alloc failed bytes=0x{:X}\n", byte_len);
   252	        return;
   253	    };
   254	
   255	    fill_surface_color(virt, pitch_bytes as usize, width, height, PRIMARY_BASELINE_COLOR);
   256	    crate::intel::dma_flush(virt, byte_len);
   257	
   258	    if !crate::intel::map_display_scanout_ggtt(
   259	        dev,
   260	        phys,
   261	        byte_len,
   262	        crate::intel::GPU_VA_DISPLAY_PRIMARY_BASE,
   263	    ) {
   264	        crate::log!(
   265	            "intel/display: primary-boot-surface ggtt map failed bytes=0x{:X} gpu=0x{:X}\n",
   266	            byte_len,
   267	            crate::intel::GPU_VA_DISPLAY_PRIMARY_BASE
   268	        );
   269	        return;
   270	    }
   271	    crate::intel::ggtt_invalidate(dev);
   272	
   273	    let Some(_stride_reg) = plane_stride_reg_value(pitch_bytes) else {
   274	        crate::log!(
   275	            "intel/display: primary-boot-surface stride encode failed pitch=0x{:X}\n",
   276	            pitch_bytes
   277	        );
   278	        return;
   279	    };
   280	    let Some(surface_reg) = u32::try_from(crate::intel::GPU_VA_DISPLAY_PRIMARY_BASE).ok() else {
   281	        crate::log!("intel/display: primary-boot-surface gpu addr out of range\n");
   282	        return;
   283	    };
   284	
   285	    log_primary_plane_probe(dev, pipe, "before-arm");
   286	    let ctl_before = crate::intel::mmio_read(dev, pipe.plane_ctl_off);
   287	    let surf_before = crate::intel::mmio_read(dev, pipe.plane_surf_off);
   288	    let (_, _, surf_live, _) = program_primary_plane_and_wait(
   289	        dev,
   290	        pipe,
   291	        width,
   292	        height,
   293	        pitch_bytes,
   294	        surface_reg,
   295	        "init-arm",
   296	    );
   297	    log_primary_plane_probe(dev, pipe, "after-arm");
   298	    log_pipe_scanout_probe(dev, "after-primary-init");
   299	    let surf_armed = crate::intel::mmio_read(dev, pipe.plane_surf_off);
   300	    let ctl_after = crate::intel::mmio_read(dev, pipe.plane_ctl_off);
   301	    let ok = surf_live == surface_reg || surf_armed == surface_reg;
   302	
   303	    let primary_surface = PrimarySurface {
   304	        width,
   305	        height,
   306	        pitch_bytes,
   307	        phys,
   308	        virt,
   309	        pipe,
   310	    };
   311	    *PRIMARY_SURFACE.lock() = Some(primary_surface);
   312	    log_primary_scanout_pte_wi
[... truncated in extract ...]

[2026-05-11T01:32:50.680Z] tool-output line=197
------------------------------------------------------------------------------------------------
Chunk ID: b01f42
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 4974
Output:
    88	const GPU_VA_GPGPU_TILE_ARENA_BASE: u64 = 0x0400_0000;
    89	const GPGPU_EU_KERNEL_OFFSET_BYTES: usize = 0x3000;
    90	const GPGPU_WALKER_SCRATCH_OFFSET_BYTES: usize = 0x3800;
    91	const GPGPU_TILE_ROWS: usize = 256;
    92	const GPGPU_TILE_K_DIM: usize = 2048;
    93	const GPGPU_TILE_WEIGHT_BYTES_PER_ELEM: usize = 2;
    94	const GPGPU_TILE_X_BYTES_PER_ELEM: usize = 4;
    95	const GPGPU_TILE_OUTPUT_BYTES_PER_ELEM: usize = 4;
    96	const GPGPU_TILE_TARGET_TILES: usize = 3;
    97	const GPGPU_WEIGHT_TILE_BYTES: usize =
    98	    GPGPU_TILE_ROWS * GPGPU_TILE_K_DIM * GPGPU_TILE_WEIGHT_BYTES_PER_ELEM;
    99	const GPGPU_X_VECTOR_BYTES: usize = GPGPU_TILE_K_DIM * GPGPU_TILE_X_BYTES_PER_ELEM;
   100	const GPGPU_OUTPUT_TILE_BYTES: usize = GPGPU_TILE_ROWS * GPGPU_TILE_OUTPUT_BYTES_PER_ELEM;
   101	const GPGPU_TILE_OUTPUT_OFFSET_BYTES: usize = GPGPU_X_VECTOR_BYTES + GPGPU_WEIGHT_TILE_BYTES;
   102	const GPGPU_TILE_RECORD_USED_BYTES: usize =
   103	    GPGPU_TILE_OUTPUT_OFFSET_BYTES + GPGPU_OUTPUT_TILE_BYTES;
   104	const GPGPU_TILE_RECORD_BYTES: usize = (GPGPU_TILE_RECORD_USED_BYTES + 4095) & !4095;
   105	const GPGPU_TILE_ARENA_REQUIRED_BYTES: usize = GPGPU_TILE_TARGET_TILES * GPGPU_TILE_RECORD_BYTES;
   106	const GPGPU_TILE_ARENA_BYTES: usize = (GPGPU_TILE_ARENA_REQUIRED_BYTES + 4095) & !4095;
   326	fn init_minimal_ppgtt(warm: RenderWarmState) -> bool {
   327	    if warm.ppgtt_virt.is_null() || warm.ppgtt_len < WARM_PPGTT_BYTES {
   328	        return false;
   329	    }
   330	
   331	    let pml4_off = 0usize;
   332	    let pdp_off = 4096usize;
   333	    let pd_off = 8192usize;
   334	    let pt_off = 12288usize;
   335	    let pte_present_rw = crate::intel::GEN8_PAGE_PRESENT | GEN8_PAGE_RW;
   336	    let pde_present_rw_uc = pte_present_rw | GEN8_PAGE_PWT | GEN8_PAGE_PCD;
   337	
   338	    unsafe {
   339	        core::ptr::write_bytes(warm.ppgtt_virt, 0, warm.ppgtt_len);
   340	        let pml4 = warm.ppgtt_virt.add(pml4_off) as *mut u64;
   341	        let pdp = warm.ppgtt_virt.add(pdp_off) as *mut u64;
   342	        let pd = warm.ppgtt_virt.add(pd_off) as *mut u64;
   343	        core::ptr::write_volatile(pml4, (warm.ppgtt_phys + pdp_off as u64) | pde_present_rw_uc);
   344	        core::ptr::write_volatile(pdp, (warm.ppgtt_phys + pd_off as u64) | pde_present_rw_uc);
   345	        for index in 0..PPGTT_PT_COUNT {
   346	            let pt_phys = warm.ppgtt_phys + pt_off as u64 + (index as u64) * 4096;
   347	            core::ptr::write_volatile(pd.add(index), pt_phys | pde_present_rw_uc);
   348	        }
   349	    }
   350	
   351	    fn map_region(
   352	        warm: RenderWarmState,
   353	        gpu: u64,
   354	        phys: u64,
   355	        len: usize,
   356	        entry_flags: u64,
   357	    ) -> bool {
   358	        let pt_off = 12288usize;
   359	        for page in 0..len.div_ceil(4096) {
   360	            let va_page = (gpu >> 12) + page as u64;
   361	            let pd_index = ((va_page >> 9) & 0x1FF) as usize;
   362	            let pt_index = (va_page & 0x1FF) as usize;
   363	            if pd_index >= PPGTT_PT_COUNT {
   364	                return false;
   365	            }
   366	            let pte_off = pt_off + pd_index * 4096 + pt_index * core::mem::size_of::<u64>();
   367	            let pte = (phys + (page as u64) * 4096) & !0xFFF;
   368	            unsafe {
   369	                core::ptr::write_volatile(
   370	                    warm.ppgtt_virt.add(pte_off) as *mut u64,
   371	                    pte | entry_flags,
   372	                );
   373	            }
   374	        }
   375	        true
   376	    }
   377	
   378	    let ok = map_region(warm, GPU_VA_RING_BASE, warm.ring_phys, warm.ring_len, pte_present_rw)
   379	        && map_region(
   380	            warm,
   381	            GPU_VA_CONTEXT_BASE,
   382	            warm.context_phys,
   383	            warm.context_len,
   384	            pte_present_rw,
   385	        )
   386	        && map_region(warm, GPU_VA_BATCH_BASE, warm.batch_phys, warm.batch_len, pte_present_rw)
   387	        && map_region(
   388	            warm,
   389	            GPU_VA_DRAW_STATE_BASE,
   390	            warm.draw_state_phys,
   391	            warm.draw_state_len,
   392	            pte_present_rw,
   393	        )
   394	        && map_region(warm, GPU_VA_VERTEX_BASE, warm.vertex_phys, warm.vertex_len
[... truncated in extract ...]

[2026-05-11T01:32:50.687Z] tool-output line=198
------------------------------------------------------------------------------------------------
Chunk ID: 89bf5c
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 2025
Output:
     1	#![allow(dead_code)]
     2	
     3	use embassy_executor::{SpawnError, Spawner};
     4	use embassy_time::{Duration as EmbassyDuration, Timer};
     5	use trueos_gfx_core::{ShaderDesc, ShaderFormat, ShaderStage};
     6	
     7	pub(crate) const MANDELBROT_GPU_SIDEQUEST_NAME: &str = "mandelbrot-gpu-sidequest";
     8	pub(crate) const MANDELBROT_FRAGMENT_SOURCE_PATH: &str =
     9	    ".codex_tmp/mandelbrot_fragment_1440p_parametric.frag";
    10	pub(crate) const MANDELBROT_FRAGMENT_SPIRV_PATH: &str =
    11	    ".codex_tmp/mandelbrot_fragment_1440p_parametric.spv";
    12	pub(crate) const MANDELBROT_FRAGMENT_SPIRV_BYTES: &[u8] =
    13	    include_bytes!("../../.codex_tmp/mandelbrot_fragment_1440p_parametric.spv");
    14	pub(crate) const MANDELBROT_TARGET_WIDTH: u32 = 2560;
    15	pub(crate) const MANDELBROT_TARGET_HEIGHT: u32 = 1440;
    16	pub(crate) const MANDELBROT_PUSH_CONSTANT_BYTES: u16 = 24;
    17	pub(crate) const MANDELBROT_GPGPU_LOOP_MS: u64 = 1000;
    18	
    19	#[derive(Copy, Clone, Debug, Eq, PartialEq)]
    20	pub(crate) enum MandelbrotGpuArtifactStage {
    21	    Fragment,
    22	    Compute,
    23	}
    24	
    25	impl MandelbrotGpuArtifactStage {
    26	    pub(crate) const fn as_str(self) -> &'static str {
    27	        match self {
    28	            Self::Fragment => "fragment",
    29	            Self::Compute => "compute",
    30	        }
    31	    }
    32	}
    33	
    34	#[derive(Copy, Clone, Debug, Eq, PartialEq)]
    35	pub(crate) enum MandelbrotPresentPath {
    36	    BufferFirst,
    37	    CopyToIntelOverlay,
    38	    DirectFramebuffer,
    39	}
    40	
    41	impl MandelbrotPresentPath {
    42	    pub(crate) const fn as_str(self) -> &'static str {
    43	        match self {
    44	            Self::BufferFirst => "buffer-first",
    45	            Self::CopyToIntelOverlay => "copy-to-intel-overlay",
    46	            Self::DirectFramebuffer => "direct-framebuffer",
    47	        }
    48	    }
    49	}
    50	
    51	#[derive(Copy, Clone, Debug, Eq, PartialEq)]
    52	pub(crate) struct MandelbrotGpuSidequestPlan {
    53	    pub(crate) name: &'static str,
    54	    pub(crate) stage: MandelbrotGpuArtifactStage,
    55	    pub(crate) source_path: &'static str,
    56	    pub(crate) spirv_path: &'static str,
    57	    pub(crate) target_width: u32,
    58	    pub(crate) target_height: u32,
    59	    pub(crate) push_constant_bytes: u16,
    60	    pub(crate) render_path: MandelbrotPresentPath,
    61	    pub(crate) fallback_present_path: MandelbrotPresentPath,
    62	}
    63	
    64	impl MandelbrotGpuSidequestPlan {
    65	    pub(crate) const fn default_fragment_buffer_first() -> Self {
    66	        Self {
    67	            name: MANDELBROT_GPU_SIDEQUEST_NAME,
    68	            stage: MandelbrotGpuArtifactStage::Fragment,
    69	            source_path: MANDELBROT_FRAGMENT_SOURCE_PATH,
    70	            spirv_path: MANDELBROT_FRAGMENT_SPIRV_PATH,
    71	            target_width: MANDELBROT_TARGET_WIDTH,
    72	            target_height: MANDELBROT_TARGET_HEIGHT,
    73	            push_constant_bytes: MANDELBROT_PUSH_CONSTANT_BYTES,
    74	            render_path: MandelbrotPresentPath::BufferFirst,
    75	            fallback_present_path: MandelbrotPresentPath::CopyToIntelOverlay,
    76	        }
    77	    }
    78	
    79	    pub(crate) const fn target_rgba_bytes(self) -> usize {
    80	        (self.target_width as usize)
    81	            .saturating_mul(self.target_height as usize)
    82	            .saturating_mul(4)
    83	    }
    84	}
    85	
    86	pub(crate) const fn mandelbrot_gpu_sidequest_plan() -> MandelbrotGpuSidequestPlan {
    87	    MandelbrotGpuSidequestPlan::default_fragment_buffer_first()
    88	}
    89	
    90	pub(crate) fn mandelbrot_fragment_shader_desc(spirv_bytes: &'static [u8]) -> ShaderDesc<'static> {
    91	    ShaderDesc {
    92	        stage: ShaderStage::Fragment,
    93	        format: ShaderFormat::SpirV,
    94	        bytes: spirv_bytes,
    95	    }
    96	}
    97	
    98	fn byte_signature(bytes: &[u8]) -> u64 {
    99	    let mut sig = 0xCBF2_9CE4_8422_2325u64;
   100	    for &byte in bytes {
   101	        sig ^= byte as u64;
   102	        sig = sig.wrapping_mul(0x0000_0100_0000_01B3);
   103	    }
   104	    sig
   105	}
   106	
   107	pub(crate) fn spawn_mandelbrot_gpu_sidequest(spawner: Spawner) -> Result<
[... truncated in extract ...]

[2026-05-11T01:32:50.702Z] tool-output line=199
------------------------------------------------------------------------------------------------
Chunk ID: 6c5f9e
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 4690
Output:
     1	use trueos_gfx_core::{
     2	    BufferDesc, BufferId, CommandBuffer, DeviceCaps, Error, Extent2D, FenceId, GfxContext,
     3	    GfxDevice, GfxPresent, ImageDesc, ImageFormat, ImageId, ImageRegion, PipelineDesc, PipelineId,
     4	    Result, ShaderDesc, ShaderId, SwapchainDesc,
     5	};
     6	
     7	use crate::gfx::virtio_gpu_3d;
     8	
     9	pub enum Backend {
    10	    Virgl(virtio_gpu_3d::VirglGfxBackend),
    11	    None(NullBackend),
    12	}
    13	
    14	pub struct NullBackend;
    15	
    16	impl GfxDevice for NullBackend {
    17	    fn caps(&self) -> DeviceCaps {
    18	        DeviceCaps {
    19	            supports_rgbx8888: false,
    20	            supports_host_visible_buffers: false,
    21	            supports_scissor: false,
    22	        }
    23	    }
    24	
    25	    fn create_buffer(&mut self, _desc: BufferDesc) -> Result<BufferId> {
    26	        Err(Error::Unsupported)
    27	    }
    28	
    29	    fn destroy_buffer(&mut self, _id: BufferId) {}
    30	
    31	    fn create_shader(&mut self, _desc: ShaderDesc<'_>) -> Result<ShaderId> {
    32	        Err(Error::Unsupported)
    33	    }
    34	
    35	    fn destroy_shader(&mut self, _id: ShaderId) {}
    36	
    37	    fn create_pipeline(&mut self, _desc: PipelineDesc) -> Result<PipelineId> {
    38	        Err(Error::Unsupported)
    39	    }
    40	
    41	    fn destroy_pipeline(&mut self, _id: PipelineId) {}
    42	
    43	    fn create_image(&mut self, _desc: ImageDesc) -> Result<ImageId> {
    44	        Err(Error::Unsupported)
    45	    }
    46	
    47	    fn destroy_image(&mut self, _id: ImageId) {}
    48	
    49	    fn write_image(&mut self, _id: ImageId, _data: &[u8]) -> Result<()> {
    50	        Err(Error::Unsupported)
    51	    }
    52	
    53	    fn write_image_region(
    54	        &mut self,
    55	        _id: ImageId,
    56	        _region: ImageRegion,
    57	        _data: &[u8],
    58	    ) -> Result<()> {
    59	        Err(Error::Unsupported)
    60	    }
    61	
    62	    fn write_buffer(&mut self, _id: BufferId, _offset: u64, _data: &[u8]) -> Result<()> {
    63	        Err(Error::Unsupported)
    64	    }
    65	
    66	    fn submit(&mut self, _cmds: CommandBuffer<'_>) -> Result<FenceId> {
    67	        Err(Error::Unsupported)
    68	    }
    69	
    70	    fn poll(&mut self, _fence: FenceId) -> bool {
    71	        false
    72	    }
    73	
    74	    fn device_idle(&mut self) {}
    75	}
    76	
    77	impl GfxPresent for NullBackend {
    78	    fn configure_swapchain(&mut self, _desc: SwapchainDesc) -> Result<()> {
    79	        Err(Error::Unsupported)
    80	    }
    81	
    82	    fn swapchain_desc(&self) -> SwapchainDesc {
    83	        SwapchainDesc {
    84	            format: ImageFormat::Rgbx8888,
    85	            extent: Extent2D {
    86	                width: 0,
    87	                height: 0,
    88	            },
    89	        }
    90	    }
    91	}
    92	
    93	fn ensure_pci_enumerated_if_empty() {
    94	    let mut count: usize = 0;
    95	    crate::pci::with_devices(|list| {
    96	        count = list.len();
    97	    });
    98	    if count == 0 {
    99	        crate::log!("gfx: pci device list empty; enumerating before backend init\n");
   100	        crate::pci::enumerate_impl();
   101	    }
   102	}
   103	
   104	impl Backend {
   105	    pub fn init_auto(framebuffers: Option<&'static crate::limine::FramebufferResponse>) -> Self {
   106	        if let Some(v) = Self::init_virgl(framebuffers) {
   107	            crate::log_info!(target: "gfx"; "gfx: using virgl backend (auto)\n");
   108	            return v;
   109	        }
   110	        crate::log_info!(target: "gfx"; "gfx: virgl auto init failed\n");
   111	
   112	        crate::log_info!(target: "gfx"; "gfx: no accelerated backend available; gfx backend inactive\n");
   113	        Backend::None(NullBackend)
   114	    }
   115	
   116	    pub fn init_virgl(
   117	        framebuffers: Option<&'static crate::limine::FramebufferResponse>,
   118	    ) -> Option<Self> {
   119	        ensure_pci_enumerated_if_empty();
   120	        virtio_gpu_3d::VirglGfxBackend::init(framebuffers).map(Backend::Virgl)
   121	    }
   122	    pub fn context_mut(&mut self) -> &mut dyn GfxContext {
   123	        match self {
   124	            Backend::Virgl(b) => b,
   125	            Backend::N
[... truncated in extract ...]

[2026-05-11T01:32:52.992Z] tool-call:exec_command line=201
------------------------------------------------------------------------------------------------
{"cmd":"nl -ba src/intel/mod.rs | sed -n '340,388p;670,688p'","workdir":"/home/t4ce/REPOS/TRUEOS","yield_time_ms":1000,"max_output_tokens":12000}

[2026-05-11T01:32:53.024Z] tool-output line=203
------------------------------------------------------------------------------------------------
Chunk ID: 6526a1
Wall time: 0.0000 seconds
Process exited with code 0
Original token count: 670
Output:
   340	    *CLAIMED_DEVICE.lock() = Some(dev);
   341	    let fw = self::guc::load_fw();
   342	    if fw.len == 0 {
   343	        crate::log!("intel/guc: firmware module missing or invalid\n");
   344	        return;
   345	    }
   346	    crate::log!(
   347	        "intel/guc: firmware found phys=0x{:X} gpu=0x{:X} len=0x{:X} xfer=0x{:X}\n",
   348	        fw.phys,
   349	        fw.gpu,
   350	        fw.len,
   351	        fw.xfer_len
   352	    );
   353	    let ads = self::guc::alloc_ads(fw.private_data_size);
   354	    if ads.len == 0 {
   355	        crate::log!("intel/guc: ads alloc failed private_data=0x{:X}\n", fw.private_data_size);
   356	        return;
   357	    }
   358	    if !map_ggtt(dev, fw.phys, fw.len, fw.gpu) || !map_ggtt(dev, ads.phys, ads.len, ads.gpu) {
   359	        crate::log!("intel/guc: ggtt map failed fw_len=0x{:X} ads_len=0x{:X}\n", fw.len, ads.len);
   360	        return;
   361	    }
   362	    ggtt_invalidate(dev);
   363	    forcewake(dev);
   364	    let ready = self::guc::bootstrap(dev, fw, ads);
   365	    let status = self::guc::status(dev);
   366	    let (bootrom, ukernel, auth) = self::guc::describe_status(status);
   367	    crate::log!(
   368	        "intel/guc: bootstrap ready={} status=0x{:08X} bootrom={} ukernel={} auth=0x{:X}\n",
   369	        ready as u8,
   370	        status,
   371	        bootrom,
   372	        ukernel,
   373	        auth
   374	    );
   375	    if ready {
   376	        self::guc::prove_h2g_mmio_once(dev, "boot-control-ctb-disable");
   377	    }
   378	    if DISPLAY_PLANE1_BOOT_DEMO_ENABLED {
   379	        self::display::init_primary_boot_surface(dev);
   380	    } else {
   381	        crate::log!("intel/display: plane1 boot demo disabled\n");
   382	    }
   383	    crate::log!(
   384	        "intel/render: disabled reason=primary-render-module-removed gpgpu_probe=enabled\n"
   385	    );
   386	    self::gpgpu::submit_gpgpu_preflight_once();
   387	    crate::log!("intel/media: source warmup disabled trigger=trueosfs-root-mounted\n",);
   388	    if MEDIA_BOOT_DEMO_ENABLED {
   670	}
   671	
   672	pub fn active_scanout_dimensions() -> Option<(u32, u32)> {
   673	    self::display::active_scanout_dimensions()
   674	}
   675	
   676	pub fn primary_surface_gpu_addr() -> Option<u64> {
   677	    self::display::primary_surface_gpu_addr()
   678	}
   679	
   680	pub fn present_rgba_overlay_top_right(
   681	    src: &[u8],
   682	    src_width: u32,
   683	    src_height: u32,
   684	    src_pitch_bytes: usize,
   685	) -> bool {
   686	    self::display::present_rgba_overlay_top_right(src, src_width, src_height, src_pitch_bytes)
   687	}
   688

[2026-05-11T01:33:25.063Z] assistant line=205
------------------------------------------------------------------------------------------------
I did a read-only pass. Short version: TRUEOS already has a proven Intel scanout surface path, but the Mandelbrot sidequest currently only proves a tiny GPGPU sentinel buffer. Nothing connects that GPU-written buffer to the live primary/overlay plane.

**Findings**

- [src/intel/display.rs](/home/t4ce/REPOS/TRUEOS/src/intel/display.rs:214) owns the working visible path. It allocates a linear primary surface, maps it at `GPU_VA_DISPLAY_PRIMARY_BASE = 0x02000000`, arms the primary plane, paints the logo, and proves CPU scanout visibility.
- [src/intel/display.rs](/home/t4ce/REPOS/TRUEOS/src/intel/display.rs:524) has `present_rgba_surface_center`, but it is internal to `display.rs`; the public export is only [overlay present](/home/t4ce/REPOS/TRUEOS/src/intel/mod.rs:680).
- [src/intel/display.rs](/home/t4ce/REPOS/TRUEOS/src/intel/display.rs:592) has a usable fallback shape: CPU RGBA slice -> overlay DMA surface -> GGTT map at `0x03000000` -> arm overlay plane. This can show pixels if you have a CPU-visible RGBA buffer.
- [src/intel/gpgpu.rs](/home/t4ce/REPOS/TRUEOS/src/intel/gpgpu.rs:2326) sidequest output is only `SIDEQUEST_TARGET_BYTES = 64`, at `0x04000000 + GPGPU_TILE_OUTPUT_OFFSET_BYTES`; it proves one sentinel write, not a full framebuffer.
- [src/tst/mandelbrot_gpu_sidequest.rs](/home/t4ce/REPOS/TRUEOS/src/tst/mandelbrot_gpu_sidequest.rs:147) loops `submit_gpgpu_sidequest_target_buffer_probe()`, releases readiness on readback, and logs `wire-real-mandelbrot-pixel-kernel-or-scanout-copy`; it never calls a display/overlay present path.
- [src/intel/mod.rs](/home/t4ce/REPOS/TRUEOS/src/intel/mod.rs:383) explicitly logs `primary-render-module-removed`; the rich 3D render code under `src/intel/intel/render` appears parked/duplicated, not active in the compiled `src/intel/mod.rs`.
- [src/gfx/backends/mod.rs](/home/t4ce/REPOS/TRUEOS/src/gfx/backends/mod.rs:9) only wires `Virgl` and `None`. `src/gfx/backends/intel.rs` exists, but it is not in the backend enum.
- [src/r/gfx_cabi.rs](/home/t4ce/REPOS/TRUEOS/src/r/gfx_cabi.rs:4921) Mandelbrot-to-texture requires `is_virgl_active()`, so the UI Mandelbrot path will reject Intel.
- [src/gfx/backends/intel.rs](/home/t4ce/REPOS/TRUEOS/src/gfx/backends/intel.rs:558) returns `Unsupported` for `PipelineKind::Mandelbrot`, even if the Intel backend were wired.
- [src/r/spawn_service.rs](/home/t4ce/REPOS/TRUEOS/src/r/spawn_service.rs:1408) `ui2-mandelbrot-demo` is disabled; the enabled sidequest is the GPGPU sentinel task, not a visible UI demo.

**Missing Link**

A real path must do one of these:

1. Quick visible fallback: generate/read back a full RGBA Mandelbrot buffer on CPU-visible memory, then call exported `crate::intel::present_rgba_overlay_top_right(...)`.
2. Direct GPU visible path: make the GPGPU kernel write full `XRGB/XBGR8888` pixels into the live primary or overlay surface GPU VA, then issue the right post-write flush/kick and preserve plane pitch/format.
3. Cleaner display API: expose a public Intel display present primitive for “RGBA slice to primary” or “GPU VA to plane flip,” instead of relying on private `display.rs` helpers or `may_help` code.

**Main Risks**

- `0x04000000` is used by the GPGPU tile arena and also by the dormant Intel gfx backend image allocator, so wiring both naïvely risks GPU VA collisions.
- Current sidequest artifact is SPIR-V fragment metadata, but there is no active Intel SPIR-V/fragment pipeline. For Intel hardware, this likely needs a compute kernel or revived 3D path.
- Pixel format/order matters: display is probing `xbgr8888-order-rgbx`, while many buffers are named RGBA.
- GPU-write visibility needs explicit HDC/render/cache flush discipline before CPU readback or display scanout.
- Direct primary writes will overwrite logo/UI; overlay is safer for first visible proof.

[2026-05-11T01:33:25.064Z] assistant line=206
------------------------------------------------------------------------------------------------
I did a read-only pass. Short version: TRUEOS already has a proven Intel scanout surface path, but the Mandelbrot sidequest currently only proves a tiny GPGPU sentinel buffer. Nothing connects that GPU-written buffer to the live primary/overlay plane.

**Findings**

- [src/intel/display.rs](/home/t4ce/REPOS/TRUEOS/src/intel/display.rs:214) owns the working visible path. It allocates a linear primary surface, maps it at `GPU_VA_DISPLAY_PRIMARY_BASE = 0x02000000`, arms the primary plane, paints the logo, and proves CPU scanout visibility.
- [src/intel/display.rs](/home/t4ce/REPOS/TRUEOS/src/intel/display.rs:524) has `present_rgba_surface_center`, but it is internal to `display.rs`; the public export is only [overlay present](/home/t4ce/REPOS/TRUEOS/src/intel/mod.rs:680).
- [src/intel/display.rs](/home/t4ce/REPOS/TRUEOS/src/intel/display.rs:592) has a usable fallback shape: CPU RGBA slice -> overlay DMA surface -> GGTT map at `0x03000000` -> arm overlay plane. This can show pixels if you have a CPU-visible RGBA buffer.
- [src/intel/gpgpu.rs](/home/t4ce/REPOS/TRUEOS/src/intel/gpgpu.rs:2326) sidequest output is only `SIDEQUEST_TARGET_BYTES = 64`, at `0x04000000 + GPGPU_TILE_OUTPUT_OFFSET_BYTES`; it proves one sentinel write, not a full framebuffer.
- [src/tst/mandelbrot_gpu_sidequest.rs](/home/t4ce/REPOS/TRUEOS/src/tst/mandelbrot_gpu_sidequest.rs:147) loops `submit_gpgpu_sidequest_target_buffer_probe()`, releases readiness on readback, and logs `wire-real-mandelbrot-pixel-kernel-or-scanout-copy`; it never calls a display/overlay present path.
- [src/intel/mod.rs](/home/t4ce/REPOS/TRUEOS/src/intel/mod.rs:383) explicitly logs `primary-render-module-removed`; the rich 3D render code under `src/intel/intel/render` appears parked/duplicated, not active in the compiled `src/intel/mod.rs`.
- [src/gfx/backends/mod.rs](/home/t4ce/REPOS/TRUEOS/src/gfx/backends/mod.rs:9) only wires `Virgl` and `None`. `src/gfx/backends/intel.rs` exists, but it is not in the backend enum.
- [src/r/gfx_cabi.rs](/home/t4ce/REPOS/TRUEOS/src/r/gfx_cabi.rs:4921) Mandelbrot-to-texture requires `is_virgl_active()`, so the UI Mandelbrot path will reject Intel.
- [src/gfx/backends/intel.rs](/home/t4ce/REPOS/TRUEOS/src/gfx/backends/intel.rs:558) returns `Unsupported` for `PipelineKind::Mandelbrot`, even if the Intel backend were wired.
- [src/r/spawn_service.rs](/home/t4ce/REPOS/TRUEOS/src/r/spawn_service.rs:1408) `ui2-mandelbrot-demo` is disabled; the enabled sidequest is the GPGPU sentinel task, not a visible UI demo.

**Missing Link**

A real path must do one of these:

1. Quick visible fallback: generate/read back a full RGBA Mandelbrot buffer on CPU-visible memory, then call exported `crate::intel::present_rgba_overlay_top_right(...)`.
2. Direct GPU visible path: make the GPGPU kernel write full `XRGB/XBGR8888` pixels into the live primary or overlay surface GPU VA, then issue the right post-write flush/kick and preserve plane pitch/format.
3. Cleaner display API: expose a public Intel display present primitive for “RGBA slice to primary” or “GPU VA to plane flip,” instead of relying on private `display.rs` helpers or `may_help` code.

**Main Risks**

- `0x04000000` is used by the GPGPU tile arena and also by the dormant Intel gfx backend image allocator, so wiring both naïvely risks GPU VA collisions.
- Current sidequest artifact is SPIR-V fragment metadata, but there is no active Intel SPIR-V/fragment pipeline. For Intel hardware, this likely needs a compute kernel or revived 3D path.
- Pixel format/order matters: display is probing `xbgr8888-order-rgbx`, while many buffers are named RGBA.
- GPU-write visibility needs explicit HDC/render/cache flush discipline before CPU readback or display scanout.
- Direct primary writes will overwrite logo/UI; overlay is safer for first visible proof.

[extracted_items=139]
